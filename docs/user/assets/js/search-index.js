// GENERATED FILE - do not edit. Rebuilt by tools/DocGen from docs/user/src/**.md.
// The search UI that reads it is the hand-written assets/js/docs-search.js.
window.CRF_DOCS_SEARCH = {"v":1,"p":[["index.html","Documentation","Documentation","A lightweight, cross-platform RF circuit simulator — DC, S-parameters, harmonic balance, and loadpull.",""],["quick-start/index.html","circuitRF Quick Start","Quick Start","For engineers who already know circuit simulators. In a couple of pages: what circuitRF is, how it's organized, and how to run a simulation and see a plot.",""],["new-user-guide/index.html","Welcome to circuitRF!","New User's Guide","Never used a circuit simulator before? Perfect — this guide starts from the very beginning and gets you to your first working simulation. No prior EDA experience assumed, and no question is too basic.",""],["reference/index.html","circuitRF Reference Guide","Reference Guide","The complete technical reference. Plain and direct — look things up here.",""],["reference/workspace.html","The Workspace","Reference Guide","The window everything else happens in — documents in the middle, tool panels around them, and a folder on disk behind it all.","project, folder, library, docking, panels, window, tree"],["reference/settings.html","Settings","Reference Guide","Every tab of the circuitRF Settings dialog, control by control — what each one changes, when it takes effect, and which of them are shared with harmonicaRF and wBond.","preferences, options, configuration, theme, dark mode, colours, colors"],["reference/units.html","Units","Reference Guide","Database units, display units, the snap grid, and what circuitRF accepts when you type a value into a field.","SI prefix, DBU, database unit, grid, snap, mil, mm, micron, engineering notation"],["reference/expressions.html","Expressions","Reference Guide","One expression language runs everywhere a value can be computed: global variables, component parameters, SDD device equations, and measurements. Anywhere you can type a number, you can type an expression — `2*pi*freq0`, `polar(0.1, 35)`, `if(Vg > Vth, gm*Vg, 0)`.","variable, parameter, formula, equation, function, math, conditional, if"],["reference/grid.html","Grid & Connectivity","Reference Guide","Connecting components must be easy, and connections must be unambiguous: \"do these two pins touch?\" has to have exactly one answer. circuitRF achieves both with **two grids** — a coarse connection grid that all electrical points snap to, and a fine authoring grid for everything cosmetic.","connectivity, net, wire, junction, snap, node"],["reference/pins-ports-terms.html","Pins, Ports & Terms","Reference Guide","Three concepts that sound interchangeable but aren't. Getting them right is the difference between a circuit that simulates what you intended and one that doesn't.","pin, port, term, termination, excitation, source impedance, reference plane"],["reference/file-formats.html","File Formats","Reference Guide","circuitRF stores a project as a folder of small, human-readable, text files — diffable and version-control friendly. The core idea: the **schematic** (what you draw) is the source of truth; the **netlist** (what the engine runs) is derived from it.","file extension, cws, cwsuser, csch, csym, clay, ctech, cem, cnl, ccell, cdd, format, on disk"],["reference/history.html","History","Reference Guide","Everything circuitRF has kept of a workspace, in one list — the states it kept for you automatically, and the versions you kept on purpose. What is in it, how to find something, and how to go back.","history, restore, undo, revert, safety net, save point, version, commit, release, share, compare, conflict, rename, correct, title"],["reference/schematic-editor.html","The Schematic Editor","Reference Guide","Where a circuit is drawn: placing components from the Library Palette, wiring them, editing their values, and setting up the analysis that simulates the result.","schematic, draw, wire, place, net, instance, hierarchy, test bench, testbench"],["reference/components.html","Components","Reference Guide","Every component in the standard library — its symbol, what it does, and its parameters.","resistor, capacitor, inductor, transmission line, microstrip, MLIN, part, symbol, library, bead, ferrite"],["reference/spice-import.html","Importing a SPICE Model","Reference Guide","What circuitRF carries across from a .model card or a .subckt, what it leaves behind, and why — including where to read the list of parameters that did not come with it.","SPICE, model card, subckt, subcircuit, netlist import, lib, BJT model, diode model, transistor model"],["reference/system-components.html","System Components","Reference Guide","The ideal blocks a system diagram is drawn out of — circulator, coupler, hybrid, balun, switch, amplifier, attenuator, filter, duplexer and mixer. What each one is for, what \"ideal\" costs you, and where passive intermodulation comes from.","mixer, amplifier, filter, attenuator, behavioural block, system level, intermod, IP3"],["reference/dynamic-symbols.html","Dynamic Symbols (SDD, ZPort, SnP)","Reference Guide","Most components have a fixed glyph. Some do not — their symbol is generated from a parameter rather than drawn once. For three of them the pin count itself follows the port count; for others the picture changes while the pins stay put. This chapter explains how those symbols are built and how to control them.","SnP, Touchstone component, ZPort, s2p component, data file component"],["reference/symbol-editor.html","Symbol Editor","Reference Guide","The Symbol Editor draws the glyph a cell shows on a schematic — its body art plus the pins that connect it. It is a focused drawing tool, not a cell editor: it never changes a cell's port count or parameters, only how the cell looks.","symbol, csym, pin placement, artwork, glyph"],["reference/sdd.html","The SDD (Symbolically-Defined Device)","Reference Guide","circuitRF's user-authored nonlinear device. You write each port's current (and charge) as an equation in the port voltages; the engine differentiates it automatically and balances it like any built-in device. It is how the FET models in the examples are defined, and the extension point for any nonlinearity the built-in parts don't cover.","symbolically defined device, equation defined device, EDD, nonlinear model, behavioural model, custom device"],["reference/nonlinear-capacitor.html","The Nonlinear Capacitor & the C–V Editor","Reference Guide","A capacitor whose value depends on the voltage across it — a varactor/varicap, or the junction capacitance of a device. circuitRF models C(V) as a polynomial (Taylor) series, and the C–V Editor fits that polynomial to a curve for you.","CV, C-V, varactor, Taylor series, nonlinear capacitance, junction capacitance"],["reference/veriloga.html","Compiled Verilog-A Models","Reference Guide","Running a compact model you supply — a published physics-based transistor model, or one you wrote — from Verilog-A source or from a compiled artefact, with no kit and nothing to install inside circuitRF.","Verilog-A, VerilogA, OSDI, compact model, compiled model, vendor model, va"],["reference/match.html","The Match Component","Reference Guide","Direct synthesis of a bandpass matching network that absorbs both terminations.","matching network, impedance match, Fano, Norton transform, bandpass, LC ladder, tuner"],["reference/simulations.html","Simulations","Reference Guide","Every analysis circuitRF runs, what it computes, a short overview of the method, and the full set of settings. Algorithm details are deferred to the design notes / white papers; this chapter is the operational reference.","analysis, sweep, DC, S-parameters, harmonic balance, HB, loadpull, load pull, parametric sweep, run, simulate"],["reference/measurements.html","Measurements","Reference Guide","A measurement is a named figure of merit computed from a run's results — Gain, PAE, Pout, whatever you can write as an equation over the analysis output. It draws no current and stamps nothing; it is cube algebra evaluated after the analyses finish, and its result plots like any other trace.","measure, post-processing, expression, equation, derived result, post process"],["reference/netlist.html","The Netlist (.cnl) Format","Reference Guide","A circuitRF netlist is a human-readable text description of a circuit and what to simulate. It's the same thing the schematic editor produces internally, and it's what the engine actually runs.","netlist, cnl, elaborated netlist, flat netlist, node numbering"],["reference/data-display.html","The Data Display","Reference Guide","Plots and tables, trace cards, axes, markers — and how a run's cubes become curves.","plot, graph, chart, curve, trace, marker, Smith chart, axes, legend, cdd, results viewer"],["reference/derived-metrics.html","Derived Metrics","Reference Guide","Stability, passivity, group delay and the two-terminal passive readouts — every equation, written out.","ESR, ESL, Q factor, self-resonance, SRF, Ceff, Leff, capacitance, inductance, stability, mu, Rollett, K factor, MAG, MSG, passivity, group delay, sigma max, decoupling, MLCC, fixture, shunt-through"],["reference/wsprobe.html","The WSProbe","Reference Guide","Nodal transfer functions for stability and in-situ design — the wsp matrix, the driving-point functions, the bidirectional impedances, the published stability margin, and an appendix on stability itself.","WSProbe, Winslow, stability, driving point impedance, H0, Y0, loop gain, bidirectional impedance, NDF, Ohtomo, Kurokawa, envelope, stability margin, return difference, Rollett, Barkhausen, Bode, Middlebrook, Tian, bifurcation, normalized determinant function"],["reference/plot-types.html","Plot Types","Reference Guide","The Data Display offers four plot types. Which one you can use depends on whether the data is real (scalar) or complex — that's the single most important rule below.","rectangular, Smith chart, polar, table, log axis, plot kind"],["reference/npy-export.html","Results & Data Export","Reference Guide","Every run's results are written to disk as a self-describing NumPy `.npy` file you can read from Python, MATLAB, or any tool that speaks NumPy — no re-running the simulation. This chapter covers where results live, how to export them in other formats, and how to read the `.npy` in a few lines of Python.","Touchstone, s2p, snp, sNp, export, npy, numpy, MATLAB, mat, CSV, save results, data out, python"],["reference/cli.html","The Command Line","Reference Guide","circuitRF runs without the GUI — not just its engines, but authoring, validation, resolution and drawing too. One executable, fifteen verbs — S-parameters, DC, harmonic balance, loadpull, loadpull pursuit, electromagnetic extraction, layout interchange, creating a workspace or a cell, importing a part, rendering a document as a picture, checking a design, explaining what it resolved to, reading a result back, an elaborated-netlist dump, and an MCP server. Every one of them answers --json. This chapter is the operational reference for all of them, including a worked EM run and a worked render, each from an empty folder.","CLI, command line, command-line, terminal, shell, console, headless, batch, script, scripting, automation, verbs, exit code, stdout, circuitrf, MCP, Model Context Protocol, agent, AI, LLM, JSON-RPC, stdio, tool server, integration, render, image, SVG, PNG, PDF, export, picture, screenshot, plot, viewport, layers, extents, thumbnail"],["reference/layout-editor.html","The Layout Editor","Reference Guide","Drawing artwork: the technology, the tools, snapping, and the schematic it belongs to.","layout, artwork, PCB, copper, drawing, DRC, polygon, trace, via"],["reference/pcells.html","PCells","Reference Guide","Parameterised cells: what they are, placing one, driving it, and editing it by its handles.","PCell, parameterised cell, parametric cell, generator, python cell"],["reference/pdk-integration.html","PDK Integration","Reference Guide","Importing a process design kit and simulating the parts it supplies.","PDK, process design kit, foundry kit, vendor kit, library import, design kit"],["reference/pdk-authoring.html","PDK Authoring","Reference Guide","Building a kit: the OpenPDK layout, Python PCells, models, technology and artwork.","PDK, process design kit, kit authoring, OpenPDK, PCell python, kit"],["reference/stackup.html","The Stackup","Reference Guide","The layers your board is actually made of — and, because it decides the ground plane, where the negative terminal of every EM port is.","dielectric, substrate, layers, board, PCB, Er, epsilon r, permittivity, loss tangent, tan delta, copper thickness, prepreg, technology, ctech"],["reference/mom-engine.html","The MoM Engine","Reference Guide","The planar method-of-moments solver: what it does, what it will not do, and how it works.","EM, electromagnetic, method of moments, MoM, planar solver, full wave, extraction, Green's function, mesh"],["reference/em-setup.html","EM Setup","Reference Guide","The EM Setup panel, control by control, and where the results land.","EM, electromagnetic, cem, ports, mesh, extraction, simulate layout, substrate"],["reference/antennas.html","Antennas","Reference Guide","Patch antennas in the planar solver — the pattern, the numbers that go with it, and the limits that bound both.","antenna, patch, microstrip antenna, radiation pattern, far field, directivity, gain, realized gain, radiation efficiency, beamwidth, polarization, axial ratio, cross-pol, Ludwig-3, surface wave, resonance, inset feed, probe feed, slot"],["reference/harmonicarf.html","harmonicaRF","Reference Guide","Interactive harmonic load-pull on a single device, at the speed of a mouse drag.","harmonic termination, loadpull, load pull, Smith chart, interactive, class F, waveform engineering, PA design"],["reference/wbond.html","wBond","Reference Guide","Bondwire arrays: geometry, inductance, the 3D kernel, and S-parameters out.","bondwire, bond wire, wirebond, wire bond, ribbon, die attach, package"]],"s":[[0,"","","A lightweight, cross-platform RF circuit simulator — DC, S-parameters, harmonic balance, and loadpull. Quick Start → For engineers who already know circuit simulators. Run a simulation and see a plot in a couple of pages. Start here if you have used SPICE or a vendor RF tool. New User's Guide → Never used a circuit simulator? What a library, a cell and a simulation are — explained from the ground up, with worked examples. Reference Guide → The complete technical reference. Every analysis, every component, the layout and EM engines, the file formats and the two bundled tools."],[0,"what-is-circuitrf","What is circuitRF?","circuitRF studies the frequency response and nonlinear behaviour of RF circuits — from a handful of components to hierarchical, multi-port designs. It is an RF simulator, not a SPICE simulator : the analyses and the workflow are built around the RF/microwave problem, the file formats are human-readable, and loadpull is a first-class experiment. Results are stored as NumPy .npy datasets, so they drop straight into Python or MATLAB. Alongside the circuit engines it carries a planar method-of-moments EM solver for layout, a 3D bondwire kernel , and harmonicaRF — an interactive harmonic load-pull bench for a single device."],[0,"everything-in-this-documentation","Everything in this documentation","Every page below is in one reading order. Start anywhere and follow Next at the foot of each page to reach the end without coming back here. Alpha circuitRF is in active development. These docs track the current build and details may shift between releases. Every figure in them is a vector capture of the running application, regenerated from the live interface rather than drawn by hand — so a picture here is what the build does."],[1,"","","For engineers who already know circuit simulators. In a couple of pages: what circuitRF is, how it's organized, and how to run a simulation and see a plot."],[1,"what","What circuitRF is","circuitRF is a lightweight, cross-platform RF circuit simulator. If you've used a SPICE tool or a commercial RF/microwave EDA suite, you'll be at home — but the analyses and the workflow are built around the RF problem , not the time-domain transient problem. It is deliberately not a SPICE simulator . Simulations available: DC — operating point (linear and nonlinear), the bias prerequisite for HB. S-parameters — linear multiport network parameters over a frequency sweep, with renormalization and embedded Touchstone ( .sNp ) blocks. Harmonic Balance (HB) — steady-state nonlinear analysis, single- and two-tone, with power/source continuation for convergence at drive. Reports Pout, gain, efficiency, PAE, spectra, intermodulation. Loadpull / sourcepull — a first-class experiment: sweep the source/load reflection coefficient over a Smith-chart grid and contour the figures of merit. Loadpull Pursuit automatically searches for the terminations that optimize a metric. Parametric sweeps — wrap any analysis in one or more swept variables (available power, a bias voltage, a frequency, any user variable). Data Display: results plot on rectangular, Smith, and polar charts, and in tables. Measured data (Touchstone, .spl , .lpcwave ) overlays simulated data on the same axes — a measured loadpull contour plots exactly like a simulated one. Results are Python-native Every run writes its results as a NumPy .npy dataset (a named bundle of labeled, unit-bearing arrays — the DataSet / DataCube model). You can plot it in circuitRF or load it straight into Python/MATLAB. Export to .mat , Touchstone, .spl , and .lpcwave is built in."],[1,"organize","How a project is organized","circuitRF is a hierarchical, cell-based tool (like the commercial RF suites, not like a flat SPICE deck). The pieces: Library — a collection of cells you can reuse and reference. circuitRF can reference several libraries at once. Cell — a reusable building block with up to three views: a Symbol (the glyph drawn when the cell is instanced, and where its ports sit), a Schematic (the electrical contents), and a Layout (the physical artwork, drawn against a technology and simulated by the planar method-of-moments solver). A cell declares parameters that an instance can override — this is how hierarchy passes values top-down. Hierarchy — a schematic can instance other cells as sub-cells. Push into a sub-cell to edit it, pop back out; an edit to a cell affects every instance. TestBench — the thing you actually simulate: a top schematic plus its analyses and measurements. No project? No problem You don't have to build a cell library to try something. Use File → New Schematic to open a standalone schematic — a scratch sheet you can wire up and simulate immediately, with no workspace or cell structure. Save it into a workspace later if it's worth keeping."],[1,"workspace","The workspace window","A workspace is a folder holding a .cws file and a folder per cell — membership is the filesystem, so the Project panel is showing you what is on disk. Open one and you get the window below: documents in tabs down the middle, tool panels docked around them. The workspace window: a schematic open in the document area, the Project, Properties, Library and Messages panels around it, and a layout waiting in the second tab. Everything in it is movable. Drag a tab to reorder it, against an edge to split the area, or clear of the window to float it; drag a tool panel by its tab to re-dock it anywhere. View ▸ Hide Dockers ( Ctrl/⌘+Shift+H ) gives the whole window to the documents and back again, and View ▸ Reset Layout restores the arrangement you chose in Settings. The arrangement is saved into the .cws , so a workspace reopens the way you left it. Region by region, panel by panel: The Workspace ."],[1,"build","Build a schematic: components, wires, the pin grid","The schematic editor with the shipped FET S-parameter test bench open. The short version is below. The editor in full — the Library Palette, every toolbar button, the context menu, and setting up the analysis that runs the circuit — is The Schematic Editor . Place a component. Click a tile in the Library Palette (or drag it onto the canvas). A ghost follows the cursor; click to drop. The tool stays armed so you can place several. Press R to rotate the ghost, Esc to stop placing. Wire it up. Press W (or the Wire button) and click from one pin to another. Enter or double-click finishes a wire; Esc cancels it. Edit a value. Double-click a component's value label right on the schematic to edit it inline — type 50 Ω , 1.2 nH , 2 GHz and press Enter . (Double-clicking the body opens the full parameter editor.) The pin grid Connections are exact, not fuzzy. Every component pin, wire vertex, and junction lands on the connection grid (the coarse grid), and two things are connected only when they sit on the same grid point. A separate, finer authoring grid positions labels and annotations. Keep pins on the connection grid and wiring \"just works\"; a red marker flags an unconnected pin."],[1,"ppt","Pins vs. Ports vs. Terms","Three things sound similar but do different jobs. The distinction matters because it controls what becomes an external interface versus an excitation/measurement point: Concept What it is When you use it Pin An interface terminal of a cell . Pins on a cell's symbol are how the cell connects to the parent schematic that instances it. Connectivity only — no electrical model. Inside a cell you intend to reuse hierarchically, to expose its connection points. Term An S-parameter port termination — a numbered reference-impedance port (default 50 Ω). Each Term carries a Num (port index). On a test bench, to define the ports an S-parameter analysis measures between. Port The general term for an external connection point. In a cell a port is realized by a Pin ; in an S-parameter test bench a port is realized by a Term . Conceptually — \"this circuit is a 2-port.\" How you realize it depends on the context above. The two symbols that realise a port: Pin, a cell's connectivity-only interface terminal, and Term, a numbered S-parameter port termination. The Reference Guide has a fuller treatment with diagrams; for Quick Start: use Term to define S-parameter ports on a test bench, and Pin to expose a reusable cell's connections."],[1,"run","Run a simulation","Open Simulate → Setup Analyses… (or the Analyses panel) and add an analysis (e.g. S-Parameter , 1–10 GHz). For an HB example, add Harmonic Balance and drive the input with a P1Tone (available-power) source. Press Run ▶ . circuitRF extracts a netlist from the schematic, elaborates it, and runs the analysis on a background thread. The run writes results/<name>/run.npy . Open Data Displays that are already showing this result refresh automatically."],[1,"plot","See the result on the Data Display","Open a Data Display ( Ctrl/⌘+Shift+D ) and add a plot — Rectangular, Smith, Polar, or Table. In the trace card, pick the data source (your run), then the signal — e.g. S(2,1) . On a rectangular plot choose a transform such as dB20 ; on a Smith chart the complex value plots directly. For loadpull, choose the loadpull run and a contour metric (Pout, PAE). The optimum (max-power / max-efficiency) markers and interactive markers read values off the contour surface. A rectangular plot of the shipped FET test bench's S-parameters, 1-10 GHz."],[1,"cli","Headless / command line","The engine runs without the GUI — useful for scripting and batch sweeps. From a .cnl netlist (a human-readable circuit description): $ circuitrf sparam mycircuit.cnl --freq 1GHz:3GHz:50MHz -o mycircuit.s2p This reads the netlist, runs the S-parameter analysis, and writes a Touchstone file. (Make sure the circuit's port count matches the extension, or omit -o to let circuitRF name it .sNp automatically.) Every analysis runs this way, not just S-parameters — dc , hb (harmonic balance), lp and lpp (loadpull and pursuit), and em , which runs an EM setup and writes the same files the Simulate button writes. Results go to stdout and progress to stderr, so a long run can be piped and watched at the same time. The full reference is The Command Line . Next Ready to draw something for real? The Schematic Editor covers placing, wiring and analysis setup in full, and Simulations covers every analysis type. New to circuit simulators in general? The New User's Guide starts from first principles. Need exact parameters, algorithms, or the netlist format? See the Reference Guide ."],[2,"","","Never used a circuit simulator before? Perfect — this guide starts from the very beginning and gets you to your first working simulation. No prior EDA experience assumed, and no question is too basic."],[2,"why","1 · Why simulate? And what circuitRF does","Building RF hardware is expensive and slow. A circuit simulator lets you predict how a design behaves before you build it — and lets you ask \"what if?\" a hundred times in an afternoon. circuitRF answers questions like: How much does this amplifier gain at 2 GHz? How much power does it put out before it saturates? How efficient is it? What load impedance makes it happiest? circuitRF is built specifically for RF and microwave work. You'll hear it's \"not a SPICE simulator\" — that just means it focuses on the questions RF designers ask (frequency response, gain, efficiency, intermodulation, loadpull) rather than the time-domain waveforms a SPICE tool chases. If those RF words are new, don't worry — you'll meet them gently below. The fastest way in Open circuitRF, choose File → New Schematic , and you have a blank sheet to drop parts onto and simulate — no setup, no project to create first. Everything below builds on that."],[2,"window","2 · The circuitRF window","Here is what you will be looking at. The middle of the window is the document area and holds your documents — schematics, layouts, plots — one per tab. Around them sit the tool panels : the Project panel on the left is the folder you are working in, the Library on the right is every part you can place, Properties shows whatever you have selected, and Messages along the bottom is the application telling you what it did. The workspace window: a schematic open in the document area, the Project, Properties, Library and Messages panels around it, and a layout waiting in the second tab. Every region of that window, numbered and named: The Workspace ▸ The regions of the window . The folder behind it is called a workspace . It is an ordinary folder on disk with a .cws file in it and one sub-folder per cell — nothing hidden, nothing in a database — which is why the Project panel and your file browser always agree with each other. Move anything, and put it all back Every panel and every tab can be dragged somewhere else — tabbed together, docked against an edge, or pulled out into a window of its own — and where you left them is remembered per workspace. If it ever gets away from you, View ▸ Reset Layout puts everything back. See Moving, hiding and resetting the layout ."],[2,"library","3 · The Library: your box of parts","Every part you can place lives in the Library , shown as the Library Palette — a panel of tiles, each a component. Resistors, capacitors, inductors, sources, transistors, transmission lines, and more. You can filter by category or type in the search box (try \"cap\" or \"tline\"). To place one, click its tile and then click on the canvas (or drag the tile onto the canvas). A library is also where reusable building blocks you create yourself live — which brings us to cells."],[2,"cells","4 · Cells — symbol, schematic and layout","A cell is circuitRF's reusable building block. Think of it like a chip you can drop into a bigger design. A cell has up to three views — three ways of looking at the same block: A Symbol — the little glyph you see when you place the cell, and where its connection points (pins) sit. It's how the cell looks from the outside . A Schematic — the actual circuitry inside the cell: the components and wires that make it work. A Layout — the physical artwork: the copper, the substrate stackup it sits on, and the vias through it. It is drawn to real dimensions against a technology (a PCB or MMIC process), and it is what the electromagnetic solver simulates when you want the parasitics of the real thing rather than the ideal components the schematic names. Why a symbol and a schematic? Because the symbol is the convenient outside view and the schematic is the detailed inside view. When you use an amplifier cell ten times in a design, you want to see ten tidy amplifier symbols — not ten copies of the full transistor-level schematic cluttering your screen. The symbol hides the detail; the schematic holds it. And why a layout as well? Because above a couple of gigahertz the wiring stops being wiring. A 90° bend, a length of line and the gap between two traces all have electrical behaviour of their own, and none of it is in the schematic. The layout is where that behaviour comes from — you draw the artwork, and the planar method-of-moments solver turns it into S-parameters you can put back into the circuit. A cell does not need all three. A test bench is usually schematic-only; a piece of artwork imported from GDSII may be layout-only. The two are kept in step in either direction by Design ▸ Update Layout from Schematic and Update Schematic from Layout , which reconcile the components in one against the artwork in the other."],[2,"hierarchy","5 · Hierarchy: circuits inside circuits","Because a cell can contain other cells, designs are hierarchical — circuits nested inside circuits, as deep as you like. A two-stage amplifier might be one cell that contains two \"single-stage amplifier\" cells, each of which contains a transistor cell and a matching-network cell. To look inside a cell, push into it (open its schematic); when you're done, pop back out . Editing a cell changes every place it's used — fix the amplifier once, and all ten instances update. That reuse is the whole point of hierarchy: build a thing once, use it everywhere, maintain it in one place."],[2,"params","6 · Parameters & editing values","A parameter is a value you can change — a resistor's resistance, an inductor's inductance, a source's power. Cells can declare their own parameters too (say, an amplifier cell with a Gain parameter), and when you place that cell you can override the parameter for that one instance. Values flow top-down : the parent sets a value, the cell uses it. (circuitRF also catches circular definitions and tells you, so you can't tie yourself in knots.) Just double-click it The quickest way to change a value: double-click the value label right on the schematic and type the new one — 50 Ω , 1.2 nH , 2 GHz — then press Enter . The inline editor accepts units directly. (Double-click the component body for the full parameter editor with every setting.) Double-clicking a value label edits it in place: a 50 ohm resistor with the inline editor open on R."],[2,"ppt","7 · Pins, Ports, and Terms","These three sound alike but do different jobs, and keeping them straight saves confusion later: A Pin is a cell's connection point — how a reusable cell plugs into the design around it. (Pins live on a cell's symbol.) A Term is an S-parameter port : a numbered, reference-impedance (usually 50 Ω) connection where the simulator measures a network. You place Terms to say \"measure the S-parameters between here and here.\" A Port is the general idea of an external connection. Inside a cell, a port is a Pin ; on an S-parameter test bench, a port is a Term . Why it matters: a Pin just exposes a connection for reuse; a Term actively defines where and how the simulator excites and measures. Use the wrong one and you'll either have no measurement port or an unintended source impedance. The Reference Guide shows each with pictures. The two symbols that realise a port: Pin, a cell's connectivity-only interface terminal, and Term, a numbered S-parameter port termination."],[2,"sims","8 · The simulation types","Different questions need different analyses. circuitRF offers four big ones: Analysis Answers… Reach for it when… DC What are the steady voltages and currents with no signal applied? You want the bias point — and it's the foundation HB builds on. S-parameters How does this network respond, frequency by frequency, at low signal levels? Gain, matching, filters, isolation — anything linear vs. frequency. Harmonic Balance How does it behave when driven hard, where it's nonlinear? Power amplifiers, mixers — output power, compression, efficiency, intermodulation. Loadpull Which load impedance gives the best power/efficiency? Tuning a power amplifier's output match. (See §11.) You don't have to choose perfectly up front — start with S-parameters to see a network's frequency response, then move to harmonic balance when you want to drive it into nonlinear territory."],[2,"display","9 · The Data Display & plot types","Results appear in the Data Display , where you choose how to view them. Why several plot types? Because RF data comes in different flavors: Rectangular — the familiar X–Y graph (e.g. gain in dB vs. frequency). For real, scalar numbers. Smith chart — a special circular chart for impedance and reflection . If you've not seen one: it maps every possible impedance onto a disk, which makes matching networks and reflection coefficients intuitive to read. It's the RF engineer's favorite chart. Polar — magnitude-and-angle on a circular plot, for complex quantities where you care about phase. Table — the numbers themselves, in a grid. Why are some results \"complex\"? An S-parameter like S21 isn't just a size — it has a magnitude and a phase (how big, and how shifted in time). Mathematically that's a complex number . Smith and polar plots show complex values directly; on a rectangular plot you pick what to show — magnitude in dB, phase in degrees, real part, etc. You can change that complex-number format per trace, and in a Table you can switch how complex values are written (magnitude/angle, real/imag, dB/angle)."],[2,"examples","10 · Two worked examples",""],[2,"a.the-simplest-possible-dc-simulation","A. The simplest possible DC simulation","New Schematic. Place a Vdc source and a Resistor ; place a Ground . Wire the source across the resistor, and connect the bottom to ground. Set the source to 10 V and the resistor to 100 Ω (double-click each value). Add a DC analysis in the Analyses panel and press Run . Open a Data Display → Table to read the node voltage and current. (Ohm's law says 0.1 A — a good first sanity check that everything's wired right.) The New User's Guide's first worked example: 10 V across 100 ohms, with a DC analysis."],[2,"b.a-first-s-parameter-simulation","B. A first S-parameter simulation","Place two Term s (they auto-number 1 and 2) and a small network between them — for example a series inductor and a shunt capacitor, or a TLIN transmission line. Add an S-Parameter analysis over, say, 1–5 GHz. Run, then open a Data Display. On a Rectangular plot, add S(2,1) with the dB20 transform to see insertion loss vs. frequency; on a Smith chart , add S(1,1) to see the input match. Build the schematic below and you should get the response beside it — a series 2 nH and a shunt 0.8 pF between two 50 Ω Terms, swept 1–5 GHz. The second worked example: a series 2 nH and a shunt 0.8 pF between two 50 ohm Terms. What that schematic produces: S(2,1) in dB against frequency, 1-5 GHz."],[2,"loadpull","11 · Loadpull, contours & Pursuit","For a power amplifier, the load impedance you present to the transistor's output dramatically changes its power and efficiency. Loadpull finds the sweet spot: circuitRF sweeps the load reflection coefficient over a grid of points on the Smith chart, runs a harmonic-balance simulation at each, and draws contours — like a topographic map where the \"hills\" are high output power or high efficiency. You read off the impedance at the peak and design your output match toward it. Two things make this delightful in circuitRF: Measured data plots exactly like simulated data. A loadpull file from the lab (Touchstone, .spl , or .lpcwave ) overlays on the same contour plot as your simulation — perfect for checking model against measurement. Loadpull Pursuit saves you time. Instead of you choosing the grid and hunting for the optimum by eye, Pursuit automatically searches for the terminations that maximize your chosen metric (peak power, peak efficiency), homing in on the useful region of the Smith chart for you."],[2,"match","12 · Matching networks: the Match component","Sooner or later you will want to get from the impedance your transistor presents to the 50 Ω the rest of the world runs at. circuitRF has a component that designs the network for you: place a Match , double-click it, and the Match Designer opens. One thing to know before you open it, because it is the thing every experienced RF engineer trips over: Match is a matching tool, but it works over a band If you have used a single-frequency matching tool — the kind where you give it a target impedance and it hands back two parts — Match will look unfamiliar. It asks for a band , an order and a ripple , which is filter vocabulary. That is not a detour: a network that matches over a band is a filter, so those three words are simply how you say how wide and how well . You are still designing a match. Two things follow. You get more parts than a two-element L-match — but some of them are your device's own capacitance, absorbed into the network rather than bought. And parts that run to ground are normal : a ladder alternates series arms and shunt arms, and a shunt arm goes to ground by definition. There is also one button worth reading about before you press it. Shunt DC Block is not the series blocking capacitor you may be expecting in the signal path — it stops a shunt inductor from shorting your bias supply to ground, so the capacitors it adds appear in the arms that run to ground. The reference chapter explains it, and the tooltip on the button says the same thing in two sentences. The full story — what absorption buys you, how to read the solutions list, and the two worked amplifier examples — is in The Match Component . Ready for specifics? The Reference Guide documents every component, every analysis setting, and the plot types in detail. Already comfortable with simulators? The Quick Start is the fast path."],[3,"","","The complete technical reference. Plain and direct — look things up here. The Reference Guide is every chapter below. They are in one reading order, and every page carries Previous and Next links at its foot, so the whole guide can be read straight through as well as dipped into."],[3,"where-to-start","Where to start","If you are new to circuitRF Read Units and Grid & Connectivity first. Between them they cover the two conventions that are circuitRF's own rather than the industry's — how a coordinate is stored versus displayed, and what actually decides that two things are connected. Almost every early surprise is one of those two. Four chapters that are easy to miss Three of these are tools in their own right rather than components with a dialog, and the fourth is a whole class of parts. The Match Component synthesises a bandpass matching network that absorbs both terminations — closed-form, no optimiser. harmonicaRF runs harmonic load-pull on one device while you drag a marker. wBond models bondwire arrays as geometry rather than as a number you looked up. And System Components covers the ideal blocks a system block diagram is drawn out of — circulator, coupler, hybrid, balun, switch, amplifier, attenuator, filter, duplexer and mixer — including where passive intermodulation comes from and what \"ideal\" costs you."],[4,"","","The window everything else happens in — documents in the middle, tool panels around them, and a folder on disk behind it all. Every other chapter in this guide describes something you do inside one window. This chapter is about the window: what the panels are, how to move them, and what a workspace actually is once you close the application."],[4,"regions","The regions of the window","The same window with each region numbered. # Region What it is for 1 Menu bar Every command in the application. On macOS these are in the system menu bar at the top of the screen instead of in the window. 2 Toolbar The commands used most often: new, open and save, cut/copy/paste, undo/redo, run and stop, and the three buttons that show and hide the tool panels. 3 Project panel The workspace on disk: its cells, and the schematic, symbol and layout views inside each one. Double-click a view to open it. 4 Properties panel The parameters of whatever is selected in the document, editable in place. The Analyses tab beside it holds the analyses the open test bench will run. 5 Document area The documents you have open, one per tab — schematics, symbols, layouts, data displays. Tabs can be split, or dragged out into a window of their own. 6 Library panel Every component you can place, filtered by category and searchable. Click a tile to arm it, then click on the canvas to drop it. 7 Messages panel What the application did and what it thinks of it: files opened, netlists elaborated, runs finished, and any warning or error, each linking to what it is about. Where the menu bar is on macOS The figure shows the menu bar inside the window, which is where Windows and Linux put it. On macOS the same menus are in the system menu bar at the top of the screen, and the window starts at the toolbar. Nothing else differs — the commands, their order and their shortcuts are the same, with ⌘ in place of Ctrl ."],[4,"workspace","What a workspace is","A workspace is a folder. Its name is the folder's name, and it holds a .cws file plus a folder per cell . Membership is the filesystem itself: the Project panel shows you what is in the folder, so copying a cell folder in with the Finder or Explorer puts that cell in the workspace, and there is no index to repair afterwards. The .cws records configuration, never content — referenced libraries, bookmarked \"Known Files\", the default technology and assembly rules, and the other projects and kits this one uses. A cell is referenced, not embedded, so the same cell or library can belong to several workspaces at once. The full on-disk layout, file type by file type, is in File Formats . The things that describe your view of the project rather than the project itself — the panel arrangement, which documents were open, which tree categories you expanded, the colour theme — are kept in a second file beside it, the .cwsuser . It is optional: a workspace with no .cwsuser opens on the default arrangement and nothing is wrong. It is also the one file you can safely delete to get your panels back if they end up somewhere unusable. See Your own view of a workspace . Two consequences worth knowing early: A workspace is version-controllable. It is text files in folders; nothing is hidden in a binary project database. Moving a workspace is moving a folder. To send one to somebody else, use File ▸ Archive Workspace… , which additionally pulls in the things it references from outside the folder — libraries, technologies, optionally kits and results — and repoints the references at the copies, so the archive opens on a machine that has none of them."],[4,"other-workspaces","Using cells from another workspace","A cell does not have to live in the workspace you are working in. Two commands bring one in, and they answer different questions. File ▸ Reference Workspace… points this workspace at another one and gives it a short alias . It appears in the Project panel as one row at the top level, carrying a network-folder icon, and all of its cells are inside it ready to place. Nothing is copied: the cells stay where they are, and when their owner edits one, your design picks up the change the next time it draws — no restart, and nothing to re-import. A referenced workspace is re-read when you open your workspace, when you expand its branch in the Project panel, and when you press Refresh there — not every time you switch back to the window. So if a colleague has just added a cell to the shared library, press Refresh to see it. This keeps the application responsive when the library is at the far end of a slow network link. File ▸ Add Cell to Workspace… takes a single cell from another project — as does dragging a cell from one workspace window onto another. Either way you are asked the same question: Copy the cell in. You get your own independent copy. Later changes on the other side do not reach you, and yours do not reach them. If the cell places cells of its own, you are asked whether those come along as copies too or stay referenced where they are. Reference the cell where it is. That cell — and only that cell — appears in your Project panel as one row at the top level, carrying the same network icon a referenced workspace does, and is instanced from there. One master, many users. The rest of the other project's cells do not come with it. Its own sub-cells are always referenced with it — a referenced cell is the other project's, all the way down. Right-click the row and choose Remove Cell Reference to stop listing it; nothing is deleted. Which to choose Reference when the cell belongs to somebody else and should stay theirs — a company standard-parts library on a network share, a colleague's verified amplifier block, a footprint set maintained by one person for the whole team. Everyone gets the librarian's corrections automatically, and nobody has a private copy that has quietly drifted. Copy when you are about to change it, when you want the project to be self-contained, or when you are branching off a known-good design to try something. A copy is yours to break. Referencing a cell needs both workspaces on the same technology . A cell drawn for a different stack-up would place and be silently wrong, so when the two disagree the Reference option is offered greyed out with the reason beside it, and Copy is what you are left with. (Adding the reference to the workspace is not blocked — it costs nothing until a cell is actually placed — but circuitRF warns you at that point.) You are also told before copying if the cell uses parts from a kit this workspace has not imported. Because a reference is written as alias + cell name rather than as a path, moving the shared library later is one edit in one place, not a hunt through every document that used it. If a reference does break — you removed it, or the other project moved — every instance that placed that cell draws as Not Found . Right-click one, in the schematic or the layout editor, and choose Re-reference Cell… . It looks for the cell first: in this workspace, in the ones open in other windows, in the ones you already reference, and in your recent list. If it finds it, the reference is put back — usually without changing your document at all, because the alias goes back under the name it had. If it finds nothing, or two cells of that name, it asks you to point at the cell's folder. Either way the repair covers every instance of that cell in the document, in one undo step. Referencing a single cell still records that alias — it is how the cell is addressed — but the alias itself draws no row, so one referenced cell is one row. Run File ▸ Reference Workspace… on the same project later"],[4,"editing-referenced-cells","Fixing a cell in a library you reference","(This is the maintainer's side. If you only use a library somebody else looks after, the section you want is using a library another team maintains .) Referenced cells are read-only from here. You can open one, read it, push into its hierarchy and pull it about to understand it — but the first time you try to change something, circuitRF stops you and says why. The refusal comes with a button: Open ‹library› , which opens that workspace in a window of its own and takes you straight to the cell you were trying to edit. Edit it there and save normally. That is not circuitRF being fussy about a file it could perfectly well write. It is the only point in the sequence where you can still do something sensible about it. Editing a library cell in place works — the file is written, your simulation picks it up, and everything looks right: the fix reaches you and nobody else ; every colleague goes on using the cell as it was; the library's owner was never asked, and has no idea; the next time they publish, your edit is overwritten, or it clashes and there is nothing to merge it back from. Every step of that is silent. One refusal, at the moment you type, replaces the lot. The cell stays in the other workspace, and that is the point. Opening the library as a workspace of its own means the corrected cell is the library's corrected cell — everyone referencing it gets it, and the person who maintains it can see what changed. Your own workspace records nothing about the edit, because the cell was never yours. A cell is edited in one window only. If the library is already open in another window, the edit does not become possible here — that window comes forward with the cell in front of you instead, and this window closes its read-only view of it. Two editors over one file would mean two sets of unsaved changes over the same file, and whichever you saved second would silently throw the other away. If you really do maintain the library Right-click the referenced workspace's row in the Project panel and choose Allow Editing Through This Reference… . circuitRF asks once, then lets you edit its cells from here. The row is marked with a pencil afterwards, and so is the tab of every document you open from it, because writing into somebody else's project is not something to have to remember. Two people doing this in one library at the same time is not arbitrated — the same last-save-wins that applies to any shared folder. Choose Make Reference Read-Only on the same row to put it back. Existing workspaces are read-only too: a reference made before this existed is treated as read-only, because that is the safe reading and not the one you happened to have."],[4,"pencil-mark","The pencil on a document tab","Two open documents, one of them opened out of a workspace this one references with editing allowed. The pencil beside its name says that saving it writes into that other project. A tab with no pencil is an ordinary document of this workspace. The pencil appears beside a document's name when that document lives in another workspace that you have allowed editing through . It answers one question, and only that one: if I press Save, whose project does this get written into? A tab with no pencil is a document of the workspace you have open. You have not done anything wrong. The mark is not a warning and there is no error behind it. It is there because allowing editing through a reference is a deliberate, unusual choice somebody made once — often weeks ago, possibly not by you — and the moment it matters is the moment you are about to save, which is exactly when nothing on screen would otherwise mention it. A few things it is worth knowing: It never turns green, and there is no second state. The mark is either there or it is not. Green would imply a check that had passed, and nothing is being checked. It is not about unsaved changes. That is the bullet in the window title, and the two are independent. It does not depend on whether the other workspace is open. It describes the relationship between the two projects, which stays true whatever windows you have. To make it go away , right-click that workspace's row in the Project panel and choose Make Reference Read-Only . The reference keeps working; you simply read through it again, which is the default for every reference. The same pencil appears on the referenced workspace's own row in the Project panel, which is where you can act on it."],[4,"library-team","Using a library another team maintains","This is the arrangement most RF groups end up with: one person, or one team, maintains a set of verified cells, and everybody else references them. This section is the consumer's side of it. The maintainer's side is fixing a cell in a library you reference above, and the two are worth reading together."],[4,"copying-a-library","Getting the library onto your machine","If the library lives on a share you can already reach, File ▸ Reference Workspace… is all you need. If instead you were given an address — the library is kept somewhere central and handed out rather than sat on a share — use File ▸ Clone Workspace… . Paste the address, say which folder to put it in, and press Clone. What arrives is an ordinary workspace: open it, read it, reference its cells from your own designs. Two things are worth knowing before you press the button: circuitRF never asks you for a sign-in and never stores one. It uses whatever your machine's git is already set up with — whatever your IT department gave you. If the address needs a sign-in that cannot be supplied, the copy stops and says what was wanted . It will not sit there waiting. Their restore points do not come with the copy. Every version the library's author deliberately kept does come, and that is what you would ever want to look at. Their automatic restore points belong to the machine they were taken on. circuitRF starts a safety net of your own the first time it has something to record here. Once a workspace has been cloned this way, File ▸ Pull Changes brings down what is new on the copy it came from and lists it at the top of the History panel — marked, and applied to your files only if you choose one. File ▸ Push Changes sends the versions you have kept back, if you are allowed to. Neither happens by itself: circuitRF never contacts anything without being asked."],[4,"pinning","\"I want my design to keep using the version I tested against\"","By default a referenced workspace is a live link. The librarian corrects a cell, and the next time your design draws, it uses the corrected cell. That is usually what you want — it is the reason you referenced the library instead of copying its cells. It is not what you want on a design that has been signed off. Right-click the library's row in the Project panel and choose Use a Fixed Version… . From then on your design uses that version of the library and nothing else. What this actually buys you It is the difference between \"this design uses the amplifier from the shared library\" and \"this design uses the version of it we measured \". The first sentence is not reproducible: it means whatever the library contains on the day somebody opens the design. The second one is. It is also what makes going back to an earlier state complete — see below. The librarian carries on as before. They publish whenever they like and nothing in your workspace changes. When they publish something newer, the library's row in your Project panel says so, and there is a Take the Newer Version… item on it. Nothing is taken until you take it. There is no prompt and no notification that interrupts you. Taking the newer version is a change to your workspace , so it goes into your history with today's date. Six weeks later, when something has stopped working, \"when did this design start using the new library?\" is a question you can actually answer."],[4,"pinning-surprise","The part that surprises people","With a fixed version, editing a cell in the library and coming back to your design does not show the change. That is not a bug and it is not a refresh problem — it is the whole point. The design goes on matching what it was verified against, and it tells you a newer version is available instead. If you are the person maintaining the library and the person using it, this will catch you out at least once. Choose Follow the Newest Version on the library's row while you are working on it, and fix a version again when you are finished. A reference with a fixed version is always read-only from your workspace, even if you had allowed editing through it. What you would be editing is circuitRF's copy of that one version — not the library — so the change would reach nobody and would not survive."],[4,"pinning-restore","Going back, with the library included","This is the reason the feature is worth the bother. Restore points and versions record your workspace — which now includes which version of the library it uses . So going back to last Tuesday brings back last Tuesday's files and last Tuesday's library. Without that, a restore hands you your own files back and silently keeps today's library, which is a state nobody asked for and nothing tells you about."],[4,"pinning-unreachable","When the version cannot be reached","A fixed version is a name, not a copy. The cells still live in the other workspace. If that workspace is moved away, or its owner rewrites its history so the version you named no longer exists, circuitRF says so plainly and the cells stop resolving. It deliberately does not fall back to whatever the library contains now. Falling back would look like everything was fine while your design quietly used content it had never been checked against, which is precisely what fixing a version exists to prevent. If you need the library's content to travel with your design — to a customer, to an archive, to a machine that will never reach the library — that is a workspace archive , which is a different tool for a different problem."],[4,"shared","Sharing a workspace with other people","Put a workspace on a network share and several people can reach it at once. What that means depends on whether they can write to it. (That is separate from a share whose permissions stop you writing, below. A referenced workspace is read-only whether or not the folder is — the two reasons stack, and Save says which one applies.) A read-only share is the intended shape, and everything still works. If the folder's permissions allow you to read but not write — the usual arrangement for a company library, where one librarian maintains the masters — circuitRF notices when you open it and says so once. Any number of people can have it open at the same time with nothing to arbitrate. You can browse it, open its schematics, push into a hierarchy, read a cell's parameters and reference its cells from your own designs. You can even edit a document to understand it. What you cannot do is save on top of it: Save is greyed out with the reason shown, and Save As puts your version into a workspace you own. If two people open a writable workspace at the same time, circuitRF tells you. The second person to open it sees a notice naming who has it open and on which machine, and chooses: Open read-only — browse and read it without writing anything back. The safe answer, and the default. Open anyway — work normally, knowing that if you both save, the last save wins. Cancel — leave it alone. It is a notice, not a lock circuitRF cannot reliably tell across a network share whether somebody is still in there, so it never blocks you — a lock it could not clear would be worse than the problem. A session that crashed on your own machine is recognised straight away and says nothing; one left behind on another machine is ignored after several hours. The thing genuinely at risk is not your schematics — those are separate files, and a clash is visible. It is the workspace's own settings: the panel arrangement, the open-document list and the list of referenced workspaces. Two people saving those overwrite each other silently, which is why the notice exists. For a team, the arrangement that avoids the question entirely is one shared library workspace, read-only to everyone but its maintainer, referenced by each engineer's own project. Everybody reads the same masters, nobody can damage them, and each person's own work lives in a workspace only they open."],[4,"documents","Documents and tabs","The middle of the window is the document area , and everything you open lands there as a tab: schematics, symbols, layouts , data displays , technologies, EM setups , and the tool documents harmonicaRF and wBond . A fresh workspace opens on a single Welcome tab, which is a placeholder and closes like any other. Opening a document is a double-click in the Project panel. A cell can hold three views — schematic, symbol and layout — and each opens as its own tab, so the schematic and the layout of the same cell are two tabs you can put side by side. Tabs are rearrangeable, splittable and detachable: drag one along the strip to reorder it, drop it against an edge of the document area to split the area in two, or drag it clear of the window to give it a window of its own. A detached document is still part of the workspace — it saves, runs and undoes exactly as it did while docked — and where you left it is recorded in the .cwsuser ."],[4,"panels","The tool panels","Nine panels can be on screen; View ▸ Panels lists all of them, and the four used most often have toolbar buttons of their own. Panel Shows Project The workspace's cells and their views, its technologies and its libraries. Double-click to open; right-click for the actions on a cell. Library Every component you can place, by category and searchable. Click a tile to arm it, then click on the canvas to drop it. See Components . Properties The parameters of the current selection, editable in place. What it shows depends on what is selected — a component, a wire, a layout shape, a bond-wire array. Analyses The analyses the open test bench will run, and the Run button that runs them. See Simulations . Messages What the application did, with warnings and errors. Each message links back to the file or object it is about. DRC Design-rule violations from the last check, each one selectable in the layout it came from. History Everything circuitRF has kept of this workspace — the states it kept for you and the versions you kept on purpose — with a filter and a search. See History . Wire Profile Bond wires seen from the side — loop height and span. See wBond . Array Inductance The inductance computed for the selected bond-wire array. The toolbar's panel buttons are toggles , not \"open it\" buttons: press once to bring the panel back where you last had it in this workspace, press again to close it. The menu items under View ▸ Panels only ever show a panel — a menu item named after a panel must not close it."],[4,"docking","Moving, hiding and resetting the layout","Tool panels dock the same way documents do. Drag a panel by its tab: onto another panel to tab them together, against an edge to give it a column or a row of its own, or out of the window to float it. Drag a splitter to change the proportions. All of it is saved into the .cwsuser beside the .cws , so a workspace reopens arranged the way you left it — and deleting that one file is how you get the default arrangement back if it ever ends up somewhere you cannot use. Three commands cover the rest: View ▸ Hide Dockers ( Ctrl/⌘+Shift+H ) closes every tool panel and gives the whole window to the documents. Pressing it again puts them all back exactly where they were — it is a toggle, not a close. View ▸ Reset Layout returns to the arrangement Settings ▸ On Launch ▸ Window Layout names. That setting is the only place an arrangement is chosen, and it offers three: Project Tree Focus and Library Focus (Project and Library tabbed together on the left, differing only in which tab is on top) and Project Tree & Library — Project on the left, the Library in its own column to the right of the documents. The last is the shipped default and is what the figure above shows. Fit Windows to Frame on the toolbar pulls every floating window back into the workspace."],[4,"several","Switching workspaces, and working without one","File ▸ New Workspace… ( Ctrl/⌘+N ) and File ▸ Open Workspace… ( Ctrl/⌘+O ) both work in place : the window you are in becomes that workspace. Anything unsaved is offered to you first, the dock layout is rebuilt from your Window Layout setting, and the documents the incoming workspace's .cwsuser had open are reopened. File ▸ Open Recent is the same operation against a list of the last few. You do not need a workspace at all to get started. File ▸ New Schematic opens a scratch sheet that belongs to no cell and no folder; wire it up and simulate it immediately. If it turns out to be worth keeping, File ▸ Save Schematic As… puts it into a workspace as a cell. With no workspace open the Project panel lists the workspaces you had open recently instead of a tree, so getting back to one is a single click."],[5,"","","Every tab of the circuitRF Settings dialog, control by control — what each one changes, when it takes effect, and which of them are shared with harmonicaRF and wBond. Settings are per user, not per workspace , with one deliberate exception noted below. A workspace someone sends you cannot change how your copy of circuitRF behaves — that is the reason several of these controls exist here rather than in the document that uses them. The one setting that does travel Keep a history of this workspace , on the Revision Control tab, is stored with the workspace and not with your preferences. It has to be: an application-wide switch would be right for the first workspace you open and silently wrong for the second. So that one row travels with a copy of the workspace, and it is the only thing on this dialog that does. Everything else here is yours."],[5,"opening","Opening it, and when a change takes effect","File ▸ Settings… , or Ctrl , . On macOS it is in the application menu instead — circuitRF ▸ Settings… , ⌘ , — where that platform's users look for it. The dialog is not modal . It stays open while you carry on working, so you can change a setting and watch what it does without closing anything. Every tab but Color Theme writes immediately A combo box, a checkbox or a number on the General , Security & Permissions , Revision Control and Wirebonds tabs is saved the moment you change it. There is no \"apply\" step, and Cancel does not undo it — Cancel and Revert act on the colour editor only, which is the one tab that edits a live document-like thing you might want to abandon. Most settings apply to the next thing you do: a launch setting takes effect at the next launch, a wirebond default applies to the next wire you draw. The two that are live are the message timestamp format , which re-renders the Messages panel as you change it, and every colour role , which repaints the application as you drag the slider."],[5,"general","General","Settings, General: what circuitRF does on launch, how a copied picture is coloured, whether an export is design-rule checked first, and how the Messages panel stamps its lines. Help at the leading edge of the footer opens this chapter."],[5,"on-launch","On Launch","Control What it does Action What circuitRF does with no file to open: show the Welcome screen, or go straight to a new Schematic, Workspace, Data Display, Symbol or Layout, open a workspace, or start harmonicaRF. The default is Welcome . Window Layout The dock arrangement the shell opens with — Project Tree Focus and Library Focus tab the two panels together on the left and differ only in which tab is on top; Project Tree & Library (the default) puts the Project Tree on the left and the Library in its own column to the right of the documents. Show Dockers Whether the tool panels are open at launch and when a new workspace is created. Turn it off and they start collapsed, exactly as View ▸ Hide Dockers collapses them. Window Layout is also what Reset Layout resets to View ▸ Reset Layout deliberately offers no choices of its own — it restores whatever this setting names, so there is one place a layout is chosen and not two that can disagree. The panels themselves are described in the workspace chapter ."],[5,"copy-export","Copy / Export","Control What it does Copy color The light/dark variant a schematic is rendered in when you copy it to the clipboard: Follow System (the variant the application is currently showing), Force Light , or Force Dark . Pasting a dark schematic into a white document is the case this exists for. Transparent background Whether the copied picture carries its background or leaves it clear. On by default, so a pasted schematic takes the colour of whatever it lands on."],[5,"design-rules","Design Rules","Check design rules before exporting runs a design-rule check before writing GDSII, DXF or Gerber. It is on by default and it is not a gate: a clean design exports with no interruption, and when there are violations they are listed first and you can still go ahead. Catching a spacing error before it reaches a fabricator is most of what DRC is worth, and a check you have to remember to run by hand is one you will forget before the export that mattered. The rules themselves, and the panel that lists the violations, are in the layout editor chapter ."],[5,"messages","Messages","Timestamps — how the Messages panel stamps each line: Time , Date + Time , or Hidden . This one is live; the panel re-renders as you change it."],[5,"security","Security & Permissions","Settings, Security and Permissions: everything that decides what circuitRF is allowed to run or to fetch - a kit's artwork scripts, a kit's device worker, automatic updates, and the Verilog-A compiler. Each control carries its explanation as a tooltip. This tab answers one question: what is circuitRF allowed to run, and what is it allowed to fetch? Everything with that shape is collected here rather than living in whichever tab it happened to arrive in, so that a user auditing what this binary may do has one place to look. Each control carries its explanation as a tooltip — hover it to read what it governs."],[5,"external-pdks-generated-artwork","External PDKs — Generated Artwork","A kit can ship generator scripts that draw the artwork for its parameterised cells, and circuitRF asks before running any of them. The answer is remembered per kit, keyed by that kit's directory on disk, so the prompt does not nag — and because a refusal is remembered too, there has to be a way back. Ask Again… forgets every remembered answer, and the line beside it says how many are held. The next workspace that uses a kit's generators asks about it again from scratch. Trust never travels with a workspace These answers live in this installation's preferences and are deliberately absent from the .cws . A workspace arriving from somebody else with its scripts pre-marked trusted would run them on open with no prompt, which would defeat the question entirely. See the kit-authoring chapter for what a generator script is."],[5,"external-device-workers","External Device Workers","The other kind of program a kit can make circuitRF run. A kit may ship its own executable for evaluating its device models, and circuitRF starts it the first time a design uses one of that kit's parts. Allow kits to run their own device workers is on by default — every kit installed before this checkbox existed evaluates its devices through a worker, and shipping it off would have broken those workspaces silently at the next Run. Turn it off and those parts cannot be simulated; the rest of the design is unaffected, and a refusal is reported rather than swallowed. How a kit declares a worker, and what happens when one refuses, is in the kit-integration chapter . If your administrator has fixed this setting for the machine, the checkbox is disabled and a line underneath says so."],[5,"updates","Updates","Control What it does Automatic updates Downloads new versions in the background and installs them the next time the application is relaunched. Include beta releases Includes pre-release builds when looking for a new version. A sub-item of the box above, and disabled while it is off. Turning it off discards a staged beta; a staged stable version is left alone. Show release notes after an update Opens the release notes once, the first time a newly installed version is launched — never on a fresh installation, and never twice for the same version. If releases went out while the application was not launched, the versions you skipped are listed underneath , newest first, up to ten of them; a single update shows one set of notes as before. Not a sub-item of automatic updates, and deliberately not disabled with it: a version installed by hand is still a new version, and its notes are still worth reading. Last checked underneath is read from the updater's own state file and is never written here. It is the first thing to look at when wondering whether the feature is working at all."],[5,"verilog-a-compiler","Verilog-A Compiler","circuitRF loads compiled models ( .osdi ). Point a component at Verilog-A source ( .va ) instead and circuitRF builds it once with the compiler named here, caching the result until the source changes. Leave the box blank and the compiler on your PATH is used, which is what most machines want. The row is here for the machine that has two compilers, or has one somewhere PATH does not reach; a named compiler outranks PATH . Browse… picks one from disk, and Test runs it and reports what it says it is, in place, so you find out now rather than at the first simulation. circuitRF ships no compiler and links to none It starts the one you name as a separate process — the same arm's-length arrangement as building circuitRF itself with a C compiler. That is why this row belongs on the Security & Permissions tab at all: it names a program circuitRF is permitted to run . Which compiler to install, what it does with the source and where the built artefact is cached are all in the Verilog-A chapter ."],[5,"revision-control","Revision Control","Settings, Revision Control: which git circuitRF runs, who changes are attributed to, whether a history is kept for your workspaces and for this one, how long restore points are kept, and the two disk-space actions. Every control that reduces what is kept carries its consequence in a sentence beside it. circuitRF can keep a history of a workspace: the state of every file in it at points you can go back to. This tab is where you say whether it does, and what it keeps. Nothing here works without git circuitRF keeps a history by running git as a separate program. It bundles none and links to none. If your machine has no usable git, everything on this tab below the Git row is greyed out, a line says so, and no history is being kept for any workspace — whatever the switches appear to say. The Git row itself stays available, because it is how you fix that: name a git, or install one and press Detect . The rest of the tab comes to life as soon as Detect finds one. Elsewhere in circuitRF the feature is simply absent on a machine with no git — no restore-point list, no menu items, nothing. This tab is the one place it is shown and disabled instead, so that \"circuitRF is not keeping a history for me\" is something you can find out rather than something you assume."],[5,"git","Git","Leave the box blank and the git on your PATH is used, which is what most machines want. The row is here for the machine that has two of them, or has one somewhere PATH does not reach; a git named here outranks PATH . Browse… picks one from disk. Detect finds it and reports the path and the version it identifies itself as, in place. That is the answer to \"it says it can't find git\" without anyone needing to ask you for a log. It is also the one place circuitRF will tell you your git is too old — naming the version you have and the version it needs. Everywhere else, a git below that floor is simply treated as though it were not installed."],[5,"commit-identity","Commit Identity","Your name and email, recorded against every change circuitRF keeps. They are yours, not the workspace's : they apply to every workspace you open on this machine. circuitRF writes them into no git configuration file at all — not the workspace's, and not your own. Storing them with a workspace would mean that on a shared drive, the next person to open it would have their changes recorded under your name, and nobody would notice until they read a history and did not believe it. If you already use git and have set an identity, these boxes are filled in from it as a convenience. circuitRF only ever reads that setting; it never changes it. Both are needed before circuitRF starts keeping a history. The tab says so, rather than letting the first attempt fail."],[5,"what-is-kept","What Is Kept","Two switches, answering two different questions. Keep a history of my workspaces Do I want this at all? Your own preference, applying to every workspace. On by default. Keep a history of \" this workspace \" Not for this one. This workspace's own answer, which overrides the setting above for it alone . The second is stored with the workspace , which is why it travels: a copy of a workspace that was switched off arrives switched off, whatever the person opening it prefers. Turning either of these off deletes nothing Off means circuitRF stops writing . It does not remove anything it has already kept: every restore point stays listed and can still be restored, and turning it back on carries on where it left off. Nobody expects a checkbox to be irreversible, and this one is not. If you genuinely want a history gone , that is deleting one plainly named .git folder inside the workspace, with your file manager. circuitRF does not offer a button for it, on purpose. Doing it cannot harm your design — a workspace is ordinary files in a folder, and the history sits beside them rather than containing them. Delete it and the workspace opens exactly as it did, with every file present and current. A workspace is armed at the first moment there is something to record, never simply because you opened it. Looking at a colleague's workspace on a shared drive creates nothing in it."],[5,"restore-points","Restore Points","Keep them for N days is how long an automatic restore point survives. After that it is thinned : it is no longer offered, and the state it held is no longer something you can return to. Changes you recorded yourself are never thinned. Always keep at least N restore points is the rule that makes the one above safe. The newest N are kept however old they are . That matters more than it sounds: a computer's clock is not trustworthy — a flat battery, a bad time sync, or a machine that disagrees with itself about time zones can make everything look years old at once. The count floor means a wrong clock costs you nothing, which is why the field will not go below its minimum. Take a restore point when a workspace is closed is on by default. Closing is the one moment that reliably happens in every session, and it is behind you rather than in front of you. A session in which nothing was written records nothing. Take a restore point before an AI edit is always on and cannot be switched off. It is the reason the rest of this tab exists: an edit made on your behalf can span many files at once, and circuitRF records where you were first. It is shown rather than hidden so you never have to wonder whether it is happening."],[5,"disk-space","Disk Space","Most people never touch either of these. They are here because a history accumulates, and one day somebody asks where the disk went. The two answers are not the same answer. Both buttons name the workspace they act on — the one you have open. Neither opens a chooser, so the name is in the button rather than in a dialog you would have to reach to find out. With no workspace open they are greyed out and the line beneath says so. Compact above N MB, and the Compact button beside it, store the same history in less space. Nothing is discarded and nothing becomes unavailable. circuitRF does this on its own once a history passes the size named here. Reclaim after N days, and the Reclaim Space in… button beside it, are different, and this is the one destructive control in circuitRF's revision control . When a restore point is thinned its contents are not actually freed — circuitRF never destroys anything on its own, so a thinning you did not mean is recoverable. Reclaiming is you saying: those are really gone. What reclaiming destroys It permanently frees the states behind restore points that were already thinned , more than the number of days you name. After it, nobody can bring them back — not you, and not anybody you send the workspace to. Every restore point still listed, and every change you recorded yourself, survives it unchanged . Your design files are not touched. It asks first, and tells you how many states it would destroy. Nothing ever reclaims on a schedule — the number in the field does nothing on its own."],[5,"color-theme","Color Theme","Settings, Color Theme: every colour role the application paints with, edited one at a time in RGBA or hex, separately for the light and the dark variant. Every colour circuitRF paints with is a named role — schematic wire, layout metal, plot trace, selection, grid — and a theme is a value for each role, in each variant. Editing a role repaints the application immediately, so the picture you are judging is the real one. Control What it does Color Theme The theme to edit. The list is every theme circuitRF can find; edit a role and the selection reads Custom until you save. Light / Dark Which variant you are editing. A theme holds both , so switching here changes the values under the sliders, not the theme. It opens on whichever variant the application is currently rendering. Save Theme… Writes the current values out as a named theme and makes it the active one. Saving over the shipped Default is not possible — it is saved as Custom instead, so the shipped palette is always there to go back to. Role list Every role, each with a swatch of its current value. Select one to edit it; double-click one to open the colour picker instead of using the sliders. R / G / B / A sliders The selected role's colour, 0–255 per channel. The box at the right of each slider takes a typed number. # The same colour as hex — RRGGBB or RRGGBBAA . The path under the editor is the role's own name, which is what a .ccolor file records. Where a theme is looked for A .ccolor beside the workspace wins, then one in your per-user themes folder, then the palette built into the application. A workspace can therefore carry its own house colours without changing anything on your machine. Opening Settings from the macOS application menu when no workspace is open simply omits the first of the three. Which theme is active is yours, not the project's. The choice is recorded in the workspace's .cwsuser (see Your own view of a workspace ), so a workspace you receive from somebody else does not switch your theme — its .ccolor files travel with it and are there in the list, but you pick one."],[5,"wirebonds","Wirebonds","Settings, Wirebonds: the defaults a newly drawn wire is created with, and the built-in assembly rule for how close two wires may pass. These are creation defaults , per user. They describe how one shop's bonder is set up, so a .wBond arriving from somebody else must not change what the next wire you draw looks like. Existing wires keep whatever they were drawn with. Control Default What it does Points per wire 7 How many points a new wire's profile is built from — the resolution of the loop, not a mesh setting. Wire diameter 1 mil The wire gauge a new wire is created at. Shown in mil because that is the unit a bonder is specified in; stored in nanometres like every other wBond dimension. Wire material Gold Gold, Aluminium, Copper or Silver. Gold is both the RF packaging norm and the metal the 3D kernel was validated against. Wire z-height 4 mil Where both feet of a new wire land — a wire drawn in the layout view, and the wires a new wBond component is created with. Paste pitch 5 mil How far a pasted wire steps in +y to land clear of what is already there. It never re-spaces the wires on the clipboard. Zero and negative z are real values Wire z-height is the one preference here where \"absent\" and \"zero\" are different things: a foot at z = 0 lands on the reference plane and a negative one sits in a cavity below it, and both are geometry somebody bonds. The profile view's own wire tool ignores this setting entirely — there you click the height you want, which is the whole point of drawing in that view. Nudge steps stay at 1 mil and 5 mil and are not settings: they are bonder-process quantities, not display preferences."],[5,"design-rules-1","Design Rules","Wire clearance (default 0.5 mil) is circuitRF's own built-in assembly rule: how close two wires may pass, measured surface to surface between their outer edges. It is reported as an error by a design-rule check whether or not a .wasm assembly rule file is referenced — a house's rule file is not what makes overlapping metal invalid. Zero reports only wires that actually touch. A .wasm 's own spacing rules are checked as well as this one, never instead of it. Everything about the wires themselves — loop height, span, the array basis, the inductance the kernel computes and the S-parameters it exports — is in the wBond chapter ."],[5,"footer","The footer: Help, Revert, Cancel, Close","Help sits at the leading edge and opens this page. Everything that acts on the dialog is grouped at the trailing edge. Button What it does Revert Puts the colour editor back to the theme that was active when the dialog opened, without closing it. It does not undo anything on the other tabs — those were already saved. Cancel The same restore, and closes. Again, colours only. Close Keeps the current colours and records the active theme as your preference."],[5,"where","Where the settings are stored","One preferences.json , in circuitRF's per-user state directory — %LOCALAPPDATA%\\circuitRF on Windows, ~/Library/Application Support/circuitRF on macOS, ~/.local/share/circuitRF on Linux. Saved themes are .ccolor files in a themes folder beside it. One file serves all three applications circuitRF, harmonicaRF and wBond share that directory, so External Device Workers , Updates and the Verilog-A Compiler are one setting each, not three — set in any of the three applications, honoured by all of them. harmonicaRF has no workspace and therefore its own smaller Settings dialog, which hosts those same three sections; the controls are literally the same controls, not a second copy of them. An absent key means the default, so a machine with no preferences.json at all is a correctly configured one. Nothing here is written until you change something."],[6,"","","Database units, display units, the snap grid, and what circuitRF accepts when you type a value into a field. circuitRF keeps three things apart that other tools tend to conflate: how a number is stored , how it is shown to you , and what grid new geometry lands on . Keeping them separate is what makes \"change the units\" an instant, lossless operation here instead of a destructive one."],[6,"dbu","The database unit (DBU)","Every layout coordinate in circuitRF is a 64-bit integer in database units . Not a floating-point millimetre — an integer count. The reason is exactness. After a rotate, a paste, a boolean operation and a GDSII round-trip, two vertices that ought to coincide must compare exactly equal, or the geometry silently develops hairline gaps and self-intersections that only show up at the fab. Integers give that guarantee; doubles do not. It also makes \"is this vertex on the grid?\" a division with no remainder rather than a comparison against a tolerance. One DBU is one nanometre by default. The layout property that sets this is DbuPerMicron , and its default is 1000 . That specific number is chosen, not inherited: 1 µm = 1000 DBU exact 1 mil = 25400 DBU exact Both are exact integers at 1 nm resolution. A metric-authored MMIC and an imperial-authored PCB are therefore commensurable in the same database, a cell can be copied from one to the other with an exact transform, and mil↔µm conversion never produces a fractional coordinate. That single choice removes the whole family of \"the imported board is 0.0003 mil off grid\" problems. At long range, ±9.2 × 10 18 DBU is ±9.2 × 10 9 metres of addressable space. There is no practical ceiling."],[6,"storage-vs-display","Storage versus display","The single most common source of confusion The display unit is a presentation setting. Changing it moves nothing. A vertex stored at 25400 DBU reads 25.4 when the display unit is µm and 1 when it is mil. The stored integer is identical in both cases, and switching back and forth returns the byte-identical file. So changing the display unit is free: every shape stays exactly where it was, stays exactly on whatever grid it was on, and the change needs no undo entry beyond a view preference. It round-trips perfectly. The display unit is saved in the .clay file, purely so a layout reopens in the unit it was authored in. It carries no other meaning. The available display units are nm, µm, mm, mil and inch , and the default comes from the technology — mil for the PCB starter technology, µm for the MMIC one."],[6,"resolution","Changing the DBU resolution is a migration","DbuPerMicron is a different matter, and circuitRF treats it as one. It is not a preference toggle : It may only change by an exact integer ratio . A non-integer ratio is rejected outright. Refining (1 nm → 0.1 nm, ratio 10) is always lossless — every coordinate is multiplied by ten and the operation proceeds, logged to the Messages pane. Coarsening (0.1 nm → 1 nm) is lossy the moment any coordinate is not divisible by the ratio. The whole design, including every referenced sub-cell, is pre-scanned first; if any vertex would move, the specific offenders are reported to Messages and the change requires confirmation. It is never a silent snap. The whole thing is one undoable operation. In practice almost nobody touches it. The default is right for both target markets, and because refinement is always lossless, starting at 1 nm is safe even for a process that later needs finer resolution."],[6,"snap","Layout units and the snap grid","The snap grid is a third, independent number: the pitch that new and edited vertices land on. It is stored in DBU and it is yours to change at will. R5 in one sentence: changing the snap grid never re-snaps existing geometry. Moving from 1 µm to 0.5 µm does not move a single existing vertex. There is a separate, explicit Snap selection to grid command for when you actually want that, and it reports any shape it could not snap without self-intersecting. This is the opposite of the schematic's rule, and it is right for layout. In a schematic an off-grid pin is a correctness bug — it breaks connectivity. In a layout an off-grid vertex is merely unusual: imported GDSII, a 45° diagonal and a flattened arc all legitimately produce vertices between grid points. PCB starter technology MMIC starter technology Display unit mil µm Snap grid 1 mil 5 nm DbuPerMicron 1000 1000 The layout toolbar's Snap field sets this grid pitch, and offers a ladder of multiples of the technology's own default rather than a fixed list. F9 toggles it off and back to the last non-zero value. Grid snap and geometry snap are two different things The Snap field is the grid . Geometry snap — snapping to a pin, a corner, a midpoint — is a separate feature with its own toolbar toggle (F3 or S ), and its capture radius is not a setting at all: it is a fixed 8 device pixels converted to DBU at the current zoom, so it stays the same size on screen at every zoom level. See Geometry snap in the layout editor chapter."],[6,"wbond","wBond units","Bondwire geometry is stored in nanometres — plain nanometres, not DBU. At the default 1000 DBU/µm the two happen to coincide exactly, which is why the conversion between them is one deliberate, single-place bridge rather than something spread through the code. Everything a wire edit does quantises to one nanometre . On a 500 µm loop height that is a relative error of 2 × 10 -6 — physically about a fifth of a millionth of a mil. Inductance in the wBond panel is always read out in pH , fixed, never auto-ranged. That is deliberate: the panel exists so you can watch a number while dragging, and a unit that switches mid-drag fakes a 1000× jump."],[6,"typing","Typing a value into a field","There are two vocabularies, because there are two different kinds of field."],[6,"layout-dimension-fields","Layout dimension fields","A width, a length, a coordinate, a DRC rule value. These accept a bare number — interpreted in the current display unit — or a number with one of these suffixes, case-insensitively and with any amount of whitespace: Suffix Means Example (none) the current display unit 2.5 nm nanometre 250nm u , um , µm micrometre 50u mm millimetre 2.9mm mil thousandth of an inch 115mil in , inch inch 0.25in Leading + / - and scientific notation ( 1.5e3 ) are accepted. Anything else is rejected and the field reverts — it does not silently guess."],[6,"component-parameter-fields","Component parameter fields","A resistance, a capacitance, a frequency. These are evaluated by the expression engine , and the unit is a separate column in the row , not part of the number. The unit column accepts these: Prefix Multiplier Unit family Spellings T 10 12 Frequency Hz kHz MHz GHz THz G 10 9 Inductance H mH uH nH pH fH M 10 6 Capacitance F mF uF nF pF fF k 10 3 Resistance Ohm mOhm kOhm MOhm GOhm TOhm m 10 -3 Length metre nm um mm cm mil in inch u 10 -6 Angle deg rad n 10 -9 Voltage / current / power V kV mV uV nV · A mA uA nA · W mW uW kW p 10 -12 Logarithmic dB dBm dBc dBW f 10 -15 The glyphs Ω and µ are accepted and normalised — kΩ is kOhm , µF is uF . A prefix on its own is also a valid unit, so a row whose value is 2.2 and whose unit is p resolves to 2.2 × 10 -12 . \"m\" is milli, not metre A bare m is the SI prefix milli . The metre has its own spelling, metre , and every other length ( mm , um , mil , …) reduces to it. This is a deliberate decision, not an oversight: re-pointing m at the metre would silently multiply every hand-authored C=1m in an existing netlist by a thousand, with nothing anywhere reporting it. The logarithmic units are measurement functions rather than scale factors, so dBm carries no multiplier."],[6,"unit-field","A unit is a field, not part of an expression","The expression parser has no unit-suffix production : 2 GHz is not an expression, and asking the parser for one is an error at the GHz . What saves you is that nothing asks it to. A VAR row whose unit column is empty has any trailing unit token lifted out of the expression first, so writing RFfreq = 2 GHz in the schematic VAR editor gives you the variable you meant — the 2 becomes the expression and GHz the unit — and a .cnl , which has no unit column at all, has always been read the same way. The two entry points cannot make identical text mean two different things. The lift is applied only when the text does not already parse and the split makes it parse. That restraint is the point: because every bare SI prefix is a valid unit, a token-based rule would tear 2 * f into 2 * plus femto and R * m into R * plus milli — expressions that are legal and common. Those parse as they stand, so nothing is lifted from them. Where it still does not help The lift needs a separate token . 2GHz , with no space, is one word to the splitter and stays a parse error — and because an unresolvable global is skipped during elaboration, that leaves no variable at all rather than a wrong value. The same is true of a bare 60u in a PCell parameter. When in doubt, put the number in the expression and the unit in the row's unit column, which is unambiguous everywhere. See also: Expressions · The Layout Editor · wBond · Components · PCells ."],[7,"","","One expression language runs everywhere a value can be computed: global variables, component parameters, SDD device equations, and measurements. Anywhere you can type a number, you can type an expression — `2*pi*freq0`, `polar(0.1, 35)`, `if(Vg > Vth, gm*Vg, 0)`."],[7,"where","Where expressions are used","Consumer What it evaluates Result Global variables (VAR) a testbench variable Real or Complex Cell parameters a parameter default or an instance override Real or Complex SDD device equations i = f(v) , q = f(v) over port voltages Real (and its derivatives) Measurements a figure of merit over result cubes Real or Complex Same grammar, operators, and functions everywhere; only the available operands differ. circuitRF parses an expression once into a tree and evaluates it against a scope — it never does text substitution. That makes it fast (an SDD equation is parsed once, evaluated thousands of times) and correct (no fragile longest-name-first variable replacement)."],[7,"values","Values: Real, Complex, Bool, String","Every value carries a kind : Real — a plain number. Component values like R , L , C are Real ( 50 stays 50 , not 50+j0 ). Complex — appears as soon as the imaginary unit j enters, or from an operation that is mathematically complex (e.g. sqrt of a negative). Impedances resolve here. Write imaginary values with j* : j*4 , 2 + j*3 . Bool — the result of a comparison or logical operator; used only as a condition in if / ?: . A parameter that resolves to Bool is an error (a condition isn't a component value). String — a double-quoted literal ( \"spline\" , \"path/to/x.s2p\" ). Storage only — no string operators. Used for genuinely textual parameters (the SnP block's File , InterpMode , …). Ordering needs real operands < <= > >= require Real operands (complex numbers are unordered); == / != work on Real and Complex. Real ∘ Complex promotes to Complex."],[7,"operators","Operators & precedence","Lowest-binding (evaluated last) to highest-binding (tightest): Operators Notes ?: (ternary) cond ? a : b — same as if(cond, a, b) || logical OR && logical AND == != equality (Real or Complex) < <= > >= ordering (Real only) + - (binary) * / + - ! (unary prefix) ^ (power) binds tighter than unary minus: -2^2 == -4 ; right-assoc: 2^3^2 == 2^(3^2) function call, ( ) , atoms highest"],[7,"constants","Constants & functions","Constants: j = (0,1) , pi , e . These (and all function names) are reserved — a variable may not shadow them. Group Functions Trig sin cos tan asin acos atan atan2(y,x) Hyperbolic sinh cosh tanh Exp / log / power exp log (natural) log10 sqrt pow(x,y) abs Misc min(a,b) max(a,b) sign(x) , if(cond,then,else) Complex → Real real(z) imag(z) abs(z) mag(z) (= abs) phase(z) (degrees) phase_rad(z) (radians) Real,Real → Complex polar(mag, phase_deg) — e.g. polar(0.1, 10) is 0.1∠10° phase and the .phase cube transform both use degrees . dB / dBm are measurement functions, not general built-ins (and so are never unit suffixes)."],[7,"units","Units","A unit attaches at the assignment level (after the expression), and scales the value by a linear factor — L = L1 nH , Z = 50 Ohm , M = 0.5 pH . Units are not part of the expression grammar. Domain Units SI prefixes T G M k m u n p f (1e12 … 1e-15) Frequency Hz kHz MHz GHz THz Inductance H mH uH nH pH fH Capacitance F mF uF nF pF fF Resistance Ohm kOhm MOhm Length m mm um mil Angle deg rad dB and dBm are not units They are logarithmic, not linear scale factors, so they are functions — dB(...) , dBm(...) — never a trailing unit on a value."],[7,"vars","Variables, scope & cell parameters","Global variables (authored in a VAR block) are visible everywhere. Cell parameters pass top-down : an instance binds overrides in the parent scope; the cell evaluates its own defaults and component values in its own scope, then passes its scope down to its sub-cells. A name resolves to the local cell variable/parameter first, then the global — a cell never sees a parent's locals or a sibling's, which is what keeps a cell's meaning independent of where it's placed. freq is reserved Lowercase freq is the simulator's current stamping frequency (Hz), injected when the engine evaluates a frequency-dependent value (a Z_Port impedance, an SDD H[w] ). You can't name a variable freq . It is distinct from a source's capital- Freq tone parameter."],[7,"functions","User-defined functions","Define a function whose body is an expression in the same language, with any number of parameters: gm(vgs, vth) = beta * tanh(vgs - vth) A call binds arguments positionally into a fresh scope whose parent is the definition's scope (so a user function sees globals, not the caller's locals). User functions compose with built-ins and with each other, and are subject to the same cycle detection."],[7,"cycles","Cycle detection & errors","circuitRF detects and rejects dependency cycles across the whole graph — global variables, cell-parameter defaults, and overrides — and reports the chain (e.g. a → b → a ) rather than hanging. Every error names the offending text, never a silent zero or NaN: Cycle — the dependency chain. Unresolved name — the name and the scope it was sought in. Type error — Bool where a number is required; ordering on a complex operand; a non-Bool if condition. Arity / unknown function — wrong argument count or an undefined function. Domain — log(0) , sqrt of a negative in a real SDD context, division by zero (reported with context). SDD equations are real-only SDD device equations operate on real time-domain voltages and produce real current/charge, so j is disallowed there and sqrt / log of a negative is a domain error rather than a promotion to Complex. circuitRF differentiates SDD equations automatically (forward-mode AD) to get the solver's conductance/capacitance — including through an if , where it differentiates the branch that is taken. For tough convergence prefer soft switching ( tanh ) over a hard if . See The SDD . See also: Measurements · The SDD · VAR component . Full design: docs/design/expressions.md ."],[8,"","","Connecting components must be easy, and connections must be unambiguous: \"do these two pins touch?\" has to have exactly one answer. circuitRF achieves both with **two grids** — a coarse connection grid that all electrical points snap to, and a fine authoring grid for everything cosmetic."],[8,"two","The two grids","Connection grid P Authoring grid p Default pitch 100 units (the classic 100-mil EDA pitch) 5 units ( p = P/20 ) Job electrical connection cosmetic placement What snaps to it component pins, wire endpoints, wire bends, junction dots symbol body art, label offsets, net-label positions, canvas objects Can you change it? fixed for a design (design-level) freely, anytime (cosmetic) Every electrically-connectable point lands on P exactly — an integer multiple, verified by arithmetic, not by tolerance. Everything that carries no electrical meaning lives on the finer p , which is always a refinement of P ( p = P/k ), so a point on P is always also on p . A connection point can never fall \"between\" cells."],[8,"why","Why two grids","One grid can't serve both masters. \"Easy to connect\" wants a coarse grid (few, well-spaced targets a wire snaps to cleanly); \"freedom to place art and labels\" wants a fine grid. Splitting them resolves the conflict: pins/wires/dots on coarse P so connection is trivial and exact; bodies/labels/decorations on fine p so authoring has room. Counter-intuitive but important Authoring freedom is controlled by p , not P . Making P finer does not give more freedom — it makes connection harder (targets get denser, easier to snap to the wrong one) and breaks the 100-mil convention every EDA tool and RF engineer assumes. Want finer placement? Shrink p (increase k ). Leave P = 100 ."],[8,"exact","Connection is exact equality","Two pins are connected if and only if their connection-grid coordinates are equal — not \"within a few pixels.\" Because placement, wire drawing, and dragging all snap to P at the moment of input, coincident points are bit-for-bit equal, and the extracted netlist's connectivity is decided by exact grid coincidence. There is no fuzzy radius that could disagree with what you see on screen. This is what makes the netlist trustworthy: a connection, once made, is unambiguous. A small junction dot is drawn only where incident wire segments form a real branch (a horizontal and a vertical segment meet). Three collinear segments overlapping draw no dot — but still read as connected."],[8,"paste","Pasting across grids","If you copy from a schematic authored on a different connection grid and paste into one using P = 100 , circuitRF detects, warns, and snaps : The clipboard payload records the grid it was authored on. On paste, if the source grid differs, you get a message — e.g. \"Pasted content was created on a 50-unit grid; this schematic uses 100. Pins were snapped to this grid — verify connections.\" Pasted pins, wire endpoints, and dots are snapped to the destination grid (as one undoable action); decorations snap to the fine grid. Internal coincidences within the pasted group are preserved where possible, and any point that can't land cleanly is reported — never a silent off-grid pin."],[8,"changing","Changing the grids","Display grid (which lines are drawn) and the authoring grid p — cosmetic, change anytime. p is constrained to p = P/k (a refinement of P ). Connection grid P — fixed for a design. It is stored in the schematic file and is stable; changing it could strand existing connection points off the new grid, so it is treated as a deliberate migration, not a slider. In v1, P stays at the design's grid size; you change only p and the display."],[8,"editor","The schematic editor","Everything above is enforced by the editor you draw in. Its canvas, its toolbar button by button, the Library Palette and how a component gets from one to the other are on their own page: The Schematic Editor . See also: Schematic Editor · File formats (where the grid is stored) · Symbol Editor (pins on P , art on p ). Full design: docs/design/grid-and-connectivity.md ."],[9,"","","Three concepts that sound interchangeable but aren't. Getting them right is the difference between a circuit that simulates what you intended and one that doesn't."],[9,"summary","The one-paragraph version","A Port is the abstract idea of an external connection. circuitRF realizes a port two different ways depending on context: inside a reusable cell , a port is a Pin (a connectivity-only interface terminal); on an S-parameter test bench , a port is a Term (a numbered, reference-impedance termination the simulator excites and measures). So: Pin = a cell's connection point; Term = an S-parameter measurement port; Port = the general concept either one realizes. Pin Term Role Cell interface terminal S-parameter port termination Electrical model None (connectivity only) Reference impedance + excitation/measurement Lives on A cell's symbol A test-bench schematic Key parameter Num (+ optional Name , polarity) Num + Z (reference impedance) Use it to… Expose a reusable cell's connections to its parent Define where S-parameters are measured, and the port impedance"],[9,"pin","Pin — the cell interface terminal","Pin A Pin marks a point where a cell connects to the world outside it. When you build a reusable cell (say, an amplifier), you place Pins where its gate-in, drain-out, and bias connections should be; those Pins become the cell's symbol pins , and the parent schematic wires to them. A Pin carries no electrical model — it is pure connectivity. Its Num orders the cell's ports; Name labels them; an optional Plus / Minus polarity pairs two Pins into one differential port. Why \"no electrical model\" matters A Pin does not source, load, or terminate anything — it just says \"this net is exposed.\" If you want a 50 Ω measurement port, a Pin alone won't give you one; that's a Term's job."],[9,"term","Term — the S-parameter port","Term A Term is an active S-parameter port: it presents a reference impedance ( Z , default 50 Ω) and is the point at which an S-parameter analysis injects a wave and measures the scattered result. Place two Terms on a network and run S-parameters to get the 2-port S-matrix; their Num values (auto-assigned 1, 2, …) become the port indices you read as S(2,1) , etc. The P1Tone source can also act as a numbered port for power-driven work."],[9,"ground","A note on Ground","Ground Ground (net 0 ) is the global reference — not a port at all. A Term's impedance is referenced to it; a Pin may connect to it like any net. Every port voltage is ultimately measured against ground unless a component declares its own floating reference (see the SnP floating-reference option in Dynamic symbols )."],[9,"choosing","Choosing the right one","Building a reusable cell and need to expose its connections? → Pin . Setting up an S-parameter measurement on a test bench? → Term (one per port). Driving a power amplifier and also want it to be a numbered port? → P1Tone with a Num . Just need a reference node? → Ground . See also: Components › Pin / Term · Dynamic symbols · New User's Guide for a gentler take."],[10,"","","circuitRF stores a project as a folder of small, human-readable, text files — diffable and version-control friendly. The core idea: the **schematic** (what you draw) is the source of truth; the **netlist** (what the engine runs) is derived from it."],[10,"principle","Schematic vs. netlist","Two complementary artifacts, related one way at the engine boundary: .csch (schematic: placement, wires, labels, canvas objects, view state) │ net extraction — headless, deterministic ▼ design model ≡ what a .cnl represents │ ▼ engine → DataSet (results) .csch — the schematic: where each component sits, how wires route, net labels, junction dots, canvas objects, zoom/pan. It carries no elaborated netlist, matrices, or results. This is what you edit and save. .cnl — the netlist, and only the netlist: components, parameters, nets, variables, analyses, measurements. A hand-authored .cnl and one produced by extracting a schematic are the same kind of artifact — a pure netlist the engine consumes. See the Netlist format chapter. A hand-authored .cnl with no .csch is fine — it simulates, it just has no drawing to edit visually until you draw one."],[10,"family","The file family at a glance","Extension What it is .csch Schematic view — placement, wires, labels, canvas objects, analyses, measurements. .csym Symbol view — a cell's glyph: drawing primitives + pins mapped to ports. See the Symbol Editor . .ccell Cell manifest — the cell's declared parameters + defaults, which view is primary per type, and the IsTestBench flag. .clay Layout view — the cell's physical geometry: shapes on layers, instances, PCell placements, bitmaps, the display unit and the snap grid. A real, first-class view; see The Layout Editor . .cdd Data Display config — placed plots/tables/contours, their binding to a run's results, markers, view state. .cnl Netlist — the engine's input (derived from a schematic, or hand-authored). .cws Workspace config — the top-level \"what am I working on\" document. References libraries, known files, the default technology and assembly rules, and any other projects or kits this one uses. .cwsuser Your own view of a workspace — the panel arrangement, which tabs were open, which tree categories you expanded, and the colour theme. Sits beside the .cws . Optional, personal, and safe to delete; see Your own view of a workspace . .clib Library manifest — name, version, metadata (cells are discovered by scanning, not listed here). .ccolor Color theme — a named light+dark palette for rendering. .ctech Technology — the layer table (GDSII layer/datatype pairs, colours, purposes), the substrate stackup, the DRC rules, and the default display unit and snap grid. Shared at workspace level: every layout in the workspace resolves against one of these. See Technology . .cem EM setup — one electromagnetic run's configuration: which layout, the stackup mapping, the ports and their reference impedances, the mesh settings, the frequency plan and the de-embedding choice. See EM Setup . .charm harmonicaRF document — the DUT, the source and load termination planes at every harmonic, the package, the display configuration and the markers. See harmonicaRF . .wBond wBond design — bondwire geometry: the wires, their arrays and profiles, the substrate and the solver settings. Self-contained and shareable. See wBond . These are circuitRF's own formats. The interchange formats it reads and writes — GDSII, DXF, Gerber plus Excellon drill, and .kicad_pcb board files — are not in this family and are not documents you open: they are imported into cells, or exported from them. See Interchange for what each carries and what it cannot. Which of these are documents you open .csch , .csym , .clay , .cdd , .ctech , .cem , .charm and .wBond each open as a tab in the workspace. .ccell , .clib , .cws and .cwsuser are manifests the application maintains for you — though double-clicking either half of a workspace opens the whole workspace. .cnl is the engine's input and is normally derived rather than edited."],[10,"hierarchy","Workspace › Library › Cell","The conceptual hierarchy maps to folders of the small files above — never one monolithic blob."],[10,"cell-a-folder-of-views","Cell = a folder of views","<CellName>/ .ccell manifest: parameters + defaults + primary view per type + IsTestBench schematic/ *.csch schematic views symbol/ *.csym symbol views layout/ *.clay layout views A cell need not have every view. A view sub-folder with exactly one file makes that file primary by default; .ccell records the primary when there are several. A placed component references its cell by relative path and resolves its glyph through the cell's primary symbol — so a broken path shows a \"Not Found\" glyph, and a resolved cell with a missing primary symbol shows a plain-rectangle stand-in. The cell folder is the unit you copy to share or reuse a cell."],[10,"library-a-folder-of-cells","Library = a folder of cells","<LibraryName>/ .clib manifest: name, version, metadata <CellA>/ … <CellB>/ … Membership is filesystem-is-truth — the Project Tree discovers cells by scanning, so the .clib stays lightweight (no cell index). The standard component libraries ship in this shape. File → Add Library points the workspace at an external library folder."],[10,"workspace-the-project-that-references-the-above","Workspace = the project that references the above","A workspace is a folder (its name = the folder name) containing a .cws file. Membership is the filesystem; the .cws records configuration only: referenced libraries, \"Known Files\" bookmarks, the default technology and assembly rules, and the other projects and kits this one uses. It references, never embeds — the same cell or library can be used by multiple workspaces. Beside it sits a second, optional file: the .cwsuser , which is yours rather than the project's."],[10,"cwsuser","Your own view of a workspace","The .cwsuser holds the things that describe your afternoon rather than the design : where you put the panels, which tabs were open and which one was in front, which categories of the Project Tree you had expanded, and which colour theme you were using. Nothing in it affects a simulation, a schematic or a layout. Three things follow, and all three are deliberate: It is optional, always. A workspace with no .cwsuser opens perfectly normally, on the default arrangement — exactly like a brand-new one. Nothing is reported and nothing is wrong. This is the usual state of a workspace somebody hands you. It does not travel. It is left out of a workspace archive , because your monitor layout is of no use to whoever you send the archive to. It is carried by File ▸ Save Workspace As , because that copy is still yours, on the same machine, and the window switches straight to it. Deleting it is a repair, not a risk. If your panels have ended up somewhere unusable, close the workspace, delete the .cwsuser , and open the workspace again. You get the default arrangement back and lose nothing but the arrangement. Double-clicking either the .cws or the .cwsuser opens the whole workspace — they are two halves of one thing. A .cwsuser on its own, with no .cws beside it, is not a workspace, and circuitRF says so rather than opening an empty window."],[10,"testbench","What makes a TestBench","A TestBench is not a separate file type — it is a cell whose schematic carries analyses and measurements , making it runnable. \"TestBench\" is a role, marked by the IsTestBench flag in the cell's .ccell , which the Project Tree uses to show it as runnable. A testbench is authored, saved, and version-controlled exactly like any other cell. Port numbers are 1-based for you, positional in the netlist Everything you see — symbol pins, dialogs, error messages — numbers ports from 1 (a VNA has port 1 and port 2, never port 0). The .cnl doesn't list port numbers at all: a component line names its nets in terminal order , and the engine infers the port from position. Net extraction emits nets in the symbol's terminal order — the one seam where the two conventions meet, and a tested one."],[10,"netlistcnl","The generated netlist.cnl","The GUI does not keep per-cell .cnl files. When you simulate a TestBench, extraction writes a single netlist.cnl to the workspace root and the engine runs it. It is: Overwritten every run — one scratch netlist for the latest simulation, whichever testbench produced it. Stamped with provenance — a header comment records which TestBench produced it and when (e.g. ; netlist.cnl — generated from TestBench \"PA_loadpull\" at 2026-06-06T14:22:31Z ). Human-inspectable and re-runnable — it's exactly what the engine saw, and you can re-run it headless from the command line . It is a generated scratch artifact, not part of the saved project — the .csch is the source of truth."],[10,"rules","Shared rules","Text, JSON-friendly, human-diffable , with stable key ordering for clean diffs. Paths, not payloads — bitmaps store a file path, never pixels; a missing image shows a placeholder box, never a crash. Relative paths (under the project) are preferred so a moved or shared project keeps its images; absolute paths are accepted. Enums serialize as names , so files stay readable and stable across versions. Runtime identity is never persisted — internal object IDs are regenerated on load and carry no meaning across sessions. Alpha policy — a format_version is written and rejected on mismatch; there is no silent migration. (circuitRF is pre-release; formats may break and regenerate until near release.) See also: Netlist format · Grid & Connectivity · Symbol Editor . Full design: docs/design/project-file-formats.md ."],[11,"","","Everything circuitRF has kept of a workspace, in one list — the states it kept for you automatically, and the versions you kept on purpose. What is in it, how to find something, and how to go back. circuitRF uses Git under the hood for revision control. To enable History tracking, Git must be installed and then configured in the circuitRF Settings ."],[11,"two-kinds","Two kinds of entry, one list","circuitRF records what you did and never edits it. What you wrote about it is yours until you have shared it. Those are two different promises and it is worth having both of them in mind from the start: the files, the times and the order they happened in are fixed for good, and the line you typed on an entry is a label you can correct. Nobody writes a careful title if they believe a careless one is permanent, so it is not — and the moment a version leaves your machine, the line is drawn and a correction sits beside the original instead of replacing it. circuitRF keeps two different things about a workspace, and the History panel shows both. A restore point is the whole workspace as it stood at one moment: every cell, every schematic, every layout, the workspace's own configuration. circuitRF takes them mostly by itself — when you close the workspace, when you ask for one, and before an assistant changes anything. They are a safety net, and they never leave this machine. A version is a statement. You decide on it, you give it a title, and it is what you come back to six weeks later and what goes with the workspace when it leaves your machine. restore points versions who makes them circuitRF, mostly automatically you, on purpose how many dozens, thinned as they age as few as you decide what they are for getting back from something that went wrong saying \"this one is finished\" do they travel? no — they belong to this machine yes — they go with a copy or a clone They are still two kinds of thing, and the list says which is which. A version carries a small coloured tag; a restore point does not. The tag is not a mark of importance — it means this one has a title, nothing tidies it away, and it travels with a copy , which are three promises the unmarked entries do not make. They are in one list because the alternative was worse. A list with three hundred automatic entries and four deliberate ones mixed among them is a list nobody reads — so the panel opens showing only the entries somebody stated an intent for, and the automatic ones are one click away in the filter. Two separate panels answered the same problem by making you decide which one to open, and that question is hardest at the moment you have to ask it, which is the moment something has gone wrong. It is not undo either. Undo is per-editor and per-keystroke, it covers the last few minutes inside the document you are looking at, and it ends when you close the window. A restore point is workspace-wide, action-grained and durable — it is still there next week. The two are deliberately unrelated, and between them they cover the whole range: undo for the last few minutes in one view, restore points for everything else. Going back is never a one-way door Before it replaces anything, circuitRF keeps a restore point of the state you are in right now — including work you have done since the last one. So if you go back and it was the wrong moment, you can come forward again. That entry appears in the list like any other, the panel offers it to you by name the moment you arrive, and tidying up never removes it."],[11,"when","When circuitRF keeps a state","Three moments, and no others: moment why this one you ask for one — File ▸ Keep This State… your judgement about what matters beats any rule circuitRF could invent the workspace closes the one moment that reliably arrives in every session, including the ones where you never thought about history before an assistant changes anything so anything an AI assistant did can be undone in one action Not on every save. One thing you do — widening a match network — writes a layout, a schematic and the workspace file, so a restore point per file save would keep fragments of an edit, some of them half-finished. And a list with three hundred entries in it is a list nobody reads, which would make it useless exactly when you needed it. Not on a simulation run, and not on a timer. Runs are frequent and usually change nothing; a sweep would leave dozens of near-identical entries. And an entry made because you went to lunch is one you cannot predict, at a moment that meant nothing, labelled with a time instead of a reason. A moment where nothing changed keeps nothing. Close a workspace you only looked at, or press Keep This State twice, and there is one entry, not two. Nothing is created until something is worth keeping Opening a workspace to look at it writes nothing at all — which matters most for a workspace on a shared drive that belongs to somebody else. circuitRF starts keeping a history at the first moment that would actually record something, and tells you once when it does."],[11,"keeping","\"I want to keep this one — it's what I'm sending out\"","File ▸ Keep This Version… , or the tag button at the top of the History panel. One field: a title. Write what the design is at this moment, not what you did to it — \"Output match retuned for 3.5 GHz\" , not \"changed some stuff\" . Six weeks from now that line is the only thing that tells one version from another, and it is what anyone you send the workspace to will read first. What is recorded is the whole workspace as it stands : every cell, every schematic, every layout, the technology, the workspace's own configuration. Not just the document you have open. Nothing changed means nothing to keep, and you are told before you type If the workspace is exactly as it was when the last entry was made, both keep buttons are greyed and hovering one says why. Two entries holding identical content would be two entries you could not tell apart. Change anything and they come back. Anything unsaved is saved first, in the same words a close asks in — so what gets recorded under the title you just wrote is what you are looking at, not what was last written to disk. Afterwards, one line appears in the Messages panel saying what was kept and giving the version's identity — a short string of letters and digits. You will almost certainly never need it. It is there for the day you do: it is what to give someone helping you look at the workspace with tools other than circuitRF. Simulation results are not kept in a version, for the same reason they are not kept in a restore point: a result is a function of the design and the engine, so the design is the thing worth recording. See what is kept, and what is not — the same table applies."],[11,"panel","The History panel","View ▸ Panels ▸ History , or the clock button on the workspace toolbar. A list, newest first. Each row is what you scan , and it reads left to right in three parts. The time — just now , 7 minutes ago , 3 hours ago for anything inside the last day, and the clock time beyond that — with the date under it, carrying the year whenever the entry is not from this year. The column is the same width on every row, so everything to the right of it lines up. The entry's own short name , in a fixed-width face: seven letters and digits, such as f15b942 . This is what one entry is called. A time tells two entries apart; it is not something you can quote, point at, or give to somebody helping you — and forty automatic entries share their wording. It is the first few characters of the full identifier in the entry's own expander, and it is what the buttons and the menu name their destination by. The label — the title you wrote, or a short line saying how the entry came about for one nobody titled: closed , save-point , before going back , and \"before: widen the output match\" for an assistant's batch, which quotes what the assistant said it was about to do. Those generated ones are deliberately short and deliberately repetitive: they name the kind of moment, which is all a label can honestly do, and the short name beside them is what tells one from another. A version carries its tag. On a workspace more than one person works in, the row says who kept it. And a row says the three things that change what it means : that it was brought back from an earlier state, that a file was left out of it, or that tidying up has tidied it away. Open a row for what a row cannot carry: the full date and time with its time zone, a sentence spelling out how the entry came about, whether it has left this machine or is still only here, its ordering number, and its identifier in full . The row shows the first seven characters of that same string, and Copy the identifier puts the whole of it on the clipboard — it is one value shown two ways, never two values. You will almost certainly never need the long form; it is there for the day you do, to give someone helping you look at the workspace with tools other than circuitRF. Right-click a row for everything you can do to it: Go to \"…\" — see going back below. It names where it will take you, and it quotes only words a person wrote. A titled entry reads Go to \"Output match retuned for 3.5 GHz\" ; one nobody titled reads Go back to f15b942 , naming that entry's own short name, because go back to this state read identically on every row and said nothing about which of forty it would act on. Compare to Current — lists the documents that entry holds which your files no longer do. Nothing is changed. If they hold the same files, it says so. Keep permanently — marks an entry so circuitRF's own tidying-up will never remove it. Ones you asked for, and the one labelled before going back , are already marked. Bring this back — on an entry marked tidied away , puts it back in the list as an ordinary one. See tidying up . Rename this entry… , Edit Comments… and Tidy this away — the label or the title, what happens to a version you have already sent, and putting an entry out of the way. See \"I typed the title wrong\" . Copy the identifier . There is no delete. Nothing on this menu destroys anything: Tidy this away is the same putting-away that automatic tidying does, and the entry stays in the list marked tidied away until you bring it back. The only thing that actually frees space is Settings ▸ Revision Control ▸ Reclaim space , which works by age over everything already tidied away — never on one entry you picked. See tidying up . Two buttons at the top of the panel create something: keep this state writes a restore point, and keep this version records a titled version. They are the same two things File ▸ Keep This State… and File ▸ Keep This Version… do. Both go grey when there is nothing to record , and hovering one says so: this"],[11,"the-filter","The filter","The funnel opens five tick boxes: versions , states you kept , before an assistant , kept automatically , and tidied away . Everything is on except kept automatically — the entries circuitRF takes when you close the workspace, which are the ones that arrive three hundred at a time. Those entries are still being taken. Closing the workspace is the one moment that reliably happens in every session, including the ones where you never thought about history at all, so hiding them from a list is a decision about the list and never about what is kept. Tick the box and they are there. The setting is yours and it belongs to this workspace: it is remembered next time you open it, and it is not something anybody else inherits."],[11,"search","Search","The magnifier opens a field, and it searches what a person wrote — the titles you gave your versions, what an assistant said it was about to do, and who kept each entry. \"I broke the match network yesterday\" is a search, and scrolling a year of entries to perform it is doing the machine's work for it. If entries that circuitRF has tidied away also match and the filter is hiding them, the panel says so on a line of its own with a count. An incomplete answer that says it is incomplete is a very different thing from a wrong one. If a title has been corrected , the search finds the entry under both wordings — the one in front of you and the one it replaced. Looking for a line you have already corrected is very often exactly why you are searching."],[11,"the-same-list-without-the-window","The same list, without the window","circuitrf history list <workspace> prints what this panel shows, with the same default: every entry somebody stated an intent for, and the automatic ones hidden. --include-automatic reveals them, --kinds versions,save-points,ai-batches,automatic,tidied-away picks what to show, and --search <text> searches the same three fields the magnifier does. See the command line . If circuitRF is not recording History is off for this workspace, or the workspace sits inside another version-controlled folder: the buttons stay where they are and say why rather than disappearing, and a strip at the foot of the window says so for as long as the workspace is open. A button that quietly vanished would look exactly like a feature that had never been built."],[11,"going-back","Going back — \"I broke the match network yesterday\"","Open View ▸ Panels ▸ History , find the entry — the titles and the dates are what you are looking for, and the search is faster than scrolling — then either press the Go to \"…\" button under the list, or right-click the row and choose the same item there. They are one action with two places to reach it, and both name the entry they will take you to. What happens, in order: If anything is unsaved, circuitRF asks about it, in the same words it asks when you close a workspace. A restore is built from what is on disk, so unsaved edits sitting on top of it would leave you with a workspace matching neither state. The state you are in now is kept, so you can come forward again. The files go back. Anything created since is taken away, because yesterday's files plus today's new cell is a workspace that never existed. Only the documents whose files actually changed are re-opened. Your panel layout, your other open tabs, the project tree and the Messages panel are already correct and are left exactly as they are. Going back to a state that differed in one schematic touches one schematic. Undo does not survive going back Every open document's undo history is cleared, deliberately. An undo after going back would re-apply the last few minutes of the state you replaced onto the file you just brought back — producing a document that never existed at any moment: perfectly well-formed, and wrong. Going back covers this workspace's files . Anything in a workspace this one refers to is not part of this workspace's history and is left exactly as it is."],[11,"forward","\"I went back and I want to come forward again\"","You went back to Tuesday. It was the wrong Tuesday, or you only wanted to look. The afternoon you just replaced is not gone , and this is the twenty seconds in which people believe it is. The panel tells you, at the top of the list, on arrival. One line, in two halves: Now at f15b942, Tue 14:32. Your work up to 15:07 is kept as a3c91de. Where you are, and where the afternoon went — each named by the entry's own short name and by its time, because a name answers which and a time is what you are actually navigating by. Under it, a button that says Go back to a3c91de — the second half of that line, again. It names its destination , not a direction: going back and coming forward are one operation performed twice, so a button labelled Come forward again could be pressed for ever and never tell you where the last press had landed. Press this one and you are back where you were, and the name on it changes to the state you just left. It is not a different or a scarier operation than the one that brought you here — it is the same one. Coming forward keeps a restore point of this state first, exactly as going back did, so there is no step in this that you cannot undo by taking it again. If there is nowhere to go, the button is not there. Going back to a state whose files were identical to the ones you already had replaced nothing, so there is no earlier afternoon to return to; the line then says only where you are. Nothing was created to make this possible. The entry the line points at is the one circuitRF wrote before it replaced a single file, and it is in the list with everything else. Tidying up never removes it."],[11,"comparing","What changed between two versions","Select a version and the panel lists the documents that differ between it and the one before it: which cells changed, which were added, which were removed. Right-click any entry — a version or a restore point — and choose Compare to Current to ask the other question: what that entry holds that your files no longer do. When the two hold the same files it tells you so, rather than showing you an empty list. That is deliberately where it stops. circuitRF does not show you a line-by-line comparison of what is inside a schematic or a layout — open both and look, or go back to one of them. A list of changed coordinates is not a picture of what moved."],[11,"titles","\"I typed the title wrong\"","Right-click the entry. What you get depends on which of three things it is, and the difference is only ever about who else has already read it . the entry what you can do why a restore point Rename this entry… — change the label to anything. You can also Tidy this away . Nothing chains to a restore point, no copy takes one and no send carries one. It is your machine's own safety net and the label is yours alone. a version you have not shared Edit Comments… — the title changes, and that is the end of it. Nobody else has seen it, so there is nothing to put out of step. a version you have sent to another copy Edit Comments… — this adds a correction beside the original. The original string is on somebody else's disk. circuitRF can put your wording in front of it; it cannot reach into their copy and take the old one out. Correcting a title never changes what the version holds. The files, who kept it and when are the ones already recorded, in all three cases. A correction is about the sentence, never the design. circuitRF works out which case applies — you do not have to know whether a version has been sent. If it cannot tell (there is another copy configured and this machine has never heard from it), it takes the cautious reading and offers you the correction rather than the rename. A correction does not erase the original On a version that has been shared, the original wording stays in the history and can still be read by anyone holding a copy of this workspace, including the copies already sent. What the correction buys you is that your wording is what they see first. If a title contains something that must never have left your machine at all, that is a different problem and it has a different answer . Letting a restore point go is the same thing tidying up does. It stops appearing in the main list, it is still there under tidied away in the filter, you can bring it back, and it frees no disk space until you reclaim — which you have to ask for. There is nothing here that destroys a state. Where a correction goes. It travels with the version it is attached to: whoever pulls or clones this workspace next sees your corrected wording in their own History panel, with the original one click away in the entry's expander, exactly as you see it here. Headless: circuitrf history rename <workspace> --point <n> --label \"…\" , circuitrf history forget <workspace> --point <n> , circuitrf history retitle <workspace> --title \"…\" (the newest version) and circuitrf history correct <workspace> --version <id> --text \"…\" ."],[11,"restore-then","\"I went back to an old version, then kept working\"","This is one line of work, not two, and there is nothing to manage. You go back to Tuesday's version. The state you were in a moment ago is kept first, so nothing is lost — it is in the History list like any other entry, and the panel offers it to you by name if you picked the wrong moment. You keep working. The files on disk are Tuesday's, plus whatever you do next. You keep a version. That version records that it was brought back from Tuesday's , naming it and the date it was kept. That last part is the one worth knowing about. Without it, the list would show two versions in a row where the second undoes most of the first, and there would be nothing anywhere saying why — it would read as a change of mind. The line says what actually happened, and it appears only on the version that followed a going-back. The next one after that is your own work again and carries no such line. Going back is still not a one-way door, even after you keep a version The restore point taken just before you went back survives everything you do afterwards, including keeping a version on top of the mistake. If you went back to the wrong moment and only noticed later, that entry is still in the History list."],[11,"gaps","Gaps in the list","If history was switched off for a stretch, the list shows that stretch as a gap , with its dates and the reason — \"Recording was off from 3 Mar to 17 Mar. Anything done in between is not in this history.\" It is not shown as a quiet fortnight in which you happened not to keep anything. Those are completely different situations and only one of them means the work is recoverable."],[11,"results","\"My results are gone\"","They were never kept, and that is deliberate — go back to a restore point and your .npy , .spl , .lpcwave and .mat files are still sitting exactly where they were. A result is a function of the design and the engine , so the design is the thing worth keeping: run the analysis again and you have the results back. It is not about disk space. It is also why going back does not delete them: bringing back last Tuesday's design should not destroy the hours of simulation sitting beside it."],[11,"started","\"circuitRF says it started keeping a history\"","The first time a workspace reaches one of the three moments , circuitRF creates a small folder inside the workspace to keep restore points in, and says so once in the Messages panel. Where the setting is — Settings ▸ Revision Control. You can turn it off entirely, or off for one workspace. It cannot harm your design. The folder is called .git ; it sits beside your cells and holds only copies of them. Deleting it removes every restore point and touches nothing else. Nothing was created before that moment. If you only opened the workspace and looked, nothing was written at all."],[11,"copy","\"I saved a copy and my history didn't come with it\"","File ▸ Save Workspace As makes a copy of your design and the copy starts a history of its own. The original keeps every restore point it had. That is on purpose. A history holds every earlier version of every file it ever kept , including files you deleted long ago — so a copy made for one customer must not carry another customer's deleted artwork inside it, invisibly. Save Workspace As says so on the message it posts when the copy is made. If you want the history to travel, use File ▸ Archive Workspace and include history — read the next section first."],[11,"customer","\"I'm sending this workspace to a customer\"","File ▸ Archive Workspace… puts the workspace into one .zip : every cell, every technology, the workspace file, and whatever else you tick — kits, referenced files, results. The history is not in it unless you say so , and there is a tickbox for saying so."],[11,"the-one-sentence-that-matters","The one sentence that matters","A file you deleted from the workspace is still in the history. A history holds every earlier version of every file it ever kept, and that includes files that are not in the workspace any more. If you imported one customer's artwork, finished with it, deleted it, and then archived this workspace for a different customer, including the history would send that artwork with it — and nothing in the visible file tree would show it. That is why the tickbox starts unticked. Everything else circuitRF is careful about here is a loss you can recover from; this one is the opposite, and nobody can undo it afterwards."],[11,"what-each-choice-sends","What each choice sends","what the recipient gets history left out (the default) a working workspace with no history. Completely normal — it opens, everything is present and current, and if they switch recording on they start a history of their own. history included the same workspace, plus every earlier version of every file it ever kept, and every restore point — so they can go back to any of them. Including it is often the right thing to do. A design handed to a partner with its history is a far better handover than a snapshot, and inside your own organisation it is usually what you want. The point of the default is only that it should be a decision, not something that happens by itself."],[11,"what-circuitrf-tells-you-when-you-tick-it","What circuitRF tells you when you tick it","Before you press Archive, the dialog states — for this workspace, not in general: how much larger the archive becomes; how many versions and restore points it carries; and how many files are in the history that are not in the workspace now, and what they are called. That last line is the one to read. If you recognise a filename on it, you have found the problem while you can still do something about it."],[11,"and-it-shows-you-every-title-that-is-about-to-leave","And it shows you every title that is about to leave","Before the archive is written, circuitRF lists the version titles the recipient will read — all of them, with their dates, and with any corrections you have made shown in place of what they correct. This is worth more than any of the correction mechanisms above , and it is not really about careless wording. The expensive version of this problem is a customer's name, or a part number, sitting in a title that is about to go to a different customer. Nobody can catch that from memory — forty titles written over six months are not something anyone can recall — and it takes about ten seconds to read. It is not a box to tick. If a title says something it should not, there is a Correct… button on its own row — the list re-reads itself afterwards, so what you are looking at is what is actually about to leave. The newest version can be retitled outright and any of them can take a correction; see \"I typed the title wrong\" . The same list appears in front of Push Changes and in front of copying a workspace , for the same reason and from the same computation. In front of a send it shows only the versions the other copy does not have yet, because those are the ones actually going. An archive is the only way a restore point travels A copy of a workspace ( File ▸ Save Workspace As ) carries no history at all. An archive with history included carries the whole of it — your versions and your restore points. It is the strongest handover circuitRF can make."],[11,"there-is-no-just-the-last-few-versions","There is no \"just the last few versions\"","It sounds like the best of both, and it is not offered, deliberately. It cannot be done without changing every entry in the history, so the copy you sent could never be compared with the one you kept. Worse, it would invite exactly the wrong belief: having chosen \"the last ten\", you would assume the thing you were worried about was gone — and a filter that missed one file, or a file renamed before it was deleted, would be a leak you had explicitly tried to prevent and been told was handled. Two honest choices are better than a third that is nearly true. If something must never leave your machine, take it out of the history before you send it — that is yours to do, with git — or send the archive without the history."],[11,"other-copy","Exchanging versions with another copy","If this workspace was brought here with File ▸ Clone Workspace… , two more items on the File menu apply to it. Pull Changes brings down what is new on the copy it came from and lists it . The versions waiting for you appear at the top of the History panel, marked on the copy this came from — not here yet , with a line above the list saying how many. Nothing in your own files is touched by the pull itself. Select one to see which documents it changes; Go back to this puts those files into your workspace — and, like every other way back, keeps what you have now as a restore point first, so it is not a one-way door. Push Changes sends the versions you have kept back to that copy. Your restore points stay here: they are your machine's safety net and mean nothing on anybody else's. Neither happens by itself. circuitRF never contacts anything without being asked, and it holds no sign-in of its own — it uses whatever your machine's git is already set up with. If a sign-in is needed that cannot be supplied, the operation stops and says what was wanted rather than waiting. If the other side has moved on since you last brought its changes in, Push Changes says so and sends nothing. Bring the changes in first, choose where the two disagree, and send again."],[11,"two-of-you","\"Two of us are editing the same workspace\"","If two people change the same document and the changes come together, circuitRF shows you both versions and asks which one you want . Who wrote each, when, and what else changed alongside it. You pick one, and that document becomes that version whole — exactly the bytes that person wrote. circuitRF will not combine them, and that is a statement about geometry and connectivity rather than about effort. Interleaving two sets of edits to a polygon's outline can produce a shape that is invalid, or valid and wrong , in a file that opens without a word of complaint. The same is true of a schematic's connections: two people's wires merged line by line can produce a netlist that elaborates, simulates, and is not the circuit either of them drew. A design that is silently wrong is worse than being asked a question , because at least the question is visible. This is also why the big centralised design-management systems in this industry lock a cellview while someone has it open rather than merging afterwards. When merging is not possible, keeping two people out of one file is the only strategy left. circuitRF does not run a lock server — but it is why referenced workspaces are read-only by default , which is the cheapest version of the same idea. Choose, do not reconcile If both versions contain work you need, the answer is not to hand-merge the file. Keep one, open the other version's copy of the document beside it, and redo the changes you want in the editor — where the tool can tell you whether the result is a valid design."],[11,"kept","What is kept, and what is not","in a restore point? cells, schematics, symbols, layouts, technologies yes the workspace file — analyses, references, configuration yes simulation results ( .npy , .spl , .lpcwave , .mat ) no — re-run the analysis your own panel layout, open tabs and colour theme no — they are yours, not the design's generated PCell artwork no — it is rebuilt from the layout a folder inside the workspace that keeps a history of its own no — it is left entirely alone a file you answered \"never include files like this\" about no — and it is not in a restore, and not in a copy anyone takes a file left out because nobody was there to be asked not yet — the entry says so and names it, and circuitRF asks the next time you keep a state yourself anything saved while history is switched off for the workspace no — but everything recorded before you switched it off is still there anything at all, when your workspace sits inside another version-controlled folder no — circuitRF records nothing there, and says so the history, in a workspace archive not unless you tick it — and you still have it either way. It is the only way a restore point ever travels: see sending a workspace to a customer restore points, in a workspace copied from an address (File ▸ Clone Workspace…) no — and not recoverable. The versions its author kept do come; their restore points belong to the machine they were taken on, and yours start here. See using a library another team maintains which version of a referenced library your design uses yes — so going back brings the library back with your files, not just the files. See going back, with the library included the title you typed on a version you have not shared yes — and it is yours to correct. See \"I typed the title wrong\" the title you typed on a version you have already sent yes, and it stays. You can add a correction that shows in place of it and travels with it; the original wording stays in the history and can still be read by anyone holding a copy the label on a restore point yes — and it is yours to correct, or to let go of, at any time. It never left this machine a restore point circuitRF tidied away yes, still — it stays in the list marked tidied away , and you can bring it back, until you reclaim the space (which asks first)"],[11,"unusually-large-files","Unusually large files","The first time something much larger than a design document would go into the history — an imported artwork file, a whole fabrication set — circuitRF asks what it is: Include it — this is design input. It is kept: one copy now, and one more only when it changes. Leave it out this time — nothing is written, and you will be asked again. Never include files like this — it is a by-product. Files matching it are not kept, so they are not in a restore and not in a copy . They stay on disk exactly as they are. There is deliberately no \"keep it once, then ignore it\": it cannot be made to work honestly. Either nothing would change while the button said it had, or your history would hold one stale version of a file that is missing from every copy anyone else takes. At a moment nobody is at — a workspace closing, or an assistant working — circuitRF does not stop to ask. It leaves the file out, records that it did, shows the entry as incomplete with the file's name, and asks you the next time you keep a state yourself. Leaving a file out can be undone; putting one in cannot."],[11,"tidying","Tidying up — \"I can't get back to last month\"","circuitRF thins the automatic restore points as they get old, so a workspace you have had open every day for two years does not carry two years of entries. Two rules decide what goes , and the first one is the one worth knowing: The newest ones are always kept, however old they are. Settings ▸ Revision Control ▸ Always keep at least sets how many. This is not a nicety: a computer's clock is something anyone can change, and a machine that wakes up believing it is 2140 would otherwise think every restore point had expired. The count floor means a wrong clock costs you nothing at all, which is also why the field will not go below its minimum. After that, age. Settings ▸ Revision Control ▸ Keep them for sets how long. Ones you asked for are never thinned. Nor is anything you marked Keep permanently , nor the entries that mark where you switched history off and on again. Thinned is not deleted, and the list still shows it A restore point circuitRF tidies away stays in the list, marked tidied away and shown a little dimmer. Select it and press Bring this back and it is an ordinary entry again. Thinning stops circuitRF offering a state; it does not destroy it, and nothing destroys it until you ask — see \"my disk is full\" below. So turning the retention setting down is not the door it looks like. If you set it to seven days and then want last month back, the entries are still there and still marked; bring one back and go to it. The only thing that ever actually removes them is Reclaim Space , which asks first and says exactly what it will destroy. If circuitRF ever tells you it was about to tidy away far more than usual and did not , that is the safety catch working: it means the dates it is reading do not make sense — almost always a clock that has jumped. Nothing was removed. Check the machine's date and time, and the next tidy-up will be ordinary."],[11,"enclosing","\"My workspace is inside another version-controlled folder\"","If your workspace folder sits inside a folder that already has a version history of its own — a project repository, a shared checkout — circuitRF stops. It keeps no history for that workspace , it says so once when you open it, and a strip at the foot of the window says History held for as long as the workspace is open. Why it stops rather than joining in. A restore point captures everything in the workspace, including files you never opened — that is the whole point of it. Doing that inside a folder somebody is also using for something else would sweep up work in progress that was nothing to do with circuitRF, under a message circuitRF wrote. And circuitRF will not add its own settings files to a folder that belongs to someone else's project. What is therefore not being kept: nothing at all. No restore points are taken, none of the three moments records anything, and an AI assistant is stopped rather than allowed to edit with no floor under it. How to fix it: move or copy the workspace to a folder that is not inside the other one. As soon as it is on its own, circuitRF starts keeping restore points at the next moment worth keeping, and says so. A folder inside your workspace is a different thing A library somebody cloned into your workspace keeps its own history and is left entirely alone — circuitRF excludes it, tells you once which folder it skipped, and everything else in the workspace is kept normally. That is not the held state."],[11,"already","\"This workspace already had version control and circuitRF asked me something\"","If the workspace folder itself already has a version history — you set one up, or you were given one — circuitRF does not take it over. It asks, once, and remembers your answer. your answer what happens Use circuitRF's settings (recommended) circuitRF keeps restore points here and applies its own settings for how the history is looked after. Everything already in the history stays exactly as it is. Keep my settings circuitRF keeps restore points here under your existing settings. See below for what that costs. Don't keep a history here circuitRF records nothing for this workspace, and the window says History held . You can change your mind in Settings ▸ Revision Control. What \"keep my settings\" actually costs. Two things, and both are about getting work back , not about tidiness: A restore point circuitRF tidies away is destroyed permanently after a fortnight , instead of staying recoverable for as long as you leave it. The whole reason the recovery window is measured in weeks is that a design that is wrong but well-formed often is not noticed for weeks — so a fortnight is inside the window this feature exists to cover. The way back from a mistaken reset or rewrite of your own work disappears after thirty days. Everything already in your history survives whichever answer you give. And one thing is circuitRF's either way: it records under the name in Settings ▸ Revision Control, and it never runs the scripts your own tools attach to a change. A restore point that fired somebody's build hook — or that a hook could refuse — would not be a safety net."],[11,"off","Turning it off, and getting rid of it entirely","These are two different things and it matters which one you mean. what circuitRF does what happens to the history you already have Off writes nothing at all kept, listed, and still restorable Removed not something circuitRF does — you delete one folder gone, permanently Off — Settings ▸ Revision Control ▸ Keep a history of this workspace . Untick it and circuitRF stops writing. It deletes nothing. Every restore point you already have stays in the list and you can still go back to any of them; tick it again and it carries on where it left off. There is no checkbox anywhere in circuitRF that destroys a history, and there is no \"delete all history\" command, deliberately. The setting belongs to the workspace, not to you, so: The stretch while it was off is shown as a gap , with an entry at each end saying that recording stopped and started. It is not shown as a quiet week in which you happened not to save anything. It travels. A copy or an archive of a workspace that was switched off arrives switched off, whatever the person opening it prefers — and they are told, with the setting one click away. An AI assistant is stopped , told plainly that no restore point can be kept, and offered the switch — before anything is changed. See below . Removed — delete the .git folder. That is the whole of it: one folder, plainly named, sitting beside your cells, using your file manager. It removes every restore point and touches nothing else. Removing the history cannot harm your design Your design was never inside the history in any meaningful sense. A workspace is ordinary files in a folder; the .git folder sits beside them and holds copies. Delete it and the workspace opens exactly as it did, with every file present and current. Nothing you can do to the history can damage the design."],[11,"disk","\"My disk is full and it says the history is taking the space\"","Two settings answer this, and they do completely different things. Compact — Settings ▸ Revision Control ▸ Compact Now . Stores the same history in less space. Nothing is discarded, nothing becomes unavailable, and it is safe to press at any time. Try this first; on a workspace with large layouts in it, it very often is the whole answer. Reclaim Space — the same tab, below it. This is the one destructive control in the feature, and it exists because thinning frees a restore point's listing and not its contents : a large file you once included and later regretted goes on taking space for as long as you leave it. Reclaiming frees that space for good. It destroys only states that were already tidied away , and only ones tidied away longer ago than the age you set. It asks first, tells you how many, and says in plain words that after it nobody can bring them back. Every restore point still listed, and every change you recorded yourself, survives it untouched, and your design files are never touched at all. Nothing reclaims on a schedule. circuitRF never destroys a state on its own; the age field on its own does nothing until you press the button."],[11,"rewriting","\"Something went in that shouldn't have\"","This section is about a file . If it is a title you want to change, that is a different thing entirely and you can do it — the two rules are separate on purpose. The files in a version are a record, and circuitRF never alters what was recorded. That is the whole value of it: a version whose contents could be quietly edited later is not something you can point at and say \"this is what I sent\". So if something is in the history that must not be — a very large file you regret including, or something that should never have left your machine — circuitRF has no button for it, deliberately, and this is the honest answer rather than a dead end: It is possible, and it is yours to do, with git on the command line. The history circuitRF keeps is an ordinary git repository sitting in a folder called .git beside your cells; every standard tool reads it, including whatever your IT department already supports. Rewriting a history to remove something is a well-documented operation and there are good guides to it. Why circuitRF will not do it for you: rewriting replaces every entry in the history, so every copy anyone else has taken of this workspace stops matching and cannot be brought back into line. That is a consequence nobody should trigger from a menu item, and it is worth doing when it is worth that. If it is only about disk space, it probably is not what you want — Compact and Reclaim Space do that without breaking anything."],[11,"assistants","History and AI assistants","Before an AI assistant changes anything in your workspace, circuitRF keeps a restore point , labelled with what the assistant said it was about to do — \"before: widen the output match\" . Going back to that entry undoes everything the assistant did, in one action. This is automatic, you do not have to ask for it, and it happens before the first change rather than after the last: an assistant that stopped halfway still leaves the restore point exactly where it should be. It is also a floor the assistant cannot go around. If circuitRF cannot keep a restore point first — history is off for this workspace, the workspace sits inside another version-controlled folder, the workspace has not been saved anywhere yet, or you have unsaved changes open — the assistant is stopped and told, and nothing is changed. Switching history off switches this off too , which is the one case where two settings pull against each other. That is a perfectly reasonable thing to choose and a bad thing to discover by accident, so circuitRF says it out loud at the moment it matters: an AI edit asked for while history is off is refused before anything is modified , with a button offering to turn history back on. Being stopped is the feature working, not failing: it is not permitted to make its own backup arrangements instead, because you would then be told you were protected by something you could not go back through. An assistant's restore points are subject to the same tidying-up as the automatic ones. If one of them matters, select it and press Keep permanently ."],[12,"","","Where a circuit is drawn: placing components from the Library Palette, wiring them, editing their values, and setting up the analysis that simulates the result. The schematic is the electrical view of a cell — what the circuit is , as opposed to what it looks like on a symbol or what gets manufactured in a layout. It is the view you spend most of your time in, and it is the only one an analysis runs on."],[12,"orientation","Orientation","The schematic editor with the shipped FET S-parameter test bench open. A schematic is a view of a cell , exactly as its symbol and its layout are; the cell folder holds schematic/ , symbol/ and layout/ side by side and a cell need not have all three. Opening one opens a document tab. You do not need a cell library to start. File ▸ New Schematic opens a standalone scratch sheet you can wire up and simulate immediately, with no workspace around it, and save into a workspace later if it turns out to be worth keeping. Two things about the canvas are worth knowing before you draw on it: Connection is exact, not proximity. Every pin, wire vertex and junction lands on the coarse connection grid , and two things are joined only when they occupy the same grid point. A second, finer authoring grid positions labels and annotations. That rule, and what happens when you paste between designs with different grids, is Grid & Connectivity . Standard-library parts are drawn vertically. R, L, C, the sources, Term and Ground all put port 1 at the top and port 2 at the bottom, so a horizontal signal path means rotating them as you place them. That is the normal workflow, not a workaround — see Orientation convention ."],[12,"palette","The Library Palette","The Library Palette on the All category: every built-in component, four tiles to a row at the width the default dock layout gives the left column. The palette is where every placeable part lives. It has two controls above the tiles and they do different jobs: The category picker. All is the default and shows the whole built-in library in the palette's own pinned order — the parts you reach for most, first. All - Alphabetical is the same set sorted by name, Common is a short curated set, and Recently Used is what you have placed lately. Below those come the built-in categories — Lumped, Devices, System , Nonlinear, Sources, Terminals, Transmission Line, Microstrip, Matching, Data Files — and then one entry per imported kit, with the kit's own groupings indented beneath it. See PDK integration for where kit entries come from. System sits directly after Devices, because a system block is a signal-path part and a user who has found Devices is one row away from the blocks they want. It holds the ideal blocks a system block diagram is drawn out of — circulator, coupler, hybrid, balun, switch, amplifier, attenuator, filter, duplexer — plus the two mixer tiles, which keep Devices as their primary category and appear under both. See System Components . The search box. Matches a part's display name and its search terms, and a kit part's kit name as well. Searching inside a built-in category narrows within it; searching from All searches everything, built-ins and kits together. The tile grid is a reflow grid : the number of columns follows the panel's width, so widening the palette gets you more tiles per row rather than bigger tiles."],[12,"placing","Placing a component","There are two gestures, and they place the identical thing — a kit part in particular resolves through the same path either way, so a drag and a click can never disagree about what you get. Click a tile to arm it. The tile highlights, and a ghost of the symbol follows the cursor over the canvas. Click on the canvas to drop one. The tool stays armed , so click again to place another — this is the fast way to lay down five capacitors. Click the tile a second time, or press Esc , to disarm. Or drag the tile onto the canvas. The ghost follows the drag, snapped to the connection grid, and the component lands where you release. A drag places exactly one part and leaves nothing armed, which is what you want when you are placing one part in the middle of doing something else. While a placement is armed, R rotates the ghost 90° counter-clockwise and Shift + R clockwise — the same two keys that rotate a selection when nothing is armed, so there is one pair of rotate keys to remember rather than two. The ghost is the real symbol, not a stand-in For a part from an imported kit the ghost is built by resolving the cell's own primary symbol, not from its generic SymbolKind . Kit parts share one kind, so a ghost drawn from the kind alone would show a plain box during the drag and then place the kit's real artwork — the drag showing one thing and the result another."],[12,"wiring","Wiring, values and labels","Wire is W (or the toolbar button). Click from one pin to another; Enter finishes the wire in progress and returns you to Select, Esc cancels it. Select is S , and Esc from any tool comes back to it. Double-click does three different things depending on what is under the cursor, and the distinction is worth learning because it is the fastest edit in the application: Double-click on What opens A component's value label ( C = 1 pF ) An inline edit box over the value itself. Type 1.2 nH , 50 Ω , 2 GHz and press Enter . A component's body The full parameter editor for that instance — every parameter, not just the displayed ones. A wire A net label on that wire, so you can name the net and refer to it in a measurement. Arrow keys nudge the selection by one connection-grid step, or five with Shift held. F5 begins Move Labels , for pushing a crowded label block clear of the artwork without moving the component it belongs to. Values are expressions, not just numbers: a VAR block on the sheet declares variables that any component value can refer to, and the same expression language drives sweeps and measurements. See Expressions and Measurements ."],[12,"context-menu","The context menu","Right-clicking a component opens its context menu. Right-clicking a component gives you the operations that apply to it : Edit Parameters , rotate and mirror, the two label commands, Labels ▸ for choosing which parameters show on the sheet, Disconnect , Copy and Delete . Items are disabled rather than hidden , so their positions stay put and you learn the menu by muscle memory. Two more items appear only on a cell instance — Push In and Open Cell in New Tab . Those two are genuinely absent on a resistor rather than greyed, because they are not operations a resistor has; Hierarchy below is what they are for. Right-clicking empty canvas gives a different, one-item menu: Pop Out. Nothing on the component menu applies when the click hit nothing, so the canvas menu carries only the one thing that always does — going back up a level. It is greyed out, with the reason on its tooltip, when you are already at the top of the schematic."],[12,"hierarchy","Hierarchy: putting one schematic inside another","Hierarchy is drawing a circuit once and using it in many places. A bias network, a matching section, a whole amplifier stage — draw it as its own cell , then drop that cell into a bigger schematic as a single component. The big schematic stays readable, and one edit to the cell reaches every place it is used. Two things have to be true before any of it works, and they are the two that trip people up: Hierarchy needs a cell. A schematic on its own is not reusable — only a cell can be placed into another schematic. Making the cell is step 1 below, and it is not optional. The cell goes into a different schematic. A cell cannot contain itself, so the schematic you place it in must be another one. Pushing in is then how you get back down to the first."],[12,"step-by-step","Step by step: a cell inside another schematic","Create the cell. File ▸ New ▸ New Cell… ( ⇧⌘N / Ctrl + Shift + N ), give it a name, and circuitRF makes the cell folder and opens its schematic. The command needs an open workspace — a cell is a folder inside one — so if it is greyed out, open or create a workspace first ( The Workspace ). Draw the sub-circuit, and give it pins. Wire it up as you would any schematic, then place a Pin at every point the outside world needs to connect to. The pins are the cell's ports — the pin numbered 1 becomes port 1 — and a cell with no pins places as a component nothing can be wired to. Pin, Port and Term is worth reading once if that distinction is new. Save with ⌘S . Open the schematic that will use it — a different one, and a saved one. A cell instance records where the cell is relative to the schematic holding it , so a scratch sheet from File ▸ New Schematic has nowhere to record it from; placing into one is refused with \"Save the schematic before placing a cell\" in Messages. Save it into the workspace first, or start it as a cell of its own. Drag the cell out of the Project Tree and onto the canvas. That is the placement gesture. Cells you author are not in the Library Palette — the palette carries the built-in library and any imported kits, and your own cells live in the Project Tree instead. It lands as one component with your cell's pins on it. If the cell has no symbol yet, you are offered an auto-generated one, built from the pin count from step 2. Accept it to keep moving; draw a proper one later in the Symbol Editor . Wire it in and set its parameters like any other component. Double-clicking its body opens the parameter editor for that instance. Push in to edit the cell from here. Click the instance once to select it, then use any of: Right-click ▸ Push In — the item appears only on a cell instance. Right-clicking empty canvas offers Pop Out to come back up. Toolbar button 18 , and 19 to come back out ( the toolbar below numbers them). ⌘] / Ctrl + ] in, ⌘[ / Ctrl + [ out — also on the View menu as Push Into Cell and Pop Out . You are now editing the cell's own schematic, in the same tab . A breadcrumb bar appears above the canvas showing how deep you are ( X1 , the instance you came through); every step in it is clickable, so you can jump straight back to any level rather than popping out one at a time. Push in again from there to go deeper. Once that round trip works, everything else about hierarchy follows from it: An edit to a cell reaches every instance of it, live. That is the whole point — and it is also the thing to be careful about, because pushing in through X1 and changing a value changes X2 too. They are the same cell. Push In is not the only way in. Right-click ▸ Open Cell in New Tab opens the cell as its own document instead, which is what you want when you are going back and forth and would rather have both on screen than one behind the other. Depth is not limited to one level. A cell can instance other cells, and the breadcrumb grows a step each time you push in. Elaboration flattens the whole tree into one netlist before an analysis ever runs, so depth costs you nothing at simulation time. Parameters pass downward. A cell can publish parameters that the parent sets per instance, so two instances of one cell can carry different values. See Expressions for how a cell evaluates them in its own scope. Only the top schematic is simulated. An analysis attaches to the test bench — the schematic you press Run on — and the hierarchy below it is flattened into that run. A cell in the middle of a tree has no analyses of its own."],[12,"views","The other two views","The schematic is one of a cell's three views, and each is edited in its own editor: View Editor What it owns Schematic this page the electrical contents — instances, nets, values Symbol Symbol Editor the glyph an instance draws, and where its pins sit Layout Layout Editor the physical artwork, for fab handoff and EM The symbol is what step 4 above places; the layout is what gets manufactured. A cell need not have all three — the cell folder holds schematic/ , symbol/ and layout/ side by side and any of them may be absent. Two commands under the Design menu move work between the schematic and the layout — Update Layout from Schematic ( ⌘U ) and Update Schematic from Layout ( ⇧⌘U ). Neither ever runs by itself; both are described in Schematic ⇄ layout ."],[12,"analyses","Simulating: an Analysis","A simulation in circuitRF is called an Analysis , and it is configured before it is run. An analysis is not a property of the circuit — it attaches to the test bench , the top schematic you actually simulate — so drawing a circuit does not by itself give you anything to run. Open Simulate ▸ Setup Analyses… (the same list is also available as a dock panel), add the analyses you want, then press Run — ⌘R / Ctrl + R , or the ▶ button at the top of the list. Simulate > Setup Analyses on a test bench carrying two analyses: a DC operating point and a harmonic-balance run wrapped in a Pin drive sweep. The example above is an ordinary two-analysis test bench: a DC operating point, and a Harmonic Balance run wrapped in a parametric sweep of the drive level Pin . Reading the panel: The checkbox enables or disables a row. A disabled analysis stays in the design and is not run — the way to park an analysis without deleting it. The badge is the analysis type: DC , SP , HB , LP , LPP , and SW for a parametric sweep. A sweep is indented under the analysis it wraps , which is how the panel shows that Pin is sweeping HB1 rather than standing on its own. Analyses run in the order listed. Results file names the one file this run writes into the workspace's shared results/ folder. Leave it blank to use the schematic's own name; name it to keep a baseline instead of overwriting the current results. Adding or editing a row opens the analysis editor, where the analysis is actually configured: The analysis editor on that harmonic-balance analysis: the type, the tone, the harmonic order, and the parametric sweep that wraps it. The dialog sizes to its content up to 650 px and scrolls past that, which is why the sweep rows run off the bottom. The Type picker at the top decides everything below it — the body changes to the settings that type has. Here it is the harmonic-balance body: the fundamental tone (given as the expression RFfreq , resolved and previewed underneath), the unit, how many harmonics to retain, the single-versus-multi-tone choice, an Advanced block for the convergence controls, and the Parametric Sweeps section that wraps this analysis in one or more swept variables. The dialog grows to fit whatever the chosen type needs and scrolls beyond that, which is why the sweep's own Start / Stop / Step row runs off the bottom of the figure above. Every analysis type, its full settings, and what it computes are in Simulations — DC , S-Parameters , Harmonic Balance , Parametric Sweep , Loadpull and Loadpull Pursuit . What a run produces Every analysis returns a DataSet — a named bundle of labelled, unit-bearing arrays — and the whole run goes into one .npy file. Open a Data Display to plot it, or read it straight into Python or MATLAB ( Getting results out ). Post-processing a run into named quantities is what Measurements are for. The same run, headless An analysis set up here runs identically from the command line — circuitrf sparam , dc , hb , lp or lpp against an elaborated netlist , with Simulate ▸ Generate Netlist producing the .cnl . See The Command Line ."],[12,"toolbar","The toolbar","Schematic editor toolbar # Button What it does 1 Zoom to Fit (F) 2 Reset to 1:1 zoom 3 Zoom Box (Z) 4 Select (S) 5 Wire (W) 6 Place Ground (Shift+G) 7 Place Term (T) 8 Place Pin (P) 9 Rotate 90° CCW (R) 10 Rotate 90° CW (Shift+R) 11 Mirror Horizontal (M) 12 Mirror Vertical (Shift+M) 13 Delete (Del) 14 Snap: Fine Grid (G) 15 Disable → Open circuit (Q) — toggles if all selected are already disabled 16 Disable → Short circuit (toggles if all selected are already disabled) 17 Save schematic (Ctrl+S) 18 Push Into Cell (Ctrl+]) 19 Pop Out (Ctrl+[) Grouped by what you are doing, left to right: View — Zoom to Fit, reset to 1:1, and a zoom box. F is the one to learn; a schematic is usually bigger than the window. Draw — Select, the Wire tool, and the three things you place constantly: Ground , Term (a numbered port with a reference impedance, for an S-parameter run) and Pin (a connection point of the cell you are drawing). Which is which is worth reading once. Orient — rotate 90° either way and mirror in either axis, applied to the selection. Delete — on the selection. Snap — cycles the snap mode. Electrical points always land on the connection grid whatever this says; the snap mode governs the authoring grid. Disable — mark selected components open or short . This takes an element out of the circuit without deleting it, which is what you want when you are bracketing a problem. Save . Hierarchy — push into the selected cell, or pop back out. See also: Symbol Editor · Layout Editor · Grid & Connectivity · Components · Pins, Ports & Terms · Simulations · File formats ( .csch )."],[13,"","","Every component in the standard library — its symbol, what it does, and its parameters. Symbols are rendered from the live drawing engine, with their connection leads and their pins shown unconnected — the component as you meet it in the palette, before anything is wired to it. Parameter tables are read from the component registry, so the defaults, units and on-schematic visibility below are the as-placed values by construction rather than by transcription. Reading the tables The first table is the component's terminals : the nets its netlist line takes, in the order it takes them. That order is what the models themselves read — a diode's anode before its cathode, a MESFET's gate before its drain — so it is worth checking before hand-writing a .cnl line; the figure above it shows where those pins sit, not which is which. Where a terminal has a name it carries it, and where it does not the sentence under the table says whether the order matters — most two-terminal parts may be wired either way round, and a few that look symmetric may not. Read that sentence rather than the numbers: a resistor's ends are interchangeable, an inductor's stop being interchangeable the moment a Mutual couples it to another, and a taper's two ends are its two widths. In the parameter table below it, Name is the parameter key, as it appears in the editor and in the netlist. Default and Unit are the as-placed value. A parameter marked shown appears as a label on the schematic by default; the rest are available in the parameter editor. Units accept SI prefixes ( pF , nH , GHz , kΩ )."],[13,"lumped-elements","Lumped elements",""],[13,"resistor","Resistor (R)","R An ideal, frequency-independent resistor. R is the resistance. Net Terminal 1 1 2 2 The nets of an instance line, in this order. The two terminals are interchangeable — swapping them gives the same circuit. Name Default Unit On schematic R 1 Ω shown"],[13,"inductor","Inductor (L)","L An ideal inductor. L is the inductance. Couple two inductors with a Mutual element. Net Terminal 1 1 2 2 The nets of an instance line, in this order. Interchangeable on its own — but NOT once coupled. A Mutual (M) couples the two elements' branch currents, and each branch current runs from that element's own terminal 1 to its terminal 2, so swapping one of them reverses the sign of the coupling. Terminal 1 is the dotted end. Name Default Unit On schematic L 1 nH shown"],[13,"capacitor","Capacitor (C)","C An ideal, linear capacitor. C is the capacitance. For a voltage-dependent capacitance, see NonlinearC . Net Terminal 1 1 2 2 The nets of an instance line, in this order. The two terminals are interchangeable — swapping them gives the same circuit. Name Default Unit On schematic C 1 pF shown"],[13,"srlc","Series RLC (SRLC)","SRLC A resistance, an inductance and a capacitance in series , as one part. R , L and C are the three values; the branch impedance is R + jωL + 1/(jωC) . This is the shape a real capacitor takes above a few hundred megahertz, which is the usual reason to place one: a ceramic capacitor's datasheet gives an ESR and an ESL, and those go straight into R and L . The part is series-resonant at 1/(2π√(LC)) , where its impedance falls to R — below that frequency it behaves as a capacitor, above it as an inductor. At DC the series capacitance makes the branch an open circuit. Its pins sit exactly where a plain R , L or C 's do, so you can swap one in for another without moving any wires. Its inductance can be coupled with a Mutual , which names the SRLC instance in place of an inductor. Net Terminal 1 1 2 2 The nets of an instance line, in this order. Interchangeable on its own — but NOT once coupled. A Mutual (M) couples the two elements' branch currents, and each branch current runs from that element's own terminal 1 to its terminal 2, so swapping one of them reverses the sign of the coupling. Terminal 1 is the dotted end. Name Default Unit On schematic R 1 Ω shown L 1 nH shown C 1 pF shown"],[13,"prlc","Parallel RLC (PRLC)","PRLC The same three values in parallel — a tank. R , L and C all sit across the same two nodes, giving an admittance of 1/R + jωC + 1/(jωL) . At the parallel resonance 1/(2π√(LC)) the reactive parts cancel and the part is purely resistive at R , which is what makes it the natural way to enter a measured resonance: R sets the peak impedance, L and C set where it sits and how sharp it is. At DC the ideal inductor shorts it out. Pin positions and Mutual coupling work exactly as they do for the SRLC above. Net Terminal 1 1 2 2 The nets of an instance line, in this order. Interchangeable on its own — but NOT once coupled. A Mutual (M) couples the two elements' branch currents, and each branch current runs from that element's own terminal 1 to its terminal 2, so swapping one of them reverses the sign of the coupling. Terminal 1 is the dotted end. Name Default Unit On schematic R 1 Ω shown L 1 nH shown C 1 pF shown"],[13,"bead","Ferrite Bead (Bead)","Bead A two-terminal linear element: Rdc in series with a parallel L / Rp / Cp tank. A bead is not an inductor and not a series RLC. The number a data sheet gives — \"600 Ω at 100 MHz\" — is an impedance , and the whole point of the part is that most of it is resistive at the frequency it is quoted at. That is what makes a bead absorb rather than reflect, and why it damps a supply rail where an inductor of the same reactance would ring against the decoupling capacitance. An inductor's loss is zero and a series RLC's is a constant R ; a bead's rises from nothing at DC to a maximum at its ferromagnetic resonance and falls again above it. Each element stands for a real mechanism: Parameter What it is Rdc The winding's own resistance. It is what the part looks like at DC, and in a power rail it is what sets the drop. L The low-frequency inductance — what the impedance rises along. Rp The core loss . It caps the impedance: at the parallel resonance the reactive branches cancel and |Z| is Rdc + Rp , which is the peak a data sheet plots. Nothing else sets that peak, so a bead entered without it has no maximum at all. Cp The parallel (inter-turn) capacitance — what takes the impedance back down above resonance. A bead is not a filter above its own resonance, and this is why. Zero means not modelled for each of the three parallel elements , never \"a short\" or \"a zero-ohm resistor\". Rp = 0 removes the loss branch and leaves an ideal L ; Cp = 0 removes the capacitive branch and the impedance goes on rising; L = 0 leaves a plain Rdc . At DC the bead is Rdc and nothing else , because the inductive branch shorts the tank out. That is both the physics and what a DC operating point needs from this part — a bead in a supply rail must not open it. Saturation is not modelled and cannot be A bead's inductance falls with DC bias current, sometimes by most of it, and this is a linear element. The four numbers describe the part at whatever current they were measured at, which is why a bead chosen from a small-signal impedance curve can behave quite differently in the rail it was chosen for. Net Terminal 1 1 2 2 The nets of an instance line, in this order. The two terminals are interchangeable — swapping them gives the same circuit. Name Default Unit On schematic Rdc 0.01 Ω shown L 0 uH shown Rp 0 Ω shown Cp 0 pF shown"],[13,"nonlinearc","Nonlinear Capacitor (NonlinearC)","NonlinearC A capacitor whose capacitance varies with voltage, C(V), modelled as a polynomial (Taylor) series. Enter the coefficients directly, or generate them from a C–V curve with the C–V Editor. C0 is the constant term; C1 , C2 , … are the higher-order coefficients, added in the editor or generated by the C–V Editor. Full treatment: The Nonlinear Capacitor & the C–V Editor . Net Terminal 1 1 2 2 The nets of an instance line, in this order. C(V) is evaluated at V(terminal 1) − V(terminal 2), so any odd coefficient (C1, C3, …) makes the two terminals distinct. With C0 alone the capacitor is linear and they are interchangeable. Name Default Unit On schematic C0 1 pF shown"],[13,"mutual","Mutual Inductance (M)","M Couples two existing inductors — named by instance — with a mutual inductance, the basis for transformers and coupled resonators. It has no pins of its own; Inductor1 and Inductor2 are the instance names of the two coupled inductors and M is the mutual inductance between them. Either name may be an L , an SRLC or a PRLC — all three carry an inductor the coupling can act on, and all three spell its value L . Naming anything else is reported as an error when the design is elaborated. No terminals — this component connects to nothing on the canvas. Name Default Unit On schematic Inductor1 \"L1\" — shown Inductor2 \"L2\" — shown M 0 pH shown"],[13,"match","Matching Network (Match)","Match A synthesised bandpass matching network placed as a single two-pin component, ground being the common return. Its whole design rides in a hidden Design parameter, so what it stamps is a property of the design rather than of the component type: the ladder it contains is the synthesised one minus whatever the two external terminations already supply, since absorbing those reactances is the point. Edit it in the Match Designer. Match has its own chapter The synthesis, the Designer's four panes, the Norton-transform rack, the Probe button and a worked two-stage interstage example are all in The Match Component . Read that before placing one — this entry is only the component's parameter table. Net Terminal 1 1 2 2 The nets of an instance line, in this order. Not interchangeable: terminal 1 is the R1 side and terminal 2 the R2 side. The synthesised ladder is stored R1-first, so swapping the two reverses every asymmetric match. Name Default Unit On schematic F1 1.8 GHz — F2 2.2 GHz — Bands 1 — — F3 0 GHz — F4 0 GHz — F5 0 GHz — F6 0 GHz — Order 4 — — Response ChebyshevFano — — Form Bandpass — — R1 50 Ω — R2 10 Ω —"],[13,"sources","Sources",""],[13,"vdc","DC Voltage Source (Vdc)","Vdc A fixed DC voltage — gate and drain bias supplies. Vdc is the voltage. Net Terminal 1 + 2 − The nets of an instance line, in this order. Vdc is the voltage at + relative to −. Name Default Unit On schematic Vdc 0 V shown"],[13,"tonesource","Tone Source (VTone)","VTone A single-tone (sinusoidal) voltage source for harmonic balance, with an optional DC offset. V is the tone amplitude, Freq the tone frequency, and Vdc a DC offset. Net Terminal 1 + 2 − The nets of an instance line, in this order. V and Vdc are the voltage at + relative to −, and Phase is referred to the same pair. Name Default Unit On schematic V 1 V shown Freq 1 GHz shown Phase 0 deg — Vdc 0 V —"],[13,"currenttonesource","Current Tone Source (ITone)","ITone The current-source dual of VTone — a single-tone sinusoidal current source with an optional DC offset. I is the tone amplitude, Freq the tone frequency, and Idc a DC offset. Use it to drive a node with a known current rather than a known voltage: an ideal current source has infinite output impedance, so it sets the current through a branch and lets the network decide the voltage. The arrow says which way the current goes. A positive I delivers current into pin 1 (the arrowhead end, the top pin as drawn) and draws it out of pin 2. Note this is the opposite of the SPICE I element, which sinks current from its first node; circuitRF uses one direction convention for every source it has, and the glyph states it so you never have to remember which. Like VTone, the + button in the parameter editor turns it into a multi-tone source: the scalar I / Freq migrate to I[1] / Freq[1] and each further tone gets its own Freq[n] , I[n] and Phase[n] . An ideal current source is an open circuit It contributes no conductance at all, and none whatsoever at frequencies it is not exciting. A node driven only by a current source therefore has no DC path to ground and the matrix is singular — that is the physics of the element, not a defect. Give the node a resistor, a termination or a bias path and it solves. Net Terminal 1 + 2 − The nets of an instance line, in this order. A positive I is delivered INTO the + terminal and drawn out of the −, which is what the arrow on the + lead points at. Note this is the opposite sense to the VCCS's arrow, which is a controlled source and is drawn the way controlled sources are. Name Default Unit On schematic I 1 mA shown Freq 1 GHz shown Phase 0 deg — Idc 0 mA —"],[13,"p1tone","RF Power Source (P1Tone)","P1Tone An RF source specified by available power with an internal source impedance — the natural drive for power-amplifier and loadpull work. It can also serve as an S-parameter port (via Num ), and accepts per-harmonic source terminations Z[k] for harmonic source-pull. Num — S-parameter port index, sharing one pool with Term. Pavl — available power. Z — fundamental source impedance; Freq — fundamental frequency; Phase — phase. Z[k] — per-harmonic source termination: Z[0] is baseband/DC, Z[2] the second harmonic, and so on. Added in the parameter editor. Net Terminal 1 + 2 − The nets of an instance line, in this order. + is the RF output and − the reference, normally ground — Term's own convention, since this is a source with Term's internal resistance in front of it. The source's internal impedance Z is in series with +. Name Default Unit On schematic Num 1 — shown Pavl 0 dBm shown Z 50 Ω shown Freq 1 GHz shown Phase 0 deg —"],[13,"pntone","Multi-Tone RF Power Source (PnTone)","PnTone The multi-tone sibling of P1Tone — an available-power RF source that injects several carriers at once from a single component , the natural drive for two-tone intermodulation work. A freshly placed PnTone is already a two-tone source (tones 1 and 2, at 1.99 and 2.01 GHz); the + and − buttons in the editor add and remove tones. Each tone has its own Freq[i] , Pavl[i] and Phase[i] ; all tones share one source impedance Z and the same per-band terminations Z[k] , where each mixing product is terminated by the band its frequency falls in. Unlike P1Tone, PnTone is not an S-parameter port — it has no Num . The tones a PnTone drives must match the tones declared on the Harmonic Balance analysis : the analysis owns the mixing grid, and the source just supplies power at those frequencies. Net Terminal 1 + 2 − The nets of an instance line, in this order. + is the RF output and − the reference, normally ground — Term's own convention, since this is a source with Term's internal resistance in front of it. The source's internal impedance Z is in series with +. Name Default Unit On schematic Freq[1] 1.99 GHz shown Pavl[1] 0 dBm shown Phase[1] 0 deg — Freq[2] 2.01 GHz shown Pavl[2] 0 dBm shown Phase[2] 0 deg — Z 50 Ω shown"],[13,"vccs","Voltage-Controlled Current Source (VCCS)","VCCS An ideal transconductance: the current it delivers is G times the voltage across a separate, purely-sensing control pair. I = G · (V(ctrl+) − V(ctrl−)) Four terminals in two pairs — the output pair ( out+ top, out− bottom) carries the current, and the control pair ( ctrl+ , ctrl− , on the left) senses the controlling voltage. The control pair draws no current at all , which is what makes the source ideal and what makes the device unilateral: nothing travels backwards through it. G is the transconductance, in siemens. The arrow says which way the current goes , and it points down : a positive G and a positive control voltage draw current in at out+ and out at out− . That is the SPICE G element's own direction, and the way a small-signal transconductance is drawn in a device model — the controlled source sinks drain current from the drain node. So a VCCS across a grounded load resistor is inverting , and a 50 Ω, G = 10 mS stage measures S21 = −0.25. Note the VCCS's arrow points the opposite way to ITone 's. That is not an inconsistency to squint past: ITone is an independent source and delivers current to its arrow pin, while the VCCS is a controlled source drawn the way controlled sources are drawn. Read each symbol's own arrow. It works in every analysis, harmonic balance included The VCCS is a linear device, so it is stamped into the matrix at every frequency the simulator solves at: DC, S-parameters, and every retained harmonic of a harmonic-balance run , plus everything built on those (parametric sweeps, loadpull, loadpull-pursuit). In HB it lives in the linear partition alongside the resistors and lines, so G is the same number at every harmonic — an ideal transconductance has no frequency dependence, no compression, and no delay. If you need any of those, the device you want is an SDD , whose equations can state them. The same is true of ITone . Net Terminal 1 out+ 2 out- 3 ctrl+ 4 ctrl- The nets of an instance line, in this order. Name Default Unit On schematic G 10 mS shown"],[13,"vcvs","Voltage-Controlled Voltage Source (VCVS)","VCVS An ideal voltage gain: the voltage it holds across its output pair is E times the voltage across a separate, purely-sensing control pair. V(out+) − V(out−) = E · (V(ctrl+) − V(ctrl−)) Four terminals in two pairs, laid out exactly as the VCCS 's are — the output pair ( out+ top, out− bottom) and the control pair ( ctrl+ , ctrl− , on the left), which draws no current at all . E is a voltage ratio and carries no unit. The two tiles differ in one thing: what the diamond carries. The VCCS's arrow points at the current it delivers; the VCVS has a ± pair down its axis instead, because there is no current direction to point at. A VCVS states a potential difference and whatever current the rest of the circuit draws through it flows — including none, if nothing is connected across the output pair. That is the ideal source's own behaviour, not a limitation. Being ideal, its output impedance is zero : it holds E times the control voltage no matter what it is loaded with. Put a series resistor after it if you want a source with an impedance. It works in every analysis, harmonic balance included Like the VCCS, the VCVS is a linear device stamped at every frequency the simulator solves at, harmonic balance included, so E is the same number at every harmonic. It carries a branch-current unknown of its own where the VCCS carries none — a relation between node voltages is not something admittances can express — but nothing about that is visible from outside. For a gain that varies with frequency, drive or bias, the device you want is an SDD . An ideal current source is an open circuit The output pair gets no conductance of its own, so — exactly as with ITone — an output node with nothing else attached to it has no DC path to ground and the matrix is singular. Load it. Net Terminal 1 out+ 2 out- 3 ctrl+ 4 ctrl- The nets of an instance line, in this order. Name Default Unit On schematic E 1 — shown"],[13,"terminals-ports","Terminals & ports",""],[13,"ground","Ground (GND)","GND The global reference node (net 0 ). No parameters."],[13,"term","Term","Term An S-parameter port: a numbered, reference-impedance termination where the simulator excites and measures. Num is the port index, auto-assigned at placement; Z is the reference impedance. See Pins, Ports & Terms for how it differs from a Pin. Net Terminal 1 + 2 − The nets of an instance line, in this order. Name Default Unit On schematic Num 1 — shown Z 50 Ω shown"],[13,"termg","Grounded Term (TermG)","TermG A Term whose second port is permanently grounded, presenting as a one-port. It is a packaging convenience rather than a parallel model: it uses Term's own engine component and glyph exactly, with the ground symbol drawn at Term's port-2 location, so a schematic that swaps Term + GND for a TermG is electrically identical. One terminal, + — an instance line writes a single net. Name Default Unit On schematic Num 1 — shown Z 50 Ω shown"],[13,"pin","Pin","Pin A cell's interface terminal — connectivity only, no electrical model. Pins on a cell's symbol are how the cell connects to the parent that instances it. Num is the interface port index, auto-assigned; Name is an optional label, and extraction uses P{Num} when it is blank. An optional Polarity of Plus or Minus , set in the editor, forms a differential pair sharing one Num . One terminal, 1 — an instance line writes a single net. Name Default Unit On schematic Num 1 — shown Name — — —"],[13,"iprobe","Current Probe (IProbe)","IProbe A 0 V series ammeter placed in a branch to read its current. Its instance name — Iout , say — is how measurements reference that current: I(\"Iout\", 1) . No parameters. Net Terminal 1 np 2 nm The nets of an instance line, in this order. A 0 V ammeter: the current it reports flows np to nm. Swapping the two negates every measurement that reads it. No parameters."],[13,"vprobe","Voltage Probe (VProbe)","VProbe A one-terminal voltage probe. Drop it anywhere on a wire and that net is reported under the probe's instance name — Vout , say. That name is what the trace picker lists and what a measurement expression reads: V(\"Vout\") . It is a label, not a part. It has one terminal, so it cannot bridge two nets; the elaborator builds no model for it, so it stamps nothing; and a design simulates identically with every VProbe deleted. Placing one can never change an answer — it can only add a name to it. On an unnamed wire the probe's name becomes the net's name. There is one row in the results, called Vout , and no n7 beside it — the automatic name was never a name anyone chose, and two rows for one point in the circuit is one row too many. On a wire you have already labelled, both names appear. The label is not overwritten: a wire labelled n_drain with a Vout probe on it shows up twice — as n_drain and as Vout — carrying the same voltage. The probe is an alias there, and either name plots and measures the same node. Two rules keep the names unambiguous, and breaking either refuses the run rather than reporting: Two VProbes may not share a name. A VProbe may not share a name with a net. A VProbe that touches nothing is reported in the Messages panel and drops out of the results, rather than reporting a plausible number for a point you never probed. So is one placed on ground, which is 0 V by definition. It is the one component you can drag off a wire. Every other symbol takes its wires with it when you move it, because moving the picture must never re-wire the circuit. A probe has nothing to re-wire, so dragging it leaves the drawing exactly as it was and puts the probe wherever you dropped it — pick it up, look somewhere else, run again, with no repair work in between. Rotating it behaves the same way, and a wire dragged out from under a probe simply leaves it behind. Select the probe and its wire together and they move as one, so the probe stays on the net. One terminal, v — an instance line writes a single net. One terminal, at the arrow's tip. It taps the net it touches and names it after the instance; there is no second terminal to get the wrong way round, and nothing it can short together. No parameters."],[13,"wsprobe","Stability Probe (WSProbe)","WSProbe A stability probe, placed in a node so that it splits the node into a generator-side terminal ( G ) and a load-side terminal ( L ). It implements T. A. Winslow, General Circuit Analysis Using The WSProbe (2023); every quantity below is that document's, under its own name. Electrically it is an IProbe — a 0 V short — and it is placed the same way: drop it onto the wire whose node you want to look at, and circuitRF deletes the stretch of wire between its two pins so the probe breaks into the run instead of being shorted by it. It perturbs nothing, so a design simulates identically with every WSProbe deleted. It takes no parameters. The letters are the orientation. G is the generator-side terminal and L the load-side one: ZG looks out of G , ZL out of L . Rotating or mirroring the symbol moves the drawing and never the pin order, so swapping the two means swapping the wires — which swaps every G / L -labelled output and negates nothing else. What it produces is not a number but a matrix. An S-parameter run with any WSProbe in it writes a wsp cube — 2N x 2N for N probes — and six named outputs per probe: H0 and Y0 , the driving-point impedance and admittance; ZG and ZL , the bidirectional impedances; LG , the bilateral loop gain; and F = 1 - LG . Two stability margins, SM_Y0 and SM_H0 , come with them. Everything else the document derives — every other loop gain, the reduced two-port at the probe, the probe-pair blocks, the global loop gains over a set of probes — is post-processing of that one matrix, and is available in the Data Display's trace card and in a measure line. Reading a node for stability is two plots, not one. Plot 1/H0 and 1/Y0 on a polar chart and look for a clockwise crossing of the negative real axis; the trace card reports the frequencies it finds, or none . Both must be checked, because a zero can hide the pole in one of them but never in both — a series resonance shows in 1/Y0 alone and a parallel one in 1/H0 alone. The stability margin is a distance, and the search is the detector. SM_Y0 and SM_H0 are unitless numbers in [0, 1] , shown in dB ( 20*log10 ), and the trace card prints each one's minimum beside the crossing search that pairs with it. A node with positive resistance on both sides can never read below -12 dB , so anything under that line certifies negative resistance on one side; the run reports any probe whose margin falls below the analysis' MarginThreshold (default -15 dB) as worth looking at. That report is a note, not a warning: a low margin is somewhere to look, not a failure. The WSProbe has its own chapter — every quantity with its equation, the probe pairs, the global loop gains, the stability envelope, the probe under harmonic balance, the NDF, and an appendix on stability analysis itself. Net Terminal 1 G 2 L The nets of an instance line, in this order. G is the generator-side terminal, L the load-side. ZG looks out of G, ZL out of L; swapping them swaps every G/L-labelled output and negates nothing else. No parameters."],[13,"tuner","Tuner / SourceTuner / LoadTuner","Tuner SourceTuner LoadTuner A programmable RF termination. The three variants are the same engine component with different glyphs and net ordering — match the symbol to its role. SourceTuner sits on the source side, LoadTuner on the load side. Z[1] — the fundamental termination. Required, and it accepts complex literals such as 50+j*10 . Add Z[2] , … in the editor, or G[k] to give a reflection coefficient instead. Zdefault — the catch-all termination for harmonics not otherwise specified, and the impedance the tuner presents at DC. Z0 — the reference impedance for any G[k] entry. BiasTee — on or off , chosen from a picker. on builds the internal bias-tee and DC supply, which the loadpull directive requires. With it off the tuner is RF-only: there is no DC path through it to the reference node. Vbias — the DC bias at the DUT-facing port when the bias-tee is on. ShowBias — true or false , chosen from a picker. Display only: it draws the bias-tee on the glyph and never reaches the engine, so the extracted netlist is identical either way. One terminal, 1 — an instance line writes a single net. Name Default Unit On schematic Z[1] 50 Ω shown Zdefault 1e-6 Ω — Z0 50 Ω — BiasTee off — — Vbias 0 V — ShowBias false — — Which analyses a tuner works in All five that terminate a circuit: DC , S-parameters , harmonic balance , loadpull and loadpull pursuit . It is an ordinary linear component, so a parametric sweep wrapping any of them sweeps it like anything else. How its frequency response is defined A tuner is not a fitted network. It presents the impedance you declared, exactly, with no interpolation between entries and no roll-off. Which entry it presents depends on the analysis: Analysis What the tuner presents DC Zdefault — but behind the ideal DC-block capacitor, so with BiasTee=on the DC seen at the pin is the supply through the choke, and with BiasTee=off the pin is open at DC. S-parameters Z[1] , flat across the whole frequency sweep. There is no harmonic structure outside harmonic balance, so the declared fundamental is the only value in play — Z[2] , Z[3] , … are ignored. Harmonic balance Z[k] at harmonic k , where k is the analysis fundamental's k -th multiple. Anything not declared falls back to Zdefault , as does any frequency that is not on the harmonic grid. Loadpull / pursuit The same per-harmonic rule, except that the harmonic being tuned takes its value from the grid point under test rather than from Z[k] . So the answer to \"what does Z[2] do in an S-parameter run?\" is nothing : declare the band you care about in Z[1] , and use several S-parameter analyses if you want several terminations. In loadpull and pursuit, the SourceTuner is also the RF generator A loadpull run needs a drive as well as terminations, and the SourceTuner supplies it : the loadpull and pursuit engines assign it the source role, which stamps a single-tone RF generator inside it, behind the impedance it presents. The generator is calibrated to available power — |V s | = √(8·P avl ·Re Z 1 ) — so the power you ask for and the impedance the DUT is driven from stay consistent as either one moves across the sweep. The drive level is not a tuner parameter. There is no Pavl row on the component, because the sweep over drive belongs to the run, not to the part. The ladder is set in the analysis: Pin start (dBm) , Pin max (dBm) and Pin step (dB) in the Loadpull and Loadpull Pursuit setup, walked upward from the start until compression is reached or the cap stops it. Outside loadpull, a SourceTuner does not drive The source role is assigned by the loadpull and pursuit engines only. Place a SourceTuner on a plain harmonic-balance testbench and it behaves as a passive termination like the other two glyphs — put a P1Tone or a tone source on the bench for the drive."],[13,"transmission-lines","Transmission lines",""],[13,"tline","Ideal Transmission Line (TLIN)","TLIN An ideal, lossless transmission line specified by characteristic impedance Z and electrical length E at a reference frequency F . Net Terminal 1 1 2 2 The nets of an instance line, in this order. The two terminals are interchangeable: the line is uniform, so neither end is the input. Name Default Unit On schematic Z 50 Ω shown E 90 deg shown F 1 GHz shown"],[13,"mlin","Microstrip Line (MLIN)","MLIN A physical microstrip line on the current technology's substrate: W is the conductor width and L the length. Unlike TLIN, its impedance and loss follow from the geometry and the substrate rather than being stated. Net Terminal 1 1 2 2 The nets of an instance line, in this order. The two terminals are interchangeable: the line is uniform, so neither end is the input. Name Default Unit On schematic W 2.9 mm shown L 10 mm shown SignalLayer — — — GroundReference — — —"],[13,"mbend","Microstrip Bend (MBEND)","MBEND A right-angle microstrip bend of width W , optionally mitred, modelled with its discontinuity rather than as an ideal corner. Net Terminal 1 1 2 2 The nets of an instance line, in this order. The two terminals are interchangeable. One width and one angle describe the whole bend, so the pins differ only in which way it turns on the canvas. Name Default Unit On schematic W 2.9 mm shown Angle 90 deg shown Miter 2 — shown SignalLayer — — — GroundReference — — —"],[13,"mtee","Microstrip T-Junction (MTEE)","MTEE A three-port microstrip T-junction. W1 , W2 and W3 are the widths of the three arms. Net Terminal 1 1 2 2 3 3 The nets of an instance line, in this order. Name Default Unit On schematic W1 2.9 mm shown W2 2.9 mm shown W3 2.9 mm shown L1 2.9 mm shown L2 2.9 mm shown L3 2.9 mm shown SignalLayer — — — GroundReference — — —"],[13,"mcross","Microstrip Cross-Junction (MCROSS)","MCROSS A four-port microstrip cross-junction, with a width per arm. Net Terminal 1 1 2 2 3 3 4 4 The nets of an instance line, in this order. Name Default Unit On schematic W1 2.9 mm shown W2 2.9 mm shown W3 2.9 mm shown W4 2.9 mm shown L1 2.9 mm shown L2 2.9 mm shown L3 2.9 mm shown L4 2.9 mm shown SignalLayer — — — GroundReference — — —"],[13,"mtaper","Linear Microstrip Taper (MTAPER)","MTAPER A linearly tapered microstrip line running from width W1 to width W2 over length L . Net Terminal 1 1 2 2 The nets of an instance line, in this order. Not interchangeable: terminal 1 is the W1 end and terminal 2 the W2 end. Name Default Unit On schematic W1 2.9 mm shown W2 1.0 mm shown L 10 mm shown SignalLayer — — — GroundReference — — —"],[13,"mklopf","Klopfenstein Taper (MKLOPF)","MKLOPF A Klopfenstein-taper microstrip line — the taper profile that gives the shortest line for a stated in-band ripple. Specify the two impedances (or the two widths), the maximum reflection GammaMax , and either a length or a 3 dB corner frequency. Net Terminal 1 1 2 2 The nets of an instance line, in this order. Not interchangeable: terminal 1 is the Z1 end and terminal 2 the Z2 end. Name Default Unit On schematic Z1 50 Ω shown Z2 100 Ω shown GammaMax 0.05 — shown L 20 mm shown Offset 0 mm shown SmoothSteps 1 — shown SignalLayer — — — GroundReference — — —"],[13,"wbond","Wirebond (wBond)","wBond A wirebond design placed as a single component. Its symbol is generated from the design it carries — two pins per wire array plus a REF pin — so both the pin count and the pin names are properties of that design rather than of the component type. The wires themselves are drawn in the layout view, not here. wBond has its own chapter The geometry, loop height and span, the inductance and capacitance models, the 3D kernel, DXF interchange and Touchstone export are in wBond . The two parameters most often left wrong — IncludeCapacitance and er — are explained there. Terminals are set by Arrays ; this component has no fixed pin geometry of its own. Name Default Unit On schematic Arrays G1 — — Source Carried — — File — — — SymbolPitch Loose — — RefPin false — — IncludeCapacitance true — — er 1 — — Temp — — — GroundPlane — — — LoopHeight — mil — Diameter — mil — Material — — —"],[13,"data-file-components","Data-file components",""],[13,"snp","Touchstone N-Port (SnP)","S2P An N-port backed by a Touchstone ( .sNp ) file — measured or modelled S-parameters embedded in the circuit and interpolated onto the analysis sweep. NumPorts — the port count. Hidden; it drives the symbol and the pin count. File — the path to the Touchstone file. RefNode — when true, expose a floating common reference pin (N+1 nets) instead of grounding each port. PinConfig and Pitch — the pin arrangement and spacing on the symbol. InterpMode , InterpDomain and ExtrapMode — how the data is interpolated between points, in which domain, and what happens beyond its frequency range. The symbol is dynamic; the pin arrangement, the pitch and the floating-reference option are covered in Dynamic symbols . Net Terminal 1 1 2 2 The nets of an instance line, in this order. How many there are is set by NumPorts ; the table shows NumPorts = 2 . Name Default Unit On schematic NumPorts 2 — — File — — shown RefNode false — — PinConfig Standard — — Pitch Loose — — InterpMode CubicSpline — — InterpDomain MA — — ExtrapMode NearestEdge — —"],[13,"spicemodel","SPICE Model or Subcircuit","SPICE A SPICE .model card or .subckt definition placed as a component and run from its own file . Nothing is copied: edit the file and the schematic follows it. File — the path to the SPICE file. Name — which definition in that file to run. A vendor file usually holds a part and every piece the part is built from, each of them a definition in its own right; leaving Name blank picks the highest-level one, the one nothing else in the file calls. PinConfig and Pitch — the pin arrangement and spacing, for a .subckt only. A .model card draws as the device itself, whose terminals are where that device's terminals are. Any parameter the chosen .subckt declares appears as an ordinary editable row below the panel, seeded at the definition's own default. The symbol is dynamic and is generated from the file. A .model card circuitRF has a device for draws as that device — a diode card draws as a diode — so the schematic says which lead is which without anything being opened. A .subckt draws as a box carrying the definition's own port names. Until a file is chosen it draws as a generic two-port, which can be placed and wired first. A model type circuitRF has no equivalent for is refused by name , in the parameter dialog at the moment the file is chosen and again at Run. Nothing is approximated: the nearest native device would simulate and be quantitatively wrong. This is the reference; the import is the copy. The project tree's Copy to Workspace as Cell… reads the same file and builds an editable cell out of it — a schematic you can open, re-symbol and change. Use that when the model is a starting point. Use this component when the file is the authority and must stay so. Because there is no .csch behind it, a SpiceModel has no pop-in: there is nothing to push into. Net Terminal 1 1 2 2 The nets of an instance line, in this order. Name Default Unit On schematic File — — shown Name — — shown Section — — — PinConfig Standard — — Pitch Loose — —"],[13,"zport","Impedance N-Port (Z)","Z2P An N-port defined by its impedance matrix Z[p,q] . NumPorts is the port count, hidden, and it drives the symbol and pin count. The symbol grows with the port count — see Dynamic symbols . Net Terminal 1 1+ 2 1- 3 2+ 4 2- The nets of an instance line, in this order. How many there are is set by NumPorts ; the table shows NumPorts = 2 . Name Default Unit On schematic NumPorts 2 — — Z[1,1] 50 Ω shown Z[1,2] 50 Ω shown Z[2,1] 50 Ω shown Z[2,2] 50 Ω shown"],[13,"nonlinear-devices","Nonlinear devices",""],[13,"sdd","Symbolically-Defined Device (SDD)","SDD2 A user-defined nonlinear device: you write each port current — and, optionally, each port charge — as an equation in the port voltages, and circuitRF differentiates it automatically for the solver. This is how the GaN FET model in the netlist example is defined. The symbol is dynamic; see Dynamic symbols . I[x,0] — the port-x current equation, seeded as a 50 Ω conductance. Weight 0 is the current itself. Q[x] , I[x,w] , H[w] — optional charge equations, higher weightings, and weighting functions. C[n] / Cport[n] — bind a control current _cn to another device's current. Net Terminal 1 1+ 2 1- 3 2+ 4 2- The nets of an instance line, in this order. How many there are is set by NumPorts ; the table shows NumPorts = 2 . Name Default Unit On schematic NumPorts 2 — — I[1,0] _v1/50 — shown I[2,0] _v2/50 — shown The SDD has its own chapter The SDD is deep — the equation grammar ( I[p,w] , Q[p] ), weighting functions H[w] , and how to reference another device's current ( _cn / C[n] ) are all covered in The SDD ."],[13,"diode","Junction Diode (Diode)","Diode A junction diode: anode at the top, cathode at the bottom. Rs is a model parameter, not a separate placed resistor — when it is non-zero the elaborator mints the internal node itself, so the schematic shows one device either way. Net Terminal 1 a 2 c The nets of an instance line, in this order. a is the anode and c the cathode; forward current flows a to c. Name Default Unit On schematic Is 1e-14 A shown N 1 — shown Isr 0 A — Nr 2 — — Rs 0 Ω shown Cj0 0 pF shown Vj 1 V — M 0.5 — — Fc 0.5 — — Bv 0 V — Ibv 1e-3 A — Nbv 1 — — Tt 0 — — Area 1 — — Xti 3 — — Eg 1.16 V — Temp 26.85 — — Tnom 26.85 — — What is NOT modelled High-level injection (a card's IKF ) — at high forward bias the current of a real diode rolls off from the ideal exponential towards a square root of it, and this model has no term for that knee, so it stays exponential and is optimistic there. An extracted Rs often absorbs some of that roll-off, which is a fit rather than the physics and does not follow temperature the way the real mechanism does. Flicker noise ( Kf / Af ) — there is no noise analysis for it to feed. Self-heating — the device temperature is a parameter, not a solved node. Sidewall junction capacitance as a separate term — Cj0 , Vj and M describe one junction. A parameter this model does not read is not offered in the palette; a card stating one has it named on import ."],[13,"fets","The FET family","Five native large-signal FET models, one per published drain-current law. They are five separate components rather than one component with a model selector, because they are not variants of one another: each has its own parameter set, and several reuse a spelling for a different quantity — the quadratic law's Beta is a transconductance parameter, while the cubic law's is a gate-voltage shift with drain bias. One kind with a selector would present the union of all five parameter sets and silently accept the wrong ones. All five share one glyph and one three-pin geometry — gate left, drain top, source bottom. The topology genuinely is the same, and the type label below the symbol names the law. The source is an ordinary pin: these are not hard-wired common-source. What all five share Terminals and ports. Three nets — gate drain source — mapped onto two ports: port 0 is (gate, source) and port 1 is (drain, source). So the first port voltage is Vgs and the second is Vds , which is the form every published FET equation is written in. Nothing has to be transposed to use a datasheet parameter set. What is modelled. The drain current and both of its derivatives — gm = ∂Id/∂Vgs and gds = ∂Id/∂Vds — computed analytically rather than by finite differences, which matters inside a Newton loop precisely where the device is most nonlinear. On top of that: optional forward gate conduction, as an ordinary diode from gate to source (set by Is and N , off when Is is zero), and gate charge (below). Parameter temperature scaling is modelled through Temp and Tnom . What is NOT modelled — read this before choosing a model The Statz/TOM-family charge formulation — it works on a smoothed effective voltage rather than on Vgs and Vgd separately, so it is a different scheme and not a parameter change to the two below. Transit-time delay. Breakdown. Self-heating — the device temperature is a parameter, not a solved node, so there is no electrothermal feedback. If your application depends on any of these, use a compiled model through VerilogA instead. Cgs, Cgd and CapModel Gate charge is selectable , because the published models disagree about it. It is a parameter rather than a per-model decision for exactly that reason: two authors implementing \"the Curtice model\" from the literature will not necessarily give it the same gate charge. CapModel Gate charge Are Cgs / Cgd linear? 0 None at all n/a — no charge storage 1 (default) Constant Cgs / Cgd Yes — linear. They are fixed capacitances, independent of bias 2 Junction (depletion) charge, applied to Vgs and Vgd separately No — bias-dependent. Cgs / Cgd are then the zero-bias values, Cj0 At CapModel = 2 the charge on each junction is the standard depletion form: Q = Cj0·Vbi/(1 − M) · [ 1 − (1 − V/Vbi)^(1 − M) ] for V < Fc·Vbi continued by its TANGENT above Fc·Vbi parameters: Cgs, Cgd zero-bias capacitances (Cj0 for the two junctions) Vbi junction potential M grading coefficient Fc forward-bias changeover, as a fraction of Vbi The tangent continuation above Fc·Vbi is not cosmetic: a hard clamp there would leave a kink in the Jacobian and stall Newton. Which to pick. Use 1 when your parameter set was extracted with fixed capacitances — which is the common case for the older laws — and when you want the cheapest evaluation. Use 2 when the extraction gives you junction parameters (Cj0, Vbi, M) and the gate swing is large enough for the bias dependence to matter, which in a power amplifier driven into compression it usually is. Cgd sees Vgd, not Vgs The gate–drain capacitance is across Vgd = Vgs − Vds, so it contributes to both ports and to the Jacobian's off-diagonal terms. That is why a change in drain bias moves the gate-side loading, and it is why Cgd dominates the input match of a device long before Cgs does. Curtice quadratic (FET_Curtice) Curtice The Curtice quadratic law: Vto , Beta , Alpha and Lambda . Net Terminal 1 g 2 d 3 s The nets of an instance line, in this order. Name Default Unit On schematic"],[13,"jfets","The junction FET","Two tiles — NJFET and PJFET — over the Shichman–Hodges square law, one set of equations with one sign changed. This is not the MESFET family with different numbers The two differ in the knee — a MESFET's is a fitted tanh or a piecewise cubic with its own parameter, a JFET's is the square law's own boundary at Vds = Vgt — and in the gate : a JFET's gate is a real p-n junction that conducts and stores depletion charge against both ends of the channel, where the MESFET family models a Schottky gate as one forward diode. Reading a JFET card as a Curtice quadratic with the tanh ignored gives a device that simulates and is quantitatively wrong through the whole knee — matched at pinch-off and matched deep in saturation, and out by tens of percent in between, for every choice of Alpha . Vgt = Vgs − Vto Id = 0 Vgt ≤ 0 cutoff = Beta·Vds·(2·Vgt − Vds)·(1 + Lambda·Vds) Vds < Vgt linear = Beta·Vgt²·(1 + Lambda·Vds) Vds ≥ Vgt saturation The gate is two junctions. Cgs and Cgd are the zero-bias capacitances of bias-dependent depletion charge, not fixed capacitors: Cgd falls as the gate-drain junction is reverse-biased, which at RF is most of the reverse isolation. Both junctions also conduct, with a diffusion term and an optional recombination term ( Isr / Nr ) that is a second exponential with its own ideality — folding it into the first would fit one decade of the gate leakage and miss the rest. The device is symmetric , like the MOS transistor: which end acts as the drain is decided by the bias. Rd and Rs are model parameters , on internal nodes circuitRF mints, not resistors you place beside the device. Vtotc shifts the pinch-off additively, in volts per degree; Betatce scales Beta in percent per degree — 1.01^(tc·ΔT) , which is not the same as 1 + 0.01·tc·ΔT once ΔT is more than a few tens of degrees. The two forms are the published ones and they are not interchangeable. What is NOT modelled Gate breakdown ; the doping-profile knee that a higher published JFET level adds (its B parameter) and that level's own channel-length modulation ( Alpha / Vk ) — there is no square-law parameter that means the same thing, so a card stating them is imported as the square law with those parameters named rather than folded into Lambda ; transit-time charge ; flicker noise ; and self-heating . n-channel (JFET_N) NJFET The gate arrow points into the channel. The channel bar is unbroken — a depletion device, conducting at zero gate bias, which is the opposite of the MOS glyph's three segments. Net Terminal 1 d 2 g 3 s The nets of an instance line, in this order. Name Default Unit On schematic Vto -2 V shown Beta 1e-4 — shown Lambda 0 — shown Is 1e-14 A — N 1 — — Isr 0 A — Nr 2 — — Cgs 0 pF shown Cgd 0 pF shown Pb 1 V — M 0.5 — — Fc 0.5 — — Rd 0 Ω — Rs 0 Ω — Area 1 — — Xti 3 — — Eg 1.16 V — Vtotc 0 — — Betatce 0 — — Temp 26.85 — — Tnom 26.85 — — p-channel (JFET_P) PJFET The gate arrow points out of the channel. Same equations, every sign reversed — so Vto is positive, as a p-channel card states it. Net Terminal 1 d 2 g 3 s The nets of an instance line, in this order. Name Default Unit On schematic Vto -2 V shown Beta 1e-4 — shown Lambda 0 — shown Is 1e-14 A — N 1 — — Isr 0 A — Nr 2 — — Cgs 0 pF shown Cgd 0 pF shown Pb 1 V — M 0.5 — — Fc 0.5 — — Rd 0 Ω — Rs 0 Ω — Area 1 — — Xti 3 — — Eg 1.16 V — Vtotc 0 — — Betatce 0 — — Temp 26.85 — — Tnom 26.85 — —"],[13,"bjt","The bipolar transistor","Two components — NPN and PNP — over one set of equations and one parameter list. This is the opposite arrangement from the FET family above, and for the opposite reason: there the five names denote five different drain-current laws, while here the parameter list is identical and only a sign differs. It is still two components rather than one with a polarity setting, because the two draw differently : the emitter arrow is the whole of what tells a reader which transistor is on the schematic, and a setting would leave the drawing and the netlist free to disagree. Terminals. Three pins — collector top, base left, emitter bottom. The base is an ordinary pin and so is the emitter: these are not hard-wired to any configuration. Rb, Re and Rc are model parameters, not separately placed resistors. When one of them is non-zero the elaborator mints the internal node itself, so the schematic shows one device either way. They are not optional detail at RF — Rb sets the input match and the noise, Re degenerates the transconductance, and all three are shunted by the junction capacitances — which is why the internal nodes are genuine unknowns rather than being folded away. Folding them would be exact at DC and wrong in harmonic balance, where an internal node carries its own harmonic content. What is modelled The standard charge-control model. Vt is kT/q at the device temperature; primed voltages are taken at the internal nodes, inside the parasitic resistances. Icc = Is·(exp(Vb'e'/(Nf·Vt)) − 1) forward transport Iec = Is·(exp(Vb'c'/(Nr·Vt)) − 1) reverse transport Ibe = Icc/Bf + Ise·(exp(Vb'e'/(Ne·Vt)) − 1) base current, emitter junction Ibc = Iec/Br + Isc·(exp(Vb'c'/(Nc·Vt)) − 1) base current, collector junction Ict = (Icc − Iec)/qb collector-to-emitter transport q1 = 1 / (1 − Vb'c'/Vaf − Vb'e'/Var) base-width modulation (the Early effect) q2 = Icc/Ikf + Iec/Ikr high-level injection qb = q1/2 · (1 + sqrt(1 + 4·q2)) Qbe = Qj(Vb'e'; Cje,Vje,Mje) + Tff·Icc/qb depletion + diffusion charge Qbc = Xcjc·Qj(Vb'c'; Cjc,Vjc,Mjc) + Tr·Iec Tff = Tf·(1 + Xtf·(Icc/(Icc+Itf))²·exp(Vb'c'/(1.44·Vtf))) Every derivative of every one of those is computed analytically rather than by finite differences, which matters inside a Newton loop precisely where the device is most nonlinear. Zero means \"not modelled\" Vaf and Var at zero mean the Early effect is switched off — never \"the Early voltage is zero volts\". The same rule applies to Ikf / Ikr (no high-level injection), Ise / Isc (no low-bias leakage) and to each of the three parasitic resistances. This is the same convention the diode's Bv follows. The base resistance is current-dependent. With Irb given it follows the standard conductivity-modulation relation, falling from Rb at zero base current towards Rbm at high current; with Irb zero it follows Rbm + (Rb − Rbm)/qb instead, which is the same physics stated through the base charge. Both are in the published model, and which one applies is decided by the parameter set. Xcjc splits the collector junction across the base resistance. That fraction of Cjc sits on the internal base node and the remainder runs from the external base to the internal collector — a real distributed effect, and the reason a device's feedback capacitance does not simply see Rb . When Rb is zero the two nodes are the same net and the halves add back to Cjc , so nothing special happens. What is NOT modelled — read this before choosing a model The substrate junction ( Cjs / Vjs / Mjs ) — a discrete RF transistor has no substrate terminal to attach it to, and adding a fourth pin would change what the symbol means. Excess phase ( Ptf ) — it is a delay, and circuitRF's weighting functions carry 1 and jω, not exp(−jωτ), so accepting the parameter would be accepting a value that does nothing. Flicker noise ( Kf / Af ) — there is no noise analysis for it to feed. Self-heating — the device temperature is a parameter, not a solved node, so there is no electrothermal feedback. Package parasitics —"],[13,"mos","The MOS transistor","Two tiles — NMOS1 and PMOS1 — over one set of equations with one sign changed, arranged like the bipolar pair rather than like the five FET laws. The 1 is the level : the Shichman–Hodges square law, the original compact MOSFET model and the one every later level is written as a departure from. This transistor has FOUR pins. Wire the bulk. Drain, gate, source and bulk . The bulk is a real terminal and not a convenience: tying it internally to the source would silently delete the body effect , which is what Gamma and Phi describe and which is worth hundreds of millivolts of threshold in a circuit where the source does not sit at the substrate potential. A part whose bulk really is tied to its source says so by wiring the pin — one wire, and then the schematic states the fact instead of hiding it. Leave the pin unwired and the substrate floats, which solves, and is a different circuit. What is modelled Vth = Vto + Gamma·(√(Phi − Vbs) − √Phi) Vgt = Vgs − Vth Id = 0 Vgt ≤ 0 cutoff = Beta·(Vgt − Vds/2)·Vds·(1 + Lambda·Vds) Vds < Vgt linear = (Beta/2)·Vgt²·(1 + Lambda·Vds) Vds ≥ Vgt saturation Beta = Kp·W/Leff, Leff = L − 2·Ld The device is symmetric. Which terminal acts as the drain is decided by the bias, not by the schematic: drive Vds negative and circuitRF swaps the two ends and evaluates the law in the orientation it is published in. A model that did not would return a plausible, wrong current for every transmission gate and every passive mixer ever drawn. Both bulk junctions are real diodes , with their own saturation currents, their own depletion charge and their own sidewall term. They are what makes the substrate connection matter, and they are why a bulk biased above the source or the drain draws enormous current — as it does in silicon. Rd and Rs are model parameters, not resistors you place. A non-zero one moves the intrinsic transistor onto an internal node circuitRF mints for it, so the schematic shows one device either way. That node is a genuine unknown rather than something eliminated locally: at RF the ohmic resistances are shunted by the junction and overlap capacitances, and collapsing them is exact at DC and wrong in harmonic balance. Where a card states a process quantity instead of a device one Several parameters come in pairs — Kp or Uo , Gamma / Phi or Nsub , Rd / Rs or Rsh with Nrd / Nrs , Cbd / Cbs or Cj / Cjsw with the junction areas. They are not alternative spellings of one number: circuitRF derives the device quantity from the process one only where the device quantity is absent, and a value you state always wins. The gate charge The intrinsic gate charge is the charge-based long-channel result , not the Meyer capacitance set that a transient simulator conventionally uses. That difference matters here more than it does in a time-stepping simulator. Meyer's model states three capacitances that each depend on more than one terminal voltage, so the charge they imply depends on the path taken through the bias space — go once around a harmonic cycle and it does not return to where it started, and a periodic steady-state solve has nothing to converge to. Integrating the channel charge directly is conservative by construction, and its derivatives reduce to exactly Meyer's capacitances wherever Meyer's are right: Cox at Vds = 0, two thirds of Cox in saturation, zero in cutoff. The channel charge is split evenly between the drain and the source. The alternative 40/60 split is the better one in a switching transient; at RF both ends see the same signal through the same channel resistance, and the even split is what keeps the device symmetric under the drain/source swap above. No Tox means no gate capacitance Tox is the only thing that sets the oxide capacitance, so a parameter set that does not state one has no intrinsic gate charge at all — only the Cgso , Cgdo and Cgbo overlaps remain. That is the published rule and circuitRF does not invent a thickness to fill it, because there is nothing on the card to"],[13,"vdmos","The vertical power MOSFET","NVDMOS and PVDMOS — a separate component from the lateral MOS pair, not a setting of it. Three pins, not four: the source-to-body short is inside the silicon, and that is exactly what turns the substrate junction into a body diode between source and drain. Three things a power MOSFET is chosen for are absent from the lateral model, and every one is what a user is asking about when they reach for this part. The body diode is a component of the circuit, not a leakage path. It is the freewheeling diode of every half-bridge and the conduction path of every synchronous rectifier during dead time, and it carries the full load current there. So it has its own saturation current, its own reverse recovery charge ( Tt ) and its own avalanche breakdown ( Bv ) — a rated mode for this part, not a failure — and its current is reported on its own branch, I:M1:body . The gate-drain capacitance collapses with drain bias, by one to two orders of magnitude. That collapse is the switching loss: the Miller plateau is the gate charge pouring into it while the drain swings. Cgdmax and Cgdmin are the two ends of a data sheet's reverse-transfer curve and Vgdt is how sharply it falls between them. A constant capacitance of either plateau value gets the switching time wrong by the ratio of the two. The gate resistance Rg is in the drive path , in series with a capacitance that large, so it sets the switching speed as much as the drive current does. Third-quadrant conduction is not an edge case here Pull the drain below the source with the gate on and the channel conducts in reverse, shunting the body diode, so the drop is I·Rds(on) rather than a diode drop. That is synchronous rectification, and it is what the part is bought for. circuitRF decides which end is acting as the drain from the bias rather than from the schematic, so this comes out right without anything being configured. What is NOT modelled Quasi-saturation — the drift region's own resistance modulating with current needs a second internal node and a drift-region model this does not have; Rd stands in for its low-current limit. Subthreshold conduction — the off-state leakage is Rds , which you state. Self-heating , which for a power device is a real omission: the junction temperature is a parameter, so a thermal model belongs around the part rather than in it. n-channel (VDMOS_N) NVDMOS The bulk arm turns and joins the source lead instead of leaving as a fourth pin — that is the source-to-body short — and the body diode is drawn explicitly on the right, conducting from source to drain. Net Terminal 1 d 2 g 3 s The nets of an instance line, in this order. Name Default Unit On schematic Vto 3 V shown Kp 1 — shown Lambda 0 — — Rds 0 Ω — Is 1e-13 A — N 1 — — Bv 0 V shown Ibv 1e-3 A — Nbv 1 — — Tt 0 — — Cjo 0 pF — Vj 0.8 V — Mj 0.5 — — Fc 0.5 — — Cgs 0 pF shown Cgdmax 0 pF shown Cgdmin 0 pF shown Vgdt 1 V — Rg 0 Ω — Rd 0 Ω — Rs 0 Ω — Vtotc 0 — — Kptc 0 — — Xti 3 — — Eg 1.16 V — Temp 26.85 — — Tnom 26.85 — — p-channel (VDMOS_P) PVDMOS Net Terminal 1 d 2 g 3 s The nets of an instance line, in this order. Name Default Unit On schematic Vto 3 V shown Kp 1 — shown Lambda 0 — — Rds 0 Ω — Is 1e-13 A — N 1 — — Bv 0 V shown Ibv 1e-3 A — Nbv 1 — — Tt 0 — — Cjo 0 pF — Vj 0.8 V — Mj 0.5 — — Fc 0.5 — — Cgs 0 pF shown Cgdmax 0 pF shown Cgdmin 0 pF shown Vgdt 1 V — Rg 0 Ω — Rd 0 Ω — Rs 0 Ω — Vtotc 0 — — Kptc 0 — — Xti 3 — — Eg 1.16 V — Temp 26.85 — — Tnom 26.85 — —"],[13,"igbt","The IGBT","NIGBT and PIGBT — an insulated-gate channel driving the base of a wide-base bipolar transistor. That structure is the model, and it is what gets the device's two defining behaviours right without either being fitted. The on-state voltage has a junction drop in it. Current leaving the collector crosses the bipolar's emitter-base junction on its way in, so Vce(sat) never falls below roughly a diode drop however hard the gate is driven. That is the whole trade against a power MOSFET — worse at low current, better at high, because the drop then stops growing. Turn-off has a current tail. The charge stored in the wide base cannot be removed through the gate; it recombines. Tau is that stored charge, and the tail is most of the turn-off loss. How the current divides between the channel and the bipolar is set by Bf , and it is the bipolar's share that is still flowing after the gate is off — I:Q1:imos and I:Q1:ic report the two separately. Bv is forward break-over , across the drift region — the V_CES rating. Note the difference from the power MOSFET 's Bv , which is an avalanche rating and a mode the part is designed to survive: an IGBT's is a limit, and past it the drift region conducts and turns the bipolar on with it. Bv = 0 means not modelled, as everywhere else. An IGBT does not conduct in reverse That is structural rather than something switched off: with the collector below the emitter the bipolar's junction is reverse-biased and there is no path. It is the opposite of the power MOSFET , whose body diode freewheels — and it is exactly why an IGBT half-bridge needs a discrete anti-parallel diode and a MOSFET one does not. Place one if the circuit has one. Its parameters are data-sheet quantities, and a transport-model card cannot be imported This is an equivalent-circuit model: its base is a lumped transit time rather than a solved carrier distribution, so there is no moving depletion boundary, no conductivity modulation of the drift region and no latch-up. Its parameters are therefore a threshold, a transconductance, a current gain and a transit time — things a data sheet gives. A .model card written for the published ambipolar transport model states something else entirely: base width, doping, carrier lifetime, mobility. Neither set can be derived from the other by renaming — that is a device-modelling extraction, not a mapping — so such a card is refused by name rather than being given this device's defaults under the card's own name. Enter the data sheet's numbers here, or run the card's own model through VerilogA . n-channel (IGBT_N) NIGBT The input side is the MOS one — a gate bar standing off a broken (enhancement) channel bar — and the output side carries the bipolar's emitter arrow. The arrow is not decoration: it is what stops a reader taking this for a power MOSFET and expecting a body diode that is not there. Net Terminal 1 c 2 g 3 e The nets of an instance line, in this order. Name Default Unit On schematic Vto 5 V shown Kp 8 — shown Lambda 0 — — Bf 0.5 — shown Is 1e-12 A — N 1 — — Tau 1 us shown Rbe 0 Ω — Rce 0 Ω — Bv 0 V shown Ibv 1e-3 A — Nbv 1 — — Cjc 0 pF — Vj 0.8 V — Mj 0.5 — — Fc 0.5 — — Cge 0 pF shown Cgcmax 0 pF shown Cgcmin 0 pF shown Vgct 1 V — Rg 0 Ω — Rc 0 Ω — Re 0 Ω — Vtotc 0 — — Kptc 0 — — Xti 3 — — Eg 1.16 V — Temp 26.85 — — Tnom 26.85 — — p-channel (IGBT_P) PIGBT Net Terminal 1 c 2 g 3 e The nets of an instance line, in this order. Name Default Unit On schematic Vto 5 V shown Kp 8 — shown Lambda 0 — — Bf 0.5 — shown Is 1e-12 A — N 1 — — Tau 1 us shown Rbe 0 Ω — Rce 0 Ω — Bv 0 V shown Ibv 1e-3 A — Nbv 1 — — Cjc 0 pF — Vj 0.8 V — Mj 0.5 — — Fc 0.5 — — Cge 0 pF shown Cgcmax 0 pF shown Cgcmin 0 pF shown Vgct 1 V — Rg 0 Ω — Rc 0 Ω — Re 0 Ω — Vtotc 0 — — Kptc 0 — — Xti 3 — — Eg 1.16 V — Temp 26.85 — — Tnom 26.85 — —"],[13,"mixer","Ideal Mixer (Mixer)","Mixer A three-port ideal mixer: what comes out of the IF port is the product of what goes into the RF and LO ports. RF on the left, LO underneath, IF on the right — the three leads are not interchangeable, which is why each one carries its name. v_IF (open circuit) = K · v_RF · v_LO You never type K . You state a conversion gain and the LO drive it holds at — ConvGain = −7 dB at Plo = +7 dBm, straight off a datasheet — and circuitRF derives the multiplier constant from them, using each port's impedance. ConvGain is a single-sideband power gain: negative is a loss. Both sidebands come out. A product of two cosines is half the sum plus half the difference, so an RF tone at 2 GHz against a 1.8 GHz LO puts equal power at 200 MHz and 3.8 GHz. The mixer does not pick one for you; a single-sideband result comes from filtering what leaves the IF port, or from an image-reject network built around two mixers — exactly as it does in hardware. Conversion gain tracks LO amplitude. That is what a multiplier does, and it is why the gain is quoted together with Plo . Drive the LO 3 dB harder than Plo and the conversion gain rises 3 dB; drive it 3 dB softer and it falls. If the LO in your test bench is not delivering Plo to the LO port, the mixer is not running at the gain you typed. Why it is a multiplier and not a switch A real diode or FET mixer commutates : the LO switches the RF path rather than scaling it, which is why a real mixer's conversion loss barely moves once the LO is hard enough to switch. An ideal switching mixer is not expressible here — a component's law is a memoryless function of its port voltages, and hard switching has a derivative no Newton step survives. The product is the ideal mixing law that is expressible, and its LO dependence is stated above rather than hidden. Non-idealities A freshly-placed mixer is ideal: the three isolations default to 200 dB and IIP3 to 100 dBm, which mean none and never compresses . Type a real number into any of them to turn that non-ideality on. Parameter What it does Zrf Zlo Zif Each port is this resistance to its reference, and the IF output sits behind Zif . Change one and that port is mismatched — the first non-ideality most circuits notice. IsoLO_RF LO leaking backwards out of the RF port. It is a real voltage at a real port, so it mixes with the LO like anything else there. IsoLO_IF LO feedthrough at the IF port — usually the one that sets an IF filter's job. IsoRF_IF RF straight through to IF, unconverted. IIP3 Input-referred third-order intercept at the RF port. Sets compression and IM3 together, through a soft limiter whose third-order term matches the intercept exactly. The limiter is a tanh , not the textbook a₁x − a₃x³ . A bare cubic turns over and goes negative past its peak, and harmonic balance then finds that root and converges cleanly onto a wrong answer. tanh is monotone everywhere and has the same third-order term. What it does in an S-parameter run It reports the port matches and the three leakages, and no conversion at all — and that is the right answer rather than a missing one. S-parameters are a single-frequency measurement, and conversion is the business of moving energy between frequencies. The arithmetic says the same thing: circuitRF linearises a nonlinear device at its DC operating point, and the mixer's RF-to-IF small-signal gain is proportional to the LO voltage, which at DC is zero. So an S-parameter sweep of a mixer is a useful measurement of the thing S-parameters can measure — how well each port is matched, and how much leaks between them. Conversion gain comes from harmonic balance. Drive the RF and LO ports as two tones, and read the IF power at the mixing product you want. For conversion gain versus frequency, wrap that harmonic-balance analysis in a parametric sweep of the RF frequency — that is the swept measurement an S-parameter run cannot be. The mixer is also a System block, and the class it belongs to — what the ideal blocks in a system diagram"],[13,"mixerd","Differential Mixer (MixerD)","MixerD The same component as Mixer — same equations, same parameters, same everything — with all six of its nets brought out as pins instead of three. Use it when a port's return is not ground. Otherwise the single-ended tile is the identical circuit with three fewer wires to draw. The pins are in ± pairs: rf+ rf− on the left, lo+ lo− along the bottom, if+ if− on the right. Swapping a pair inverts that port's voltage, which gives a circuit that still solves and is wrong — so read the marks. The box body is not a second opinion about what a mixer looks like: six leads cannot land on a circle's edge on the connection grid. The ✕ is kept, and it is the whole of the family resemblance. Net Terminal 1 rf+ 2 rf- 3 lo+ 4 lo- 5 if+ 6 if- The nets of an instance line, in this order. Name Default Unit On schematic Meaning ConvGain -7 dB shown Single-sideband power conversion gain, RF port to IF port, at the LO drive Plo names. Negative is a loss. Both sidebands are produced at this level. Plo 7 dBm shown The LO power the gain above holds at. Mixing is a product, so conversion gain tracks LO amplitude: drive the LO harder than this and you get more. Zrf 50 Ω — RF port impedance. The port is this resistance to its own reference. Zlo 50 Ω — LO port impedance. Zif 50 Ω — IF port impedance — also the source resistance the IF output sits behind. IsoLO_RF 200 dB — LO-to-RF isolation: how far below the LO its leakage at the RF port sits. The 200 dB default means none — the ideal mixer leaks nothing. IsoLO_IF 200 dB — LO-to-IF isolation, the LO feedthrough a real mixer shows at its IF port. 200 dB means none. IsoRF_IF 200 dB — RF-to-IF isolation: straight-through RF feedthrough, unconverted. 200 dB means none. IIP3 100 dBm — Input-referred third-order intercept at the RF port, which sets both compression and IM3. The 100 dBm default means the RF path is exactly linear and the mixer never compresses."],[13,"veriloga","Compiled Verilog-A model (VerilogA)","VerilogA A compact model you supply: point it at Verilog-A source ( .va , .vams ) or an already-compiled model ( .osdi ) and circuitRF runs it. No kit, no manifest, nothing to install. It is variadic — the model decides how many terminals it has, so Pins sets how many the symbol draws. The body is deliberately generic, because circuitRF does not know what the model is : drawing a transistor glyph would assert something the file has not said. The leads are named by the model once the file has been read, and numbered before that. This component has its own chapter Installing a compiler and pointing circuitRF at it, the source-to-simulation walkthrough, loading a fitted parameter set, the thermal terminal and when to leave it off, and exactly when circuitRF rebuilds, are in Compiled Verilog-A Models . The entry here is the glyph, the pins and the parameter table. Net Terminal 1 1 2 2 The nets of an instance line, in this order. How many there are is set by Pins ; the table shows Pins = 2 . Name Default Unit On schematic Meaning File — — shown The model to load: a compiled model (.osdi), or Verilog-A source (.va, .vams) which circuitRF builds once with the compiler installed on this machine and reuses until the source changes. Choosing one fills in Model and Pins below. Model — — — Which device type inside that file to place. A file usually declares one, and then this can be left blank; when it declares several, pick the one you want. Pins 2 — — How many terminals the symbol draws. It is the model's own terminal count, filled in from the file — change it only if you are drawing before choosing one. OpVars true — — Whether this instance publishes the operating-point variables its model computes — transconductances, capacitances, node temperatures. On by default; turn it off on devices you are not studying to keep a swept result small."],[13,"system-level-blocks","System-level blocks","The blocks a system block diagram is drawn out of: the level above a transistor, where a signal path is a chain of named boxes rather than a circuit. They share the mixer's drawing grammar — a block reads left to right, inputs on the left and outputs on the right, and a block whose leads are not interchangeable labels them, because connecting the wrong one gives a circuit that solves and is wrong. Every one of them is in the palette's System filter, along with the two mixer tiles. These blocks have their own chapter What each block is for, what \"ideal by construction\" costs you, which analysis answers which question, and the whole of the passive-intermodulation story — the datasheet conversion, which blocks can carry it, and how to give one to a block that cannot — are in System Components . Read that before placing one; the entries below are the glyph, the pins and the parameter table."],[13,"balun","Balun","Balun A transformer between a single unbalanced port and a balanced pair. UNB is on the left; BAL+ and BAL− are on the right, and the + / − marks say which is which — swapping them inverts the balanced signal, which is a circuit that still solves. Zbal is the impedance of each balanced port to ground, so the differential impedance across the pair is twice it. In depth: System Components › Balun . Net Terminal 1 UNB 2 BAL+ 3 BAL- The nets of an instance line, in this order. Name Default Unit On schematic Meaning Zunb 50 Ω — Reference impedance of the unbalanced port. Zbal 50 Ω — Reference impedance of EACH balanced port to ground, so the differential impedance across BAL+ and BAL− is twice this. The 50/50 default is the ordinary 1:2 balun. IL 0 dB — Insertion loss from the unbalanced port to the balanced pair, as a positive number of dB. AmpImb 0 dB — How far apart in level the two balanced outputs sit, in dB. 0 is a perfect split; the imbalance is applied symmetrically, half up on one output and half down on the other. PhaseImb 0 deg — Departure from 180°, in degrees. 0 gives exactly antiphase outputs."],[13,"circulator","Circulator","Circulator A three-port non-reciprocal junction: power entering port 1 leaves at port 2, power entering port 2 leaves at port 3, and power entering port 3 leaves at port 1. The arrow inside the circle is drawn from the Direction parameter — CW circulates 1 → 2 → 3 → 1 and CCW reverses it — so which way a circulator turns is read off the schematic rather than out of a dialog. Terminate the unused port and it is an isolator. It can carry a passive-intermod specification. In depth: System Components › Circulator . Net Terminal 1 1 2 2 3 3 The nets of an instance line, in this order. Name Default Unit On schematic Meaning Direction CW — — Which way the circulator turns: CW circulates port 1 to 2 to 3 to 1, CCW reverses it. The arrow drawn inside the symbol follows this. IL 0 dB — Insertion loss along the forward path, as a positive number of dB. Isolation 200 dB — How far below the signal the leakage the WRONG way round the circle sits. The 200 dB default means none, and the entry is not stamped at all. RL 200 dB — Return loss at every port that does not state its own VSWR. 200 dB means exactly matched. Z0 50 Ω — REFERENCE impedance of all three ports, real or complex - what S is defined against, unlike a Zin/Zout, which names what a port presents. It is NOT the way to detune the match: use VSWR1/Ang1, which set port 1's own reflection rather than the reference every port shares. VSWR1 1 — — Voltage standing-wave ratio at this port, with the other two matched - a real circulator is badly matched and this is how far. 1 means the port does not state one and falls back to RL. Pair it with the matching Ang: the SAME VSWR at a different angle is a completely different load to whatever is connected here. Ang1 0 deg — Angle of this port's reflection coefficient, in degrees. Read only when the matching VSWR states a mismatch. Frequency-flat: it is the mismatch you want to test against, not a rotating one. VSWR2 1 — — Voltage standing-wave ratio at this port, with the other two matched - a real circulator is badly matched and this is how far. 1 means the port does not state one and falls back to RL. Pair it with the matching Ang: the SAME VSWR at a different angle is a completely different load to whatever is connected here. Ang2 0 deg — Angle of this port's reflection coefficient, in degrees. Read only when the matching VSWR states a mismatch. Frequency-flat: it is the mismatch you want to test against, not a rotating one. VSWR3 1 — — Voltage standing-wave ratio at this port, with the other two matched - a real circulator is badly matched and this is how far. 1 means the port does not state one and falls back to RL. Pair it with the matching Ang: the SAME VSWR at a different angle is a completely different load to whatever is connected here. Ang3 0 deg — Angle of this port's reflection coefficient, in degrees. Read only when the matching VSWR states a mismatch. Frequency-flat: it is the mismatch you want to test against, not a rotating one. PIM -200 dBm — Passive intermod: the third-order product this block puts on its output when two carriers of PIMPc each drive its input, as an absolute level. The -200 dBm default means there is no intermod here and none is calculated at all. A part quoted in dBc converts by adding PIMPc — -153 dBc at +43 dBm is -110 dBm. PIMPc 43 dBm — Power per carrier the PIM figure above was measured at — the second half of one specification. Two carriers at this power produce a product at exactly PIM; the product then rides the third power of drive, so 10 dB less carrier is 30 dB less product."],[13,"switch","Switch","Switch An SPST switch. Its two pins are interchangeable, so they carry no names. The blade is drawn in the position State sets — lifted at 0 , closed at 1 — which is what makes a swept State readable on the page. In depth: System Components › Switch . Net Terminal 1 1 2 2 The nets of an instance line, in this order. Name Default Unit On schematic Meaning State 1 — — 1 closes the switch, 0 opens it. The symbol is drawn in the position it is set to, so a swept State reads off the schematic. Throws 1 — — How many throws the switch has: 1 for the SPST tile, 2 for the SPDT. It is what makes the two tiles one component, and it sets the pin count, so leave it alone. IL 0 dB — Insertion loss of the path the switch is making, as a positive number of dB. Isolation 200 dB — How far below the signal the leakage past an OPEN throw sits. The 200 dB default means none — the ideal switch leaks nothing, and the entry is not stamped at all. OffState Reflective — — What an open throw looks like from its own port: Reflective is an open circuit, Absorptive is a matched termination. Reflective is what a series switch does. Z0 50 Ω — Reference impedance of every port. Each port is this resistance to its own reference. RL 200 dB — Return loss of the closed path. 200 dB means the closed switch is exactly matched."],[13,"switchd","Transfer Switch (SwitchD)","SwitchD An SPDT switch: one common port on the left and two throws on the right. State selects the throw — 1 or 2 , or 0 for both open — and the blade in the symbol points at it. Both switch tiles use the SW instance prefix, so swapping one for the other does not renumber a schematic. In depth: System Components › Transfer Switch . Net Terminal 1 COM 2 T1 3 T2 The nets of an instance line, in this order. Name Default Unit On schematic Meaning State 1 — — Which throw the common port is connected to — 1 or 2, or 0 for both open. The blade in the symbol points at it. Throws 2 — — How many throws the switch has: 1 for the SPST tile, 2 for the SPDT. It is what makes the two tiles one component, and it sets the pin count, so leave it alone. IL 0 dB — Insertion loss of the path the switch is making, as a positive number of dB. Isolation 200 dB — How far below the signal the leakage past an OPEN throw sits. The 200 dB default means none — the ideal switch leaks nothing, and the entry is not stamped at all. OffState Reflective — — What an open throw looks like from its own port: Reflective is an open circuit, Absorptive is a matched termination. Reflective is what a series switch does. Z0 50 Ω — Reference impedance of every port. Each port is this resistance to its own reference. RL 200 dB — Return loss of the closed path. 200 dB means the closed switch is exactly matched."],[13,"amp","Amplifier (Amp)","Amp An ideal amplifier: IN on the left, OUT on the right. Nothing is drawn inside the triangle because the gain belongs where a reader looks for a number — the label beside the symbol. It has no bias pins and consumes no DC power, and one third-order intercept sets its intermodulation and its compression together. In depth: System Components › Amplifier . Net Terminal 1 IN 2 OUT The nets of an instance line, in this order. Name Default Unit On schematic Meaning Gain 20 dB shown Small-signal gain from the input port to the output port. Shown beside the symbol, because a triangle with a number inside it stops being readable at three digits. IP3 40 dBm shown Third-order intercept, referred to whichever port IP3Ref names. It is the amplifier's ONE nonlinearity, so it sets IM3 and compression together: the 1 dB compression point follows at IIP3 - 8.96 dB and is not separately adjustable. The 200 dBm default means the amplifier is exactly linear and never compresses. IP3Ref Output — — Whether IP3 above is an input-referred number or an output-referred one. OIP3 = IIP3 + Gain is an identity, so this is one field and a reference rather than two fields that could disagree. Output is the default, because that is the form a power amplifier's datasheet quotes. Zin 50 Ω — The impedance the input port PRESENTS, and what RLin is measured against. A complex value is conjugate-matched by a Term at its conjugate, and is accepted only while the amplifier is LINEAR - set IP3 to 200, since the tile's own default of 40 dBm is not. Zout 50 Ω — The impedance the output port PRESENTS, and what RLout is measured against. See Zin for the complex case. RLin 200 dB — Input return loss. The 200 dB default means exactly matched - no reflection entry is stamped at all. The gain you typed is what you measure at any value of it. RLout 200 dB — Output return loss. 200 dB means exactly matched. S12 200 dB — Reverse isolation. The 200 dB default means the amplifier is unilateral - the reverse path is absent, not small, which is what makes an ideal amplifier unconditionally stable. Setting it is what makes stability a question at all."],[13,"coupler","Directional Coupler","Directional Coupler Four ports in the order a coupler is always specified: 1 in, 2 through, 3 coupled, 4 isolated. The two arms run straight through the body, because that is what a coupler is — two lines that happen to be close to one another. The arrow does real work: it is the whole of what separates the coupled port from the isolated one, and a coupler drawn without it is ambiguous in exactly the way that produces a silently wrong circuit. In depth: System Components › Directional Coupler . Net Terminal 1 1 2 2 3 3 4 4 The nets of an instance line, in this order. Name Default Unit On schematic Meaning Coupling 20 dB shown How far below the input the coupled port sits, in dB. This alone sets the split — the through port gets whatever is left, so a 20 dB coupler already loses 0.04 dB through its main arm. 3.0103 dB is the equal split that makes a hybrid. Phase 90 deg — Phase of the coupled port relative to the through port, in degrees — 90 for a quadrature hybrid, 180 for an anti-phase one. This block holds it at EVERY frequency, which no real coupler does; build one from four quarter-wave TLIN arms if you need the bandwidth to be real. Directivity 200 dB — How far below the COUPLED port the isolated port sits, in dB. The 200 dB default means the isolated port is exactly isolated — no entry is stamped at all. IL 0 dB — Loss ADDED on top of the split, as a positive number of dB. It is not a substitute for the split: an ideal coupler's main-arm loss is already in the Coupling arithmetic. RL 200 dB — Return loss at every port. 200 dB means all four ports are exactly matched. Z0 50 Ω — Reference impedance of all four ports. Each port is this resistance to its own reference. PIM -200 dBm — Passive intermod: the third-order product this block puts on its output when two carriers of PIMPc each drive its input, as an absolute level. The -200 dBm default means there is no intermod here and none is calculated at all. A part quoted in dBc converts by adding PIMPc — -153 dBc at +43 dBm is -110 dBm. PIMPc 43 dBm — Power per carrier the PIM figure above was measured at — the second half of one specification. Two carriers at this power produce a product at exactly PIM; the product then rides the third power of drive, so 10 dB less carrier is 30 dB less product."],[13,"hybrid90","90° Hybrid (Hybrid90)","Hybrid90 The same component as the coupler above, at 3.01 dB with the coupled port in quadrature. Same body, same pins, same arrow; the phase written inside the frame is the difference, because the phase is the difference. In depth: System Components › 90° Hybrid . Net Terminal 1 1 2 2 3 3 4 4 The nets of an instance line, in this order. Name Default Unit On schematic Meaning Coupling 3.0103 dB — How far below the input the coupled port sits, in dB. This alone sets the split — the through port gets whatever is left, so a 20 dB coupler already loses 0.04 dB through its main arm. 3.0103 dB is the equal split that makes a hybrid. Phase 90 deg — Phase of the coupled port relative to the through port, in degrees — 90 for a quadrature hybrid, 180 for an anti-phase one. This block holds it at EVERY frequency, which no real coupler does; build one from four quarter-wave TLIN arms if you need the bandwidth to be real. Directivity 200 dB — How far below the COUPLED port the isolated port sits, in dB. The 200 dB default means the isolated port is exactly isolated — no entry is stamped at all. IL 0 dB — Loss ADDED on top of the split, as a positive number of dB. It is not a substitute for the split: an ideal coupler's main-arm loss is already in the Coupling arithmetic. RL 200 dB — Return loss at every port. 200 dB means all four ports are exactly matched. Z0 50 Ω — Reference impedance of all four ports. Each port is this resistance to its own reference. PIM -200 dBm — Passive intermod: the third-order product this block puts on its output when two carriers of PIMPc each drive its input, as an absolute level. The -200 dBm default means there is no intermod here and none is calculated at all. A part quoted in dBc converts by adding PIMPc — -153 dBc at +43 dBm is -110 dBm. PIMPc 43 dBm — Power per carrier the PIM figure above was measured at — the second half of one specification. Two carriers at this power produce a product at exactly PIM; the product then rides the third power of drive, so 10 dB less carrier is 30 dB less product."],[13,"hybrid180","180° Hybrid (Hybrid180)","Hybrid180 The rat race: again the same component, at 3.01 dB with the coupled port in anti-phase — a sum port and a difference port. Both hybrids share the HYB instance prefix with each other, and all three tiles share one engine component. In depth: System Components › 180° Hybrid . Net Terminal 1 1 2 2 3 3 4 4 The nets of an instance line, in this order. Name Default Unit On schematic Meaning Coupling 3.0103 dB — How far below the input the coupled port sits, in dB. This alone sets the split — the through port gets whatever is left, so a 20 dB coupler already loses 0.04 dB through its main arm. 3.0103 dB is the equal split that makes a hybrid. Phase 180 deg — Phase of the coupled port relative to the through port, in degrees — 90 for a quadrature hybrid, 180 for an anti-phase one. This block holds it at EVERY frequency, which no real coupler does; build one from four quarter-wave TLIN arms if you need the bandwidth to be real. Directivity 200 dB — How far below the COUPLED port the isolated port sits, in dB. The 200 dB default means the isolated port is exactly isolated — no entry is stamped at all. IL 0 dB — Loss ADDED on top of the split, as a positive number of dB. It is not a substitute for the split: an ideal coupler's main-arm loss is already in the Coupling arithmetic. RL 200 dB — Return loss at every port. 200 dB means all four ports are exactly matched. Z0 50 Ω — Reference impedance of all four ports. Each port is this resistance to its own reference. PIM -200 dBm — Passive intermod: the third-order product this block puts on its output when two carriers of PIMPc each drive its input, as an absolute level. The -200 dBm default means there is no intermod here and none is calculated at all. A part quoted in dBc converts by adding PIMPc — -153 dBc at +43 dBm is -110 dBm. PIMPc 43 dBm — Power per carrier the PIM figure above was measured at — the second half of one specification. Two carriers at this power produce a product at exactly PIM; the product then rides the third power of drive, so 10 dB less carrier is 30 dB less product."],[13,"filter","Filter","Filter A two-port filter synthesised from a prototype — Butterworth, Chebyshev, inverse Chebyshev, Bessel or elliptic, as lowpass, bandpass or highpass. Its symbol is the Match symbol — the same picture, not a related one. Impedance matching is a form of filtering, the two are built out of the same idea, and the library says so. The three stacked waves read as a frequency axis with the highest at the top, and a slash is struck through every wave the network blocks, following the Form parameter: Form Struck Passes Lowpass the top two the lowest Bandpass the top and the bottom the middle Highpass the bottom two the highest Tell a filter from a matching network by its type label and its instance name — FLT1 against MN1 — the same way the five FET laws, which also share one glyph, are told apart. Order is the prototype order, so a 3rd-order bandpass is a 6th-degree network. In depth: System Components › Filter . Net Terminal 1 1 2 2 The nets of an instance line, in this order. The two terminals are interchangeable — this block is symmetric, and its parameters describe both ports at once. Name Default Unit On schematic Meaning Response Chebyshev — — Which prototype family the response comes from: Butterworth (maximally flat magnitude), Chebyshev (equiripple passband, reads Ripple), InvChebyshev (flat passband and an equiripple stopband floor, reads Astop), Bessel (maximally flat GROUP DELAY — chosen for its phase, not its shape) or Elliptic (equiripple in both bands, reads Ripple and Astop). A parameter the family does not read is ignored, so you never have to clear a field to change family. Form Bandpass — — Lowpass, Bandpass or Highpass. The symbol strikes a line through every wave the network blocks, so the shape is read off the glyph. Lowpass and Highpass read Fc; Bandpass reads F1 and F2 and ignores it. Order 3 — — The PROTOTYPE order. A Bandpass transformation doubles the degree, so Order = 3 as a bandpass is a 6th-degree network — both conventions exist in the wild, and this one is the prototype's. The stopband slope of an all-pole family is 20 x Order dB per decade; InvChebyshev and Elliptic level off at their own floor instead. Fc 1 GHz — The cutoff, for Lowpass and Highpass. For Chebyshev and Elliptic it is the RIPPLE bandwidth edge (where the response leaves the ripple band), for Butterworth the 3.01 dB point, for InvChebyshev the STOPBAND edge — where Astop is first met — and for Bessel the reciprocal of the group delay. Ignored when Form is Bandpass. F1 0.9 GHz — Lower band edge, for Bandpass. The band centre is the GEOMETRIC mean of F1 and F2 and the response is geometrically symmetric about it, not arithmetically. Ignored for Lowpass and Highpass. F2 1.1 GHz — Upper band edge, for Bandpass. Read with F1 — the two set both the centre and the width. Ignored for Lowpass and Highpass. Ripple 0.1 dB — Passband ripple, in dB, for Chebyshev and Elliptic. The passband swings between 0 and -Ripple exactly this many times, which is what the order buys. Ignored by the other three families. Astop 40 dB — Stopband floor, in dB below the passband, for InvChebyshev and Elliptic. The stopband is equiripple AT this level rather than falling away past it — that is the trade those two families make for a sharper transition. Ignored by the other three families. Zin 50 Ω — The impedance port 1 PRESENTS - so a complex value is conjugate-matched by a Term at its conjugate: Zin = 5+j100 wants Z = 5-j100 across it for maximum power transfer. This block is stamped as its scattering matrix rather than synthesised as a ladder, so Zin and Zout may differ freely: the filter is then also an ideal lossless impedance transformer, matched at BOTH ports in its passband. Measured in a uniform 50 ohm system an unequal pair shows the transformer's mismatch, which is the answer and not a fault. Zout 50 Ω — The impedance port 2 PRESENTS. See Zin — the two are independent, and an unequal pair is a lossless transformer as well as a filter. IL 0 dB — A flat"],[13,"atten","Attenuator","Attenuator A fixed pad. Its two pins are interchangeable, so they carry no names, and the pinched bowtie reads as \"signal made smaller\". The loss shows as the label beside the symbol. With a small loss and a passive-intermod specification it is a PIM generator you can place in front of anything. In depth: System Components › Attenuator . Net Terminal 1 1 2 2 The nets of an instance line, in this order. The two terminals are interchangeable — this block is symmetric, and its parameters describe both ports at once. Name Default Unit On schematic Meaning Loss 10 dB shown How far the attenuator knocks the signal down, as a positive number of dB. 0 dB is an ideal through, which is a legitimate thing to place. Z0 50 Ω — Reference impedance of both ports. Each port is this resistance to its own reference. RL 200 dB — Return loss of both ports. The 200 dB default means exactly matched — no reflection entry is stamped at all. PIM -200 dBm — Passive intermod: the third-order product this block puts on its output when two carriers of PIMPc each drive its input, as an absolute level. The -200 dBm default means there is no intermod here and none is calculated at all. A part quoted in dBc converts by adding PIMPc — -153 dBc at +43 dBm is -110 dBm. PIMPc 43 dBm — Power per carrier the PIM figure above was measured at — the second half of one specification. Two carriers at this power produce a product at exactly PIM; the product then rides the third power of drive, so 10 dB less carrier is 30 dB less product."],[13,"duplexer","Duplexer","Duplexer An antenna port that splits into a transmit branch and a receive branch, each through its own passband — which is what the glyph draws: one junction fanning into two filters, each wave stack labelled with the arm it belongs to. ANT is on the left, TX and RX on the right. It takes a complete filter specification per arm, and its isolation is a consequence of the two responses meeting at one node rather than a parameter. In depth: System Components › Duplexer . Net Terminal 1 ANT 2 TX 3 RX The nets of an instance line, in this order. Name Default Unit On schematic Meaning Zant 50 Ω — The impedance the shared antenna port PRESENTS - the arms' own Zin, under a shorter name, so a complex value is conjugate-matched by a Term at its conjugate. The TX and RX arms both look into it, which is what makes them interact at all. TxResponse Chebyshev — — TX arm. Which prototype family the response comes from: Butterworth (maximally flat magnitude), Chebyshev (equiripple passband, reads Ripple), InvChebyshev (flat passband and an equiripple stopband floor, reads Astop), Bessel (maximally flat GROUP DELAY — chosen for its phase, not its shape) or Elliptic (equiripple in both bands, reads Ripple and Astop). A parameter the family does not read is ignored, so you never have to clear a field to change family. There is no isolation parameter: the TX-to-RX isolation is what these two responses and the antenna junction produce between them. TxForm Bandpass — — TX arm. Lowpass, Bandpass or Highpass. The symbol strikes a line through every wave the network blocks, so the shape is read off the glyph. Lowpass and Highpass read Fc; Bandpass reads F1 and F2 and ignores it. There is no isolation parameter: the TX-to-RX isolation is what these two responses and the antenna junction produce between them. TxOrder 3 — — TX arm. The PROTOTYPE order. A Bandpass transformation doubles the degree, so Order = 3 as a bandpass is a 6th-degree network — both conventions exist in the wild, and this one is the prototype's. The stopband slope of an all-pole family is 20 x Order dB per decade; InvChebyshev and Elliptic level off at their own floor instead. There is no isolation parameter: the TX-to-RX isolation is what these two responses and the antenna junction produce between them. TxFc 1 GHz — TX arm. The cutoff, for Lowpass and Highpass. For Chebyshev and Elliptic it is the RIPPLE bandwidth edge (where the response leaves the ripple band), for Butterworth the 3.01 dB point, for InvChebyshev the STOPBAND edge — where Astop is first met — and for Bessel the reciprocal of the group delay. Ignored when Form is Bandpass. There is no isolation parameter: the TX-to-RX isolation is what these two responses and the antenna junction produce between them. TxF1 0.9 GHz — TX arm. Lower band edge, for Bandpass. The band centre is the GEOMETRIC mean of F1 and F2 and the response is geometrically symmetric about it, not arithmetically. Ignored for Lowpass and Highpass. There is no isolation parameter: the TX-to-RX isolation is what these two responses and the antenna junction produce between them. TxF2 1 GHz — TX arm. Upper band edge, for Bandpass. Read with F1 — the two set both the centre and the width. Ignored for Lowpass and Highpass. There is no isolation parameter: the TX-to-RX isolation is what these two responses and the antenna junction produce between them. TxRipple 0.1 dB — TX arm. Passband ripple, in dB, for Chebyshev and Elliptic. The passband swings between 0 and -Ripple exactly this many times, which is what the order buys. Ignored by the other three families. There is no isolation parameter: the TX-to-RX isolation is what these two responses and the antenna junction produce between them. TxAstop 40 dB — TX arm. Stopband floor, in dB below the passband, for InvChebyshev and Elliptic. The stopband is equiripple AT this level rather than falling away past it — that is the trade those two families make for a sharper transition. Ignored by the other three"],[13,"annotation-components","Annotation components",""],[13,"var","Variables (VAR)","VAR A port-less annotation holding name = expression variable definitions, scoped to where it is placed — global at the test bench top, or local inside a cell. Variables are sweepable. Edit the rows in the multi-line text editor by double-clicking the VAR. No fixed parameters."],[13,"meas","Measurements (MEAS)","MEAS A port-less annotation holding name = expression measurement equations, evaluated after a run — for example Pout_dBm = 10*log10(...) . Measurements attach at the top test-bench level only. Edit the rows in the same multi-line editor as VAR. No fixed parameters."],[13,"the-full-component-list","The full component list","Component Type Category R Resistor Lumped L Inductor Lumped C Capacitor Lumped SRLC Srlc Lumped PRLC Prlc Lumped NonlinearC NonlinearC Lumped Vdc Vdc Sources VTone ToneSource Sources ITone CurrentToneSource Sources VCCS Vccs Sources VCVS Vcvs Sources P1Tone P1Tone Sources PnTone PnTone Sources GND Ground Terminals Term Term Terminals TermG TermG Terminals Pin Pin Terminals IProbe IProbe Terminals VProbe VProbe Terminals WSProbe WSProbe Terminals TLIN Tline TransmissionLine M Mutual Lumped S2P Snp DataFiles SPICE SpiceModel DataFiles Z2P ZPort Other SDD2 Sdd Other Tuner Tuner Terminals SourceTuner SourceTuner Sources LoadTuner LoadTuner Terminals VAR Var Other MEAS Meas Other Diode Diode Devices Match Match Matching MLIN Mlin Microstrip MBEND MBend Microstrip MTEE MTee Microstrip MCROSS MCross Microstrip MTAPER Mtaper Microstrip MKLOPF Mklopf Microstrip VerilogA VerilogA Devices wBond WBond Other Curtice FetCurtice Devices CurticeCubic FetCurticeCubic Devices Statz FetStatz Devices Materka FetMaterka Devices Angelov FetAngelov Devices Curtice-P PFetCurtice Devices Statz-P PFetStatz Devices Materka-P PFetMaterka Devices NJFET JfetN Devices PJFET JfetP Devices NIGBT IgbtN Devices PIGBT IgbtP Devices Bead Bead Lumped NVDMOS VdmosN Devices PVDMOS VdmosP Devices NMOS1 Mos1N Devices PMOS1 Mos1P Devices NMOS3 Mos3N Devices PMOS3 Mos3P Devices NPN BjtNpn Devices PNP BjtPnp Devices Mixer Mixer Devices MixerD MixerD Devices Balun Balun System Circulator Circulator System Switch Switch System SwitchD SwitchD System Amp Amp System Directional Coupler Coupler System Hybrid90 Hybrid90 System Hybrid180 Hybrid180 System Filter Filter System Attenuator Atten System Duplexer Duplexer System See also: Dynamic symbols (SDD / ZPort / SnP) · Nonlinear Capacitor & C–V Editor · Pins, Ports & Terms · Netlist format ."],[14,"","","What circuitRF carries across from a .model card or a .subckt, what it leaves behind, and why — including where to read the list of parameters that did not come with it."],[14,"surfaces","Two ways a SPICE file becomes a circuit","Both start from the same file and use the same translation, so everything on this page applies equally to either. Route What you get Use it when The SpiceModel component Components › SPICE Model or Subcircuit A component that runs from the file . Nothing is copied; edit the file and the schematic follows it. The file is the authority and must stay so. Copy to Workspace as Cell… (the project tree's context menu) An editable cell — a schematic you can open, re-symbol and change. The model is a starting point rather than the last word. A .model card circuitRF has a device for becomes that device , with its terminals where that device's terminals are. A .subckt becomes a schematic — every element inside it placed as an ordinary component, wired as the definition wires them, with the definition's own port names on the pins. A packaged part's parasitics are components, not parameters A vendor file for a real transistor is usually a .subckt : a .model card for the die, wrapped in the lead inductances and package capacitances that dominate its behaviour above a gigahertz or two. Those wrappers come across as ordinary R, L and C instances, because that is what they are. If you import the bare card instead, you get the die and no package — a far larger difference than anything else on this page. Leave Name blank and the highest-level definition is chosen, which is the wrapper."],[14,"report","Nothing is dropped silently","Every parameter on a card that circuitRF does not carry is named , every time: RF_NPN_DIE NPN — 34 parameter(s) — 6 not carried: AF, CJS, KF, MJS, PTF, VJS Three things about that line are deliberate: It is exhaustive. The translation walks a table of the parameters circuitRF has a home for; everything the table did not consume goes into the list. A parameter cannot be quietly absorbed, because absorbing it is not one of the outcomes. The spellings are your file's, not circuitRF's. They are echoed exactly as the card writes them, so you can search for them in the file rather than translating a name back. It survives the import. For a cell built with Copy to Workspace as Cell… the same list — and the notes below — are written onto the cell's own schematic as a text annotation. Weeks later, the cell still says what it was built from and what did not come with it. Alongside the not-carried list, the translation reports notes : decisions it made that you could reasonably want to see. Which of two published MESFET laws a card was read as and what said so; that a gate capacitance on the card switched the FET's charge model from a constant capacitance to a bias-dependent junction; that a card's RD / RS were placed as resistors beside the transistor; that a MOS card states no TOX and therefore has no intrinsic gate charge. These are not warnings — they are the reasoning, written down."],[14,"why","Why a parameter is not carried","There are five reasons, and every dropped parameter is one of them."],[14,"there-is-no-analysis-that-would-read-it","1. There is no analysis that would read it","KF and AF — the flicker-noise coefficients — appear on every semiconductor card and are carried by none of them. circuitRF has no noise analysis for them to feed. Accepting them would put two rows on the built cell that look honoured and change no result, which is worse than an empty space: a parameter this model does not read is not offered."],[14,"the-device-has-no-node-for-it","2. The device has no node for it","A bipolar card's CJS / VJS / MJS describe the collector–substrate junction . A discrete RF transistor has no substrate terminal to attach one to, and adding a fourth pin would change what the symbol means. Self-heating is the same shape of omission across every family. The junction temperature is a Temp parameter, not a solved node, so there is no electrothermal feedback and nothing for a card's thermal parameters to connect to. A thermal model belongs around the part."],[14,"circuitrfs-formulation-cannot-express-it","3. circuitRF's formulation cannot express it","A bipolar card's PTF is excess phase — a delay. circuitRF's weighting functions carry 1 and jω, not exp(−jωτ), so there is no term in the device for a delay to become. The same reasoning retires the MESFET family's transit-time delay. Also here: subthreshold conduction ( NFS ) on the MOS cards, because the classical law goes to exactly zero at threshold; quasi-saturation on a VDMOS card, which needs a drift-region model and a second internal node; the level-3 impact-ionisation substrate current; and the Statz/TOM charge formulation, which works on a smoothed effective voltage rather than on Vgs and Vgd separately and is a different scheme rather than a parameter change."],[14,"it-belongs-in-the-schematic-not-in-the-device","4. It belongs in the schematic, not in the device","A MESFET card's RD and RS are not carried, because circuitRF's MESFET family has no drain or source parasitic resistance of its own. They are not lost either: the import places them as ordinary series resistors beside the transistor and says so in a note. The same two spellings mean different things on a JFET card A JFET card's RD and RS are carried, as model parameters — circuitRF's JFET puts them on internal nodes of its own, so the schematic shows one device rather than a transistor with two resistors beside it. The two cards spell them identically and the difference is invisible once the cell is built, which is why the translation states which happened every time."],[14,"it-belongs-to-a-law-circuitrf-does-not-implement","5. It belongs to a law circuitRF does not implement","A JFET card stating B , ALPHA or VK is stating parameters of a higher published JFET level — a doping-profile knee and that level's own channel-length modulation. There is no square-law parameter that means the same thing, so they are named rather than folded into LAMBDA , which is a different quantity. The card is still read as the square law rather than refused, with a note saying the device will be optimistic where those terms matter. The MOS rule runs both ways . On a level-1 binding the six short-channel parameters ( ETA , THETA , KAPPA , VMAX , DELTA , XJ ) are not carried; on a level-3 binding LAMBDA is not, because level 3 computes the output slope from a real shortening of the channel rather than fitting it. Carrying a parameter onto a level that never reads it is the worse failure: it lands on the cell as an ordinary row, looks honoured, and is discovered much later by wondering why changing it does nothing. A card's LEVEL number is never simply obeyed On a MESFET card the level numbering is not portable — the same integer selects a different law in different dialects — so which law a card states is decided from its parameters : B appears in the Statz law and in no other, so stating it is the file's own unambiguous answer. LEVEL is listed as not carried so nobody concludes it was honoured. On a MOS card the classical numbering is portable — 1, 2 and 3 mean the same three published models everywhere — so the number is read, and 4 and above are refused (see below). LEVEL itself is still not carried: it selects a model rather than being one of its parameters."],[14,"families","What each card type leaves behind","Named omissions only — the ones circuitRF has made a decision about. Anything else your card states still appears in the not-carried list by name. The last column links to the device's own physics, where each omission is stated again from the model's side. Card Built as Named omissions The device D Junction diode KF / AF ; IKF — the high-injection knee, which this diode has no term for Diode NPN , PNP Bipolar transistor CJS / VJS / MJS , PTF , KF / AF What is modelled NMF , PMF MESFET — Curtice quadratic, or Statz where the card states B RD / RS (placed as resistors instead), LEVEL , KF / AF ; the Statz/TOM charge formulation, transit delay and breakdown The FET family NJF , PJF Junction FET, square law B , ALPHA , VK , KF / AF ; gate breakdown and transit-time charge The junction FET NMOS , PMOS MOS level 1 or level 3, chosen by the card's own LEVEL LEVEL , NFS , KF / AF ; the six short-channel parameters on level 1, LAMBDA on level 3; the level-3 substrate current The MOS transistor VDMOS Vertical power MOSFET, n- or p-channel from a bare keyword on the card Quasi-saturation and subthreshold shaping parameters; flicker noise; self-heating, which for a power device is a real omission The vertical power MOSFET BEAD Ferrite bead, four-element equivalent Saturation — a bead's inductance falls with DC bias current and this is a linear element, so it is not representable at all Ferrite Bead RES , R Resistor Everything beyond R , TC1 and TC2 — CAP / C , IND / L Capacitor, inductor Every temperature coefficient. circuitRF's resistor has TC1 / TC2 and its capacitor and inductor do not — which is a real loss of fidelity, and is exactly why it is reported rather than absorbed — A p-channel VDMOS is a bare keyword, and a bare negative threshold is not one A VDMOS card is VDMOS for both channels; a lone pchan keyword is what makes it p-channel. A card with no keyword and a negative VTO looks like a p-channel part — and equally like a (rare, real) depletion-mode n-channel one. Nothing on the card separates them, so it is read as n-channel, which is what the absent keyword means, and the ambiguity is reported rather than guessed at. If it is a p-channel part, build it against the p-channel component instead."],[14,"refused","When the whole card is refused","A model type circuitRF has no device for is refused by name — in the parameter dialog the moment the file is chosen, and again at Run. Nothing is approximated. The temptation is real: a JFET's square law looks like the Curtice quadratic with the tanh ignored, and a ferrite bead looks like a parallel RLC. Every one of those produces a cell that simulates and is quantitatively wrong, with nothing anywhere reporting it. Being told costs a minute; a plausible wrong transistor costs the measurement built around it. Refusals fall into two groups. circuitRF has no model for this — the refusal names the type and says what is missing, because \"unsupported\" sends you looking for a setting that does not exist: An IGBT card. circuitRF has an IGBT, and still cannot take this card: its parameters belong to the ambipolar-transport model and describe the silicon (base width, doping, carrier lifetime), while circuitRF's is an equivalent-circuit model parameterised by what a data sheet gives. Neither set is derivable from the other by renaming. A controlled-switch card. circuitRF's Switch is an ideal RF block set by its own parameters, not a card-driven behavioural switch. A MOS card at LEVEL 4 or above. Those are the compact-model families, whose parameters name different quantities under different spellings — almost nothing would be carried, and what came out would be this transistor wearing default numbers. Any other type, and a card naming no type at all. In each of those the suggested route is the same: run the model through the VerilogA component , which takes its parameters from the model file itself rather than from a card. The card does not say enough to build anything — refused for being incomplete, not unsupported: A BEAD stating only a DC resistance. What could be built is a resistor of a few milliohms, which is not a bead and would simulate perfectly. A resistor card stating only RSH . A sheet resistance is a resistance per square and needs a width and a length to become a value, neither of which a model card carries. A CAP or IND card stating no value. The principle behind all three: a value of zero simulates . Refusing is the only outcome that reaches you."],[14,"matters","Working out whether it matters","Ask these in order — most of the time the first one settles it. Read the value on your own card. A parameter stated as zero was doing nothing in the simulator the card was written for either. A bipolar card very often states CJS = 0 and KF = 0 , in which case the substrate junction and the flicker noise are absent from the source model too and nothing at all has been lost. Put a number on what is left. Most omissions have an arithmetic you can do in a line. Excess phase is the clearest: PTF is the phase in degrees at f = 1/(2π· TF ), so a card with TF = 17 ps and PTF = 0.4 is describing 0.4° at 9.2 GHz — below the extraction uncertainty of the package parasitics around it, and not worth a second thought. The same card at PTF = 30 would be a different conversation. Check you imported the wrapper, not the die. See the note at the top of this page. For a packaged part this is nearly always the largest term. If the omission is load-bearing, change models rather than parameters. A compiled model through VerilogA takes its parameters from the model file itself, so nothing has to be mapped onto circuitRF's parameter set and nothing is left behind."],[15,"","","The ideal blocks a system diagram is drawn out of — circulator, coupler, hybrid, balun, switch, amplifier, attenuator, filter, duplexer and mixer. What each one is for, what \"ideal\" costs you, and where passive intermodulation comes from."],[15,"what","What these blocks are, and what they are not","A system block diagram is a level above a circuit. The signal path is a chain of named boxes — a filter, a coupler, an amplifier, a mixer — and each box is described the way a datasheet describes it: a coupling in dB, an isolation in dB, an intercept in dBm, a passband and a rejection. Nothing in that description says what the part is made of, and for the questions the diagram is drawn to answer, nothing needs to. These components are those boxes. Every one of them is placed from the System filter in the Library Palette , wired like any other component, and simulated by the same engines. Together they answer: Level plans — what power is at each point in the chain. Cascaded gain, and cascaded intercept — the amplifier and the mixer each carry one, and a harmonic-balance run cascades them for you rather than through a spreadsheet formula. Image and spurious paths — what a mixer's other sideband lands on, and what the filter after it does about that. Band plans and isolation budgets — a duplexer's two arms, a circulator's reverse leakage, a coupler's directivity. Switch-state coverage — every position of every switch, in one run, because State is a swept parameter rather than a wire you move. All of it before any of the parts exist . That is what the class is for."],[15,"what-they-are-not","What they are not","They are ideal by construction , and every one of those idealisations is a place a real part will disagree with the simulation: Frequency-flat where a real part is not. The circulator, coupler, hybrid, balun, switch, attenuator and amplifier hold every number you typed at every frequency, from DC upwards. An ideal 90° hybrid is in exact quadrature at 100 MHz and at 100 GHz alike, which no physical coupler is — a branchline holds its quadrature over perhaps 10–20% of bandwidth and its coupling over less. Exactly matched, and exactly isolated, unless a number says otherwise. RL , Isolation and Directivity default to 200 dB, which does not mean \"200 dB\" — it means the term is absent . Nothing is stamped into the matrix at all, so the reverse path of a default circulator is not small, it does not exist. Type a real number to turn a non-ideality on. Lossless except where a loss is typed in. IL and Loss are the only dissipation in the family. There is no skin effect, no dielectric loss, no radiation. No spread, no temperature, no power, no noise. No manufacturing tolerance, no drift, no DC supply current, no bias pins, and no noise figure. An ideal amplifier is an ideal amplifier at any temperature and consumes nothing. \"Ideal\" means the term is absent, not small This is a convention shared by every block here and by the Mixer . A non-ideality is switched off by an honestly large number — 200 dB of isolation, 200 dBm of intercept, −200 dBm of intermod — and the model snaps that to exactly ideal rather than stamping a 10 −10 entry. A freshly placed block is therefore exactly linear, exactly matched and exactly isolated, and a measurement of it comes back at the solver's own floor rather than at a number you did not choose."],[15,"instead","What to reach for instead, and when","The moment the question is about bandwidth, dispersion, or a real part's own behaviour , an ideal block is the wrong instrument. It will answer, and the answer will be the idealisation you asked for. What to place instead: The question What to place How does this coupler behave across the band? Four quarter-wave arms — TLIN for an ideal line, MLIN and the microstrip junctions for a real one What does the actual part I bought do? SnP — its measured or simulated Touchstone file What does this piece of layout do? An EM extraction of the artwork, through the planar method-of-moments solver What does this transistor do? The FET family , the SDD , a compiled Verilog-A model , or a kit What matching network gets me there? The Match component , which synthesises one What does a real filter's loss and shape cost me? A synthesised Match ladder, a Touchstone file, or an EM run of the physical filter None of that makes the ideal block a waste of time — it is the other way round. A level plan built out of ideal blocks tells you what each part has to achieve , and that is the specification you then go and meet with a real one."],[15,"running","Which analysis answers which question","Two analyses, and the split is not arbitrary: S-parameters answer everything that is a property of one frequency at a time: port match, isolation, coupling, directivity, a filter's passband and rejection, a duplexer's arm-to-arm leakage, an amplifier's small-signal gain and stability. This is the analysis nine of the eleven blocks are entirely described by. Harmonic balance answers everything that moves energy between frequencies or depends on level: conversion gain, mixing products, compression, third-order intermodulation, and passive intermod. Drive it with two tones ( multi-tone harmonic balance ) when the question is an intercept. An S-parameter run cannot see a device convert This is worked through once, for the mixer, under Components › Ideal Mixer — and the argument is the same for every nonlinear block here. S-parameters are a single-frequency small-signal measurement about a DC operating point; conversion, compression and intermodulation are none of those things. What an S-parameter sweep of a mixer reports — the port matches and the leakages — is the right answer to the question it was asked, not a missing one."],[15,"pim","Passive intermodulation","A passive part is supposed to be linear. Real ones are not quite: a metal-to-metal contact, a ferrite, a corroded joint or a badly torqued connector all have a slightly nonlinear current–voltage relation, and two strong carriers passing through one come out with odd-order products around them. In a transmit chain those products land in the receive band, where nothing downstream can filter them out, and the specification that keeps them there is called PIM . Five blocks here can carry it: the Attenuator , the Circulator , the Directional Coupler and both hybrids. Each gains two parameters, PIM and PIMPc , which are one specification in two fields ."],[15,"what-pim-is-here","What PIM is here","A deterministic, memoryless nonlinearity — not a random or noise-like process. Two carriers in, a third-order product out at 2f₁ − f₂ and 2f₂ − f₁, at exactly the level you specified, every run. It is generated on the wave incident at each port and then routed by the block's own S-matrix, which is what a datasheet's number describes: a product born where the signal arrives, and then carried out of whichever ports the block carries things out of. On a circulator that means the product appears at the port the carriers go to, suppressed at the isolated one by the block's own isolation — routing you get for free rather than by tuning."],[15,"how-it-is-specified","How it is specified","PIM is the absolute level of the third-order product , in dBm, and PIMPc is the power per carrier it was measured at , in dBm. Both, always: a product level means nothing without the carriers it was measured against. Suppliers quote it both ways, and the two are the same number differently dressed: product (dBm) = carrier (dBm) − product (dBc) So a part specified at −153 dBc with two +43 dBm carriers — 20 W each, the usual test — is a part whose product sits at 43 dBm − 153 dB = −110 dBm and you type PIM = -110 dBm , PIMPc = 43 dBm . Turn it round to check a datasheet the other way: a part quoted at −110 dBm against 2 × 43 dBm is a −153 dBc part. Away from PIMPc the product rides the third power of drive. Third order means 3 dB of product per 1 dB of carrier, so 10 dB less carrier is 30 dB less product, and the dBc figure improves by 20 dB. That is the whole reason the carrier power has to travel with the specification."],[15,"which-blocks-carry-it-and-why-the-others-do-not","Which blocks carry it, and why the others do not","A nonlinearity in circuitRF is a memoryless function of the port voltages: i = f(v) , evaluated instant by instant, with no memory of what came before. That is a real constraint, and it decides the list: Block PIM Why Attenuator, Circulator, Coupler, Hybrid90, Hybrid180 yes Their ideal S-matrix is frequency-flat, so a memoryless law describes them exactly Balun, Switch no Excluded by design — neither is a part PIM is specified on Filter, Duplexer no, and it cannot be added Their whole purpose is frequency dependence. A rational transfer function has memory, and a memoryless nonlinearity cannot be bolted onto one inside a single component The workaround: a PIM generator you can put in front of anything An Attenuator with a small Loss and a PIM specification is a standalone PIM generator . Place one in front of a filter, a duplexer, a length of line, or anything else that cannot host PIM itself, and the products appear exactly where a real bad connector would put them. This is a better answer than attaching a memoryless nonlinearity to a rational response, and it is how a real chain is analysed anyway — the PIM comes from the connector, not from the filter body. Not Loss = 0 — give the pad a little loss A perfectly matched 0 dB attenuator is a wire , and a wire has no admittance matrix at all: det(I + S) is zero exactly , so there is no i = f(v) to write. The block refuses by name rather than producing a NaN somewhere inside a Newton iteration. Give it a small loss instead — the message says so — or a finite return loss. How small is small enough? The residual error in the product level, measured against the level you asked for: Pad loss −153 dBc (a datasheet part) −100 dBc −90 dBc 0.01 dB +0.0014 dB +0.66 dB +2.40 dB 0.1 dB +0.0001 dB +0.063 dB +0.20 dB 1 dB 0.0000 dB +0.0056 dB +0.018 dB 3 dB 0.0000 dB +0.0012 dB +0.0039 dB At any level a passive part is actually specified at, 0.01 dB is already invisible; 1 dB of loss — still an electrically negligible pad — removes the effect everywhere . Use 1 dB unless the loss itself matters to your level plan."],[15,"what-turning-it-on-costs","What turning it on costs","PIM is off by default , at PIM = -200 dBm , and anything at or below −190 dBm counts as off. Below that threshold the block is stamped as its exact linear S-matrix, costs nothing, and contributes no nonlinear unknowns to a harmonic-balance solve. Type a real number and that block becomes a nonlinear component . Two consequences: A harmonic-balance run now carries it. That is the point, and it is what an intermod measurement needs. An S-parameter run solves a DC operating point first , because that is what circuitRF does with any nonlinear device — it linearises about the operating point before sweeping. What it reports does not change. The distortion law has zero slope at zero signal, so the linearisation is the same matrix the linear block stamps: measured across eight block variants and five frequencies, the worst difference between a −170 dBm and a −40 dBm specification is below 10 −12 in S. Your match, isolation and coupling are exactly what they were."],[15,"what-the-higher-orders-do","What the higher orders do","Fifth- and seventh-order products come out too, because the distortion law is a soft limiter ( tanh ) with a series of its own — and their levels are that limiter's fixed ratios, not a fit to any measurement . There is one scale parameter, set by the third-order figure you typed, and every higher order follows from it. PIM specifies IM3 and nothing else. Say it plainly: if you compare a simulated IM5 against a measured one and they disagree, that agreement was never claimed. The same single-parameter idealisation shows in IM3 itself once the drive gets close to the limiter's own scale — the product falls slightly below the ideal 3:1 extrapolation, by 0.06 dB at 30 dB below the equivalent intercept and 0.6 dB at 20 dB below it — which is exactly how an intercept has to be read off a bench measurement too. Known gap: a 90° hybrid with PIM on, in a multi-tone run The quadrature hybrid is the one PIM-capable block whose S-matrix is genuinely complex, and it needs a frequency-domain factor that only the single-tone harmonic-balance solver honours today. In a two-tone run — which is the analysis PIM exists for — that factor is dropped and the block degenerates to four open circuits. The failure is loud rather than plausible: essentially nothing reaches the through port and the source node reads about 6 dB high. Every real-S block is unaffected and fully correct in multi-tone runs — the attenuator, the circulator, the in-phase coupler and the 180° hybrid. For a quadrature hybrid, put the PIM on an attenuator beside it instead, exactly as you would for a filter."],[15,"catalogue","The blocks, one by one","One entry each, in the order the palette lists them under System . The parameter tables live in Components , one section per block, and each entry below links to its own."],[15,"mixer","Mixer, and MixerD","Mixer An ideal three-port mixer: the IF port carries the product of the RF and LO ports, so both sidebands come out and the conversion gain tracks the LO drive. MixerD MixerD is the same component with all six nets brought out as pins, in ± pairs, for when a port's return is not ground. Both are full members of the System family — they carry an intercept, three isolations and per-port impedances. They are documented in full, with the conversion-gain arithmetic and the non-ideality table, under Components › Ideal Mixer and Differential Mixer . This entry is a pointer, not a second copy."],[15,"amp","Amplifier (Amp)","Amp An ideal gain block: IN on the left, OUT on the right, Gain in dB and one third-order intercept. It has no DC power consumption and no bias pins , by design — there is no supply, no efficiency, no PAE and no thermal node. If those are the question, the answer is a real device model and a harmonic-balance load-pull, not this block. IP3Ref says whether the intercept you typed is input- or output- referred; the default is Output, because that is the form a power amplifier's datasheet quotes, and OIP3 = IIP3 + Gain is an identity so there is deliberately one field rather than two that could contradict each other. P1dB is not a separate knob. One nonlinearity sets compression and intermodulation together, so the 1 dB compression point follows from the intercept and lands at IIP3 − 8.96 dB input-referred. That is the soft limiter's own value, and it is not the textbook cubic's −9.64 dB — the two differ by two-thirds of a decibel, which matters if you are checking against a hand calculation. The amplifier is unilateral unless you turn S12 on. With no reverse path there is no feedback loop, which is what makes an ideal amplifier unconditionally stable at every frequency and every termination; setting S12 is what makes stability a question at all. Return loss does not change the gain Gain , RLin , RLout and S12 are the four entries of the block's S-matrix and each is exactly the number you typed. Mismatching a port does not quietly re-scale S21, the way a Thévenin-source formulation would — a datasheet states gain and return loss as independent measurements, and so does this block. Parameters: Components › Amplifier ."],[15,"atten","Attenuator (Atten)","Attenuator A fixed pad. Two interchangeable pins, a Loss in dB, a return loss, and — because a pad is where a connector usually is — an optional PIM specification. Loss = 0 is a legitimate thing to place: it is an ideal through, and it stamps and solves as one. With a small loss and a PIM figure it becomes a standalone PIM generator , which is the supported way to give a filter, a duplexer or anything else a passive-intermod contribution. See Passive intermodulation above for the arithmetic and for why the loss must not be exactly zero in that role. Parameters: Components › Attenuator ."],[15,"balun","Balun","Balun A transformer between one unbalanced port ( UNB , on the left) and a balanced pair ( BAL+ and BAL− , on the right), with AmpImb and PhaseImb for the two ways a real balun departs from a perfect split. Zbal is per port. It is the reference impedance of each balanced port to ground, so the differential impedance across the pair is twice it — the 50/50 default is the ordinary 1:2 balun, 100 Ω differential presenting 50 Ω single-ended. As an impedance transformer the ratio is n = √(2·Zbal / Zunb) , and a differential load R is seen at UNB as R · Zunb / (2·Zbal) . Ports 2 and 3 are not matched, and neither are a real balun's A lossless reciprocal three-port cannot have all three of its ports matched — that is a theorem, not an implementation limit — and a real balun does not isolate its balanced ports from each other either. Read one at a time, BAL+ and BAL− each show a reflection; in the modal basis that is a clean through from UNB to the differential mode and a total reflection for the common mode, which is exactly what an ideal balun is. The unbalanced port does not couple to the common mode at all. A consequence worth knowing before you wire one. A resistor floating between BAL+ and BAL− , with nothing else pinning them, says the same thing the ideal common-mode open says, and the two together leave the common-mode potential undetermined — a genuine floating node. Use two half-value resistors with the tap grounded instead: it is the identical differential load, it pins the common mode, and it changes the answer not at all. If what you want is an exact ideal transformer with no common-mode behaviour to think about, that is a two-port with unequal port impedances rather than a balun — a Filter with Zin ≠ Zout is exactly that inside its passband. Parameters: Components › Balun ."],[15,"circulator","Circulator","Circulator Three ports, and power goes round them one way: 1 → 2 → 3 → 1. It is the only non-reciprocal component in circuitRF — S21 ≠ S12 on purpose, and the whole point of the part. Terminate one port and it is an isolator. Direction is a parameter, and it is drawn on the symbol : CW circulates 1 → 2 → 3 → 1 and CCW reverses it, and the arrow inside the circle follows. A schematic therefore never hides which way a circulator turns — see Dynamic symbols . At the default Isolation the reverse entry is not stamped at all , so the forward/reverse ratio is infinite rather than 200 dB, which is what makes a terminated circulator behave as a real isolator rather than as a very good one. The ideal circulator's S-matrix has no impedance matrix whatever — det(I − S) = 0 exactly — which is why this family is stamped from the definition of S rather than converted to Z. Its admittance does exist, and is (1/Z0)·[[0, 1, −1], [−1, 0, 1], [1, −1, 0]] : antisymmetric, with a zero diagonal. Detuning the port match, in magnitude AND phase. A real circulator is notoriously badly matched, and what a power amplifier connected to port 1 actually feels is not a return loss but a complex reflection — the same |Γ| at a different angle is a completely different load. VSWR1 with Ang1 set port 1's own reflection directly, and VSWR2 / Ang2 , VSWR3 / Ang3 do the same for the other two: S_pp = ((VSWRp − 1) / (VSWRp + 1)) ∠ Angp so an amplifier on port 1, with the other two ports matched, sees exactly Z0·(1 + Γ)/(1 − Γ) . VSWR = 1 means \"not stated\" and that port falls back to RL , so the datasheet form — one return loss for the whole part — still works and nothing changes for a design that never touches these. The detune is flat with frequency, deliberately: it is the mismatch you want to test a PA against, not a rotating one. Do not reach for Z0 to do this A complex Z0 is accepted, but it is the reference every port shares and the reference IL and Isolation are stated against — and with an ideal circulator the reflection it produces at port 1 is the PRODUCT of what ports 2 and 3 are terminated in, because the wave leaves port 2, reflects, circulates to port 3, reflects again, and only then comes back. It is not the number you typed and it is not monotone in anything you would think to turn. VSWR1 / Ang1 is the port's own reflection, which is what a VSWR figure on a datasheet means. It can carry a PIM specification. Parameters: Components › Circulator ."],[15,"coupler","Directional Coupler","Directional Coupler Four ports in the order a coupler is always specified: 1 in, 2 through, 3 coupled, 4 isolated. Coupling alone sets the split, and the arrow on the body is what separates the coupled port from the isolated one. The through arm is not free. Coupling is a lossless split — t = √(1 − c²) — so an ideal 20 dB coupler already loses 0.044 dB through its main arm, and a 10 dB coupler loses 0.46 dB. IL is loss added on top of that, and it scales all three transmission paths together so that Directivity keeps meaning what it says. Parameters: Components › Directional Coupler ."],[15,"duplexer","Duplexer","Duplexer An antenna port that splits into a transmit branch and a receive branch, each through its own passband: ANT on the left, TX and RX on the right. It is two complete filter specifications (every Filter parameter, prefixed Tx and Rx ) plus one shared antenna impedance. There is no Isolation parameter, and that is the point of the component. A duplexer's TX→RX isolation is a consequence of its two responses meeting at one node; a number you typed would be overriding the physics with an assertion. What it achieves is what the far arm's own rejection allows — with the default band plan at order 5, the measured TX→RX leakage runs from about −89 dB at the TX band's lower edge to −57 dB at the worst point across both bands. The arms load each other — that is why real duplexers have phasing lines An out-of-band ideal bandpass arm reflects essentially everything (|S11| = 0.999999967 — nothing is dissipated, which is what ideal buys), but not at zero phase: at a neighbouring band's centre its reflection sits at about −23°, and a unit-magnitude reflection at a non-zero angle is a reactance . It loads the junction, so each arm's transmission is not quite what the same filter would do standing alone — up to 0.144 in amplitude for adjacent bands, falling to 0.040 when the two bands are widely separated. The antenna match runs about −12 dB across each band. That is the same statement as \"a real duplexer needs a phasing line\", and the fix is the same one: put a TLIN in an arm and tune its length. There is deliberately no hidden length inside the component. Parameters: Components › Duplexer ."],[15,"filter","Filter","Filter A two-port filter synthesised from a prototype: Response picks the family — Butterworth , Chebyshev , InvChebyshev , Bessel or Elliptic — and Form picks Lowpass, Bandpass or Highpass. A parameter the chosen family does not read is ignored rather than refused, so changing family never means clearing a field first. It wears the Match component's symbol, and that is deliberate The Filter and the Match network share one glyph — the same picture, not a related one. Impedance matching is a form of filtering , the two are built out of the same idea, and the library says so rather than pretending otherwise. Tell them apart the way you tell the five FET laws apart: by the type label and the instance name , FLT1 against MN1 . The stack of waves with a slash through each blocked band follows Form — see Dynamic symbols . Order is the PROTOTYPE order. The bandpass transformation doubles the degree, so Order = 3 as a bandpass is a 6th-degree network . Both conventions exist in the wild; this one is the prototype's. For the three all-pole families the far stopband falls at 20 × Order dB per decade; InvChebyshev and Elliptic put transmission zeros on the jω axis instead, which is what buys their sharp transition and why their stopbands level off at Astop rather than continuing to fall. How much that selectivity is worth, at order 5 with 0.1 dB of passband ripple and a 60 dB floor: the elliptic reaches −60 dB at 2.04 × the band edge, against Chebyshev's 3.41, Butterworth's 3.98 and Bessel's 15.57. Bessel is not competing — it is chosen for group delay , which it holds within 1% out to a band that grows with every order. Zin and Zout are independent. The block is stamped as its scattering matrix rather than synthesised as a ladder, so an unequal pair is free: the filter is then a lossless impedance transformer as well as a filter , matched at both ports in its passband. Measured in a uniform 50 Ω system an unequal pair shows the transformer's mismatch, which is the answer and not a fault. IL lays a flat loss on top, and it dissipates — it multiplies S21 and leaves S11 alone, the way a real filter's loss does. Either may be COMPLEX — Zin = 5+j100 Ohm — which is a filter designed to work between reactive terminations rather than resistive ones, the ordinary case at the ports of a real device. Every ideal system block takes a complex port impedance the same way: the Attenuator , Switch , Circulator , Coupler , both hybrids, the Balun , the Duplexer , and an Amplifier left linear. Zin is what the port presents — so terminate it in the conjugate Zin and Zout name the impedance that port presents , so a filter at Zin = 5+j100 is conjugate-matched — maximum power transfer — by a Term at Z=5-j100 Ohm , which is where it measures the prototype's own response. A Term at 5+j100 , the same value, is a near-total mismatch. The duplexer's Zant , TxZ and RxZ are the same quantity under shorter names. A parameter spelled Z0 is different, and deliberately: it is the reference impedance S is defined against — the attenuator's, the switch's, the circulator's, the coupler's — and the two differ by a conjugate. The parameter name is what tells you which you are looking at, and every block with a real port impedance is unaffected either way. A block that is NONLINEAR refuses a complex impedance rather than reading its real part: an Amplifier with a finite IP3 (which is the tile's own default of 40 dBm — set IP3=200 for the linear stamp), any block with PIM turned on, and the Mixer . The refusal names the component and the port. A parameter that is only ever read as a real number — a dB, a frequency, an order — refuses a complex value the same way, for the same reason: the alternative is the model quietly using its default and reporting nothing. Parameters: Components › Filter ."],[15,"hybrid180","180° Hybrid (Hybrid180)","Hybrid180 The same component as the directional coupler, seeded at 3.0103 dB with the coupled port in anti-phase — a sum port and a difference port. Everything in the Directional Coupler entry applies, and 3.0103 dB is simply the equal split written to the precision that makes c = t = 1/√2 . Parameters: Components › 180° Hybrid ."],[15,"hybrid90","90° Hybrid (Hybrid90)","Hybrid90 The same component again, at 3.0103 dB with the coupled port in quadrature — the standard building block of a balanced amplifier, an image-reject mixer or a reflection-type phase shifter. The quadrature is exact at every frequency , which is the sharpest idealisation in this chapter. A branchline coupler holds 90° over a useful band and its split over a narrower one; this block holds both from DC upwards. When the bandwidth is the question, build one out of four quarter-wave TLIN arms — or four MLIN arms and the microstrip junctions — and sweep it. It is also the only one of the three coupler tiles that is unitary : a lossless, matched, reciprocal four-port with directivity must have its coupled arm in quadrature. At 0° or 180° the matrix is energy-consistent under any single-port excitation but not simultaneously realisable — which circuitRF stamps anyway, because you are allowed to type numbers a physical part could not have. Parameters: Components › 90° Hybrid ."],[15,"switch","Switch","Switch An SPST switch. Two interchangeable pins, an insertion loss for the path it is making, and an isolation for the path it is not."],[15,"switchd","Transfer Switch (SwitchD)","SwitchD The SPDT: one common port on the left, two throws on the right. The same engine component as the SPST — they share the SW instance prefix, so swapping one for the other does not renumber a schematic. State is a parameter, not a pin — so sweep it Which throw is closed is a number , not a wire you move: 1 is the SPST's only throw, 1 or 2 selects an SPDT's, and 0 opens everything. So a parametric sweep over State simulates every switch position in one run , and a State naming a throw that does not exist simply closes nothing — by the same rule, not by a special case, which is what makes the sweep safe at every value. And the symbol is drawn in the position it is set to: the blade lifts for 0 and points at the selected throw otherwise, so a swept state is readable on the page. See Dynamic symbols . OffState is the choice that changes the neighbouring circuit. Reflective makes an open throw an open circuit — what a series switch does, and what sends the signal straight back at whatever is attached to it. Absorptive makes it a matched termination instead. If there is a filter, an amplifier input or a length of line on that throw, the two answers are not close to each other. Parameters: Components › Switch and Transfer Switch . See also: Components · Dynamic symbols · Simulations · The Match Component ."],[16,"","","Most components have a fixed glyph. Some do not — their symbol is generated from a parameter rather than drawn once. For three of them the pin count itself follows the port count; for others the picture changes while the pins stay put. This chapter explains how those symbols are built and how to control them."],[16,"why","Why some symbols are dynamic","An N-port's symbol can't be a fixed picture — a 2-port and a 5-port need different numbers of pins. For the SDD , ZPort , and SnP , circuitRF generates the body and pins from the NumPorts parameter. Change the port count and the symbol regrows, with pins landing on the connection grid so wiring stays clean. A second, smaller group keeps its pins and changes only what is drawn between them: the Match network and the Filter redraw their passband picture from Form , the Circulator turns the way Direction names, and the two switch tiles draw their blade in the position State names. Those three are covered in the last three sections below."],[16,"sdd","SDD — 2N differential pins","SDD at 2 ports (4 pins) The SDD exposes 2N pins as differential ± pairs — for an N-port, pins ordered 1+, 1−, 2+, 2−, … . Each port's voltage is the difference across its pair: port p's voltage _vp = V(p+) − V(p−) , which is exactly the _v1 , _v2 , … you use in the equations. This differential layout lets each port float with its own reference rather than being forced to ground. (In the netlist example the FET writes gate 0 drain 0 — each port's minus pin tied to ground.) The I[port, weight] / Q[port] equation grammar is summarized under Components › SDD ."],[16,"zport","ZPort — N signal pins","ZPort at 2 ports The impedance N-port grows with NumPorts and is defined by its impedance matrix Z[p,q] (an N×N grid of entries). Its pins are the N signal ports; the parameter editor lists one Z[p,q] row per matrix entry."],[16,"snp","SnP — arrangement, pitch & floating reference","SnP at 2 ports The Touchstone block's symbol is the most configurable. Besides NumPorts and the File , three options shape the symbol and its reference convention:"],[16,"pin-arrangement-pinconfig","Pin arrangement ( PinConfig )","How the N pins are laid out around the body (e.g. the default Standard arrangement). Choose the layout that wires most cleanly into your schematic; it changes only the symbol geometry, not the electrical behavior."],[16,"pitch-pitch","Pitch ( Pitch )","The spacing between pins (e.g. Loose by default). Tighter pitch packs a high-port-count block into less space; looser pitch leaves room to wire each port. Pins always remain on the connection grid."],[16,"external-reference-pin-refnode","External reference pin ( RefNode )","By default each port is referenced to ground, so an N-port shows N nets . Set RefNode = true to expose a common external reference pin instead — now the block has N + 1 nets , and all ports are measured against that shared reference rather than ground. Use this when the data block's reference is not circuit ground (a floating measurement). N or N+1 nets This is the \"N-or-N+1\" rule: a frequency-domain N-port lists either N nets (each port to ground) or N+1 nets (the last being the common reference). It applies to SnP, ZPort, and similar frequency-domain blocks — not to 2-terminal R/L/C. Interpolation ( InterpMode ) and out-of-range behavior ( ExtrapMode ) control how the file's data is sampled onto the analysis sweep — see Components › SnP ."],[16,"spicemodel","SPICE Model — the whole glyph follows the file","SPICE The others on this page change size with a number you set. The SpiceModel component changes what it is a picture of , and you set nothing: it reads the file its File and Name parameters name, and draws whatever is in there. A .model card circuitRF has a device for draws as that device — a diode card draws as a diode, with its anode where a diode's anode is. The point is that the schematic then says which lead is which without anything being opened. A .subckt draws as an N-port box carrying the definition's own port names , not port numbers. PinConfig and Pitch lay that box out, meaning exactly what they mean on an SnP. Before a file is chosen it draws as a generic two-port. That is deliberate, not a placeholder: it can be dropped and wired first and pointed at a file second. Edit the file and the schematic follows it — there is no copy of the interface anywhere to go stale, which is why this component has no pop-in and no .csch of its own. See Components › SPICE Model or Subcircuit ."],[16,"form","Match & Filter — the picture follows the Form","Filter, drawn for Form = Bandpass The Filter and the Match network share one glyph — the same picture, not a related one. Impedance matching is a form of filtering, and the library says so. You tell the two apart by the type label and the instance name ( FLT1 against MN1 ), the same way the FET laws that share a glyph are told apart. The picture is a stack of waves with a line struck through each one the network blocks , so the shape is read straight off the schematic: Form What is drawn What passes Lowpass the high wave struck through low frequencies Highpass the low wave struck through high frequencies Bandpass the outer waves struck through the band between F1 and F2 Both pins stay where they are, so changing Form never disturbs your wiring. Because the glyph already says which shape the network is, Form is not captioned beside the symbol — it would say the same thing twice. Nor is the band: Fc and F1 / F2 are alternatives, so whichever pair the form does not read would be a caption naming a frequency the filter is not at. Open the properties panel for the numbers. The duplexer draws two of them The Duplexer 's glyph carries the same passband stack twice, once in each arm, because a duplexer is two filters sharing an antenna node. Its own picture is fixed — the two arms each have their own TxForm and RxForm , and a body drawn from both would be unreadable at tile size."],[16,"circulator","Circulator — the arrow follows the Direction","Circulator A circulator is the one component in circuitRF that behaves differently in each direction, so which way it turns is the first thing a reader needs off the page. Direction puts it there: CW circulates 1 → 2 → 3 → 1 and CCW reverses it , and the arrow drawn inside the circle follows. Direction is not a caption — it is the same parameter the model stamps from, so the picture and the electrical behaviour cannot disagree . It is hidden from the schematic labels for that reason: the arrow already says what a Direction = CW caption would say. The three pins stay exactly where they are when it changes, so reversing a circulator never disturbs your wiring. Full treatment: System Components › Circulator . Parameters: Components › Circulator ."],[16,"state","Switch & Transfer Switch — the blade follows the State","Switch Both switch tiles draw themselves in the position they are set to . State is a plain number naming which throw is closed — 0 opens everything, 1 is the SPST's only throw, 1 or 2 selects one of the SPDT's — and the blade lifts or points accordingly. SwitchD That is worth more than tidiness. Because State is a parameter and not a pin , a parametric sweep over it simulates every switch position in one run — and the schematic redraws itself at each point, so a swept state is readable rather than hidden in a dialog. State is therefore hidden from the schematic labels: the blade already says it. The pins never move; Throws (1 for the SPST tile, 2 for the SPDT) is what sets the pin count and is not something to change by hand. Full treatment: System Components › Switch . Parameters: Components › Switch and Transfer Switch . See also: Components · System Components · Pins, Ports & Terms · Netlist format ."],[17,"","","The Symbol Editor draws the glyph a cell shows on a schematic — its body art plus the pins that connect it. It is a focused drawing tool, not a cell editor: it never changes a cell's port count or parameters, only how the cell looks."],[17,"what","What it edits","A symbol ( .csym ) is a dumb glyph : a list of drawing primitives plus a set of pins, each mapped to one of the cell's ports. It owns geometry only — the cell owns the port count, parameters, and identity, and decides which of its (possibly several) symbols is primary. A schematic instance draws the cell's primary symbol; editing the symbol updates every instance of that cell, live. Open it from View → Open Symbol Editor (docked or as a tear-off window), or by opening a .csym file. New scratch symbols can be created without a workspace. A cell has up to three views and each has its own editor: this one, the Schematic Editor for the electrical contents, and the Layout Editor for the physical artwork."],[17,"primitives","Drawing primitives","A symbol is built from typed primitives in component-local coordinates (100 units = one connection-grid square): Primitive Use Line, Polyline / Path leads, zigzags, general open shapes (free-angle, not Manhattan-routed) Rect, Rounded Rect device boxes, terminating-impedance frames Circle / Ellipse source bodies, dots Arc inductor coils Triangle / Polygon ground symbol, arrowheads (stroked or filled) Quad / Cubic curve the capacitor's curved plate Sine, Half-wave parameterized smart-paths (amplitude, cycles) for AC/tone-source art Text labels — string, anchor, font size, style (regular / bold / italic / condensed) Bitmap a reference image to trace over (lowest Z, editable opacity, lockable; not grid-snapped) Each drawable primitive carries a stroke width , a fill flag , and a color role — never a literal RGB. Symbols draw in theme roles ( symbol-line , symbol-text ), so they stay correct in light and dark mode automatically. There is no color picker; stroke width and font size/style are editable, color is not."],[17,"pins","Pins & ports","A pin is a placed marker mapped to one of the cell's ports. The editor shows the cell's ports as the set you can map; you place a pin and assign it a port. An unmapped port is legal — it's an open circuit If a port has no pin, the symbol still works: that port is treated as an open circuit. A half-authored symbol is always safe, and a soft panel flags any unmapped port without blocking editing. A pin's position is body geometry; its identity is the port mapping. The lead you draw from the body to the pin is ordinary art, but the pin tip itself always lands on the connection grid. Moving a pin can disconnect wires (for now) In this release, connectivity is positional, so moving a pin relocates it on every instance and wires at the old location become unconnected — the editor warns before applying such a move. circuitRF is committed to migrating to logical pin binding (where wires follow a moved pin, as in other schematic editors); the position-vs-identity split above is designed to make that a clean addition."],[17,"grids","Two grids: pins vs. art","This is the two-grid rule applied to symbols: pins snap to the connection grid P (100 units) so placed instances always connect, while body art snaps to the fine authoring grid p (5 units) for drawing freedom. Bitmaps are exempt from both — placed and resized freely as tracing aids. You can toggle the art-grid snap; the pin grid is always enforced."],[17,"orientation","Orientation convention","The standard library follows the conventional EDA look: 2-terminal parts are vertical — Resistor, Inductor, Capacitor, sources, Term, Ground: port 1 at top (0,-200) , port 2 at bottom (0,+200) . Place them rotated 90° in a horizontal signal path (the normal workflow). Multi-terminal boxes/devices are horizontal — FET, ZPort, SDD, and the auto-generated box use left/right ports."],[17,"tools","Tools & editing","Symbol editor toolbar # Button What it does 1 Zoom to Fit (F) 2 Select / Move (S) 3 Line — click and drag 4 Polyline — click points; Enter or double-click to finish 5 Rectangle — drag corner to corner 6 Rounded Rectangle — drag corner to corner 7 Circle — drag from centre 8 Ellipse — drag bounding box 9 Arc — drag from centre; 270° sweep 10 Triangle — click 3 points (auto-commits) 11 Polygon — click points; Enter or double-click to close 12 Quadratic Bézier — click start, control, end (3 points) 13 Cubic Bézier — click start, ctrl1, ctrl2, end (4 points) 14 Sine wave — drag to set length and amplitude 15 Exponential taper — drag to set length and start width 16 Text — click anchor, type, Enter to place 17 Pin — click empty space to place; use Select tool to move, delete, or rotate pins (P) 18 Insert Bitmap… 19 Snap: Fine Grid (G) 20 Rotate Selection 90° CW about origin (R) 21 — Stroke: 22 Normal stroke weight 23 Thin stroke weight 24 Thick stroke weight 25 Undo (⌘Z / Ctrl+Z) 26 Redo (⌘⇧Z / Ctrl+Shift+Z) The toolbar is grouped by what you are doing, and the groups are worth learning as groups: View — Zoom to Fit is the one you will press most; a symbol is small and the canvas is not. Select — the tool everything returns to. Press S , or Esc twice. Straight and curved outlines — Line and Polyline for the strokes a symbol is mostly made of; Quadratic and Cubic Bézier when a hand-shaped curve is what the part looks like. Closed shapes — Rectangle, Rounded Rectangle, Circle, Ellipse, Arc, Triangle, Polygon. Reach for these for the body of a device rather than drawing four lines. The two RF shapes — a Sine wave and an Exponential taper, because a coupler and a taper are drawn far more often in this application than in a general drawing tool. Text and pins — the Text tool for labels, the Pin tool for the things that actually connect. A symbol with no pins is a picture. Bitmap — for tracing over a datasheet outline; see the note on locking one below. Snap and rotate — the same snap ladder the schematic uses, and rotation about the origin, which is what keeps a pin on grid. Stroke weight — thin, normal, thick, applied to the selection or to what you draw next. Undo / Redo — on the symbol's own stack. Gestures: Two-point drag — Line, Rect, Rounded Rect, Circle, Ellipse, Arc, Sine, Half-wave. Multi-point click — Polyline, Polygon, Triangle, Quad/Cubic curve (Enter or double-click to commit). Text — click an anchor, type, Enter to commit. Esc — cancels the gesture in progress, or clears the selection when idle. Everything is undoable on the symbol's own undo stack, with full select / move / delete / rotate, an inspector for the selected primitive's coordinates and text, a resize gripper, and copy/paste (including between symbols). Any schematic showing the cell re-renders live as you edit."],[17,"locked","Locked symbols","The standard-library symbols (R, L, C, V, Tone, Term, GND, Port, FET, ZPort, SDD) and the annotation symbols (VAR, MEAS, the analysis directive) are defined but not user-editable . You can place them but not edit them; the editor opens a locked symbol read-only. This is a cell-level flag, so the cell — not the symbol file — owns it."],[17,"autogen","Auto-generated symbols","For a cell with no symbol yet, circuitRF can generate one on command (never automatically): an outer rectangle with a thinner inset, odd-numbered ports down the left edge and even down the right, each with a short lead and a port-number label inside the box; the box grows in height with the port count. Pin tips land on the connection grid by construction. A 3-port symbol is special-cased to the conventional device look — port 1 on the left at center, ports 2 and 3 on the right (matching the FET). The result is an ordinary editable symbol you then refine; re-running generation is an explicit command that regenerates from scratch (discarding manual edits, with a warning). See also: Schematic Editor · Layout Editor · Dynamic symbols (how SDD/ZPort pins grow) · Grid & Connectivity · File formats ( .csym ). Full design: docs/design/symbol-editor.md ."],[18,"","","circuitRF's user-authored nonlinear device. You write each port's current (and charge) as an equation in the port voltages; the engine differentiates it automatically and balances it like any built-in device. It is how the FET models in the examples are defined, and the extension point for any nonlinearity the built-in parts don't cover. SDD at 2 ports"],[18,"what","What an SDD is","Instead of compiled physics, an SDD defines a device's terminal behavior with expressions . Per port, you write the port current (and optionally charge) as a function of the port voltages. The engine evaluates those expressions in the time domain, differentiates them by automatic differentiation for the Jacobian, and solves them in DC, harmonic balance, and S-parameters. The equation notation deliberately mirrors other simulators' SDD/EDD so reference models transcribe directly."],[18,"ports","Ports & nets (2N differential pins)","An N-port SDD binds 2N nets , in +/− pairs: p1+ p1− p2+ p2− … pN+ pN− . The port count is half the net count. The voltage the equations see for port p is the differential _vp = V(p+) − V(p−) . An odd net count — or an equation referencing a port beyond the nets supplied — is a setup error (named, never silently truncated). Terminal names follow the FET convention for 2–4 ports ( g d , g d s , g d s t ). SDD:X1 p1+ p1− I[1,0]=_v1/50 ; a 50 Ω conductance at port 1 SDD:X2 g 0 d 0 I[1,0]=… I[2,0]=… ; a 2-port (FET-style), each port's minus to ground"],[18,"vars","Equation variables","Inside an SDD equation you may reference: Variable Meaning _v1 … _vN The differential port voltages (the device's own ports). _c1 … _cM Control currents — the current flowing in another device, bound via C[n] (see below ). scope variables Any named parameter/variable in scope (model coefficients B , Sc , TV0 , …). freq The frequency global (Hz) — used only in weighting-function H[w] expressions, never in the time-domain current/charge equations. SDD equations are real-only (real time-domain voltages → real current/charge); the imaginary unit j is not allowed in an I[p,w] / Q[p] expression. The full function set and operators are the shared expression language ."],[18,"eqns","The equations: I[p,w] , Q[p]","The core assignment is I[p,w] — the contribution to port p 's current through weighting function w : p — the 1-based port index. w — the weighting-function index: 0 , 1 , or a user-defined w ≥ 2 . Single-index shorthand: I[p] ≡ I[p,0] (a memoryless current); Q[p] ≡ I[p,1] (a charge — the capacitive path). The two-index forms always work too. Each expression is parsed once and evaluated per time sample in dual arithmetic, so the value and its derivatives (the conductance/capacitance the solver needs) come out together. Domain errors (e.g. log / sqrt of a non-positive argument on an overshooting solver iterate) clamp and warn rather than killing the solve."],[18,"weights","Weighting functions H[w]","A weighting function H[w](ω) is a frequency-domain multiplier applied to the spectrum of I[p,w] . The total port current sums over all weights: i_p(t) = Σ_w IFT{ H[w](ω) · FT{ I[p,w]( v(t) ) } } That is: evaluate I[p,w] in the time domain , transform, scale each frequency by H[w](ω) (evaluated in the frequency domain ), sum, transform back. This split is the whole point — it lets a memoryless voltage expression acquire frequency-dependent (reactive, dispersive) behavior."],[18,"the-two-built-in-weights","The two built-in weights","H[0] = 1 (identity) — I[p,0] contributes its spectrum unchanged: a memoryless/conductive current. This is the I[p] path. H[1] = jω (time derivative) — assigning a charge here, I[p,1] = Q(v) , yields the current i = dQ/dt : the capacitive path. (At DC, ω = 0 , so charge passes no DC current — exactly as a capacitor should.) This is the Q[p] path."],[18,"user-defined-weights-hw-w-2","User-defined weights H[w] , w ≥ 2","Higher weights are SDD parameters, declared as expressions of freq ( ω = 2π·freq ): SDD:X1 p1+ p1− I[1,2]=_v1 H[2]=1/(1 + j*2*pi*freq*tau) tau=1n Here I[1,2] = _v1 is scaled in the frequency domain by a single-pole low-pass H[2] — a port current that is the voltage filtered by a first-order RC response. H[w] is shared across all ports; an I[p,w] whose H[w] is undeclared is a setup error naming the missing weight. Example — a nonlinear capacitor as a 1-port SDD For a charge Q(V) , one assignment on the H[1]=jω (charge) path gives i = dQ/dt : SDD:X1 c+ c− I[1,1] = 10e-12*_v1 − 0.75e-12*_v1^2 + (0.1e-12/3)*_v1^3 This is physically identical to the dedicated NonlinearC device (the compiled fast path). See also the C–V Editor ."],[18,"control","Control currents — referencing another device's current","An SDD equation can reference the current flowing in another device and use it like any other variable — the current-controlled complement to the voltage-controlled _vn . This is how you build current mirrors, current feedback, and sensed-current behavior."],[18,"binding-a-control-current","Binding a control current","Two instance parameters declare the reference; the equation then reads it as _cn : C[n] = <instance> ; bind _cn to the current in device <instance> Cport[n] = <port> ; (multi-port referenced devices only) which port's current n is the 1-based control index — C[1] defines _c1 , C[2] defines _c2 , … <instance> is the instance name of a sibling device in the same schematic (no cross-hierarchy paths in this release). Cport[n] is required only for multi-port referenced devices (SnP, ZnP); omit it (or set 1) for two-terminal devices. Every _cn used in an equation must have a matching C[n] , or it is a setup error naming the missing index."],[18,"which-devices-current-can-be-sensed","Which devices' current can be sensed","Device Cport needed? What _cn is DC voltage source ( Vdc ) no source branch current Tone source ( VTone ) no source branch current Current probe ( IProbe ) no the probed series current Inductor ( L ) no inductor branch current Series RLC ( SRLC ) no the branch current Parallel RLC ( PRLC ) no the current in its inductor only, not the whole part Touchstone N-port ( SnP ) yes the selected port's current Impedance N-port ( ZPort ) yes the selected port's current Referencing any other device kind (R, C, a node) is a setup error listing the allowed kinds."],[18,"sign-convention","Sign convention","_cn carries the branch-current sign of the referenced device: current flows from the device's first net to its second . For an IProbe:IP1 a b , _cn > 0 means conventional current flows a → b through the probe. If a mirror comes out inverted, flip the sign in the equation ( -beta*_c1 ) rather than re-wiring."],[18,"examples","Examples","(a) Current mirror / sense-and-scale. A drain current proportional to the current sensed in an inductor: L:Lsense nsrc nx L=1n SDD:Xmirror g 0 d 0 I[1,0] = _v1/1e6 ; high-Z gate (a DC path for the solver) I[2,0] = beta*_c1 ; drain current = beta × the sensed current C[1] = Lsense ; _c1 = current in Lsense beta = 5 (b) Current-feedback transconductor. Sense a bias supply's current and fold it into a gate-controlled drain current: Vdc:Vdd vdd 0 Vdc=5 SDD:Xcc g 0 d 0 I[2,0] = gm*_v1 - kfb*_c1 ; drain current: gm·Vgs minus feedback on supply current C[1] = Vdd ; _c1 = current drawn from the 5 V supply gm = 0.05 kfb = 0.1 (c) Control current on the reactive path. _cn can appear in any I[p,w] , including the H[1]=jω charge path — here a current sensed in port 2 of a Touchstone block drives a displacement-like term: SnP:S1 in 0 out 0 File=coupler.s2p SDD:Xq g 0 d 0 I[2,1] = tau*_c1 ; charge ∝ sensed current → current = d/dt(tau·_c1) via H[1]=jω C[1] = S1 Cport[1] = 2 ; _c1 = current in port 2 of the S-parameter block tau = 1n"],[18,"params","Parameter summary","Name Meaning NumPorts Number of ports N (drives the 2N differential pins). Hidden. I[p,w] Port-p current contribution through weight w. I[p] = I[p,0] (current); Q[p] = I[p,1] (charge). H[w] User weighting function (w ≥ 2), an expression of freq . ( H[0]=1 , H[1]=jω are built in.) C[n] / Cport[n] Bind control current _cn to another device's (port's) current. (scope vars) Any named model coefficients you reference in the equations."],[18,"analyses","Which analyses honor each feature","Every simulation type honors the SDD — DC, S-parameters, Harmonic Balance, Loadpull, and Loadpull Pursuit. Loadpull and Loadpull Pursuit are built on the harmonic-balance engine, so the full I[p,w] / Q[p] / H[w] behavior applies to the DUT at every termination they evaluate. Nonlinear DC — full I[p,w] , charge (drops at DC), and control currents (exact — the referenced current is a solver unknown). Harmonic Balance — full weighting sum, with the control-current Jacobian coupling for fast convergence. Loadpull & Loadpull Pursuit — the SDD is balanced by HB at each termination, so all SDD behavior is honored throughout the sweep/search. S-parameters — small-signal linearization at the DC bias, including the control-current coupling. A nonlinear capacitor reduces to jω·C(0) , matching NonlinearC. Note on control currents specifically: the _cn control-current path is wired for single-tone HB; multi-tone / two-tone control-current coupling is a follow-on. The SDD's own current/charge equations are honored in all analyses regardless. See also: Components › SDD (the symbol + at-a-glance parameters) · Dynamic symbols (how the pin count grows) · Expressions (the language) · Netlist format (a worked GaN-FET SDD). Full design: docs/design/sdd.md + docs/design/sdd-control-current.md ."],[19,"","","A capacitor whose value depends on the voltage across it — a varactor/varicap, or the junction capacitance of a device. circuitRF models C(V) as a polynomial (Taylor) series, and the C–V Editor fits that polynomial to a curve for you."],[19,"model","The Taylor-series C(V) model","Nonlinear capacitor (NonlinearC) The capacitance is a power series in the port voltage V — a Taylor-series expansion about V = 0: C(V) = C0 + C1·V + C2·V² + C3·V³ + … The coefficients C0, C1, C2, … are the model's parameters. C0 is the zero-bias capacitance; the higher terms bend the curve. A polynomial is used because it's smooth and differentiable everywhere, which the harmonic-balance solver needs (it works with the charge, the integral of C(V)·dV, and its derivatives). You can enter the coefficients by hand, or — far easier — fit them to a measured or modeled C–V curve with the C–V Editor."],[19,"params","Coefficients (C0, C1, …)","Name Default Unit Meaning C0 1 pF Constant term — the zero-bias capacitance (shown). C1, C2, C3, … (none) — Higher-order coefficients. Add them by hand, or let the C–V Editor generate them. A NonlinearC with only C0 behaves as an ordinary linear capacitor."],[19,"editor","The C–V Editor","Rather than guessing polynomial coefficients, open the C–V Editor (from the NonlinearC's parameter editor — Edit C–V… ) and give it the curve you want to model: Enter the C–V table — pairs of (voltage, capacitance) points describing the device's behavior across the bias range of interest. Choose a fit order — the highest polynomial degree to use (higher order follows a wigglier curve but can overfit; pick the lowest order that tracks the data well). Review the preview — the editor overlays the fitted polynomial on your points so you can see the quality of the fit. The C-V Editor: a measured C(V) table, the fit order, and the polynomial it fits."],[19,"apply","The Apply step (important)","Press Apply to generate the coefficients Editing the C–V table or the fit order does not change the component on its own. You must press Apply — that's the step that fits the polynomial and writes the C0, C1, C2, … coefficients back onto the component . Until you Apply, the component still carries its previous coefficients. So the workflow is always: edit the table / order → check the preview → Apply . The Apply is a single undoable change. After Apply, the generated coefficients are ordinary parameters on the NonlinearC — visible and editable in the parameter editor, saved with the schematic, and used by the simulator. Re-open the C–V Editor any time to refit. See also: Components › NonlinearC · SDD (equation-defined nonlinear devices) · Harmonic Balance ."],[20,"","","Running a compact model you supply — a published physics-based transistor model, or one you wrote — from Verilog-A source or from a compiled artefact, with no kit and nothing to install inside circuitRF."],[20,"what","What this component is for","VerilogA The VerilogA component runs a compact model you supply. It needs no kit, no manifest and nothing installed into circuitRF — you point it at a file and place it. That is the path to take when the device you need is a published physics-based compact model. Those are distributed as Verilog-A source: a few thousand lines of analog equations, a manual, and one or more fitted parameter sets. What this page buys you is the whole of that route — source in, a placed five-terminal device out, running DC, S-parameters and harmonic balance like any other component. Two file types, one component The File parameter takes either Verilog-A source ( .va , .vams ) or an already-compiled model ( .osdi ). Source is compiled once, by the compiler on your machine, and reused. If you already have a compiled artefact, point at it and no compiler is involved at all."],[20,"compiler","The compiler, and why it is yours","circuitRF does not contain a Verilog-A compiler and never will. The established compilers for this model format are GPL-3.0; circuitRF is MIT, and bundling or linking one would change that. So circuitRF does what a build system does with a C compiler: it runs a compiler you installed , as a separate program, and loads the file that comes out. Nothing is linked and nothing is redistributed. Install one of your choosing , then either put it on PATH — in which case there is nothing to configure — or name it explicitly: Settings ▸ Security & Permissions ▸ Verilog-A Compiler Leave the box blank to use whatever is on PATH . Fill it in when you have more than one, or when yours lives somewhere PATH does not reach. A compiler you name wins over PATH — that is the whole point of naming one. Press Test and circuitRF will run it and report what it says it is, which is the fastest way to confirm the setting before you need it. For a headless run — a script, CI, a batch job — set CRF_VERILOGA_COMPILER to the compiler's path. It outranks PATH and leaves nobody's settings file touched. If no compiler can be found, circuitRF says so at the moment you choose the file, names what to install and where to point it, and changes nothing else. The .osdi route keeps working."],[20,"walkthrough","A worked example, end to end","This is the whole path for a model you have just downloaded. It assumes only that you have the model's source and one of its parameter sets — which is what a model family actually ships. Put the source where you want it. Keep the family's own folder layout intact: models of this size `include parameter and macro files beside the source, and circuitRF passes the source's own directory to the compiler as an include path so those resolve exactly as they do when you build by hand. Place a VerilogA component. It is in the palette under Devices — search for \"Verilog-A\", \"OSDI\" or \"compact model\". Set File . Click Browse… beside the File row and choose the .va . circuitRF compiles it — once, taking a few seconds for a model of this size — and then reads it. The parameter editor tells you underneath which compiler ran, where the artefact was written, and whether anything was actually rebuilt ; on every later visit it will say the source has not changed. If the compiler refuses, you get its own diagnostics, verbatim : file, line and column, exactly as it printed them. That is deliberate — the line number is the whole value of a compiler error, and a paraphrase would be worse than the error. Check Model . A file usually declares one module and circuitRF fills this in for you. When it declares several, the row becomes a picker — choose the one you want. Check Pins . circuitRF fills this in from the model's own terminal count and then locks it, because the model has stated it. See below for the one case where you deliberately set it lower. Load the parameter set — Load Parameters… at the foot of the parameter editor. See below . Wire it and run. From here it is an ordinary nonlinear component. Attach a DC analysis to sweep a bias point, S-parameters to get small-signal behaviour about that point, and harmonic balance for large-signal power, gain and efficiency. Everything in Simulations applies unchanged, and so does harmonicaRF — a compiled model is a valid DUT there too. Start at DC Run a DC sweep before anything else. A compact model that is mis-parameterised, or whose thermal terminal is wired the wrong way, shows it as a bias point that will not converge — and that is far easier to read at DC than in the middle of a harmonic-balance sweep."],[20,"terminals","Terminals, Pins , and the thermal one","The symbol is a plain box , deliberately. circuitRF does not know what your model is — it could be a transistor, a diode or a whole subcircuit — so drawing a transistor glyph would assert something the file has not said. The leads are named by the model . Once circuitRF has read the file, each lead is labelled with the model's own name for that terminal — d , g , s , b , dt — rather than 1..5 . On a five-terminal part with five identical leads, numbers are the single largest source of mis-wiring, and the model has already told circuitRF which is which. A lead the model does not name falls back to its number, on its own; you may see a mix."],[20,"the-thermal-terminal","The thermal terminal","Many physics-based models expose a thermal terminal : a node whose \"voltage\" is a temperature and whose \"current\" is a power. You have two honest choices, and circuitRF supports both. What you draw What the model does When to use it Pins = the model's full count, thermal terminal wired Reads the temperature you impose on that node You are building the thermal network yourself — a resistance and capacitance from that node to an ambient source that sets the baseplate temperature. Pins = one less , thermal terminal omitted Sees that the terminal is unconnected and grounds it internally The ordinary case. The model handles its own self-heating and you draw a four-pin part. circuitRF tells the model how many terminals you connected; that is what the model's own $port_connected reads. When you set Pins one below the model's count and the omitted terminal is thermal, the parameter editor says so — that a thermal terminal is deliberately left off and the model is handling its own self-heating. It is not a warning. It is there because a symbol with one lead fewer than the model declares otherwise reads as a mistake, and \"fixing\" it is the one thing you must not do: Never leave a thermal terminal drawn but unwired A thermal node with a pin on it and no thermal network attached is a floating node with no DC solution . It does not fail quickly or clearly — it spends the whole continuation budget and then reports a residual. Either omit the pin (and let the model ground it) or attach a real thermal network. There is no third option."],[20,"parameters","Loading a fitted parameter set","A fitted parameter set for a model of this size is 50 to 200 numbers. Both of the ways to get them in are at the foot of the parameter editor: Add Parameter… — a searchable list of everything the model declares, with the model's own units, descriptions and defaults. Adds one, seeded at the model's own default. Use it to change a handful of values. Load Parameters… — reads a whole fitted set from a file. Use it for anything larger. Load Parameters… reads sets written as Verilog-A parameter declarations , which is the form these families actually ship: parameter real vxo = 1.3e7; // saturation velocity parameter real beta = 1.8; parameter integer nf = 4; Comments are ignored (including one containing a semicolon), from […] and exclude ranges are ignored, and both 1.3e7 and engineering notation like 2.4p are read as written. The whole load is one undo step . Two behaviours worth knowing: Case is aligned to the model's own spelling. A set written vxo reaches a model that declares VXO . The match is case-insensitive only — a genuine typo is not quietly turned into something the model accepts; it is reported by name. Names the model does not declare are reported, never dropped in silence. circuitRF tells you exactly which ones did not land. This is the common case, not the exotic one: a set written for a different version of the same family will have names that have since been renamed or removed, and a silent drop would leave the device running on the model's own defaults for all of them and looking perfectly healthy. Absent means \"the model's default\" circuitRF only writes the parameters your set actually assigns. A parameter that is not on the component is not sent to the model at all, which already means use your own default . That is why the whole declared list is never materialised as rows: freezing today's defaults into the design would mean recompiling with a changed default silently had no effect."],[20,"opvars","Reading back what the model computed","A compact model computes far more than the currents it hands the solver. Transconductances, junction capacitances, an overdrive voltage, a junction temperature — a physics-based model declares tens of them, and they are the numbers a designer actually reasons about. Verilog-A calls them operating point variables ; circuitRF reads them back after every solve and publishes them as a result cube named OP . They are never parameters. A parameter is something you tell the model; an operating-point variable is something it tells you, and it does not appear in the parameter editor for the same reason a voltmeter has no dial."],[20,"what-you-get-and-where","What you get, and where","At a DC operating point each one is a number, on a labelled axis whose entries are <instance>.<name> — X1.gm , X1.cgs , X1.tj . Wrapped in a parametric sweep it becomes a curve per quantity, one point per bias, in the Data Display's signal list beside V and I . At a harmonic-balance point each one is a waveform , because that is what it is: a transconductance at 30 dBm of drive swings between pinch-off and full channel once per RF cycle, and no single number describes it. So OP there is a spectrum on the same harmonic axis V and I use — harmonic 0 is the cycle average, which is what \"gm at this drive\" usually means, and the higher entries say how hard the quantity is being swung. In a measurement, name one through the analysis it came from: measure gm_mS = DC1.OP(\"X1.gm\") * 1000 measure gm_avg = HB1.OP(\"X1.gm\", 0) A name the model does not declare is refused, by name, with the available ones listed — it is never quietly dropped. Turning it off per device The parameter editor's Report operating-point variables checkbox is on by default. A model declaring forty quantities, on a design holding a dozen such devices, swept over a few hundred bias points, is a result carrying thousands of names — so turn it off on the devices you are not studying. Off costs nothing at all: the device is not asked, so neither the round trip nor the cube happens. Two kinds of quantity are declared and not plotted. A model may declare an op-var as an integer or as a string. An integer arrives as an ordinary number. A string cannot: a result cube holds numbers of one kind, and there is nowhere in one to put a word. Such a quantity is still reported as declared, so you can see that it exists; it simply never appears in OP ."],[20,"rebuild","When circuitRF rebuilds, and where the artefact goes","A simulation of an unedited model compiles nothing. The compiled artefact is cached, and the cache key is: the content of the source — not its path and not its timestamp, so re-saving a file without changing a character costs nothing; the content of every file it `include s , so editing a parameter or macro file beside the source does rebuild — hashing only the top file would hand you a stale artefact that runs and looks correct; the compiler's own identity , so upgrading your compiler rebuilds even though your source has not changed by a byte. Change any of those and you pay exactly one recompile. Change none of them and the compiler is not asked to build anything, however many times you run. Artefacts are written to circuitRF's own per-user cache — not beside your source. A model family is often installed as a read-only tree, and writing build output into someone else's delivery is wrong even where it would succeed. The parameter editor names the exact path each time it reads the file, so a rebuild you were not expecting is visible rather than mysterious."],[20,"limits","What is not supported","No noise analysis from a compiled model. Aging and degradation parameters are forwarded to the model like any other parameter and then do nothing — there is no aging analysis and no stress history to drive one. Verilog-AMS digital constructs are the compiler's business, not circuitRF's. If your compiler builds the file, circuitRF will run it. Net Terminal 1 1 2 2 The nets of an instance line, in this order. How many there are is set by Pins ; the table shows Pins = 2 . Name Default Unit On schematic Meaning File — — shown The model to load: a compiled model (.osdi), or Verilog-A source (.va, .vams) which circuitRF builds once with the compiler installed on this machine and reuses until the source changes. Choosing one fills in Model and Pins below. Model — — — Which device type inside that file to place. A file usually declares one, and then this can be left blank; when it declares several, pick the one you want. Pins 2 — — How many terminals the symbol draws. It is the model's own terminal count, filled in from the file — change it only if you are drawing before choosing one. OpVars true — — Whether this instance publishes the operating-point variables its model computes — transconductances, capacitances, node temperatures. On by default; turn it off on devices you are not studying to keep a swept result small."],[21,"","","Direct synthesis of a bandpass matching network that absorbs both terminations."],[21,"orientation","If you have used a single-frequency matching tool","Read this first if your instinct is \"give me a target impedance, 50 Ω, and a part count.\" Match does exactly that job — it just does it over a band , and that changes what comes out in three ways that surprise people. The one sentence A network that matches over a band is a filter — that is a theorem, not a design choice — so Match asks for a band and an order the way a filter designer does. It is still a matching tool. You are not being asked to design a filter, and there is no filter specification hiding underneath: order and ripple are how you say how wide and how well . What Match is not. It is not an L / π / T tuner that transforms one impedance at one frequency, and it is not a Smith-chart tool that walks you round constant-R and constant-G circles element by element. Those give you the smallest network that is exact at a point and falls apart either side of it. Match gives you the widest band the load's Q allows, which is a different network with a different part count. If a single frequency is genuinely all you need, place the L and C yourself — Match is the tool for the case where a point match is not enough. Three things that follow, and that the rest of this page assumes you have read: The part count is higher than a two-element L-match, and some of the parts are already on your board. The smallest network Match offers is order 2, which is four elements in every form (see Bandpass, lowpass and highpass ); an L-match is two. But the ones the terminations supply are not parts you buy — a transistor's C gs or C ds is absorbed and becomes an element of the ladder, drawn at the end that supplies it. Read the count off the ladder, not off the order. See Absorption for why that is the whole point of the component. Elements to ground are normal, and they are half of every ladder. A matching ladder alternates series arms and shunt arms; the shunt arms go to ground because that is what a shunt arm is. Seeing capacitors and inductors between a node and ground is the network working, not a bug. Shunt DC Block does not put a capacitor in the through path. If you came here expecting the series blocking capacitor a legacy tool drops into the signal line, see Shunt DC Block — the button solves a different problem, and Match never inserts a through-path block. This is the single most-misread control in the Designer."],[21,"what","What Match is","Match is a two-port component that synthesises a bandpass LC matching network matching both of its ports simultaneously to the network around it. Place it, tell it what is on each side and over what band, and it produces a ladder — element by element, with values. It is direct synthesis : closed-form, procedural, no optimiser. There is no seed, no convergence, no \"run it again and see if it finds something better\". The same specification always produces the same network, and when a specification has no solution at the order you asked for, you are told so with numbers rather than left watching an optimiser fail to converge. The component stamps the ladder element by element on internal nodes — not as a lumped ABCD block — so it behaves correctly at DC and in harmonic balance, and it is identical by construction to the cell that Flatten to Cell writes."],[21,"the-symbol-tells-you-what-it-is","The symbol tells you what it is","A Match puts no parameter text on the schematic — no F1 , no F2 , no Order . Everything about it is edited in the Match Designer, and everything about it is read there or in the Properties panel, so three numbers beside the symbol were three numbers that could not be acted on where they stood. The glyph says the part you need at a glance. Its three stacked waves are a frequency axis, highest at the top, and a slash means that band is blocked: The symbol shows The network is slashes top and bottom bandpass — the middle band passes slashes on the top two waves lowpass slashes on the bottom two waves highpass two smaller bandpass glyphs, side by side dual-band three smaller bandpass glyphs, two below one tri-band The five Match glyphs. A slash across a wave means that part of the spectrum is blocked; two or three smaller bandpass groups mean two or three bands. It follows the design: apply a lowpass solution and the symbol on the page becomes a lowpass symbol, and switching Bands to Dual or Tri splits the wave stack into two or three smaller ones. The glyph is drawn from the component's own Form and Bands parameters, so a schematic printed for a review states the topology of every matching network on it without anyone opening a Designer."],[21,"absorption","Absorption, and why it is the whole point","Most matching networks tune out a termination's reactance: the transistor has 10 pF across its input, so you resonate it away and match what is left. That works, over a narrow band, because the resonance only cancels at one frequency. Match absorbs each termination's reactance into the network instead. A transistor's C gs or C ds becomes an element of the matching filter — one of the ladder's own capacitors, in the position the filter wanted a capacitor anyway. Why this is not a trick Fano's bound says how much bandwidth you can get matching a reactive load, and it depends on the load's Q. A network that tunes the reactance out and then matches a resistance gives away most of that bound. A network that treats the reactance as part of the filter gets the widest bandwidth the load's Q permits — which is the best any network can do. \"Efficient broadband impedance-matching structures are necessarily filter structures.\" — Matthaei, Young & Jones Two consequences worth being clear about: The absorbed elements are not inside the component. The external network supplies them — that is what makes the match real. The Designer always draws them immediately beside the termination that supplies them — that position, and the one-line legend under the schematic, are how you tell which elements you do not have to buy. They are drawn at full brightness like everything else: the part you are matching against is the last thing that should be hard to read. Q is the entire content of a termination , as far as the synthesis is concerned. At band centre ω₀ = √(ω₁ω₂) with fractional bandwidth w : a parallel R‖C has Q = ω₀RC , a series R+C has Q = 1/(ω₀RC) . R re-appears later only as an impedance scale and as the Norton-transform target. The higher-Q end drives the synthesis , because it is the binding constraint — the one Fano's bound limits. The far end falls out of the synthesis and is then reconciled against its real termination. Order parity is not free. An end absorbing a series reactance needs a series arm there; an end absorbing a shunt reactance needs a shunt arm; and arms alternate. So one-series-one-shunt forces an even order and like-kind forces an odd one. The order picker offers only the parities that can work — {2, 4, 6} or {3, 5}, or all of 2…6 when an end is purely resistive — and changing a topology adjusts the order rather than presenting an impossible one, saying so when it does."],[21,"designer","The Match Designer","Double-click a placed Match to open the Designer. (The properties panel for a selected Match shows a compact summary and an Open Match Designer… button; the design blob is never rendered as a text row.) The Match Designer on the design a freshly placed Match carries: the specification pane, the solutions list under it, the synthesised ladder with its value grid, and the transform rack. That is a freshly placed component: 50 Ω to 10 Ω over 1.8–2.2 GHz, order 4 — a real 5:1 transformation, arriving with a solution already applied so there is something to look at rather than an identity. Four regions, left to right and top to bottom: Specification — the two terminations, the band, the order, the response shape and the options. Network — the synthesised ladder as a real circuitRF schematic , with the value grid in a scroll view underneath it. The schematic draws each element with its instance name and value, out-of-range values in red, a grounded termination at each end, and a brace under the elements each transform produced. Right-click either view for Copy , which puts the drawing on the clipboard as something a schematic page can paste and PowerPoint can paste as vector art; the grid's menu also offers Copy as CSV . Response — two rectangular plots, drawn by the Data Display's own plot control from an S-parameter run of the full design (ladder plus both terminations, port references R1 and R2). |S11| and |S21| on the first — S21 against the right-hand axis , because the two live decades apart and one shared scale renders the insertion loss as a flat line on the ceiling; phase and group delay on the second. Markers, their info boxes and the plot's own Copy work exactly as they do in a Data Display. The plot band defaults to the design band ±10%. Transforms — the linked Norton slider rack, and below everything the status strip : Q1, Q2, worst in-band return loss, insertion loss, ripple, and the achieved-versus-required Π N². There is no \"nearest standard value\" column, deliberately What counts as a realizable value is your call and depends on the flow: in an MMIC flow a capacitor is designed to its value and an E24 series is meaningless, and on a board the available series depends on the vendor and the package. The grid shows the synthesised value and the sliders move it; deciding what is buildable stays with the person who knows the process. Refusals appear in the status strip, with numbers. \"No real root at this order\", \"the far end is not absorbable\", \"the transforms cannot reach the target\" — each names the quantity that failed, and the affected termination turns red."],[21,"spec","The specification pane","Each termination is a resistance in series or in parallel with one reactive element — C, L, or none. The little RC pictogram shows which topology you have picked at a glance. Every numeric field is an ordinary circuitRF value-and-unit pair, so unit entry works exactly as it does elsewhere. Control What it does Topology Series or parallel. This is a physical statement about the network you are matching, and it changes which order parities are available. R , X kind , Value The termination. X = – means purely resistive. Shunt DC Block Not a series blocking capacitor in the through path — Match never inserts one of those. It puts a DC-blocking capacitor in series with every shunt inductor that would otherwise short this end's bias to ground, and enlarges each inductor to compensate — see Shunt DC Block . Available whenever such an inductor exists; greyed out, with the reason, otherwise. Conjugate Targets Z* instead of Z — which flips the reactance sign, and so turns a measured parallel R‖C into a parallel R‖L target. Bands Single, Dual or Tri. Dual and Tri match two or three bands at once — see Multiband . Band f1, f2 The passband. Everything is computed at ω₀ = √(ω₁ω₂) with fractional bandwidth w . Band f3, f4 The second band, when Bands is Dual — or the middle band, when it is Tri. Band f5, f6 The third band, when Bands is Tri. Order Number of in-band match points, 2–6, restricted to the parities the terminations permit in bandpass form. Multiband counts match points per band and offers 1–3. The element count depends on your two terminations: 2n (or 4n multiband) for a mixed or resistive pair, and one arm more — 2n + 1 , or 4n + 2 — when both ends are the same topology. The tooltip beside the picker states which. Response Chebyshev — single-match (optimum), Chebyshev — double-match (exact), Butterworth, or Bessel where feasible. Form Bandpass, lowpass or highpass. Not a control — you choose it by applying a solution, and every solution card names its form. Q-adjust Offers solutions synthesised at a raised Q, which trades a little bandwidth for element values that may be easier to build. Allow negative components Widens the Norton slider ranges past the positivity threshold. Off by default, and it is off for a reason — see below. Conjugate is the right target for a small-signal stage and usually the wrong one for a PA output A power amplifier's load should come from load-pull (R opt ), not from the device's own output impedance. Conjugate-matching a PA output gives you maximum small-signal gain and the wrong large-signal load."],[21,"probe","Probe: reading the terminations off your own circuit","Each termination carries a Probe button that looks outward from that pin into the circuit the Match is placed in, and fills in R, topology, reactance kind and value for you. What it does, so you know what you are getting: It extracts the enclosing test bench, deletes the Match instance so the probe cannot measure itself, and attaches a 50 Ω Term to the net that pin was on. It keeps every DC source and bias network — the interesting case is a transistor, and a transistor's small-signal impedance is only meaningful at its operating point. If the DC solve fails, the probe refuses and reports the DC failure rather than returning an impedance computed at zero bias. It runs an S-parameter sweep over the design band and converts to Z. It fits all four two-element models — series R+C, series R+L, parallel R‖C, parallel R‖L — each as a linear least-squares fit in its natural domain, then scores each by mean |ΔΓ| over the band. That single bounded metric ranks all four on equal terms. All four fits are shown with their residuals in Γ units , so you can take the second-best when you know better. The best physical fit is applied. If even the best residual is poor (mean |ΔΓ| above 0.05 by default) the result is still applied but flagged : the external network is not well described by a two-element model over this band — which is the honest answer for a network with an in-band resonance, and points you at narrowing the band. A probed termination is a snapshot, not a live link. It records where it came from and shows a badge; editing the value by hand clears the badge to manual, and your override always wins. Changing the surrounding circuit does not silently re-synthesise the network — re-probing is always an explicit action. The button is greyed out, with a reason, when the pin is unconnected, when its net has nothing else on it, when the schematic has unresolved errors, or when the Match is inside a cell rather than in a test bench — there is no external network to look at from inside a definition."],[21,"dcblock","Shunt DC Block: why the capacitor lands in a shunt arm","This is not the series DC block you are thinking of In most matching tools \"DC block\" means a capacitor in the through path , in series with the signal, put there so the stage either side can sit at its own bias. Match never inserts one of those — if you want one, place it in the schematic outside the Match component, where it belongs. What this button does is the opposite direction: it protects the bias from the network , by putting a capacitor in series with each shunt inductor — the arms that run to ground. So the new capacitors appear in the shunt arms , stacked under the inductor they block. That is the feature, not a mis-synthesis. A shunt inductor at a biased node is a short across the supply. If the end of your ladder is a shunt inductor and the termination behind it carries DC — a drain, a gate — the network as synthesised puts your supply on ground. The Shunt DC Block toggle on that termination's card fixes it, and fixes the thing people normally get wrong afterwards. Click it and two things happen: A capacitor goes in series with that inductor. It appears in the network pane as an element of its own, named after the inductor it blocks — L1 blocks with L1blk — and it is written into the flattened cell as a real component. The inductor is enlarged so the branch is unchanged at band centre. The branch's reactance is now ωL′ − 1/ωC instead of ωL , so L′ = L + 1/(ω₀²C) makes it exactly what the synthesis asked for at ω₀. That is the number the ladder shows: L1 reads its compensated value, and the status line says what it was before. The compensation is exact at band centre and second-order away from it . The branch's own series resonance sits below the band at f_s = 1/(2π√(L′C)) , and between the two band edges the effective inductance runs a little either side of what the synthesis wanted. The status line states all of it: DC block at termination 1: 1 nF in series with L1 (105.9 pH, from 99.5); branch resonates at 489.1 MHz; inductance ±1.3 % across the band. Feed the bias through L1, not through a separate choke. Bigger is better, and the default is chosen for you. The seed puts f_s at about one tenth of band centre, which keeps the spread under a percent — and it is capped, because at a low band with a small end inductor that rule alone asks for tens of nanofarads: fine on a board, impossible on an MMIC. The cap is in Settings ▸ DC block default . Past that, the value is yours: type anything positive into L1blk and it is compensated exactly at ω₀ whatever it is. Above f_s > f₀/5 the line turns to a warning and tells you what a ten-times-larger part would buy — measured on a 20 % band, a 500 pF block costs about 3 dB of worst-case return loss where 10 nF costs none. Typing 0 removes the block. Feed the bias through the compensated inductor, not through a separate choke This is the half that no amount of RF design catches. Put the block in the branch and then feed the drain through a separate choke, and the block resonates against that choke through the drain node — a parallel pole in the middle of the baseband, tens of kilohms at a few megahertz, and no lossless network can remove it. Feed the supply through the compensated inductor instead, with the block as its far-end decoupling, and the residual poles are between your decoupling capacitors, where a small series resistance on a capacitor carrying no RF damps them. Check it the ordinary way: an S-parameter or AC sweep of Z at the drain node in the schematic. The block goes on the first shunt inductor your bias current would reach. A series inductor in the way doesn't stop it, a real series capacitor does, and your FET's own input capacitance is not a real capacitor on the board. So a termination whose end arm is a series arm — a gate modelled as R in series with C_gs — still gets a block: the arm's capacitor is the device's own and is left out of the flattened cell, the arm's inductor passes DC, and the ladder's next shunt inductor is what would short the gate bias."],[21,"transforms","Norton transforms: moving values without moving the response","This is the part of the window that makes a synthesised network buildable. A Norton transform replaces an L-section of two like-kind elements with a π or T of three like-kind elements plus an ideal transformer of ratio N , then absorbs the transformer by scaling the rest of the network (impedances by N², so L·N² and C/N²). The property that matters The transfer function is unchanged. Only the element values and the terminating resistance move. That is how a 2.1 pH inductor becomes something a PCB can actually build — without giving back any of the match you just synthesised. The rack has one row per applied transform: a π/T selector, a numeric box, a slider, and a lock. Which two elements a transform acts on is read off the schematic above it , where a brace spans exactly the elements that transform produced. + add lists the pairs currently available by element name; − remove removes the last. The slider ends where the maths does. Past a positivity threshold the transform produces negative elements, so the range is bounded at N = 1 + Z₁/Z₂ when stepping up and N = Z₂/(Z₁+Z₂) when stepping down. Turning on Allow negative components widens it; the values then go negative, which is occasionally what a designer wants and usually not. Which pairs are transformable is structural : two like-kind elements at most three positions apart, of opposite orientation, with matching intervening elements — and never an end element carrying an absorbed termination reactance , because moving it would break the absorption. Link (on by default) keeps the product on target. The transforms must satisfy Π N² = R far,target / R far,synthesised . Drag one slider and the unlocked others re-solve so the product stays on target, each clamped to its own range. Locked rows are never touched. With a single transform and link on, N is fully determined and its slider is disabled. When the product cannot reach the target within the ranges, the offending termination goes red and the design is flagged as matched at one end only. A transform on an inductor pair can make the network unsolvable at DC A Norton transform produces three elements of the same kind as the pair it replaced. Three ideal inductors in a π are, at DC, a loop of ideal shorts — which is a singular system, so the network will sweep S-parameters happily and refuse to DC-solve at all. A capacitive transform puts a series capacitor in the middle branch, which is a DC open, and stays solvable. Both are legitimate; if your design has to DC-solve, prefer the capacitive solution from the list."],[21,"solutions","The solutions list","The solutions list, slid out: every valid transform set, simplest first. Solutions ▸ slides out a docked list: every valid transform set for this specification, so you can click through candidates and watch the ladder and the response change live. Each row shows a badge (current / previously applied / never applied), the transform count, the element pairs each transform acts on, the Q-adjust value when non-zero, and the response type. Ordering is by transform count, then by position, then by Q-adjust — the simplest realizable solution first. Within one form, order and family the response is the same for every row; what differs is the element values you would have to build."],[21,"forms","Bandpass, lowpass and highpass","The list covers three network forms , and the filter's first group turns each on and off. A bandpass network is two-element arms resonant at band centre. A lowpass network is series inductors in the through path and shunt capacitors to ground; a highpass network is the other way round. All three are matched between f1 and f2, and all three use 2n elements at order n (2n + 1 when both terminations are the same topology) — the lowpass form is not the cheaper one. What the lowpass and highpass forms buy is tame element values at wide bandwidth — there are no resonators, so the L's and C's stay within a factor of a few of each other instead of spreading over decades — plus a DC path (lowpass) or a DC block (highpass), which is what a bias network wants. What they cost is return loss that depends on the impedance ratio : the ladder is transparent at DC, so |Γ(0)| = (r−1)/(r+1) is fixed by the two resistances and comes out of the same budget as the in-band match. At a 2:1 band, four elements, that is −22.2 dB into a 2:1 ratio and −10.5 dB into a 10:1 one, against a bandpass order-2 network's −16.4 dB at any ratio. Which is better depends on your numbers, so the panel lists all three and you read the return loss off the cards. Two consequences worth knowing before you go looking for them: A lowpass or highpass network has no Norton transforms. Every like-kind element in it sits in the same orientation, so there is no pair to transform; the ratio is already on target and the transform rack says so instead of showing an empty list. Each form absorbs half of the termination kinds, and the impedance ratio decides which end takes which. A lowpass ladder puts its series inductor against the lower -impedance port and its shunt capacitor against the higher -impedance one. So a shunt capacitance on the high side of a step-down absorbs; the same capacitance on the low side of a step-up does not, and the refusal says so and points you at bandpass form. Two terminations of the same topology are fine here too, at one element more. The ladder then has an odd element count, both of its ends face the same way, and both are absorbed. Its two ends flip together, so a parallel pair has to be analysed from the higher resistance and a series pair from the lower; if you have it the other way round the refusal says which end to analyse from. Odd counts are Chebyshev only. The math behind the three forms All three forms come out of one two-parameter prototype family , evaluated in the squared frequency variable u = Ω² with the band mapped onto [-1, 1] . With a = F1/F2 : x(u) = (2u − 1 − a²) / (1 − a²) the band [a², 1] mapped onto [−1, 1] Φ(u) = T_n(x(u))² Chebyshev T_n = the nth Chebyshev polynomial Φ(u) = x(u)^(2n) Butterworth |Γ(u)|² = (K + ε²·Φ(u)) / (1 + ε²·Φ(u)) Φ has maximum 1 in band , so the worst in-band reflection is |Γ|²_worst = (K + ε²)/(1 + ε²) — which is why the two free parameters are exactly K and ε² , and why the panel can quote a return loss before it has drawn an element. K is not free: it is pinned at DC, and that is the whole cost of the lowpass and highpass forms. A ladder of single elements is transparent at u = 0 — a lowpass ladder's inductors are shorts and its capacitors are opens — so the network's reflection there is whatever the two resistances make it: Γ(0) = (r − 1)/(r + 1) r = R_far / R_analysis K = Γ(0)² That number comes out of the same budget as the in-band match, which is the whole reason a large impedance ratio costs return loss in these forms and costs nothing in bandpass form — the figures quoted above are this expression. ( K = 0 exactly is a trap rather than the ideal case — the numerator's roots then sit in double pairs on the jω axis and there is no well-defined spectral factor — so circuitRF floors it at 1e-12, a −120 dB ceiling that is past anything a matching network means. The equal- resistance case r = 1 drops the pin entirely and makes ε the free parameter instead.) With K and ε² chosen, the reflection's"],[21,"dualband","Multiband: two or three bands at once, and the gaps left alone","Set Bands to Dual and a second pair of edges appears. The network is then matched over f1–f2 and f3–f4 together , and the region between them is deliberately not matched. Tri adds a third pair, f5–f6, and two such gaps. That is the whole idea rather than a compromise. The Fano bound is a fixed budget spread over all frequency; a single wide match from f1 to f4 spends it across the whole span, gap included, and everything spent between f2 and f3 is wasted if your application does not use those frequencies. A dual-band network spends the budget in the two bands and leaves the gap reflecting. For 20 Ω ‖ 2.5 pF into 50 Ω over 2.4 GHz and 5 GHz Wi-Fi, eight elements reach −31.8 dB in both bands , against −18.8 dB for a single-band eight-element network covering the whole span. The gap mismatch is the design working, so the status strip states it. Beside the worst in-band return loss you get a line like gap 2.5–5.15 GHz: max |S11| 0.445 (−7.0 dB) , and that number rises with order — a higher-order network is bigger in the gap, and that is exactly where the extra in-band return loss comes from. Three things to know before you type four frequencies: Type the edges in any order. f1…f6 are one ordered list of passband boundaries, so if you put a number in the wrong field the Designer sorts them back into increasing order and renumbers the fields in front of you. What it cannot fix is two edges that are equal — a zero-width band — and that stays a refusal. The bands must be geometric mirrors of each other — f1·f4 = f2·f3 , which is the same as equal ratio bandwidths f2/f1 = f4/f3 . Yours will not be. circuitRF keeps the wider band exactly, widens the narrower one away from the gap until the ratios match, and says so in one line under the fields: \"Band 1 widened to 2.201–2.5 GHz to mirror band 2 about 3.588 GHz.\" The design is always to the effective bands, never silently to the ones you typed. Where the two ratio bandwidths differ a lot the stretch is severe and this is the wrong tool — a 0.9–1.0 GHz band paired with 3–6 GHz would be designed as 0.5–1.0 GHz, which is not what you asked for. Order is match points per band , so orders 1, 2 and 3 give 4, 8 and 12 elements for a mixed termination pair — the same twelve a single-band order-6 network gives, which is the fair comparison. Two bands stop there. Three bands go on to orders 4, 5 and 6 — 16, 20 and 24 elements — for the reason the Feasibility section below explains: a tri-band spec with a narrow middle band is not three bands at all until order 4. Higher is not automatically better; the solutions list spans every order and ranks by return loss, so pick by the number on the card rather than by the order. Two terminations of the same topology get one arm more , not a refusal. A shunt-C-to-shunt-C interstage needs a ladder whose two ends face the same way, which is an odd number of arms, so those orders give 6, 10 and 14 elements instead. You do not choose this — your terminations do, and the order picker's tooltip says which count you are getting. The extra arm buys the absorption, not return loss: at the same order the odd ladder measures within 0.01 dB of the even one. Everything else is unchanged. The terminations are read at ω₀ — which is now the gap centre, where every arm of the ladder is transparent — the ladder is an ordinary bandpass ladder with twice as many arms, so Norton transforms, absorption, the excess-element rule, the solutions list and Flatten all work exactly as they do for a single band. Three bands Tri works the same way with one difference in the rule above: the middle band is the one that is kept , because a three-band response has to be symmetric about its own centre and only the middle band can straddle it. Bands 1 and 3 are widened onto each other's mirror image about ω₀ = √(f3·f4), and the note names every band that moved. Switching from Dual to Tri therefore moves your existing second band out to f5–f6 and seeds a new middle band between them, rather than"],[21,"feasibility","Feasibility: what is possible before you synthesise anything","A lossless network cannot match a reactive termination arbitrarily well over an arbitrary bandwidth. There is a hard ceiling, it depends only on the terminations and the bands, and the Designer shows it beside every result and before any of them — the status strip's line reads Fano ceiling 6.4 dB (termination 2, over the bands) quoted positive like the return loss above it, and naming which of the two ends is the one setting it. Hover it for both ends, the ceiling over the bands as you typed them, the ceiling over the whole span from your lowest edge to your highest, and — for a multiband spec — how much of the ceiling the mirror widening cost. The line is there even when the synthesis refuses , which is when it usually matters most: a refusal and a ceiling of −3 dB are the same fact, and only one of them tells you what to change. When the line ends \"— at the ceiling\" , the design is within a dB of what physics allows and no amount of searching will improve it. Anything else is headroom."],[21,"which-end-limits-you-and-what-makes-it-worse","Which end limits you, and what makes it worse","Two of the four terminations are limited by total bandwidth , and two by your lowest band edge : your termination what costs you R ‖ C (shunt capacitance) total bandwidth — the sum of all your band widths R + L (series inductance) total bandwidth R + C (series capacitance) the lowest frequency you ask for R ‖ L (shunt inductance) the lowest frequency you ask for That difference matters. With a series capacitance, moving your lowest band edge up by a few hundred megahertz can be worth more than everything else put together; with a shunt capacitance it is the total width that counts and where the bands sit hardly matters at all."],[21,"the-hints","The hints","When the ceiling is genuinely what is stopping you — the search came back empty, or what it found is already against the wall — a line appears under the solutions list saying so and offering up to four one-variable changes that would reach −15 dB: The best any lossless network can do here is -6.4 dB, set by termination 2 (1.25 Ω + 5 pF series) over 2.25–3 / 4.5–5 / 7.5–10 GHz. To reach -15 dB: termination 2's capacitance at or above 11.7 pF; or band 1 starting at 2.86 GHz instead of 2.25; or without band 1 the ceiling over bands 2 and 3 is -32.1 dB; or band 1 as 2.25–2.5 GHz mirrors band 3 without widening (ceiling -13.8 dB). Each clause holds everything else fixed and solves for the one thing it names, so they are alternatives rather than a recipe. They are ceilings, not designs : reaching one still needs an order and a family that fit, and the ladder that gets there may be a longer one than you wanted. It is a hint and never a refusal — solutions that exist are still listed beside it."],[21,"does-your-order-actually-use-the-gaps","Does your order actually use the gaps?","A multiband network buys its in-band return loss by leaving the gaps alone. At low order and with a narrow middle band, the prototype may not exclude them at all — the equal-ripple polynomial on your bands turns out to be the same polynomial as a single wide match over the whole span, and the result is one broad mediocre match instead of two or three good ones. This is not a fault in the synthesis; at that order no polynomial does better. The Frequency Band card says so when it happens: At order 2 the tri-band prototype does not exclude the gaps — this is a single-band match over 2.25–10 GHz (ceiling -3.1 dB). The gaps open at order 4 (rise ×2.9). and each gap line in the status strip carries the same measurement as a prototype rise factor: ×1 means the gap is not being excluded, and the larger it grows the more of the budget is being reclaimed from the gap and spent in your bands. If the note says no offered order opens them, the band geometry itself is the problem — widen the middle band, or move the outer bands closer together."],[21,"worked","Worked example: a two-stage FET interstage match","The interstage problem between two amplifier stages: stage 1's output, 200 Ω ‖ 0.125 pF, into stage 2's input, 1.25 Ω + 10 pF, over 3.3–5.0 GHz. A 160:1 impedance transformation with a reactance at both ends — exactly the case where tuning-out gets you a few percent of bandwidth and absorption gets you forty. The two-stage interstage example, solved: 200 ohm || 0.125 pF into 1.25 ohm + 10 pF over 3.3-5.0 GHz, with the solution applied and the element values it produces. Reading the figure, in the order you would work: The two terminations, as specified. Parallel 200 Ω ‖ 125 fF on the left; series 1.25 Ω + 10 pF on the right. Band 3.3–5.0 GHz, order 4, Chebyshev/Fano. The Q values the status strip reports: Q1 = 0.638, Q2 = 3.134. The series end is the higher-Q end, so it is the analysis end and the synthesis prescribes its absorbed element there. The ladder, from the analysis end. C4 = 10 pF is the load's own 10 pF, exactly — that is the absorption, visible as an element you did not have to add. Then L4 ≈ 154 pH , C3 ≈ 82.9 pF , L3 ≈ 18.5 pH and onward. The far end does not land on 200 Ω by itself. Synthesised bare, it comes out at 1.68 Ω, so the required Π N² is 119.03 — and the status strip says so. That is what the transforms are for. Two Norton transforms applied — the first-ranked solution from the list — bring the product to 119.027 / 119.027 and the strip reads ✔ matched . The achieved response: worst in-band return loss 16.66 dB, insertion loss 0.095 dB, ripple 0.036 dB , over a 42% fractional bandwidth into a 160:1 transformation with a capacitive load at both ends. That is the number to compare against whatever you were doing before. The element values shown are what the synthesis computes, and they are the same values the component stamps and the same values Flatten to Cell writes out."],[21,"dualband-worked","Worked example: the same stages, matched over two bands","The same two stages as the previous section: 200 Ω ‖ 0.125 pF into 1.25 Ω + 10 pF. This time the radio only ever operates in two narrow bands with 200 MHz of nothing between them, so there is no reason to spend the Fano budget on the gap. Everything below is reproducible — place a Match , open the Designer, and type these numbers."],[21,"what-to-type","What to type","Field Value Termination 1 — topology / R / X kind / value Parallel · 200 Ω · C · 0.125 pF Termination 2 — topology / R / X kind / value Series · 1.25 Ω · C · 10 pF Bands Dual Band f1, f2 1.75 GHz , 1.9 GHz Band f3, f4 2.1 GHz , 2.2 GHz Order 3 (three match points per band ) Response Chebyshev — single-match Form Bandpass (multiband is bandpass only) Response pane ▸ ± band 20 % The dual-band worked example: 200 ohm || 0.125 pF into 1.25 ohm + 10 pF, matched over 1.75-1.9 GHz and 2.1-2.2 GHz together at three match points per band. The band-2 edge has been widened to mirror band 1, the solutions list is out with the applied card checked, and the ladder carries both terminations as absorbed elements."],[21,"reading-the-result","Reading the result","A note appears under the band fields, and it is not a warning: \"Band 2 widened to 2.1–2.28 GHz to mirror band 1 about 1.997 GHz.\" The two bands as typed have ratio bandwidths of 1.0857 and 1.0476, which no even |Γ(jΩ)|² can produce; band 1 is the wider one so it is kept exactly, and band 2 is widened away from the gap to 2.28 GHz. The design is to 1.75–1.9 and 2.1–2.28 GHz — you get the band you asked for and a little more, never less. 116 solutions, and the applied one is the first card: Chebyshev · dual-band · order 3 · 1 transform · (L1, L2) · RL 17.06 dB . The list is the whole order × family cross-product ranked simplest first, and the strip along the bottom names what is currently applied. Fourteen elements. Order 3 with a mixed termination pair is 4n = 12 ; one more comes from the Norton transform (a Π replaces two like inductors with three) and one more is the excess capacitor CFano beside termination 1, where the synthesis wanted more shunt capacitance than the stage's own 125 fF supplies. C1 = 125 fF and C6 = 10 pF are the two terminations themselves, absorbed — elements you do not have to buy, and the line under the schematic says so. One Norton Π on L1 / L2 at N = 7.198 brings the achieved Π N² to 51.816 against a required 51.816. Without it the far end lands nowhere near 200 Ω. The Response pane's readout card carries the rest, and these are the numbers worth checking against your own run: Readout This design Q1, Q2 (both at ω₀ = 2π · 1.9975 GHz, which sits in the gap ) 0.308, 6.49 Fano ceiling, and which end sets it 25.9 dB, termination 2 Worst in-band return loss, across both bands 17.06 dB Insertion loss / ripple 0.086 dB / 0.050 dB Gap, peak reflection over 1.9–2.1 GHz 0.511, i.e. −5.8 dB Two of those are worth a sentence each. The ceiling is set by termination 2, and that tells you what to change. A series capacitance is limited by the lowest frequency you ask for, not by total bandwidth — so if this design were short of return loss, moving f1 up would buy more than anything else on the panel. The gap number is the design working , not failing. The budget a single wide match from 1.75 to 2.28 GHz would have spent on 200 MHz of unused spectrum is in the two passbands instead."],[21,"the-response","The response","The dual-band example's response, plotted at +/-20% of the band: |S11| against the left axis, |S21| against the right. Both passbands are matched; the region between them is not, and that is the design working rather than failing. Two matched passbands, a reflecting gap between them, and the whole thing plotted at ±20 % of the band so you can see what happens on either side as well. |S11| is on the left axis and |S21| on the right, because at 0.086 dB of insertion loss a shared scale renders |S21| as a flat line on the ceiling."],[21,"what-the-order-buys-and-what-it-costs","What the order buys, and what it costs","Every row below is the same specification with only Order changed, and every one is a row the solutions list offers: Order Elements Worst in-band return loss Gap, max reflection 1 6 10.5 dB −8.9 dB 2 10 14.7 dB −8.9 dB 3 14 17.1 dB −5.8 dB The gap number rises as the in-band number improves , and that is the mechanism rather than a side effect: a higher-order network reclaims more of the budget from the gap and puts it in the bands. If the gap mattered to you, this is the trade you would be making in the wrong direction — and the panel states it so the choice is yours. For comparison , set Bands back to Single and ask for 1.75–2.28 GHz at order 6. That is also fourteen elements — and it reaches 14.35 dB , in the passbands and everywhere between them alike. The dual-band network buys 2.7 dB in both bands for the same part count, and the only thing it gave up is 200 MHz the application never uses."],[21,"flatten","Flatten to Cell","Flatten to Cell… turns the design into an ordinary editable cell. What it writes: Two Pin components , matching the Match symbol's pins, so the cell is pin-compatible with the component it replaces. The network as ordinary L and C instances , placed on a grid — series arms along the spine, shunt arms to ground, with wires. A series L+C arm is written as two components , not one element with a C= parameter, because your next action is to edit, sweep or replace individual elements. Both terminations , as a Term plus the absorbed reactive element, all disabled (open) . They record what the network was designed for without affecting the netlist. A text annotation listing the design: band, order, response, both terminations, achieved return loss, insertion loss, ripple, Π N², and the date. The design record itself , carried onto the cell, so Re-open in Match Designer… can reconstruct it later. A flattened cell that has forgotten what it was is a dead end. The values are the ones you were looking at. Every element is written at the significant digits set in Settings — an inductor the pane showed as 1.201 pH lands in the cell as 1.201 pH, not as a fifteen-digit rendering of the double behind it. Raise that setting before flattening when you want the network carried at full precision; the flattened cell is a rounded copy of the design, and at three digits its response differs from the Match component's by about one part in a thousand. A checkbox, on by default, replaces the instance in place with one of the new cell. The symbol and pin positions are identical, so the wires stay connected and the schematic is immediately runnable. The whole operation — create cell, write files, replace instance — is one undoable command. When you would flatten. When you want to hand-tune an element, sweep one, substitute a real component model or a PCell for an ideal one, or lay the network out. What you lose is the live link: the cell is a circuit, not a design. Editing an element changes the circuit and nothing re-synthesises. That is the point of flattening, and it is also why the design record travels with it. Why the terminations are disabled rather than omitted. Omitted, the design intent is lost the moment someone opens the cell. Enabled, the cell would short its own ports when placed. Disabled, the cell simulates correctly against the real circuit and anyone who wants to reproduce the Designer's plot can enable the two Term s and run an S-parameter analysis on the cell alone."],[21,"exports","Getting the design out","Three files, from the Export button: Touchstone ( .s2p ) of the design response, with the per-port references R1 and R2 written as the file's own. Component listing ( .csv ) — instance, type, value, unit: the same rows as the grid view. Prototype g-values ( .csv ) , for anyone checking the synthesis against a published table. And copy and paste works, from all four views — which is usually the faster route, because nothing lands on disk and nothing has to be re-imported. Right-click You get the network schematic , or the value grid Copy — the ladder as a real schematic selection. It pastes into a circuitRF schematic page as live components and wires, and into a document or slide as vector art (SVG and PDF, plus a PNG; on Windows an enhanced metafile, which is the vector form an office suite pastes). the value grid Copy as CSV as well — the same rows as the file export, straight onto the clipboard. either plot the Data Display's own Copy , which puts the chart on the clipboard as vector art with its markers and info boxes. Pasting the network into a schematic page is worth knowing about on its own, when what you want is the ladder inside a bench you already have open rather than a new cell beside it. It is not the same circuit Flatten to Cell writes, and the difference is deliberate. Copy gives you the picture you are looking at — the elements at the values and positions the pane drew them, plus both terminations as ordinary live components. Flatten writes a cell that simulates : interface pins, the terminations parked in annexes and disabled, and a design annotation. Copy for the drawing, Flatten for the cell. Settings holds the display units per dimension, the significant digits, the minimum Q for Q-adjusted solutions, and whether to offer them at all. Inductance and capacitance default to pH and pF rather than to Auto : a fixed unit makes a column of values directly comparable, where Auto picks per value and leaves you converting \"1.53 nH\" against \"680 pH\" in your head."],[21,"refs","References","The synthesis rests on published work, and it is worth naming so a sceptical reader can check it: Matthaei, L. Young, E. M. T. Jones, Microwave Filters, Impedance-Matching Networks, and Coupling Structures , McGraw-Hill 1964 / Artech 1980 — §4.09 (matching networks with a prescribed load decrement), §4.12 (Norton transforms). Levy, \"Explicit formulas for Chebyshev impedance-matching networks, filters, and interstages,\" Proc. IEE , vol. 111, no. 6, pp. 1099–1106, June 1964 — the closed-form recursion the prototype uses. Dawson, \"Closed-form solutions for the design of optimum matching networks,\" IEEE Trans. MTT , vol. 57, no. 1, pp. 121–129, January 2009. Shea, Transmission Networks and Wave Filters , Van Nostrand 1929, p. 325 — the Norton transforms. Fano, \"Theoretical limitations on the broadband matching of arbitrary impedances,\" J. Franklin Inst. , 1950 — the bound that makes absorption the right idea, and perfection impossible."],[22,"","","Every analysis circuitRF runs, what it computes, a short overview of the method, and the full set of settings. Algorithm details are deferred to the design notes / white papers; this chapter is the operational reference. How results are stored Every analysis returns a DataSet — a named bundle of labelled, unit-bearing arrays ( DataCube s), each a single kind, real or complex. A whole run's DataSets go into one file in the workspace's shared results/ folder, and you name that file in the Results file field above the analyses list; leaving it blank uses the schematic's own name. This is what the Data Display plots and what .npy / .mat export produces. Full detail: Where results live ."],[22,"dc","DC (operating point)","Computes: the steady-state node voltages and branch currents with no signal applied — the bias point. For nonlinear circuits this is the prerequisite to harmonic balance. Method: modified nodal analysis (MNA); nonlinear devices solved by Newton–Raphson with gmin and source stepping for convergence robustness. Setting Meaning Tol Newton convergence tolerance on the residual. MaxIter Maximum Newton iterations. gmin / source stepping Continuation aids that ease a hard bias point into convergence. Read currents directly with an IProbe in series, and name a voltage with a VProbe on the wire."],[22,"s-parameters","S-Parameters","Computes: the linear, small-signal multiport network parameters over a frequency sweep — the S-matrix at each frequency, with renormalization to the port reference impedances. Ports are defined by Term components (or P1Tone port numbers). Method: complex MNA solved per frequency point; each port is excited in turn and the scattered waves are extracted and renormalized. Embedded Touchstone (SnP) blocks are interpolated onto the sweep. Setting Meaning Frequency sweep Start / Stop / Step (or point count); one or more segments. Units accepted ( GHz , MHz , …). Reference impedance Per-port reference (from each Term's Z ); the result renormalizes to it. NDF=yes Also compute the normalized determinant function — the rigorous count of right-half-plane poles of the whole network. PassiveVars= / PassiveParams= name what passivates a user-supplied model. MarginThreshold= The stability margin in dB below which a WSProbe is reported as worth looking at. Default -15 ; none disables. A WSProbe anywhere in the circuit adds the wsp matrix and its derived quantities to this run, at the cost of a few extra back-substitutions and no extra factorization. See The WSProbe . For the ideal System Components this is the analysis that answers almost everything about them: port match, isolation, coupling and directivity, a filter's passband and rejection, a duplexer's arm-to-arm leakage, and an amplifier's small-signal gain and stability. From the CLI: circuitrf sparam circuit.cnl --freq 1GHz:10GHz:50MHz -o out.s2p ."],[22,"harmonic-balance","Harmonic Balance","Computes: the steady-state response of a nonlinear circuit driven by one or more tones (up to six) — the spectrum (DC + harmonics, and mixing products when there is more than one tone) at every node. From it come Pout, gain, efficiency, PAE, compression, and intermodulation. Method: the circuit is split into a linear sub-network (solved in the frequency domain) and nonlinear devices (evaluated in the time domain and transformed by FFT). A multidimensional Newton solve drives the harmonic-balance residual — frequency-domain KCL — to zero, using a conversion-matrix Jacobian. Power/source continuation ramps the drive to reach convergence deep into compression. Multi-tone analysis uses a diamond-truncated mixing spectrum. For the ideal System Components this is the analysis for everything an S-parameter run cannot reach — a mixer's conversion gain and its mixing products, an amplifier's compression and IM3, and passive intermodulation on the blocks that carry it. Two tones ( multi-tone ) are what an intercept measurement needs. Setting Meaning Typical Tone The fundamental frequency (a value or a variable such as RFfreq ). Two or more tones for intermodulation. 2 GHz ToneUnit Unit for the tone value when it doesn't carry its own (a unit on the referenced variable wins). GHz MaxHarm Highest harmonic order retained (5 → DC plus 5 harmonics). Higher captures sharper nonlinearity at more cost. 5–7 FFTOverSample Oversampling factor for the time grid used by the nonlinear FFT. Raise to reduce aliasing on stiff devices. 1–2 Tol Newton convergence tolerance on the HB residual. 1e-6 DriveStepping Power/source continuation: IfNecessary (ramp only when a direct solve stalls), or always/never. IfNecessary GuardHarmonic Extra guard harmonic(s) beyond MaxHarm for anti-alias safety. 0 Lambda Newton damping factor (1 = full step; lower damps an unstable solve). 1 MaxIter Maximum Newton iterations per solve point. 100 SSStart / SSStop / SSNpts / SSStep / SSUnit / SSLog The small-signal \"tickle\" sweep a WSProbe is driven over, around the converged large-signal operating point. Absent, there is no small-signal solve and the run is unchanged. See The WSProbe under harmonic balance . 0.1-10 GHz SSMaxHarm Sidebands retained in the conversion matrix for that solve. = MaxHarm MarginThreshold As for S-parameters, applied at each operating point. -15 dB Multi-tone analysis adds the extra tones and a mixing order (≥ 5 at two tones, to capture close-in IM products; lower it as tones are added)."],[22,"parametric-sweep","Parametric Sweep","Computes: nothing new on its own — it wraps another analysis and re-runs it across one or more variables (drive power, a bias voltage, frequency, any user variable). Nest sweeps to cover several variables; the innermost analysis is what each point solves. Setting Meaning Var The variable to sweep (e.g. Pin , RFfreq ). Start / Stop Sweep endpoints (units accepted). Step or Npts Step size, or a point count; linear or logarithmic. Inner The analysis (or inner sweep) this sweep wraps — how sweeps nest. A whole sweep tree writes a single results file named after its innermost analysis."],[22,"loadpull","Loadpull / Sourcepull","Computes: figures of merit (Pout, gain, efficiency, PAE) as the load (or source) reflection coefficient is swept over a grid on the Smith chart — the classic loadpull experiment — ready to draw as contours . Harmonic terminations can be swept too (harmonic loadpull). Method: a programmable Tuner presents each grid termination; a harmonic-balance solve runs per point (with previous-point continuation for convergence); the FOMs are collected into the DataSet over the Γ-grid. The Data Display fits a surface and extracts contour lines; measured loadpull files ( .spl / .lpcwave ) plot identically. Setting Meaning Tuned port / role Load or source side, and which DUT port the tuner terminates. Γ grid The set of reflection-coefficient points (a Smith-chart grid, typically within \\|Γ\\| ≤ 0.9). Tuned harmonic Fundamental, or a specified harmonic for harmonic loadpull. Power / bias The drive level and bias at which the pull is performed (often a power sweep too)."],[22,"gam-file","The termination grid file ( .gam )","The grid of terminations a loadpull visits lives in a small, human-readable text file with a .gam extension — one termination per line. It is kept out of the netlist on purpose: a grid is data (often hundreds of points), and a real tuner system works the same way, reading a \"pattern file\" of impedance states to visit. A loadpull analysis points its grid at a .gam file ; a Loadpull Pursuit can write one for you (its recommended terminations). Each point is either an impedance (Ω) or a reflection coefficient Γ , in one of three column layouts. An optional header line (starting # ) declares the form, the reference impedance, and the layout; if you omit it, circuitRF assumes impedance and infers the layout from the first data line. The parser is deliberately forgiving — blank lines and ; or # comment lines are skipped. Header token Meaning Default gamma / impedance Values are reflection coefficients Γ, or impedances in Ω. impedance Z0=<value> Reference impedance for the Γ↔Z conversion. 50 Ω re_im Two columns: real, imaginary. inferred from the first line: a j / i marker ⇒ re+j*imag , else re_im mag_ang Two columns: magnitude, then angle in degrees . re+j*imag One column, a complex literal (e.g. 80+j*10 , 0.5-j*0.3 ). Header tokens are case-insensitive and may appear in any order, e.g. # gamma Z0=50 mag_ang . A Γ grid in magnitude/angle form (rings on the Smith chart): # gamma Z0=50 mag_ang ; concentric rings of load reflection coefficient 0.00 0 0.20 0 0.20 90 0.40 0 0.40 90 0.40 180 0.60 0 0.80 30 Multi-frequency grids One .gam file can hold a different grid per frequency , for frequency-swept loadpull. Start each block with a bare freq=<value><unit> line ( Hz / kHz / MHz / GHz / THz ); every point below it belongs to that frequency until the next freq= line. circuitRF reads the block whose frequency is nearest the one being simulated. A file with no freq= line is a single grid used at every frequency. # impedance Z0=50 re+j*imag freq=1.8GHz 80+j*10 60-j*5 freq=2.2GHz 85+j*5 70+j*0 Using one. In the Loadpull editor, set the grid field to your .gam file (paths are resolved relative to the netlist). Getting one. A Loadpull Pursuit with an OutputGrid path writes its recommended terminations as a .gam — always in impedance … re+j*imag form, with a generated-by header and a # WARNING: line if any near-non-convergent points were dropped. That output reads straight back into a standard loadpull, including the multi-frequency layout for a frequency-swept pursuit."],[22,"loadpull-pursuit","Loadpull Pursuit","Computes: the load (or source) terminations that optimize a figure of merit — found automatically by a query-minimizing search rather than by gridding the whole Smith chart. It reports two optima at constant compression — MXP (maximum output power) and MXE (maximum efficiency) — recommends a conjugate-match source impedance for each, and can run a focused high-fidelity loadpull around them, all from one analysis. Why use it: it automates the real-world PA loadpull procedure — find the optimum, then loadpull a focused grid around it with a sensible source match — into a single unattended run. Point it at a DUT whose optimal terminations are unknown, run, and come back to a complete loadpull dataset already concentrated at the right terminations. It is repeatable (great for characterizing many devices with one netlist), avoids wasted query points far from the optima, and avoids non-convergent terminations. It complements, it doesn't replace, a standard loadpull. Pursuit finds the useful terminations and the region of interest; a standard loadpull then shows the performance tradeoffs across that region. Pursuit shares all Loadpull settings except the Γ grid (it generates terminations rather than reading them), plus the keys below."],[22,"how-a-pursuit-run-works","How a pursuit run works","Search — a steepest-ascent search in the VSWR plane finds MXP, then MXE (seeded from MXP, so the second search is cheap). Each \"query\" is one drive-to-compression harmonic-balance run; queries are cached so MXE reuses MXP's data. Recommend — builds a focused-around-the-optima set of recommended terminations (dense near MXP/MXE, sparse further out) and a conjugate-match source impedance for each optimum. Loadpull — optionally runs a standard loadpull over those recommended terminations, using a recommended source match, producing the high-fidelity contour data."],[22,"search-method","Search method","SteepestAscent (default) — fits the gradient and ascends along a fixed direction, shrinking the step on rejection; a final polynomial refinement pins the optimum. Fast and adequate for the near-1-D real-axis optima typical of PA loadpull. IteratedQuadratic — a trust-region search that re-fits the local curvature at every step, so it follows a curved 2-D ridge instead of committing to one direction. Reuses the same query cache, so its query count stays comparable."],[22,"pursuit-settings","Pursuit settings","Key Meaning Default EffType The MXE (efficiency) criterion: drain efficiency DE or PAE . DE SearchMethod SteepestAscent or IteratedQuadratic (above). SteepestAscent ZsourceOBO Output back-off (dB from compression) at which the input impedance is sampled for the auto-Zsource report. Granularity set by PinStep . 5 VSWR1 (focused) Focused box size (VSWR-circle radius) around MXP and MXE — the dense sampling region. 1.5 VSWR1_resolution Grid spacing (N×N samples) inside each focused box. 4 VSWR2 (broad) Broad box size (VSWR-circle radius) for the surrounding coarse grid. 3 VSWR2_resolution Grid spacing (N×N samples) for the broad box. 4 keepNonconvergingPoints If false, drop recommended points near terminations found non-convergent during the search (and warn). false nonconvergentVSWR Exclusion radius (VSWR) around known non-convergent terminations. 1.05 OutputGrid Path to write the recommended terminations as a .gam file . Absent → no file written. none CreateLoadpullResult Run the follow-on standard loadpull over the recommended terminations. Independent of OutputGrid . on LoadpullResultZsource Source match the follow-on loadpull uses: MXE , MXP , or None (use the Source Tuner's own Z1). MXE The recommended terminations always exist in memory; OutputGrid controls only the file, CreateLoadpullResult controls only the follow-on simulation — the two are orthogonal. Auto-Zsource — a recommended source match After each optimum is found, pursuit backs the drive off by ZsourceOBO dB, computes the DUT input impedance Zin there, and reports Zsource = Zin* (the conjugate match) — a ready-to-use input-match target, per optimum, without a separate sourcepull. The DUT must compress MXP/MXE are defined at compression . If the DUT does not reach Compression within PinMax , the search aborts with a clear message (raise PinMax or check bias/load) — it never silently raises your PinMax cap. Full design + algorithm: docs/design/loadpull_pursuit.md (in the repo)."],[22,"two-tone","Multi-Tone Harmonic Balance (2 to 6 tones)","Computes: the steady-state spectrum of a nonlinear circuit driven by several carriers at once — every harmonic and every intermodulation (mixing) product. Two tones is the standard test for linearity : third-order intermodulation (IM3), intercept points, spectral regrowth, and the asymmetry between the lower and upper IM products. Three or more tones is the natural setting for mixers (RF, LO and their products) and for multi-carrier work. Two tones is enough for an ideal mixer on its own — one for the RF, one for the LO — and the converted power reads off the IF port at the mixing product you want. Multi-tone is not a separate analysis — it is Harmonic Balance with more than one tone. Everything from the single-tone chapter (the linear/nonlinear split, the Newton solve, drive continuation, all the convergence knobs) still applies. This chapter covers only what is different. Up to six tones are supported. Everything below is written for two because that is the common case, but it holds at any tone count: read \"(k₁, k₂)\" as the full list \"(k₁, …, k T )\". The one thing that changes with tone count is how far you can push Max mix order — see How many tones, at what order ."],[22,"two-tone-setup","Setting it up","In the Harmonic Balance analysis editor, switch the Single / Multi-tone toggle to Multi-tone . Three things change: The single Tone (f₀) field is replaced by a tone list , starting with Tone 1 and Tone 2 . + Tone adds another (up to six); the × on a row removes it, down to a minimum of two. Max harmonics is replaced by Max mix order — the truncation that bounds the two-dimensional mixing spectrum (see below). Every tone accepts an expression, so a common pattern is Tone&nbsp;1 = RFfreq − Spacing/2 , Tone&nbsp;2 = RFfreq + Spacing/2 with RFfreq and Spacing as variables you can sweep. Beside Max mix order the editor shows how many mixing products the current tone count and order retain, and warns when that is over the limit — so you can see the cost of an order before you run it. The analysis owns the tones, not the source The fundamentals come from the HB analysis (the tone list, written as NumFreqs=N Tone[1]=… Tone[N]=… in the netlist), not from the source. A PnTone (or V_nTone ) just supplies power at those frequencies. Every source tone must land exactly on the analysis grid — an off-grid drive frequency is rejected at setup with a clear message rather than producing silent garbage."],[22,"two-tone-grid","The mixing grid and mixing order","With two fundamentals, a spectral line is no longer indexed by a single harmonic number. Each line is a pair of integers (k₁, k₂) sitting at the physical frequency f = k₁·f₁ + k₂·f₂ and its mixing order is m = |k₁| + |k₂| . The carriers are (1, 0) and (0, 1) at order 1; a third-order intermod such as 2f₁−f₂ is (2, −1) at order 3. Solving for every possible (k₁, k₂) is infinite, so circuitRF keeps a finite set bounded by Max mix order : retain (k₁, k₂) ⟺ |k₁| + |k₂| ≤ MaxMixOrder That inequality is a diamond (a rotated square) in the (k₁, k₂) plane — hence \"diamond truncation.\" A diamond rather than a full rectangular box because the high–high corner products (e.g. 5f₁+5f₂) carry no meaningful energy in a real intermod test, while every low-order product that does matter is inside the diamond. MaxMixOrder is the two-tone analog of single-tone MaxHarm : order 5 retains products down to IM5. The number of products grows quadratically with the order (≈ 2·order² lines), so a two-tone run is much larger than a single-tone run at the same order — order 5 yields dozens of mixing products versus a handful of harmonics. With T tones the same rule reads retain (k₁,&nbsp;…,&nbsp;k<sub>T</sub>) ⟺ |k₁| + … + |k<sub>T</sub>| ≤ MaxMixOrder , and the diamond becomes a T-dimensional one. There is still a single order knob — no per-tone limits."],[22,"multi-tone-size","How many tones, at what order","The retained product count is what actually costs you, and it grows steeply with tone count — much faster than the quadratic growth two tones would suggest. The default MaxMixOrder&nbsp;=&nbsp;5 is sized for two tones; lower it as you add tones : Retained mixing products. Configurations over the 600-product limit are refused, with a message naming the largest order that fits. Tones order 2 order 3 order 4 order 5 2 7 13 21 31 3 13 32 63 116 4 21 65 161 341 6 43 189 645 (refused) 1827 (refused) Practical starting points: order 5 at 2–3 tones , order 4 at 4 tones , order 3 at 5–6 tones . On a single-FET PA, six tones at order 3 solves in a few seconds. Too large is refused before the run starts, not after If the tone count and mixing order together ask for more products than the solver will take, circuitRF refuses immediately — not after a long solve — and the message tells you the largest MaxMixOrder that would work at that tone count. The editor shows the same number live beside the field, so you normally see it before you press Run. Equally spaced tones put two products at the same frequency With evenly spaced carriers — the usual multi-carrier stimulus — different mixing products can land on the same physical frequency. At 1.99 / 2.00 / 2.01 GHz, both (1, −1, 0) and (0, 1, −1) sit at 10 MHz. They stay separate products with their own phasors, and the spectrum plot shows both stems at the same position — they are not added together. Use the marker readout, which names the product, to tell them apart. (k₁, k₂) Order Frequency What it is At f₁=1.99, f₂=2.01 GHz (0, 0) 0 0 DC / baseband 0 (1, −1) 2 f₁ − f₂ IM2, the tone spacing (baseband) 0.02 GHz (1, 0) 1 f₁ Carrier 1 1.99 GHz (0, 1) 1 f₂ Carrier 2 2.01 GHz (2, −1) 3 2f₁ − f₂ IM3 lower 1.97 GHz (−1, 2) 3 2f₂ − f₁ IM3 upper 2.03 GHz (3, −2) 5 3f₁ − 2f₂ IM5 lower 1.95 GHz (−2, 3) 5 3f₂ − 2f₁ IM5 upper 2.05 GHz (2, 0) 2 2f₁ 2nd harmonic of carrier 1 3.98 GHz (1, 1) 2 f₁ + f₂ 2nd-harmonic sum band 4.00 GHz The IM \"order\" is the sum of the integer coefficients: IM3 = |2|+|−1|, IM5 = |3|+|−2|. Choose closely-spaced tones (small Δ) so the IM3 and IM5 products fall right beside the carriers. Reading the spectrum in the Data Display A multi-tone result plotted as a spectrum uses a mixIndex axis: each stem is a mixing product, drawn at its physical frequency, labelled with its (k₁, k₂) tag — or (k₁, …, k T ) at more tones. The spectrum is shown single-sided — the negative-frequency products (the conjugate halves, e.g. f₁−f₂ and f₂−f₁) are folded onto their positive frequency, so each product appears once. Arrow keys step a marker in frequency order, which is what you want with tightly spaced IM products. Everything here works the same at any tone count."],[22,"two-tone-meas","Measuring intermodulation (IMD2 / IMD3)","A measurement reads a mixing product out of the result by its (k₁, k₂) tag — at three tones the tag simply has three entries, e.g. HB1.V(\"Vout\",&nbsp;\"(1,1,-1)\") for the triple-beat product f₁+f₂−f₃. The classic figures of merit are the intermodulation ratios in dBc — an IM product relative to its carrier: IMD2 = dB( HB1.V(\"Vout\", \"(1,-1)\") ) - dB( HB1.V(\"Vout\", \"(1,0)\") ) dB IMD3 = dB( HB1.V(\"Vout\", \"(2,-1)\") ) - dB( HB1.V(\"Vout\", \"(1,0)\") ) dB Replace HB1 with your analysis name and \"Vout\" with your output node. IMD2 uses the baseband product (1,−1) = f₁−f₂; IMD3 uses the lower third-order product (2,−1) = 2f₁−f₂ (use (−1,2), referenced to the (0,1) carrier, for the upper IM3). To get the curve vs Pin , sweep the drive power (a parametric sweep over the variable your source Pavl references) — the same expression then returns IMD vs Pin instead of a single value. Two ways to address a product — use the accessor for IMD The accessor HB1.V(\"Vout\", \"(1,-1)\") names the node and the product tag and keeps any swept axes automatically , so the same expression works whether or not you have added a Pin sweep — one value without it, a curve over Pin with it. The bracket form HB1.V[:, \"Vout\", \"(1,-1)\"] is positional: one token per cube axis (a leading : to keep the Pin sweep), so it must be re-edited when a sweep is added or removed. Prefer the accessor for IMD metrics you keep; reach for the bracket when copy-pasting from a trace card. See Referencing analysis results ."],[22,"two-tone-z","Source impedance and harmonic terminations","With dozens of products spread across the spectrum, each one needs a source termination. circuitRF assigns it by frequency , not by mixing order. It defines a band-center fundamental — for two tones the average f_c = (f₁ + f₂) / 2 — and places each product in the harmonic band nearest its frequency: band n = round( |f| / f_c ) → Z[n] if declared, else the shared Z So band 0 (|f| < f_c/2) is the baseband and uses Z[0] ; band 1 (around f_c) holds both carriers and all the close-in IM3/IM5 products and uses Z[1] (or the default Z ); band 2 (around 2·f_c) holds the second-harmonic products and uses Z[2] ; and so on. The decision is purely the product's frequency: as a high-order product such as (3, −1) drifts down past the band-1/band-2 boundary it switches from Z[2] to Z[1] automatically — no order threshold is hard-coded. This is the same harmonic-termination model as P1Tone , and a PnTone presents its shared Z and per-band Z[k] to the whole multi-tone spectrum this way. Why baseband termination matters In single-tone HB the baseband is decoupled. In two-tone it is not — the IM2 product (1, −1) at f₁−f₂ lands at baseband, and its termination Z[0] feeds back into the carriers and the IM3 levels. Studying source/load baseband-termination effects is a primary reason to run two-tone, so set Z[0] deliberately."],[22,"two-tone-vs-single","What's different from single-tone","IM3 asymmetry. The lower IM3 (2f₁−f₂) and upper IM3 (2f₂−f₁) generally have different levels in dBc, not just a phase difference — a direct consequence of the device's nonlinear I–V/Q–V behaviour and its loading. The full diamond resolves both, so the asymmetry is a real output of the run, not an artifact. A fully coupled solve. The two-tone result is not two single-tone solutions superimposed. The carriers, harmonics, IM products, and baseband are all unknowns in one Newton solve with a single conversion-matrix Jacobian — so the IM levels capture the true bidirectional coupling between the carriers and the baseband. A bigger spectrum, a bigger cost. Because the product count grows quadratically with MaxMixOrder , raise it only as far as you need: 5 captures IM5 (usually enough); higher captures IM7+ at noticeably more cost. Commensurate tones. f₁ and f₂ must be commensurate (a rational ratio) so every mixing product lands exactly on the grid and the waveform is exactly periodic — closely-spaced tones like 1.99 / 2.01 GHz satisfy this naturally. Full design + algorithm: docs/design/harmonic-balance.md and the harmonic-termination model in docs/design/p1tone-harmonic-terminations.md (in the repo)."],[22,"cli","Running an analysis from the command line","Everything above is driven from the GUI's Run button, and everything above also runs headless. The command-line driver takes a .cnl netlist — hand-authored, or extracted from a schematic — and runs one analysis: circuitrf sparam <file.cnl> [--freq start:stop:step] [-o out.sNp] circuitrf dc <file.cnl> circuitrf hb <file.cnl> harmonic balance; runs the sweep if one wraps it circuitrf lp <file.cnl> loadpull over the directive's Gamma grid circuitrf lpp <file.cnl> loadpull pursuit: searches for MXP / MXE circuitrf em <file.cem> electromagnetic extraction of the layout it names circuitrf elab <file.cnl> dump the elaborated netlist From a source checkout, put dotnet run --project src/Cli -- where circuitrf appears. The full reference for every verb, its options, its output and its exit codes is The Command Line — including a worked EM run from an empty folder. This section covers only the part that is about analyses rather than about the driver."],[22,"cli-hb","Harmonic balance from the command line","dotnet run --project src/Cli -- hb hero2.cnl -o out.mat That runs the netlist's harmonic-balance analysis — single- or multi-tone; the same verb does both — prints the spectra as tables, and writes out.mat . Option What it does -a , --analysis <name> Which analysis to run. Optional when the file declares only one HB chain. --set <var=expr> Override a global variable before elaboration , so anything derived from it follows. Repeatable. --maxharm K Override MaxHarm . --maxmix M Override MaxMixOrder (multi-tone only). --tol t , --max-iter N Override the convergence tolerance and the iteration cap. --rows N , --all How much of each printed table to show. Default is a truncated head. --diag Engine convergence diagnostics to stderr. -o , --export <path> Export the results. The extension picks the format : .mat , .npy or .txt . --kits <dir> A folder of installed kits, for externally-supplied device models. Repeatable; valid on any verb. dotnet run --project src/Cli -- hb hero5.cnl --set Pavl_dbm=0 -o hero5.txt The sweep runs, whichever name you give When a parametric sweep wraps an analysis, the sweep is what runs — and naming the inner analysis with -a is promoted to its outermost enabled wrapper, with a line on stderr saying so. That promotion exists because running the inner analysis alone gives a converged, plausible, complete-looking result with the sweep axis silently missing. It is the same rule for hb , lp and lpp : a frequency-swept loadpull has exactly that shape."],[22,"cli-lp","Loadpull and pursuit from the command line","dotnet run --project src/Cli -- lp hero3.cnl --pin -20:1:15 -o hero3.spl dotnet run --project src/Cli -- lpp hero3B.cnl --out-grid found.gam -o hero3B.npy lp prints one row per Γ grid point — where it was, how it stopped, and its figures of merit at the last converged, non-tickle drive step — rather than the raw [gridPoint × driveStep] cubes. lpp prints its MXP and MXE optima first, then the follow-on grid. Both take the HB options above, plus --pin , --compression , and one grid option each: --grid for lp , --out-grid for lpp . lp also exports .spl and .lpcwave , the loadpull interchange formats the Data Display reads back. Full detail ."],[22,"cli-em","Electromagnetic extraction from the command line","dotnet run --project src/Cli -- em Amp.cem em takes a .cem EM setup and needs no other arguments — the layout and the technology resolve by walking up to their own workspaces, exactly as they do in the GUI. It writes the Touchstone and the diagnostics .npy where the Simulate button writes them , so a schematic's SnP reference survives a headless re-run. Full detail, with a worked example ."],[22,"cli-measure","Measurements, and why headless is the regression harness","The measure lines on the TestBench are evaluated exactly as the GUI evaluates them, and a measurement that fails to evaluate is reported on stderr and the run continues. A .cnl that works headless works when opened , which is what makes the command line usable as a regression harness: the two paths share the elaborator, the engines and the measurement evaluator. See also: the netlist format (how analyses are written) · Components · Plot types · Measurements · Results & data export · The command line ."],[23,"","","A measurement is a named figure of merit computed from a run's results — Gain, PAE, Pout, whatever you can write as an equation over the analysis output. It draws no current and stamps nothing; it is cube algebra evaluated after the analyses finish, and its result plots like any other trace. The MEAS component holds your measurement equations"],[23,"what","What a measurement is","It is the RF engineer's \"equations\" pane. Once your analyses have produced their result cubes, a measurement pulls values out of them and computes a derived quantity: Gain = dB(...) , PAE = ... , Idc = DC1.I(\"Iout\") . The result is a new cube added to the run's measurements group, addressable by its bare name and plottable like any trace. Measurements use the same expression language as everything else, with operands extended to cube quantities."],[23,"authoring","Authoring — the MEAS component","Drop a MEAS component on the schematic (it is an annotation — no ports, nothing emitted to the netlist) and edit its rows, one per line, exactly like a VAR block: Gain = dB( HB1.V(\"Vout\", 1, All) / HB1.V(\"Vin\", 1, All) ) dB Pout = 0.5 * real( HB1.V[:, \"Vout\", 1] * conj(HB1.I[:, \"Iout\", 1]) ) W PAE = 100 * (Pout - Pin) / Pdc % Each row is name = expression [unit] . Row order — and the order of multiple MEAS components — is the declaration order the evaluator uses (so later rows can reference earlier ones). A duplicate name is a reported conflict; the first definition wins. MEAS at the top testbench level feeds TestBench.Measurements ; a MEAS inside a sub-cell is ignored (with a warning)."],[23,"refs","Referencing analysis results (two notations)","A measurement reads an analysis by the name it appears under in the results tree ( HB1 , SP1 , DC1 , …). There are two equivalent notations for pulling a value out of a cube — mix them freely."],[23,"accessor-name-keyed-durable","1. Accessor — name-keyed (durable)","You name the node/branch and the harmonic; remaining axes default to All (kept whole). HB1.V(\"Vout\") node Vout, all harmonics, all sweep points HB1.V(\"Vout\", 1) node Vout, fundamental, all sweep points HB1.I(\"M1:d\", 1, All) device-port branch current, fundamental, swept DC1.I(\"Iout\") no sweep → a scalar Why use it: it is order-independent — the engine locates each axis by name, so adding or reordering sweep axes never breaks the expression. This is the right choice for measurements you author by hand and keep. For a two-tone run the spectral axis is mixIndex , so the second argument is the mixing-product tag \"(k₁,k₂)\" instead of a harmonic number: HB1.V(\"Vout\", \"(1,0)\") carrier 1 (f₁) HB1.V(\"Vout\", \"(1,-1)\") IM2 product (f₁−f₂) Because the accessor keeps swept axes automatically, the same expression works with or without a Pin sweep (one value vs a curve) — see measuring intermodulation ."],[23,"bracket-positional-fast-copypaste","2. Bracket — positional (fast copy/paste)","One token per cube axis, in cube-axis order (numpy-style): : keeps the axis, a name or integer fixes and drops it, a:b keeps a sub-range. Pout_W = 0.5 * real( HB1.V[:, \"Vout\", 1] * conj(HB1.I[:, \"Iout\", 1]) ) Why use it: it is exactly what the Plot Inspector's trace card writes for you. Dial in a trace, copy the shorthand string straight into a measurement, done — no remembering argument order. The catch: brackets are positional, so a later outer sweep can shift the axis a hand-edited bracket addresses. Rule of thumb Prefer the accessor for durable hand-authored measurements; reach for the bracket when copy-pasting from a trace you already have on screen."],[23,"sparam","S-parameters: 1-based ports","On an S-parameter cube, the i / j axes carry 1-based port numbers — the way RF engineers name S-parameters — in both notations: s21 = SP1.S(2, 1) S21 over frequency s21_db = dB( SP1.S[:, 2, 1] ) S21, freq kept (:), ports fixed s11 = SP1.S[:, 1, 1] S11 SP1.S[:, 2, 1] is S21 , not \"row 2 column 1 by zero-based index.\" A port outside 1..nPorts is a clear error listing the available ports."],[23,"wsprobe","WSProbe accessors","A run with a WSProbe in it carries one wsp matrix plus a named set of outputs per probe, and both are reachable by accessor: SP1.wsp the whole matrix, over {freq, row, col} SP1.wsp(3, 13) one element traced over frequency — the document's wsp(3,13) SP1.idx(\"GATE\") the probe's 1-based index, as an integer SP1.H0(\"GATE\") and Y0, ZG, ZL, LG, F, SM_Y0, SM_H0 the same way row and col are 1-based matrix positions , matched by axis value rather than by position, so they are the reference document's own indices with no arithmetic in between — and, like the S-parameter ports above, a positional slice would pin the frequency axis instead. HB1.* resolves identically for a probe under harmonic balance , over ssfreq rather than freq . A worked measurement — the smaller of a probe's two stability margins, and the frequencies at which the driving-point locus actually crosses: measure SMgate = wsp_stability_margin(SP1.wsp, SP1.idx(\"GATE\")) measure fosc = wsp_unstable_freq_kurokawa(1 / SP1.H0(\"GATE\")) An unknown probe label is an error that lists the probes present. Every derived metric of The WSProbe can be written this way, and evaluates identically in the window and under circuitrf sparam ."],[23,"compose","Composition & scope","Measurements are evaluated in declaration order, and each is in scope by name for the ones after it — so a complex figure of merit builds from intermediates. Also in scope: every global VAR variable (by name). The element-wise helpers conj , real , imag , mag , phase , dB , dB10 , dBm , log10 , ln broadcast over cubes. Referencing an unknown analysis raises an error naming the available ones; a failing measurement is reported as a run note and does not fail the whole run."],[23,"swept","Swept variables","A global variable that is also a parametric-sweep axis is injected as a 1-D cube (one element per sweep point) rather than a scalar — so Pin_avail = Pin over a 10-point Pin sweep yields a 10-element curve. Its axis matches the swept analysis's axis, so it broadcast-aligns: Gain = dB(HB1.V(\"out\",1,All)) - Pin resolves element-wise. A non-swept global stays a scalar. See also: Expressions (the language) · MEAS component · Plot types . Full design: docs/design/measurements.md ."],[24,"","","A circuitRF netlist is a human-readable text description of a circuit and what to simulate. It's the same thing the schematic editor produces internally, and it's what the engine actually runs."],[24,"what","What a netlist is, and why you'd use one","Most users never write a netlist by hand — you draw a schematic and press Run , and circuitRF extracts the netlist for you. But the .cnl file is worth understanding because: It is human-readable and diff-friendly — you can read, review, and version-control a design as text. It is the engine's input contract : the schematic, the CLI, and the engine all meet here. The headless CLI runs a .cnl directly — every verb but em takes one. It's the clearest way to see exactly what got simulated — components, parameter values after resolution, analyses, and measurements, all in one place. Syntax basics: one statement per line; ; begins a comment (to end of line); whitespace separates tokens; values may carry a unit ( 50 Ohm , 2 GHz , 1 mF ); and expressions may reference variables and parameters."],[24,"example","A complete example","This is a single-FET GaN power-amplifier test bench: a harmonic-balance power sweep, swept again over frequency, with measurements for gain, efficiency, and return loss. We'll walk through every line below. ; netlist.cnl — generated from TestBench \"HBTest.csch\" define MyFET (gate drain) parameters Periphery_mm=1 Sv = -0.837 Sc = 0.71 TV0 = 4.268 TC = 1.507 th = 0.001 a = 0.176 g = 0.089 lam = 0.0012 B = 1130 SDD:X1 gate 0 drain 0 I[1,0]=_v1/50 I[2,0]=Periphery_mm*(B*TC*tanh(_v2*a*(tanh(g*(TV0 - _v1 + _v2*th + Sc*ln(exp(-(Sv - _v1)/Sc) + 1)))+1))*ln(exp(-(2*TV0 - 2*_v1 +2*_v2*th + 2*Sc*ln(exp(-(Sv - _v1)/Sc) + 1))/TC) + 1) * (_v2*lam + 1))/2 end MyFET Pin = 0 RFfreq = 2 GHz C:C1 Vin n1 C=1 mF L:L1 n2 n3 L=1 mH L:L2 n4 Vout L=1 mH R:R2 n5 0 R=80 Ohm C:C2 n5 n6 C=1 mF P1Tone:P1 n1 0 Pavl=Pin dBm Z=50 Ohm Freq=RFfreq Phase=0 deg Z[0]=1 Ohm Z[2]=30 Ohm Vdc:V1 n2 0 Vdc=-3.05 V Vdc:V2 VDD 0 Vdc=48 V MyFET:X1 n3 Vout Periphery_mm=1 IProbe:Iout Vout n6 IProbe:Iin Vin n3 IProbe:IDC VDD n4 C:C3 Vout 0 C=0.3 pF analysis HB1 type=hb Tone=\"RFfreq\" ToneUnit=MHz MaxHarm=5 FFTOverSample=1 Tol=1e-6 DriveStepping=IfNecessary GuardHarmonic=0 Lambda=1 MaxIter=100 analysis HB1_sweep_Pin type=parametric_sweep Var=Pin Start=0 Stop=30 Step=1 Inner=HB1 analysis HB1_sweep_RFfreq type=parametric_sweep Var=RFfreq Start=1 Stop=3 Npts=3 Unit=GHz Inner=HB1_sweep_Pin measure Pin_avail_dBm = Pin measure Pin_deliv_W = real(0.5*HB1.V(\"Vin\",1)*conj(HB1.I(\"Iin\",1))) measure Pin_deliv_dBm = 10*log10(Pin_deliv_W*1000) measure IRL_dB = Pin_deliv_dBm - Pin_avail_dBm; input return loss measure Pout_W = real(0.5*HB1.V(\"Vout\",1)*conj(HB1.I(\"Iout\",1))) measure Pout_dBm = 10*log10(Pout_W*1000) measure Gp_dB = Pout_dBm - Pin_deliv_dBm ; power measure Gt_dB = Pout_dBm - Pin_avail_dBm ; transducer measure PDC_W = real(HB1.V(\"VDD\",0)*HB1.I(\"IDC\",0)) measure Eff = Pout_W/PDC_W*100 labelednets VDD Vin Vout"],[24,"walk","Line-by-line walkthrough",""],[24,"w-header","1 · Header comment","; netlist.cnl — generated from TestBench \"HBTest.csch\" Any line starting with ; is a comment. When circuitRF generates the netlist from a schematic it stamps a provenance line naming the source test bench. Comments are ignored by the engine."],[24,"w-define","2 · Cell definition ( define … end )","define MyFET (gate drain) parameters Periphery_mm=1 Sv = -0.837 ... SDD:X1 gate 0 drain 0 I[1,0]=_v1/50 I[2,0]=Periphery_mm*(...) end MyFET A define block declares a reusable cell — here a GaN FET model named MyFET with two ports, gate and drain . Inside: parameters Periphery_mm=1 — the cell's parameter interface with a default. An instance can override it (and X1 below does). Parameters pass top-down : the override is evaluated in the parent's scope, then used inside the cell. Sv = -0.837 , Sc = 0.71 , … — local variables (model coefficients) scoped to this cell. They feed the device equations. SDD:X1 gate 0 drain 0 … — a Symbolically-Defined Device : you write the port currents as equations and circuitRF differentiates them automatically for the solver. The four nets are two differential ports: port 1 = gate – 0 , port 2 = drain – 0 . In the equations, _v1 and _v2 are those two port voltages. I[1,0]=_v1/50 — the current into port 1 (a simple 50 Ω gate input); I[2,0]=… — the drain current, the GaN I–V law scaled by Periphery_mm . The index notation is I[port, weight] — weight 0 is the current itself. (The SDD equation grammar is covered under Components › SDD .) Cells vs. the top level Everything inside define … end is a template . It isn't simulated until it's instanced at the top level (see MyFET:X1 below). The lines outside any define block are the top-level test bench."],[24,"w-globals","3 · Global variables","Pin = 0 RFfreq = 2 GHz Top-level global variables , usable anywhere a value is expected. They're also sweepable — and both are swept by the analyses below ( Pin is the drive level, RFfreq the fundamental). A variable may carry a unit ( 2 GHz , 48 V , 10 dBm , 50 Ohm ); when it does, that unit wins wherever the variable is referenced. Any unit, not just the scaling ones Both kinds work here: the ones that scale a number ( GHz , mF , pH ) and the ones that only name a quantity ( V , A , W , dBm , Ohm , deg ). Before circuitRF 1.0 VDS = 48 V was a parse error while RFfreq = 2 GHz was fine; the two are the same thing now. The unit is only ever read off the end of something that is not already a valid expression, so an expression whose last token happens to spell a unit is left alone — x = 2 * f is a multiplication, not two femtoseconds."],[24,"w-components","4 · Components & instances","C:C1 Vin n1 C=1 mF L:L1 n2 n3 L=1 mH ... P1Tone:P1 n1 0 Pavl=Pin dBm Z=50 Ohm Freq=RFfreq Phase=0 deg Z[0]=1 Ohm Z[2]=30 Ohm Vdc:V1 n2 0 Vdc=-3.05 V Vdc:V2 VDD 0 Vdc=48 V MyFET:X1 n3 Vout Periphery_mm=1 IProbe:Iout Vout n6 ... Each line is Type:Name net1 net2 … key=value … . The first tokens after the name are the nets the component's pins connect to (order = the component's pin order); the key=value tokens are parameters . Nets named 0 are ground. Reading the key lines: C:C1 Vin n1 C=1 mF — a capacitor between nets Vin and n1 . The large 1 mF caps and 1 mH chokes here are DC-block / bias-feed elements. P1Tone:P1 … — the RF power source driving the input. Pavl=Pin dBm sets the available power from the swept Pin variable; Freq=RFfreq takes the fundamental from RFfreq ; Z=50 Ohm is the fundamental source impedance. Z[0]=1 Ohm and Z[2]=30 Ohm set the source termination at the baseband (DC) and 2nd-harmonic zones — harmonic source-pull terminations. Vdc:V1 n2 0 Vdc=-3.05 V and Vdc:V2 VDD 0 Vdc=48 V — the gate and drain DC bias supplies (fed to the device through the L chokes). MyFET:X1 n3 Vout Periphery_mm=1 — an instance of the MyFET cell: gate→ n3 , drain→ Vout , with the Periphery_mm parameter overridden (here to the same value as the default). IProbe:Iout Vout n6 — a 0 V series current probe (ammeter). Its instance name ( Iout , Iin , IDC ) is how measurements read its branch current, e.g. I(\"Iout\", 1) below. VProbe:Vg n3 — a one-terminal voltage probe . It stamps nothing: the elaborator reads the one net it names and publishes that net's voltage under the probe's own instance name, so V(\"Vg\") reads it and the trace picker lists it. On a net the schematic never named, extraction has already written the probe's name as the NET's name — you will see VProbe:Vg Vg — and there is one row in the results; on a net a label already named, the probe's name is a second name for it and both appear. Its name must not collide with a net name or with another VProbe: either refuses the run. A VProbe naming a net nothing else reaches reports nothing and says so."],[24,"w-analyses","5 · Analyses (HB + parametric sweeps)","analysis HB1 type=hb Tone=\"RFfreq\" ToneUnit=MHz MaxHarm=5 FFTOverSample=1 Tol=1e-6 DriveStepping=IfNecessary GuardHarmonic=0 Lambda=1 MaxIter=100 analysis HB1_sweep_Pin type=parametric_sweep Var=Pin Start=0 Stop=30 Step=1 Inner=HB1 analysis HB1_sweep_RFfreq type=parametric_sweep Var=RFfreq Start=1 Stop=3 Npts=3 Unit=GHz Inner=HB1_sweep_Pin The first line is the harmonic-balance analysis; the next two wrap it in swept variables. Each analysis line is analysis Name type=… key=value … . The HB settings: Setting Meaning Tone The fundamental tone — here the variable RFfreq . ToneUnit Unit applied to the tone value when it doesn't carry its own (a unit on the variable wins). MaxHarm Highest harmonic order kept in the spectrum (5 → DC + 5 harmonics). FFTOverSample Time-grid oversampling factor for the nonlinear FFT (1 = minimum). Tol Newton convergence tolerance on the harmonic-balance residual. DriveStepping Power/source continuation: IfNecessary ramps the drive only when a direct solve won't converge. GuardHarmonic Extra guard harmonic(s) beyond MaxHarm for aliasing safety (0 = none). Lambda Newton damping factor (1 = full step). MaxIter Maximum Newton iterations per solve point. HB settings are detailed in Simulations › Harmonic Balance . The two parametric_sweep lines compose by Inner= : HB1_sweep_Pin runs HB1 at each Pin from 0 to 30 dBm in 1-dB steps (31 points); HB1_sweep_RFfreq runs that whole power sweep at each of 3 frequencies from 1 to 3 GHz ( Npts=3 , Unit=GHz ). The result is a 3-frequency × 31-power harmonic-balance sweep — frequency is the outer (slow) axis, power the inner. Sweeps wrap, they don't replace A parametric_sweep never changes the inner analysis; it re-runs it across a variable. Nest them with Inner= to sweep more than one variable. The innermost non-sweep analysis ( HB1 ) is what each point actually solves. What a directive may say type= takes one of six tokens, and each one has a fixed set of keys: type= Verb that runs it Keys it must have dc dc (none) sparam sparam start , stop hb hb Tone , or Tone[1] for a multi-tone run loadpull lp Tone , LoadTuner , SourceTuner , Grid loadpull_pursuit lpp Tone , LoadTuner , SourceTuner parametric_sweep (the inner analysis's) Var , Inner , and either Values or Start + Stop A key circuitRF does not recognise is refused, by name, with the legal ones listed — and so is a type= token it does not recognise. Every missing required key is reported at once rather than one per run. Before 1.0 an unrecognised key was accepted and thrown away in silence, so a directive could check clean and then run something quite different from what it said. The schematic editor's own key names are accepted as aliases, since a natural way to write a directive is to copy them out of a saved .csch : an Lp / Lpp / Psa prefix and an Expr / Name / Path suffix are stripped, so LpLoadTunerName= is LoadTuner= and PsaVarName= is Var= . So are the editor's short type tags — sp , lp , lpp , sweep . Frequency units on a sparam directive A frequency may state its unit three ways, and they mean the same thing: analysis SP1 type=sparam start=0.5 GHz stop=6 GHz npts=551 analysis SP1 type=sparam start=0.5 stop=6 npts=551 Unit=GHz analysis SP1 type=sparam start=\"0.5\" startUnit=GHz stop=\"6\" stopUnit=GHz npts=551 Unit= sets startUnit , stopUnit and stepUnit together; any of the three given individually wins over it. A bare number with no unit anywhere is hertz. Changed in 1.0 Unit= used to be read by parametric_sweep and silently ignored by sparam , so the second line above swept 0.5 Hz to 6 Hz . It reported no error at any stage, and the S11 it produced was flat and entirely plausible-looking — the terminations' DC reflection coefficient, nine orders of magnitude from the band that was asked for. If you have a .cnl written against the old behaviour that deliberately relied on the unit being dropped, it will now sweep the band it states. An exported Touchstone file's declared unit follows its data, so a header"],[24,"w-measures","6 · Measurements","measure Pin_avail_dBm = Pin measure Pin_deliv_W = real(0.5*HB1.V(\"Vin\",1)*conj(HB1.I(\"Iin\",1))) measure Pout_W = real(0.5*HB1.V(\"Vout\",1)*conj(HB1.I(\"Iout\",1))) measure PDC_W = real(HB1.V(\"VDD\",0)*HB1.I(\"IDC\",0)) measure Eff = Pout_W/PDC_W*100 measure IRL_dB = Pin_deliv_dBm - Pin_avail_dBm; input return loss A measure line computes a named result from an expression, evaluated at every sweep point. Expressions use the same engine as everywhere else, plus accessors into the run's data: HB1.V(\"Vin\", 1) — the voltage at net Vin from analysis HB1 , at harmonic 1 (the fundamental). HB1.I(\"Iin\", 1) is the current through probe Iin at the fundamental. Harmonic 0 is DC — used for PDC_W . real(0.5·V·conj(I)) is average power into a port; Pout_W / Pin_deliv_W apply it at the output and input. Eff is drain efficiency in percent. Measurements can reference earlier measurements ( IRL_dB uses Pin_deliv_dBm and Pin_avail_dBm ). A trailing ; text on a measure line is a comment/label (e.g. ; input return loss ). Results land in the run dataset and plot like any other signal. (Authored on a schematic, these come from a MEAS component — see Components › MEAS .)"],[24,"w-labeled","7 · Labeled nets","labelednets VDD Vin Vout Records which nets the user explicitly named (versus auto-numbered nodes like n1 ). The Data Display uses this to default its node picker to the meaningful nets, so you see VDD , Vin , and Vout first rather than every internal node. See also: Simulations for the full analysis settings and algorithms · Components for each part and its parameters · New User's Guide for a gentler introduction."],[25,"","","Plots and tables, trace cards, axes, markers — and how a run's cubes become curves. The Data Display is where results are looked at. It is a document , like a schematic or a layout: it opens as a tab in the workspace, tears off into its own window, and is saved to disk as a .cdd file that reopens exactly as you left it."],[25,"free-floating","It plots files, not runs","The one thing to understand first A Data Display is not bound to one schematic or one run. It reads files — .npy results, Touchstone .sNp , measured load-pull — and it will happily hold traces from several at once. That is what lets you overlay a measurement on a simulation, or last week's baseline on today's, without either of them knowing about the other. Practical consequences: A run overwrites its results file, and every trace bound to that file refreshes. Nothing needs to be re-added. To keep a baseline, name a different results file for that run — see Where results live — and point a trace at it. A trace remembers the file it came from, so a saved .cdd reopens against the same data. A new Data Display starts empty . Nothing is auto-plotted; you author it."],[25,"anatomy","Anatomy of a display","The Data Display document. A display holds one or more tabs ; each tab is a canvas holding plots ; each plot holds traces ; a trace may carry markers . Plots are moved and resized on the canvas directly. The Plot Inspector — one panel per selected plot — is where the plot's type, its traces and its axes are edited. There are four plot types, and which one will accept which data is the single most important rule in the Data Display. It has its own page: Plot Types ."],[25,"cubes-to-traces","From a DataSet to a curve","A run produces one DataSet : an ordered map of groups to named DataCubes . One group per analysis — HB1 , DC1 , SP1 — plus a measurements group, plus a default group ( \"\" ) that a flat Touchstone file lands in. Cubes are addressed Analysis.Cube : HB1.V , SP1.S , measurements.PDC . A bare name resolves in the default group first, then in measurements , so you can write PDC and mean measurements.PDC . An analysis cube must stay qualified — a bare V would find the wrong group. A DataCube is an N-dimensional array, real or complex throughout, with named axes . Each axis has numeric values, a unit, and sometimes string labels — the node axis of V is labelled with your net names (and with the name of any VProbe you placed, which adds a row for the net it taps), and the branch axis of I with your IProbe names. Cube Axes, from single-point to fully swept Kind V [node] → [node, harmonic] → [sweep…, node, harmonic] Complex for HB, real for DC I [branch] → [branch, harmonic] → [sweep…, branch, harmonic] Complex or real S [freq, i, j] , or [sweep…, freq, i, j] Complex a measurement a scalar, or [sweep…] Real or complex A trace is a one-dimensional slice through a cube. Authoring a trace means saying, for each axis of the cube, what to do with it: use it as the X axis, iterate it as a family of curves, or pin it to one value. That is the whole model, and the trace card is its interface."],[25,"trace-card","The trace card","The Plot Inspector: a trace card reading S(2,1) from a swept S-parameter run. Reading top to bottom: Identity — what to plot. A group selector (an analysis, or Measurements) and then an item selector. For an analysis group the item selector is a compact V / I pair, because voltage and current are deliberately symmetric: one mental model, one set of controls. Both are always offered; if you pick one whose cube is not in the result you get an explicit empty state rather than a blank card. For S-parameters, an item is a matrix element. Axis roles — how to slice it. One row per axis of the cube, each offering three roles: Role What it does X Use this axis along the bottom of the plot. Exactly one axis may be X — or none, which makes the trace a scalar. Fam Iterate this axis as a family of curves. At most one, and it needs an X. Fix Pin this axis to a single value, chosen in the selector beside it. The node/branch label axis defaults to Fix — it is a selector, never the X axis. The default X prefers freq when the cube has one, then falls back to the first non-label axis. A cube with no non-label axis at all — a DC operating point with no sweep — has no X and resolves to a scalar, which is what a Table is for. The eye on the label-axis row reveals the entries that are normally hidden: every node beyond the ones you named, and every device-port branch beyond your IProbes. It is one control shared by the node and branch rows. A net label and a VProbe both count as naming a node, so both are visible without it. Spec — the transform, and the shorthand text box (below). Style — line, symbol, per-port Z0 for a network trace, and the number format on a Table. Derived metrics. Beneath the matrix elements the item selector also offers quantities computed from the S-matrix rather than read out of it — stability factors, maximum gain, passivity, group delay, and the ESR / C / L / Q readouts a passive part is specified in. Selecting one reveals its own controls: the ordered port pair, and for a passive readout the Fixture that decides how the two-terminal impedance is recovered. Every equation is written out in Derived Metrics ."],[25,"against-a-harmonic-balance-result","Against a harmonic-balance result","A trace card configured against a harmonic-balance drive sweep. The same card, pointed at an HB drive sweep. The extra axes are what change: V now carries a harmonic axis and a swept-power axis alongside the node axis, so there is a real choice to make about which is X, which is the family, and which is pinned. Simulated S-parameters and a Touchstone file take different paths They look the same on the plot and they are not the same underneath. A Touchstone file lands in the default group and is handled by the network path — group \"S-Parameters\", matrix-element items, per-port Z0 available. An S-parameter run result lands in a named group ( SP1 ) and is a first-class cube: freq defaults to X, i and j become 1-based port selectors, and dB20 is the default transform on a rectangular plot. The Z0 cube beside it is a per-port reference impedance, not a signal, and is never offered as one."],[25,"shorthand","The spec shorthand","The text box on the spec row is a two-way view of the binding: every control on the card writes it, and editing it rewrites every control. The grammar is: [transform] CubeName[ token, token, … ] transform — optional, either prefixed ( dB20 V[…] ) or as a function ( mag(V[…]) ). One of dB20 dB10 dB mag phase real imag conj . CubeName — qualified ( HB1.V ) or bare ( PDC ). token — one per axis, in the cube's own axis order: Token Role : the whole axis, as X a:b a narrowed range, as X ~ this axis is the family \"Vout\" fix to a labelled entry — a net or probe name 3 fix to index 3 A bare CubeName with no brackets means the whole cube — every axis : . A spec with no : and no ~ at all is a scalar , valid on a Table: DC1.V[\"Vout\"] . Port numbers are 1-based; every other index is 0-based On an S/Y/Z cube's i and j axes a bare integer is a port number , so SP1.S[:, 2, 1] is S21 and SP1.S[:, 1, 1] is S11 — the way an RF engineer names them. Every other axis ( freq , a sweep, harmonic ) uses a 0-based index, and labelled axes use quoted names. A port outside 1..N is reported, not clamped. Validity is checked as you type: exactly one X or none, at most one ~ , and a ~ needs an X. Anything else is reported inline under the box rather than silently producing a different curve."],[25,"families","Families of curves","A family is one trace object that renders N curves — not N traces. Mark an axis Fam (or type ~ in its position) and every value of that axis draws its own curve, with one legend entry and one set of style controls governing all of them. dB20 HB1.V[:, \"Vout\", ~] every harmonic of Vout, versus the swept axis SP1.S[~, :, 2, 1] S21 versus frequency, one curve per sweep point That distinction matters when you change your mind about the sweep: you edit one card, not twenty."],[25,"versus","Plotting against another quantity","A spec may end with vs <x-spec> , which plots the trace against that quantity instead of against the cube's own swept axis: Gain vs Pout dB20(HB1.V[:, \"Vout\", 1]) vs Pout This is how you get gain against output power rather than against drive, which is the form a PA designer actually wants. On the card it is the vs X row. The X side inherits the Y side's swept axis and family by axis name, so you rarely have to restate them, and the X side may even come from a different loaded file."],[25,"axes","Axes and limits","Each plot carries axis labels and limits of its own, edited from the Plot Inspector. A trace may be assigned to a secondary (right-hand) Y axis on a rectangular plot, which is how gain in dB and efficiency in per cent share one frame legibly. Autoscale is on until you set a limit; setting one turns it off for that axis, so a figure you have framed deliberately does not re-frame itself on the next run."],[25,"db-floor","The decibel floor","A decibel trace bottoms out at −250 dB. Below that the curve is drawn flat at −250, and a readout says so with the inequality rather than a bare number — a marker info box reads dB(S(1,1)) ≤ -250.00 , a table cell ≤ -250.00 . This matters more often than it sounds, because a quantity that is analytically zero does not come back as zero. An ideal 50 Ω line terminated in 50 Ω has S(1,1) ≡ 0, and what the solver returns is double-precision roundoff: magnitudes scattered over a few orders of magnitude with the occasional exact zero mixed in. Converted to dB with no floor, that is a trace flickering across a 59 dB band with spikes to −∞ — and autoscale then sizes the axis to the flicker, so anything real sharing the plot is squashed into a line. The floor turns the noise into a flat line at the bottom of the frame and lets the rest of the plot keep its scale. −250 dB was chosen to sit below anything a result can physically mean and above anything roundoff can produce: thermal noise power in a 1 Hz bandwidth is −204 dB(W), while roundoff on a quantity of order one reaches −285 dB. dB , dB10 and dB20 all share the one floor. The floor is applied to the picture only. A measurement expression, and any value you export to .npy , .mat or Touchstone, carries the unclamped number — so dB(S(1,1)) written as a measurement can report a value below −250 dB even while the plot of the same quantity shows the floor. A number's value never depends on whether it happened to be drawn."],[25,"markers","Markers","Drop a marker on a trace to read its value. Markers are per-trace, they persist in the saved .cdd , and on a load-pull contour they read the interpolated surface rather than the nearest grid point — which is the point of fitting a surface at all. The maximum-power and maximum-efficiency locations are flagged on a contour plot automatically."],[25,"contours","Load-pull contours","Load-pull contours on the Gamma plane, interpolated from a 61-point termination grid. A load-pull sweeps the termination Γ presented to the device over a grid, and at each grid point drives the device up in power to a target gain compression. The raw result is therefore two-axis — {grid point, drive step} — and every figure of merit is a value over that field. Contours are how you look at it. The form a contour takes is always the same: a metric, at a constant value of a different metric. Pout at constant 3 dB compression. Efficiency at constant Pout. Gain at constant back-off. A contour trace card: the metric, the constraint, the levels and the interpolation. The contour card asks for exactly that: Metric — what is being contoured (Pout, PAE, gain, …). Constraint — the other metric held constant, and its value. Compression is the common case and has its own control. Levels — either a start/step/stop range or a count, plus whether to draw iso-lines, fills or labels. Substrate — the Γ plane (a Smith chart) or the Z plane (a rectangular plot). Frequency — pinned, for a frequency-swept load-pull. Interpolation — the radial-basis kernel, its smoothing and its shape parameter. Reading the figure: each closed curve is a locus of terminations that deliver the same metric value, and the labels give that value. The P and E markers are the maximum-power and maximum-efficiency optima; the distance between them is the trade-off the design has to spend. The grid points themselves can be shown, and it is worth doing at least once: a contour is an interpolated surface, and it can only be trusted where the grid actually surrounds it. Contours that run off the edge of the constellation are extrapolation. Background on the method and the surface model: Loadpull / Sourcepull and Loadpull Pursuit ."],[25,"toolbar","The toolbar","Data Display toolbar # Button What it does 1 — no tooltip 2 Add Rect Plot (Ctrl+Shift+A) 3 Add Smith Plot 4 Add Polar Plot 5 Add Table 6 New Tab (Ctrl+T) 7 Zoom In (Ctrl++) 8 Zoom Out (Ctrl+-) 9 Actual Size (Ctrl+1) 10 Fit All (F, Ctrl+0) 11 Undo (Ctrl+Z) 12 Redo (Ctrl+Shift+Z) 13 Save Display (.cdd) 14 Open Display (.cdd) 15 Export Data The groups, left to right: the source picker , which chooses the data source new traces bind to; the four plot types , which add a plot of that kind; add trace ; the zoom and fit controls; undo and redo , which cover the whole display and not just the selected plot; and save , open and export . See also: Plot types · Results & data export · Measurements · Simulations ."],[26,"","","Stability, passivity, group delay and the two-terminal passive readouts — every equation, written out. A derived metric is a quantity the Data Display computes from an S-matrix rather than reading out of it. Stability factors, gain limits, passivity, group delay, and the impedance readouts a capacitor or an inductor is actually specified in — none of these is a matrix element, and all of them are offered on the trace card beside the elements themselves. This page exists so that you never have to guess which convention was used. Every derived metric below is given as the equation the code evaluates , in the same symbols, so that a number you disagree with can be traced to an assumption rather than to a mystery."],[26,"where","Where they live, and what they will plot on","Add one exactly as you would add a matrix element: select a network source in the trace card's item selector and pick the derived metric from the list beneath the S-parameter elements. A trace card reading ESR from a two-port part file, with the Fixture control that decides how the two-terminal impedance is recovered. Picking one reveals its own controls — the ordered port pair, and for a passive readout the Fixture row, which is the subject of its own section below . Two kinds, and they do not mix in one plot: Kind Members Plots on Scalar versus frequency μ, μ′, K, |Δ|, MaxGain, passivity, group delay, and all six passive readouts Rectangular, Table Γ-plane locus Source and load stability circles Smith, Polar Picking one on the wrong plot type is not an error and is not silently ignored — the item is offered but disabled, with the reason on it . See Plot Types ."],[26,"common","What every derived metric assumes","Three assumptions are shared by everything on this page. They are stated once here rather than repeated under each formula. 1 — The reference impedance is made uniform and real first. Every formula below is derived for a single real Z 0 . A simulated S-matrix may be referenced to a different impedance at each port, and those may be complex; a Touchstone file is uniform by construction but need not be 50 Ω. So before any of these is evaluated, the matrix is renormalised — using the full power-wave form, on the whole matrix, never element by element. The target is Re(Z0) of the input port , which is why a 75 Ω part and its Touchstone file give identical answers instead of both being forced to 50 Ω. 2 — The port pair is ORDERED. For a 2-port formula the trace card offers an input and an output selector, and swapping them is a different selection, not a re-spelling of the same one. μ is the load stability factor and μ′ the source one, so (1,2) and (2,1) exchange their meanings. On a 2-port the selectors are hidden — input 1, output 2 is the only sensible reading. 3 — On an N-port, the other ports are assumed terminated in the reference impedance. Extracting a 2×2 sub-matrix from a 4-port is valid only under that assumption. It is the standard one and it is almost always what you want, but if you are comparing against a bench measurement that terminated port 3 in something else, that is where the disagreement comes from. The trace card says so in a note under the port selectors whenever N > 2. Δ, once, for the whole stability section Every stability formula below uses the determinant of the 2-port sub-matrix: Δ = S11·S22 − S12·S21"],[26,"stability","Stability: μ, μ′, K, |Δ|",""],[26,"load-stability-factor-edwardssinsky","μ — load stability factor (Edwards–Sinsky)","μ = (1 − |S11|²) / ( |S22 − Δ·S11*| + |S12·S21| ) μ > 1 ⟺ unconditionally stable. Its great advantage over K is that it is a single number with a direct geometric meaning: μ is the distance from the centre of the Smith chart to the nearest unstable load. So a larger μ is unambiguously more stable, and two designs can be compared by it. K cannot do that."],[26,"source-stability-factor","μ′ — source stability factor","μ′ = (1 − |S22|²) / ( |S11 − Δ·S22*| + |S12·S21| ) The same measure taken from the source side: the distance to the nearest unstable source termination. Note that μ and μ′ come from the same trace card control — selecting μ with the ports swapped gives μ′, which is why the port pair being ordered matters."],[26,"k-rollett-stability-factor","K — Rollett stability factor","K = (1 − |S11|² − |S22|² + |Δ|²) / (2·|S12·S21|) Unconditional stability requires K > 1 and |Δ| < 1 — both, always. K alone is not a stability criterion, which is the reason |Δ| is offered as its own trace rather than folded into K's. Plot them together. And both together are still a two-port test. A two-port that passes K and |Δ| can still oscillate, because these are computed from the reduced two-port and a pole the reduction hides is a pole they cannot see — see the WSProbe page's NDF example ."],[26,"section","|Δ|","The magnitude of the determinant above, plotted on its own so that the K > 1 test can be completed."],[26,"gain","Maximum gain: MAG and MSG","One trace, one formula, and which of the two classical quantities you get depends on K at that frequency: |S21| ratio = ------- |S12| K ≥ 1 (MAG) G = ratio · ( K − √(K² − 1) ) maximum available gain K < 1 (MSG) G = ratio maximum stable gain The transition is exactly at K = 1 and it is continuous there, so a single curve runs across the potentially-unstable region into the unconditionally-stable one without a discontinuity — which is what makes the plot readable in the first place. The card offers this in dB ( 10·log10 G ) or as a linear power ratio . The two come from the same implementation rather than the display taking a logarithm of the other, so they cannot disagree."],[26,"circles","Stability circles","Loci in the Γ plane, one circle per frequency. The load circle is the set of Γ L that put |Γ in | = 1; the source circle is the set of Γ S that put |Γ out | = 1. Load CL = (S22 − Δ·S11*)* / (|S22|² − |Δ|²) rL = |S12·S21| / | |S22|² − |Δ|² | Source CS = (S11 − Δ·S22*)* / (|S11|² − |Δ|²) rS = |S12·S21| / | |S11|² − |Δ|² | circuitRF also decides, per frequency, whether the stable region is inside or outside the circle and renders it accordingly — that is not a cosmetic choice, and reading a circle without it is how a \"stable\" design gets built around the unstable half of the chart."],[26,"passivity","Passivity, σ max","σmax = the largest singular value of S The network is passive at that frequency iff σ max ≤ 1 . The value says how far from passive, not merely whether: 1 is the boundary, 1.05 is 5 % of excess gain, and a well-behaved passive file sits just below 1 everywhere. Two things make this different from everything else on this page: It is not a 2-port formula. It is defined for any N ≥ 1 (at N = 1 it is just |S 11 |), so it needs no port pair. Scope is a choice above 2 ports. The card offers whole network (the default) or the extracted input/output pair. They answer different questions, and a sub-matrix can test passive while the full network is not — so if you are checking a de-embedding or an EM result for physicality, you want the whole network."],[26,"groupdelay","Group delay","τ(ω) = −dφ/dω φ = the UNWRAPPED phase of S21 (input → output) Plotted in nanoseconds . Three details that decide whether the number is meaningful: The phase is unwrapped first. A raw atan2 phase is a principal value in (−π, π], so any network whose transmission runs through more than half a cycle wraps somewhere inside its own passband. A difference taken across that wrap is a spike one grid-step wide that moves when you change the grid . The derivative is a central difference on the sweep's own grid (one-sided at the two ends), so the result has exactly as many points as the sweep. The grid need not be uniform. The sweep must resolve the phase to better than half a cycle per step. That is the one real limit: a sweep too coarse for that cannot have its group delay measured by any method, and unwrapping will quietly guess wrong rather than fail."],[26,"passive","The two-terminal passive readouts","Six quantities, and they exist for a specific reason: a capacitor, an inductor or a ferrite bead is specified, bought and compared in ESR, C, L, Q and self-resonance — never in S-parameters. When a vendor ships you both an S-parameter file and a model, the question you actually want to ask is whether the two agree, and answering it from S 11 in dB is possible but nobody does it. All six are point functions of one impedance Z(f) = R + jX , extracted from the S-matrix by the fixture you select. With ω = 2πf : Trace Equation Unit Defined where |Z| \\|Z\\| Ω everywhere ESR R = Re Z Ω everywhere Reactance X X = Im Z Ω everywhere C effective C = −1 / (ω·X) F X < 0 only — the part is capacitive L effective L = X / ω H X > 0 only — the part is inductive Q Q = \\|X\\| / R — R > 0 only Where a quantity has no value, the trace ends C eff above the self-resonance and L eff below it are the routine cases: the part is not a capacitor up there and asking for its capacitance has no answer. Those points are dropped — not clamped to zero, not folded to a positive value, not plotted negative. A folded-back capacitance on a log axis reads as data, which is the failure worth avoiding. The same rule gives Q no value where R ≤ 0, which is what a de-embedding that has gone wrong looks like. The traces are left in base SI — ohms, farads, henries. Unlike group delay, where nanoseconds are universal, the capacitors in one plot can run from picofarads to millifarads and there is no single scale that would not be wrong for half of them. The unit is on the trace's own label."],[26,"fixture","The fixture: the one thing you must tell it","Nothing in a Touchstone file records how the part was measured , and the readings differ by orders of magnitude. So the trace card carries a Fixture selector, and it is an explicit choice rather than something circuitRF tries to detect. Fixture The DUT is Equation Shunt-through (default) a shunt element bridging two through lines Z = (Z0/2)·S21/(1 − S21) Series-through in series with the through line Z = 2·Z0·(1 − S21)/S21 1-port the only thing at the port Z = Z0·(1 + S11)/(1 − S11) Shunt-through is the default because it is how vendor decoupling-capacitor and inductor files are measured. It is the fixture with usable dynamic range on a part that is milliohms at resonance: there, S 11 sits within a part in 10⁴ of −1, where a network analyser's directivity and calibration residuals dominate, while the same impedance appears in S 21 as a well-conditioned transmission measurement. A file with only one port has no S 21 to read and is always taken as a 1-port, whatever the selector remembers. The wrong fixture is smooth, finite and plausible Reading a shunt-through file as a 1-port returns exactly Z ∥ Z0 . Work through what that means: the misread is close to right wherever |Z| ≪ Z 0 — which for a bulk decoupling capacitor is most of its band — and saturates at Z 0 everywhere else. A 10 pF capacitor, an inductor or a bead comes back as ≈ 50 Ω, and its capacitance with it. There is no NaN, no discontinuity and nothing on the plot to notice. That is why the fixture is a control you set rather than a guess circuitRF makes on your behalf."],[26,"srf","Self-resonance","The self-resonant frequency is reported on the trace card whenever a passive readout is selected, and it is defined as: the lowest frequency at which the reactance crosses zero going capacitive → inductive , interpolated in log f . The direction is the whole definition and an unsigned zero-crossing search is wrong. A real part has several crossings: the series resonance where |Z| is a minimum — the number everyone means by \"SRF\" — and above it the mounting or package parallel resonance where |Z| is a maximum and the reactance crosses back the other way. The sign of the crossing is what separates them. If the sweep contains no such crossing, the card reports nothing rather than a band edge. \"This sweep does not contain the resonance\" is the honest answer; a clamped band edge would be read as a measurement. Interpolation is in log f because these sweeps are logarithmic, and interpolating linearly across a decade-wide gap places the crossing visibly wrong."],[26,"datavsmodel","Checking data against a model","The workflow these readouts were built for: Add the vendor's .sNp as one trace source, and a schematic built from the vendor's model as another. A Data Display reads files, not runs , so both live in one plot without either knowing about the other. Add the same readout — ESR , or C effective — from each, on one rectangular plot with a log frequency axis. Set the same Fixture on both. Where the two curves separate is where the model stops being the part. In practice that is almost always above the band the model was fitted over, and almost always in ESR before it shows in |Z| — the reactance dominates |Z| everywhere except within a whisker of resonance, so a model with the loss wrong by a factor of three still overlays convincingly on a |Z| plot. Compare ESR, not |Z|."],[26,"inside","Stability from inside the circuit","Everything on this page is computed from an S-matrix — a terminal view of a network. That is the right view for a part you are buying and the wrong one for a node inside an amplifier you are drawing, where the question is what the two halves of that node present to each other. The WSProbe answers that one. It is a component you drop into a node; it perturbs nothing and costs no extra sweep, and what it produces is the driving-point impedance and admittance at that node, the impedance looking each way out of it, every classical loop gain, and a bounded stability margin that says how close the node is rather than only whether it is stable. It also brings the normalized determinant function , which is the rigorous N-port answer that K and |Δ| are an approximation to. The two pages meet in both directions: a probe's reduced two-port is an ordinary network source, so every metric above — μ, K, |Δ|, MAG/MSG and the stability circles — applies to a stage measured inside the circuit, loaded by what actually loads it."],[26,"headless","Asking the same questions headless","Two CLI verbs answer without opening a plot. Both are read-only. Is this file sound? circuitrf check part.s2p Reports the port count, the sweep, the reference impedance, and then: a frequency axis that is not strictly increasing (an error — every interpolator here assumes a sorted axis and returns a plausible wrong number on one that is not), a reference impedance no renormalisation can use, a reference that is not 50 Ω (worth knowing, because it is carried silently into every comparison), σ max > 1, a reciprocity error, and a causality measurement. Passivity and reciprocity are reported as warnings, never errors , and they carry the number and the frequency. Nothing in a Touchstone file says what the part is: an amplifier is supposed to have gain and a circulator is supposed to be non-reciprocal. For a capacitor, either one is a defect. Only you know which you have. What the causality measurement can and cannot tell you circuitRF transforms the file to an impulse response and measures what fraction of its energy lands before t = 0. It runs only on a uniform frequency grid that reaches DC in one step — a log sweep, which is what most vendor passive files are, is reported as not evaluated with the reason, because resampling it would need an interpolation that is itself a low-pass and would manufacture the smoothness being tested for. When it does run and the number is high, two different things produce that and the measurement cannot separate them : the file may genuinely not be causal (a model fitted without a causality constraint is the usual cause), or the sweep may be too coarse to resolve its own response. A ferrite bead sampled on a grid too coarse for its impedance corner measures much like a delay line running backwards. Both matter if you intend to use the file in the time domain, so the finding is worth making — which is why it is worded as a measurement with two readings and never as a verdict. What is this part? circuitrf explain part.s2p Reports the ports, the sweep, the reference impedance, and then — under every applicable fixture, side by side — the self-resonance and the impedance floor with the ESR there. Reporting all of them is not indecision: it is the fastest way to identify an unlabelled file, because only the physical reading gives a sensible resonance and a milliohm-scale floor, and the others are obviously nonsense next to it. part.s2p (touchstone) ports 2 sweep 401 points, 1 MHz to 1 GHz, non-uniform reference impedance 50 Ω SRF (shunt-through) 22.507906 MHz |Z| min (shunt-through) |Z| = 5.057 mΩ at 22.387211 MHz, ESR 5 mΩ SRF (series-through) (nothing) |Z| min (series-through) |Z| = 796.177 Ω at 1 GHz, ESR 1.268 Ω Both verbs take --json , and every finding carries a stable diagnostic id. See the CLI reference ."],[27,"","","Nodal transfer functions for stability and in-situ design — the wsp matrix, the driving-point functions, the bidirectional impedances, the published stability margin, and an appendix on stability itself. A WSProbe is a two-terminal element you drop into a node. It is electrically a 0 V short, it changes no other answer in the run, and what it produces is not a number but a matrix — from which every stability quantity on this page is arithmetic. circuitRF implements T. A. Winslow, General Circuit Analysis Using The WSProbe (2023), and the two EuMIC papers that followed it. Every quantity below carries that work's own name and its own equation number , so a reader who has the documents can find H0 , Y0 , ZG , ZL , LG , F , SM_Y0 , SM_H0 and wsp(2i−1, 2j) here under exactly those spellings. Citations are (Eq. n) for the 2023 document, (M-Eq. n) for the 2024 stability-margin paper and (E-Eq. n) for the 2025 envelope paper; all three are listed in References ."],[27,"what","What the WSProbe is","WSProbe Two terminals and an orientation: G is the generator side and L the load side. Placed into a node, it splits that node into a G-side terminal and an L-side terminal, exactly as an IProbe does. Electrically it is a 0 V short. It perturbs nothing, and it needs one analysis sweep (§4, p. 33: \"completely nonperturbative … requiring only a single analysis sweep\"). A design simulates identically with every WSProbe deleted, and the S-parameters, the operating point and every measurement of a probed run are the numbers the unprobed run gives. What changes is only what the run reports . What it reports is the wsp matrix . For each probe the analysis applies, mathematically, two vanishingly small auxiliary generators — a series voltage vS in the probe's own branch and a shunt current iP into its G-side node — and reads, at every probe, two responses: the branch current iS flowing G → L, and the G-side node voltage vP . The two injections and the two readings, for one probe. vS is a series voltage in the probe's own branch, minus at G and plus at L ; iP is a shunt current injected into the G-side node. The responses read at every probe are the branch current iS , flowing G to L, and the G-side node voltage vP . (Redrawn for this page; the reference document's Fig. 16 is the original.) Normalising those four combinations gives the four transfer functions per probe pair, and with N probes they stack into one 2N × 2N matrix, 1-based, rows = stimulus probe, columns = response probe (Eq. 31–36)."],[27,"placing","Placing it","Drop it onto the wire whose node you want to look at. The wire is cut between its two pins, exactly as it is for an IProbe — the probe breaks into the run instead of being shorted by it. The letters are the orientation, and they are the pin order. Rotating or mirroring the symbol moves the drawing and never the pin order, so swapping G and L means swapping the wires — which swaps every G / L -labelled output and negates nothing else. Where to put probes. The reference document's own advice (§4, p. 36): on the terminals of active devices — the nodes \"where the transfer functions have the greatest chance of detecting … the Kurokawa signature\". A gate node, a drain node, and the two terminations if you intend to use the envelope . The label is the instance name , and a probe inside a sub-cell carries its full instance path: X1.GATE . Probes work at any depth of hierarchy (§4.3). In a netlist the spelling is two nets, G first: WSProbe:GATE n_gate n_gate_int Each probe is given an idx at elaboration, in flattened netlist order, 1-based. That number is what the document's equations index by and what a measure line types, so it is reported rather than guessed: circuitrf explain --analysis lists it, the trace card shows it beside every probe label, and the run writes a __WspProbes metadata cube carrying the label ↔ idx pairs."],[27,"computes","What it computes","The wsp matrix (Eq. 31–34), for probes i (stimulus) and j (response): wsp(2i−1, 2j−1) = iS_j / vS_i series current response to series voltage (= Y0 when i = j) wsp(2i−1, 2j ) = vP_j / vS_i shunt voltage response to series voltage wsp(2i, 2j−1) = iS_j / iP_i series current response to shunt current wsp(2i, 2j ) = vP_j / iP_i shunt voltage response to shunt current (= H0 when i = j) The indexing rule is stated once and then used everywhere (Eq. 33): odd rows and columns are the series half, even rows and columns the shunt half; the row is the probe that was driven and the column is the probe that was read. The matrix is stored 1-based with integer row / col axis values, so wsp(3, 13) in the document is wsp(3, 13) here with no index arithmetic in between: SP1.wsp → the whole cube, over {freq, row, col} SP1.wsp(3, 13) → one element traced over frequency (Eq. 36) SP1.idx(\"GATE\") → the probe's idx, as an integer"],[27,"the-six-default-outputs","The six default outputs","Every probe writes six named results, computed by the engine from wsp at run time — through the same library functions the trace card and a measure line call, so a run's ZG:GATE cube and a card's wsp_ZG of the same probe are bit-identical by construction: Output What it is Equation H0 driving-point impedance at the node, vP/iP Eq. 50 Y0 driving-point admittance in the branch, iS/vS Eq. 52 ZG the impedance looking out of G , under series stimulus Eq. 67 ZL the impedance looking out of L , under series stimulus Eq. 68 LG the bilateral (Tian) loop gain around the probe Eq. 92 F the return difference, F = 1 − LG — Two more come with them — SM_Y0 and SM_H0 , the stability margin . Their cubes are named the way an ammeter's are ( I:IP1 ): H0:GATE , Y0:GATE , ZG:GATE , ZL:GATE , LG:GATE , F:GATE , SM_Y0:GATE , SM_H0:GATE , each over {freq} , with any parametric sweep axis prepended. Everything else is arithmetic on one 2 × 2 block The four entries of one probe's own diagonal block reduce it to a two-port — port 1 the G-side terminal, port 2 the L-side terminal — as an admittance matrix (Eq. 44) or an impedance matrix (Eq. 48). ZG , ZL , YG , YL , every loop gain, the open-port immittances and both stability margins are a few lines over that two-port. So is Rollett's K, μ, |Δ|, MAG/MSG and the stability circles — the reduced two-port is an ordinary network source, which is what lets you apply a two-port stability criterion to a stage inside an amplifier."],[27,"reading","Reading the results",""],[27,"the-primary-metric-two-polar-plots","The primary metric: two polar plots","Plot 1/H0 and 1/Y0 on a polar chart and look for Kurokawa's start-up signature (Eq. 107 and Eq. 108). In words, three conditions at one frequency: the real part is zero or negative; the imaginary part is zero — the locus is on the negative real axis; the imaginary part is increasing with frequency — the crossing is clockwise . The third is the whole test. A counter-clockwise crossing is the steady-state side of an ordinary, stable resonance, and reporting it would flag every resonant circuit ever drawn. Check both. A zero can mask the pole in one of the two functions, but never in both (§4.10, pp. 73–79). The document's own pair of examples is the demonstration: an unstable series resonator shows the signature in 1/Y0 alone, and an unstable parallel resonator in 1/H0 alone. The two driving-point loci of an unstable series resonator, a polar plot each. 1/Y0 crosses the negative real axis clockwise at the resonance; 1/H0 never does. Both must be checked, because a zero can mask the pole in one of them but never in both. The trace card does the looking for you. Pick 1/H0 or 1/Y0 in the card's WSProbe section and it reports the crossing frequencies it found — or none , which is a real answer and is printed as one. Mark crossings puts a marker at each. The trace card's WSProbe section: which probe the quantity is taken at, which of the reference document's quantities it is, and the Kurokawa reading beside it."],[27,"the-loop-gains","The loop gains","Every loop gain on this page — including the probe's own default LG and its return difference F = 1 − LG — is offered, and every one of them carries the reference document's caveat at full weight (§4.4, p. 50): Loop gains \"are not fundamental circuit quantities and are therefore not rigorous stability measurements … not necessarily wrong, just incomplete.\" They are diagnostic and they are useful. They are not the answer. The driving-point functions are, because H0 and Y0 carry the whole network determinant in their denominators (Eq. 26–30) and a loop gain does not. The case that makes this concrete is the one with no feedback at all : two blocks joined only through the probe. Every loop gain there is exactly 0 — there is no loop — and yet the node can oscillate, because the instability is between the two blocks rather than around a loop. That is Kurokawa's own case (pp. 62–63), and what carries the signature is the open-port immittances Zop and Yop (Eq. 89, 90) and the driving-point functions themselves."],[27,"the-bidirectional-impedances","The bidirectional impedances","ZG and ZL are the impedances the two sides present under series-voltage stimulation. YG and YL (Eq. 72–78) are the admittances they present under shunt-current stimulation. ZG is not 1/YG They agree only when there is no feedback across the probe — y12 = y21 = 0 (Eq. 79–82; §4.5, p. 56). Reading one as the reciprocal of the other is the single most common misreading of this probe. Only the sums are fundamental: ZG + ZL = 1/Y0 (Eq. 69) and YG + YL = 1/H0 (Eq. 77). Feedback around a probe means a path joining its two sides other than through the probe itself . Ground does not count: two one-ports that share only the reference have z12 = 0 , and a probe with a dangling one-port on its G side has ZG = 1/YG exactly however much feedback the rest of the network carries."],[27,"stability-margin","The stability margin","A driving-point locus answers yes or no . It cannot answer how close , and the reason is that it has units : 1/H0 is an admittance and its trajectory in the complex plane is set by the node's impedance level, so two devices of very different periphery tuned to the same Rollett K give loci differing by orders of magnitude. That is the problem the 2024 paper [M] set out to fix. Its starting point is that both driving-point functions are sums of two bidirectional immittances : 1/H0 = YG + YL (M-Eq. 1) 1/Y0 = ZG + ZL (M-Eq. 2) and that Kurokawa's condition on each is a statement about the relative size of the two halves — the real parts cancelling, the imaginary parts cancelling. Normalising each half against the other gives four bounded, unitless proxies (M-rY, M-iY, M-rH, M-iH): rY = 0 if Re ZG + Re ZL ≤ 0 = ½ (1 + Re ZL / Re ZG) if |Re ZG| ≥ |Re ZL| = ½ (1 + Re ZG / Re ZL) otherwise iY = ½ (1 + Im ZL / Im ZG) if |Im ZG| ≥ |Im ZL| = ½ (1 + Im ZG / Im ZL) otherwise rH, iH: the same two functions over Re YG, Re YL and over Im YG, Im YL and the two margins are their means: SM_Y0 = ½ (rY + iY) (M-Eq. 9) SM_H0 = ½ (rH + iH) (M-Eq. 10) Both are required ([M] §IV). SM_Y0 is the margin on the series stimulus and SM_H0 on the shunt one; a series-resonant instability is seen by one and a parallel-resonant one by the other. That is the pole masking of §4 again, in margin form."],[27,"how-to-read-it","How to read it","It is bounded, [0, 1] , and unitless. Scaling ZG and ZL by the same positive real leaves SM_Y0 unchanged, which is the cross-node comparability the margin exists for. dB here is 20·log10 — the ordinary dB(…) transform, the one circuitRF applies to every unitless magnitude. So [M]'s rule of thumb, investigate any sudden decrease below −15 dB , is 0.178 linear. (Under a 10·log10 convention the same rule would read 0.032; both numbers are printed here so that nobody is misled silently by whichever convention they assumed.) A conjugate-matched node reads −6 dB, not 0. SM_Y0 = 1 means ZL = ZG with positive real part; ZL = conj(ZG) gives rY = 1 , iY = 0 and SM_Y0 = 0.5 , which is −6.02 dB. A designer who expects a matched node to top the scale will read a perfectly good node as a problem. Below −12 dB, one side of the node presents negative resistance. If Re ZG > 0 and Re ZL > 0 then rY is above 0.5 and SM_Y0 ≥ 0.25 , which is −12.04 dB. So anything under that line certifies negative resistance at that frequency — it does not merely suggest it. The margin is a distance; the crossing search is the detector. Kurokawa's third condition — the clockwise sense — is deliberately not in the margin, which measures distance to the first two only. So the trace card prints a margin's minimum and the Kurokawa reading of the driving-point function that pairs with it: SM_Y0 with 1/Y0 , SM_H0 with 1/H0 . Reading one without the other is how a −40 dB margin at a frequency nothing actually crosses gets mistaken for an oscillation. A probe against a purely resistive termination reads the resonance on the other side only. The proxy normalises one reactance by the other ; a resistive side has none to offer, so iY sits at 0.5 and the margin on that stimulus is flat. That is the definition working, not a defect — but it decides where you put the probe."],[27,"what-it-looks-like","What it looks like","The figure is a series resonator with the reactance split across the probe : a 10 Ω source termination and 1 nH on the G side, 10 pF and a negative resistance on the L side, resonant at f0 = 1.5915 GHz . With R1 = −5 Ω the circuit is stable at that termination, and the margins still have a great deal to say. Both stability margins of a series resonator with the reactance split across the probe: SM_Y0 notches at the resonance and SM_H0's minimum is somewhere else entirely - pole masking, in margin form. The analysis' threshold and the -12 dB floor are drawn beneath them. Three readings, all from the fixture's own 2001-point sweep: SM_Y0 notches to −18.06 dB at 1.5913 GHz — at f0 , and its depth is set by the negative-resistance ratio. SM_H0 's minimum is −19.85 dB at 1.7337 GHz — a different frequency, the one at which Re(YG + YL) changes sign, although the impedance sum is positive everywhere. That is the pole masking, made visible on a circuit with one loop in it. The stable case is already below [M]'s −15 dB rule at f0 . A node one negative-resistance step from oscillating genuinely has little margin. The plot draws the two lines you read against A rectangular plot carrying a margin trace draws the analysis' own threshold and the −12 dB floor beneath it, once per distinct level. A polar plot carrying any WSProbe trace marks both critical points — the origin, which a driving-point locus is read against, and +1 , which a loop gain is read against."],[27,"the-threshold-message","The threshold message","MarginThreshold=<dB> on the S-parameter or harmonic-balance directive (default −15 , [M]'s own rule; MarginThreshold=none disables it) produces one note per probe whose min(SM_Y0, SM_H0) falls below it: [circuitRF] WSProbe 'P': stability margin below −15 dB (0.178): SM_Y0 = −18.1 dB at 1.59125 GHz, SM_H0 = −19.8 dB at 1.73375 GHz. A margin below −12 dB means one side of the node presents negative resistance there. It is a note, not a warning , and the resonator above is exactly why: that circuit is stable and fires it. The message means look here , not this is wrong . circuitrf explain --analysis prints the effective threshold beside the probe list, because the default is circuitRF's and not the paper's to state. Where the margin came from [M]'s own demonstration is an amplifier that passed every polar check and Rollett's criterion in simulation, and oscillated at 1.16 GHz when it was built. Re-read as a margin, the original small-signal simulation showed a −50 dB collapse at 1.06 GHz that no binary check had a way to report. A 10 Ω de-Q resistor fixed it."],[27,"metrics","The derived metrics","Everything the reference documents derive from wsp is available in two places that are one implementation: the trace card's WSProbe section , and a measure line . They are derived metrics , in the sense the derived-metrics page uses the term — quantities computed from a result rather than read out of it. Arguments are the document's, in its own order. A wsp cube, a probe index, a 2×2 network, or a scalar-per-frequency cube: measure ZGgate = wsp_ZG(SP1.wsp, SP1.idx(\"GATE\")) measure Yred = wsp_yparam(SP1.wsp, SP1.idx(\"GATE\")) measure margin = wsp_stability_margin(SP1.wsp, SP1.idx(\"GATE\"))"],[27,"one-probe","One probe","Function Equation Returns wsp_yparam(wsp, idx) Eq. 44 the reduced two-port as [Y] , S wsp_zparam(wsp, idx) Eq. 48 the reduced two-port as [Z] , Ω wsp_H0(wsp, idx) , wsp_Y0(wsp, idx) Eq. 50, 52 Ω, S wsp_ZG , wsp_ZL Eq. 67, 68 Ω — bidirectional impedances wsp_YG , wsp_YL Eq. 76 S — bidirectional admittances wsp_zop , wsp_yop Eq. 89, 90 Ω, S — the open-port immittances wsp_loopgain(Y, kind [, Z0]) Eq. 92, 96–104 one of eight loop gains wsp_nodal_gamma(wsp, idx) App. E.7 the nodal conjugate reflection coefficient wsp_unstable_freq_kurokawa(T) Eq. 107, 108 the crossing frequencies, in Hz encirculations(SP) , enc(SP) App. E.3 the running encirclement count wsp_impedance(wsp, i, j [, stimulus]) Eq. 37, 196, 197 Ω — the even-mode transfer impedance wsp_gain(wsp, S, G, D) App. D dB GainDEFs(GamS, SM, GamL) Eq. 203–208 GT_dB , GP_dB , GA_dB , Gmax_dB The kind strings for wsp_loopgain are the document's own: BI (bilateral / Tian, the default LG ), UNI (forward synthetic circulator; FOR is accepted for it too), REV , HST (Hurst), MB and MBR (Middlebrook forward and reverse), GFT and GFTR (general feedback theorem). Z0 defaults to 50 Ω and is read only by the two circulator kinds."],[27,"the-stability-margin","The stability margin","Function Equation Returns wsp_rY , wsp_iY , wsp_rH , wsp_iH M-rY, M-iY, M-rH, M-iH the four proxies, [0, 1] wsp_SM_Y0(wsp, idx) , wsp_SM_H0(wsp, idx) M-Eq. 9, 10 the two margins, [0, 1] wsp_stability_margin(wsp, idx) — min(SM_Y0, SM_H0) wsp_sm_z(ZG, ZL) , wsp_sm_y(YG, YL) M-Eq. 9, 10 the same formula over any pair you supply"],[27,"two-probes-and-a-set-of-them","Two probes, and a set of them","Function Equation Returns wsp_yparam2(wsp, idx1, idx2) Eq. 137–139 the eight entries y11 … yf22 wsp_yparam2(wsp, idx1, idx2, \"inner\"\\|\"feedback\") Eq. 137–139 that block alone, as a two-port wsp_yparam2_residual(wsp, idx1, idx2) — the pair's own diagnostic; see §6 wsp_block_calc(wsp, idx1, idx2 [, Z0]) Eq. 140–151 sixteen: s11 … LGM wsp_block_breakout , wsp_fb_breakout App. E.5, E.6 either block as a two-port at Z0 wsp_block_design , wsp_fb_design App. E.8, E.9 either block renormalised in situ wsp_bifurcate(wsp, form, side [, probes]) Eq. 161–176 the G-side or L-side subnetwork wsp_YA , wsp_YF , wsp_ZA , wsp_ZF Eq. 161–176 the same, under the document's own names wsp_loopgain_ohtomo(wsp, probes [, active, Z0]) Eq. 177–180 the global loop gains G_i wsp_unstable_freq_loopgain(G) §7, p. 110 crossings of the +1 point, in Hz wsp_ymatrix(wsp [, probes]) Eq. 182–186 the network's own Y at the probe nodes wsp_ndf(wsp_active, wsp_passive [, probes]) Eq. 186 the probe route to the NDF"],[27,"the-envelope","The envelope","Function Returns wsp_terminate(wsp, idxS, YS, idxL, YL [, YSo, YLo]) the whole wsp of the re-terminated circuit wsp_loadpull(wsp, idxS, idxL, idx, gammaS, gammaL [, Z0]) 1/H0' and 1/Y0' over the Γ grids wsp_loadpull_unstable(…) the Kurokawa crossings found at each termination wsp_loadpull_margin(…) , wsp_loadpull_margin_env(…) the margin over the grid, and its minimum per point wsp_loadpull_ndf(…) , wsp_loadpull_ndf_enc(…) the NDF over the grid, and its encirclement count"],[27,"small-utilities-the-documents-appendix-carries","Small utilities the document's appendix carries","wsp_zsrc(R, C [, f]) and wsp_zprc(R, C [, f]) build a series or parallel RC impedance; z_to_pr / z_to_pc / z_to_pl / z_to_sr / z_to_sc / z_to_sl and their y_to_… twins convert an immittance into the element it is equivalent to; wsp_rc_renorm_s and wsp_zo_renorm_s renormalise an S-matrix to an RC or to a complex reference; _dB(M) is 10·log10|M| . The immittance models return farads and henries The reference document's y_to_pc returns picofarads and its y_to_pl nanohenries. circuitRF's return farads and henries , under the same names. Every trace and every derived metric in this program is base SI, and a scale factor hidden inside a function is exactly the class of mistake that is invisible on a plot. If you are checking a number against the document's own listing, that factor of 10 12 is the difference."],[27,"pairs","Probe pairs: a block in situ","Two probes in the generator → load orientation bracket a network. wsp_yparam2 recovers both the block between them ( y11 … y22 ) and the block that closes around them ( yf11 … yf22 ) — Eq. 137–139 — from the same wsp the single-probe metrics come from, with the circuit intact and nothing disconnected. wsp_block_calc then reports sixteen quantities of that pair (Eq. 140–151): the S-parameters of the inner block and of the feedback block at a reference Z0 ( s11 … sf22 ), four return differences ( F_LGa , F_LGf , F_LGH , F_LGM ) and the four loop gains they came from ( LGa , LGf , LGH , LGM ). Every one of them is on the trace card's list. Why this matters more than the list suggests: the inner block comes back as an ordinary two-port network. So μ , K , |Δ| , MAG/MSG and the stability circles can be applied to a stage inside the amplifier — measured where it actually sits, loaded by what actually loads it, rather than by simulating it on its own between two 50 Ω terminations. wsp_block_design and wsp_fb_design go one step further and hand back the block renormalised in situ to the RC each side actually presents, which is the form a matching-network calculation wants. The residual: is this pair actually bracketing a block? wsp_yparam2_residual is circuitRF's, not the document's. It solves the same two blocks a second time from the shunt stimuli — an independent set of equations with the same side bookkeeping — and reports how far the two answers differ, relative. Round-off (~5e-16) when the pair genuinely brackets a two-port. Order 0.1 or worse when something joins the inner region to the outside around the probes, which is the case in which the eight numbers are describing a configuration you do not have. A shunt from an inner node to ground does not raise it: ground is not a coupling path, and such an element is simply part of the inner block's own y11 . Hurst's form wants a simple pair The reference document's own warning (p. 89): the Hurst loop gain \"is most meaningful when the probe pair is closest to and capture a single dependent source — such as with a transistor. I don't recommend using a complex network between the probe pair.\""],[27,"global","Global stability: bifurcation and Ohtomo","With several probes all oriented the same way, the network splits into two subnetworks — everything on the G side of the probes and everything on the L side — and each is an N-port recoverable from wsp alone (Eq. 161–176). That is bifurcation . wsp_bifurcate(wsp, \"Y\"|\"Z\", \"G\"|\"L\") names the side explicitly, and the document's four function names are aliases with the document's own sides: Document's name Form Side wsp_YA admittance G wsp_YF admittance L wsp_ZA impedance L wsp_ZF impedance G The Y-form and the Z-form put the \"active\" network on opposite sides. That is not a slip in the document; it is a consequence of how each derivation is set up. It is also a trap, which is why circuitRF's own primitive takes the side as an argument and the aliases carry the document's sides rather than a tidier scheme. wsp_YA and wsp_ZA describe different subnetworks. Two requirements, neither of which anything can check from wsp : every probe in the set must have the named subnetwork on the same side (§6, p. 100). Two probes whose named sides meet give either a singular system or a matrix describing nothing; two probes whose same-side terminals share a node give a singular result — two ports on one node have no admittance matrix. Put each probe on its own node."],[27,"ohtomos-global-loop-gains","Ohtomo's global loop gains","wsp_loopgain_ohtomo(wsp, probes) takes the two subnetworks as scattering matrices at Z0 , forms M = SP·SA − I , and returns one loop gain per probe (Eq. 177–180): G_i = 1 + |M_{N−i+1}| / |M_{N−i}| M_0 ≡ 1 where M_{N−i+1} is the trailing principal submatrix on rows and columns i … N . The critical point is +1 , not the origin. wsp_unstable_freq_loopgain(G) looks for |G| ≥ 1 with the phase crossing zero clockwise — the mirror of the Kurokawa search, around +1 instead of on the negative real axis. The trace card's Ohtomo trace draws the +1 reference mark for you. Why the method is global rather than one more loop gain. The G_i telescope: Π (G_i − 1) = det(M) and det(M) is the Nyquist determinant of the closed loop of travelling waves. So the sum of the encirclements of +1 by the G_i equals the encirclements of the origin by det(M) — whatever the probe order and whichever side you called active, even though the individual G_i all change. With one probe it reduces to ΓP·ΓA , Jackson's stability index . Ohtomo's prerequisite The method assumes each subnetwork is stable on its own (§7, p. 108): one side purely active with no terminations that could form a loop of their own, the other purely passive. Nothing in wsp can check that, and no message will tell you it has been violated."],[27,"envelope","The stability envelope","The question the envelope answers is what happens to this node's stability when the source and load are not 50 Ω . It is the stability counterpart of a load-pull, and — this is the point — it costs no re-simulation at all . Adding a shunt admittance at a probe node is a rank-1 update of the whole wsp matrix , because every response to an injection at that node is already in it. Applied once at the source probe and once at the load probe, that yields the complete wsp of the re-terminated circuit — every H0' , Y0' , ZG' , ZL' , loop gain, margin and Ohtomo gain — from the one sweep you already ran (§9, 120; [E] §IV). It works on a large-signal wsp from harmonic balance exactly as it does on a linear one. The precondition, which is checked and refused rather than assumed. The source probe must sit directly at its termination with G facing it , and the load probe directly at its termination with L facing it, so that the termination is a pure shunt at that node. circuitRF records each probe's neighbouring top-level Term / Port and compares the probe's own bidirectional impedance on that side against the declared Z . A probe with nothing at that node is refused; a probe whose ZG disagrees with the declared Z is refused naming both numbers . That is what feedback across the probe (§9, p. 119), or a series element between the probe and its termination, produces — and being told is the intended outcome, because the update would otherwise be re-terminating something other than the termination. The trace card's Envelope sub-card: the probe each side is pulled at, a ladder of |Gamma| per side, the angular step, and the number of terminations it is about to compute. The card takes a ladder of |Γ| per side rather than one magnitude, and an angular step. That is deliberate: the interesting reading is usually three rungs either side of the ρ at which something changes, seen at once, rather than two runs and a memory of what the last picture looked like. It also prints how many terminations it is about to compute, before it computes them."],[27,"the-four-things-you-can-read-off-it","The four things you can read off it","Metric What it is 1/H0env , 1/Y0env the driving-point loci of the re-terminated circuit, per termination unstable the Kurokawa crossings found at each termination, counted SMenv the stability margin 's minimum over frequency, per termination ([E] §II) NDFenc the NDF 's origin encirclements at each termination, from a passivated run ([E] §III) The last two together are the comparison the 2025 paper [E] makes: the NDF is binary — a termination is either encircled or it is not — while the margin is a distance , and the distance carries information the count does not. The margin envelope and the NDF's encirclement count over the same terminations, on one pair of axes. The NDF answers yes or no; the margin says how close. The figure sweeps a two-device parallel amplifier's source phase at ρ = 0.9 with both quantities on one plot. The numbers are that fixture's own — a redrawn topology with circuitRF's own element values, not the paper's circuit — and the shape is what matters: terminations at which the encirclement count has already returned to zero while the margin is still tens of dB down. Those are the terminations a binary check calls fine. The envelope's NDF is the REDUCED one It is taken over the probed nodes, and it is complete only if the probe set covers every node that can hide a pole (§8, pp. 112–113). The Ohtomo fixture is the demonstration: with probes on the source, one gate and the load, the reduced NDF reads zero encirclements at a start-up the Kurokawa search reports — and adding a fourth probe on the other gate, with nothing else changed, makes the same NDF read 2. The odd mode is differential across the two gates and only one of them carried a probe. The same fixture carries a second lesson about what an envelope can see at all: an odd mode sees both ports as virtual grounds, so no source or load termination reaches it . A VSWR sweep is structurally blind to that class of instability, which is why a two-device amplifier needs a balancing resistor rather than a better match. NDFenc counts turns; the NDF summary counts poles, and they differ by two NDFenc (and wsp_loadpull_ndf_enc ) is the swept locus's own net turn , over ω ≥ 0 . The NDF: n right-half-plane pole(s) line an NDF=yes run prints is the count around the closed Nyquist contour, and Platzker's NDF(−ω) = conj NDF(ω) makes the unswept arm turn through the same angle — so poles = 2 × turns . A conjugate pair, which is what an oscillator has, is one turn here and two poles there. The two always agree about whether a termination is unstable, which is what a non-zero NDFenc means; they are not the same number, and neither is wrong."],[27,"hb","Under harmonic balance","Everything above computes wsp from an S-parameter analysis, which linearises the nonlinear devices at their DC operating point . That answers whether the design is stable with nothing driving it. It cannot answer the question a power-amplifier designer actually asks — is it stable at the drive level it ships at — and it cannot see a parametric instability at all, because that one lives at half the fundamental and an unpumped circuit has no fundamental. Harmonic balance answers both, and every derived metric on this page applies to the result with no change at all (§4.2, p. 43: \"Linear analysis, harmonic balance, or AC analysis can all be accommodated\")."],[27,"what-tickle-means-here","What \"tickle\" means here","The circuit is driven hard by its HB tones. At the converged operating point the nonlinear devices are periodically time-varying conductances and capacitances, and the probe injects a vanishingly small series voltage or shunt current at a swept frequency ssfreq — the tickle — which is in general not on the HB grid at all. The response comes back at every sideband of the fundamental; what wsp carries is the response at ssfreq itself. Ask for it on the harmonic-balance directive: analysis HB1 type=hb Tone=RFfreq MaxHarm=7 \\ SSStart=0.1 SSStop=10 SSNpts=991 SSUnit=GHz [SSStep=…] [SSLog=true] [SSMaxHarm=K] \\ [MarginThreshold=<dB>|none] SSUnit applies to start, stop and step alike — one unit for the whole sweep, as everywhere else in circuitRF. Absent SSStart / SSStop there is no small-signal solve , and the run is exactly the run it was before: a WSProbe with no SS* keys is not an error, it is a transparent element, and the run says once that it carried no transfer functions. The cubes are the same ones, over {ssfreq} instead of {freq} : HB1.wsp , HB1.H0(\"GATE\") , HB1.idx(\"GATE\") and the rest resolve exactly as their SP1.* counterparts do. Under a drive sweep the sweep axis is prepended, which is what gives you a fan . The stability margin of a pumped varactor against the small-signal probe frequency, one curve per pump level. The collapse at half the fundamental is a parametric instability, which is the case no linear analysis can see."],[27,"what-to-look-for","What to look for","At low drive the harmonic-balance answer tends to the S-parameter answer. The two analyses agree at the bottom of the drive sweep, which is the sanity check that the fan is the same quantity you have been reading all along. A right-half-plane pole of the driven circuit shows Kurokawa's signature on 1/H0(ssfreq) and 1/Y0(ssfreq) , at a tickle frequency near its imaginary part. Same search, different cube. A parametric (sub-harmonic) instability appears at ssfreq ≈ f0/2 , and at its images f0/2 + k·f0 . This is the case no linear analysis can see , and it is the reason to sweep across drive rather than to run at one level. The margin under drive. A SM_Y0 fan that collapses only above some drive level is a drive-dependent instability; the parametric case reads as a notch at f0/2 that is simply absent at low drive. MarginThreshold is applied at each operating point. One frequency this analysis refuses to answer A tickle commensurate with the fundamental at order 2 — ssfreq equal to 0, f0/2 , f0 , 3f0/2 , … — is reported as NaN with a warning , never as a plausible number. There the sideband family and its negation are the same set of frequencies, so the two are not independent and the formulation would be solving half the problem. The practical consequence, which is a property of the method and not a defect: a parametric instability at f0/2 is found by the grid points either side of it. The pole pair approaching the axis shows its crossing at the samples that bracket it, and a grid that happens to land on f0/2 exactly loses that one sample and nothing else. An odd point count over the same span usually avoids it, and the warning says so."],[27,"cost-and-what-is-refused","Cost, and what is refused","The small-signal solve is a dense factorisation per tickle frequency, shared by all the probe injections at that frequency, plus a linear-partition extraction per sideband. So the cost scales with the tickle-frequency count, the sideband count and the probe count — not with the drive sweep, which reuses each operating point. Two choices you make on the directive change what it costs, and one of them is free. The linear part of the circuit is the same at every drive level, so every sparse factorisation of the sweep is done once and reused across the whole drive sweep — and a sideband ssfreq + k·f0 of one tickle point is literally the same frequency as a sideband of another whenever the tickle step divides the fundamental. A grid step of f0/m therefore shares most of its sideband work between points , which on a long sweep is worth several times over. circuitRF never moves your grid to get it: a frequency you wrote is a frequency it uses, so the run instead reports how many distinct sideband frequencies your grid landed on and what an aligned step would have bought. SSNpts chosen so the step comes out at f0/200 costs nothing and is the free half. SSMaxHarm is the half that is not free. It truncates the sideband set, and truncating it cuts the sideband extractions linearly and the dense conversion matrix cubically — which is why it is tempting. Measured on a single device driven into compression, 1/H0 computed at SSMaxHarm = K − 2 was 7 % from the untruncated answer at the median tickle frequency and 27 % from it at the worst one, and the error does not fall monotonically as you raise it (truncation removes mixing paths; it is not a series expansion of the answer). The default is MaxHarm for that reason. Lower it to explore quickly, raise it back before you believe a margin — and the run says so once whenever it is below MaxHarm . Long tickle grids are run in parallel, several points at a time, exactly as an S-parameter sweep is; MaxParallelism governs both and the result does not depend on it. Three refusals, each by name: three or more tones (one and two are supported); the rectangular-FFT two-tone path rather than the default lattice; and a conversion system above 2000 unknowns, which states which of SSMaxHarm , MaxHarm and MaxMixOrder is binding. A run that allocated gigabytes and was killed with nothing said would be the worse outcome. Loadpull does not run the small-signal solve , and does not silently do so: at every termination of a grid it would multiply the run by the tickle count. A probed loadpull says once where the small-signal sweep lives and points at the envelope , which is the reference document's own replacement for a stability loadpull and costs no extra harmonic-balance solves at all."],[27,"ndf","NDF — the normalized determinant function","NDF = Δ / Δ0 = |Y| / |Y_passive| (Eq. 181) Y is the network's admittance matrix with every termination, bias network and bypass included and every independent source off. Y_passive is the same matrix with every dependent source, negative resistance and non-Foster element rendered passive. Δ0 then has no right-half-plane zeros by construction, so by the argument principle the clockwise encirclements of the origin by NDF(jω) count the network's right-half-plane poles (§8, p. 112). circuitRF can build Δ0 , and the reference document's reader usually cannot. The document is explicit that the passive determinant \"requires having precise access to the transconductance elements in all active devices\" (§8, p. 113) — which a black-box vendor model withholds. Every built-in active model here is circuitRF's own and knows its controlled sources exactly, so the NDF is a native analysis rather than a construction you assemble by hand. Turn it on with a knob on the S-parameter directive: analysis SP1 type=sparam start=1 stop=100000 npts=2001 log Unit=MHz NDF=yes"],[27,"what-the-run-reports","What the run reports","NDF: 2 right-half-plane pole(s) (net clockwise encirclement 1.989; NDF(100 GHz)=1 ∠ 1.8) Five properties of a correctly-built NDF, and circuitRF checks the three that a run can get wrong: Property How it is reported It has zeros only, no poles holds by construction No counter-clockwise encirclement ndf.counterclockwise if the running count falls back a whole turn NDF → 1 at high frequency ndf.no-asymptote if it is still closing on 1 at the top of the sweep — extend it upward ndf.constant-asymptote if the limit is simply not 1, quoting the limit NDF(−ω) = conj NDF(ω) holds by construction It is real at DC ndf.dc-imaginary — extend the sweep downward A constant asymptote is not a failure Passivating a negative resistance changes an element value — R → |R| — and an element value can be a factor of the network determinant where a dependent source is only ever a term in it. So the NDF of such a circuit tends to a constant that is not 1. The pole count is unaffected: a constant turns through no angle."],[27,"what-is-passivated-exactly","What is passivated, exactly","Kind What happens Passives, lines, transformers, ideal blocks, probes, terminations, sources as-is; sources are already off, and the ordinary stamp is the passive stamp Two-terminal nonlinearities (diode, C(V)) linearised at bias; they are passive at any bias R , Z_Port R → \\|R\\| ; the real part of a port impedance likewise VCCS , VCVS gain → 0 (a zero-gain controlled voltage source is still a short, so the branch stays) Amp , Mixer (system blocks) forward gain → 0; the mixer's conversion terms and its LO leak Every built-in transistor family the transconductance → 0. Output conductance, junction conductances and capacitances all stay at bias SDD you name a scaling variable : PassiveVars=\"NDFgm\" Verilog-A, external devices you name an instance parameter : PassiveParams=\"X1.gmscale\" SnP , Chain passive if σ max ≤ 1 at every sampled frequency — otherwise refused by name An S-parameter block with gain hides its dependent sources, and there is no honest way to passivate it. That is a refusal naming the instance, never a silently passive-looking NDF. The shape of an SDD's passivation matters I[2,0] = NDFgm*Ids(_v1,_v2) is the wrong shape. It scales the whole drain current, output conductance included, so Δ0 would be a different circuit rather than the same one with its controlled source removed. Freeze the controlling voltage instead: I[2,0] = Ids(NDFgm*_v1 + (1 − NDFgm)*Vgs0, _v2) At NDFgm = 0 the drain current no longer responds to the gate voltage while staying evaluated at the same bias — which is exactly what a built-in FET's own passivation does. A PassiveVars name that is not a global, or that no device reads, is a refusal. A scaling variable that scales nothing is the classic silent failure of a hand-built NDF. circuitrf explain --analysis lists the passivation each instance will use, which is how to see a refusal coming without running anything: NDF: yes; 24 instance(s), 3 carrying activity to passivate NDF M1 (VCCS) activeExact: exact — G → 0 NDF X1 (SDD) activeUserScaled: needs PassiveVars= naming a global this device reads"],[27,"k-is-not-enough","K is not enough","The reason to have this at all is that a two-port stability factor is computed from the reduced two-port and can say nothing about a pole the reduction hides. Rollett's K of a two-port whose terminal S-parameters are those of a 6 dB pad, above 1 across the band, beside the NDF's encirclement count of the loop those terminal S-parameters cannot see. The figure's circuit is a two-port whose terminal S-parameters are those of a well-behaved 6 dB pad — K > 1 and |Δ| < 1 at every frequency in the band — wrapped around an internal loop that oscillates. The loop is coupled to the through path by 1 fF, a thousandth of the tank capacitance: far too weak to move the terminal S-parameters measurably, and entirely irrelevant to whether the loop oscillates, because a start-up condition is a property of the loop and not of what is watching it. The NDF reads 2 right-half-plane poles. The stable and unstable versions of the same circuit have terminal S-parameters that agree to 1e-4, so the two-port metrics were not simply given different data."],[27,"the-probe-route","The probe route","wsp_ndf(wsp_active, wsp_passive [, probes]) (Eq. 186) computes the same ratio from the reduced matrices at the probe nodes, using a passivated run's wsp beside the ordinary one. It is the route the reference document describes and it is a genuine cross-check on the native calculation — with the reduced-NDF caveat of §8 : it is complete only over the nodes you probed. A run with NDF=yes and probes writes both cubes, wsp and wsp_passive , so the comparison needs one run rather than two."],[27,"headless","From the command line","Everything on this page runs with no display. See the CLI reference . Run a probed sweep, with the NDF: circuitrf sparam amp.cnl with NDF=yes on the directive. One line per probe follows the S summary: WSProbe P idx=1 H0(0.5 GHz)=11.597 ∠ -1.4 ZG(0.5 GHz)=10.482 ∠ 17.4 SM_Y0 min −18.1 dB @ 1.59125 GHz SM_H0 min −19.8 dB @ 1.73375 GHz --json carries the same under wsprobes (label, idx, and each margin's minimum and frequency — linear , because dB is a display convention and a document should carry the number) and under ndf . -o out.npy / .mat / .txt carry every cube. Measure anything on this page , evaluated identically in the window and headlessly: measure SMgate = wsp_stability_margin(SP1.wsp, SP1.idx(\"GATE\")) measure fosc = wsp_unstable_freq_kurokawa(1 / SP1.H0(\"GATE\")) measure Kin = wsp_yparam2(SP1.wsp, SP1.idx(\"G1\"), SP1.idx(\"D1\"), \"inner\") See what will happen before it does: circuitrf explain amp.cnl --analysis lists each probe with its idx and both terminal nets, the effective MarginThreshold , and the passivation every instance will use. Check the document: circuitrf check amp.cnl warns wsprobe.shorted when a probe's two nets are the same net — the headless twin of the schematic editor's own wire cut. A picture, with no data display to author first: circuitrf plot amp.npy -o margin.svg \\ --trace cube=SP1.wsp,probe=GATE,metric=SM_Y0,y=db probe= is what turns a cube=…wsp trace into a probe metric; metric= takes the document's own spelling. Case is load-bearing in this notation and is not folded away — LGF is one probe's forward circulator loop gain and LGf is a probe pair's feedback-as-synthetic-FET loop gain, and quietly answering one with the other would be worse than a refusal. A port-less run has no Touchstone A circuit with WSProbes and no Term or Port is a legitimate run — the reference document's own fixtures are like this — and it writes every wsp cube. Asking for a .sNp from it is refused, naming the spellings that do carry the result. Write .npy ."],[27,"caveats","Caveats, in the documents' own words","One list, because each of these is a limit of the method rather than of this implementation. Loop gains are incomplete. Every one of them, including LG and F . They \"are not fundamental circuit quantities and are therefore not rigorous stability measurements … not necessarily wrong, just incomplete\" (§4.4, p. 50). Pole masking. A zero can hide the pole in 1/H0 or in 1/Y0 , though never in both. Check both (§4.10, pp. 73–79) — and the same applies to SM_H0 and SM_Y0 . A sweep can step over a crossing. An empty crossing list means no crossing was sampled , not the circuit is stable . A resonance narrower than the grid step is invisible to any method that samples. The envelope's precondition. Both terminations must carry a probe directly at them, with the right terminal facing them. Otherwise the update is re-terminating something else, and circuitRF refuses rather than guessing. A source/load envelope cannot see every mode. An odd mode across two devices presents virtual grounds at both ports; no source or load termination reaches it. The reduced NDF sees only what you probed. Complete only if the probe set covers every node that can hide a pole (§8, pp. 112–113). Ohtomo's prerequisite. Each subnetwork must be stable on its own (§7, p. 108). Nothing checks it. Circuit fidelity. The document's own last word (p. 113): \"no stability detection method will capture an instability that is mathematically NOT contained in your simulation construction.\" The margin is a distance, not a detector. Kurokawa's clockwise-sense condition is not in it. Read the margin's minimum and the crossing search together — the trace card prints them on one line for that reason. The margin's dB convention is circuitRF's. 20·log10 , so [M]'s −15 dB rule of thumb is 0.178 linear. The paper does not state a convention."],[27,"references","References","The primary reference. Winslow, General Circuit Analysis Using The WSProbe , January 29, 2023. Public technical report, 172 pages. Cited above as (Eq. n) , (§n) and by page. The two papers that followed it. [M] T. A. Winslow, \"A Novel Stability Margin for Transfer Functions,\" Proc. 19th European Microwave Integrated Circuits Conference (EuMIC) , Paris, September 2024, pp. 291–294. DOI 10.23919/EuMIC61603.2024.10732614 . Cited as (M-Eq. n) , and its four unnumbered proxy displays as (M-rY) , (M-iY) , (M-rH) , (M-iH) . [E] T. A. Winslow, \"Stability Envelope Using Nodal Transfer Functions,\" Proc. 20th European Microwave Integrated Circuits Conference (EuMIC) , Utrecht, September 2025, pp. 254–257. DOI 10.23919/EuMIC65284.2025.11233915 . Cited as (E-Eq. n) . The earlier and adjacent work. Winslow, \"A Novel CAD Probe for Bidirectional Impedance and Stability Analysis,\" 2018 IEEE/MTT-S International Microwave Symposium (IMS) . DOI 10.1109/MWSYM.2018.8439210 . The original probe paper, which the 2023 document supersedes mathematically. Winslow, \"Introduction to the WSProbe,\" 2025 IEEE BiCMOS and Compound Semiconductor Integrated Circuits and Technology Symposium (BCICTS) . DOI 10.1109/BCICTS63111.2025.11211431 . Winslow, Stability Analysis Methods for Microwave Power Amplifiers: A Modern Perspective , IMS 2024 keynote. Two printed equations this page cites only in corrected form E-Eq. 11 — the bidirectional impedances under mismatch — has its generator and load numerators swapped as printed. M-Eq. 5 and M-Eq. 6 print Z-forms of YG and YL whose off-diagonal terms are swapped; the Y-forms beside them are right and are what circuitRF implements. Both were found numerically: on a reciprocal network the difference is invisible, and on a non-reciprocal one it is order 1. Every quantity on this page is the corrected form."],[27,"the-stability-literature-this-pages-appendix-draws-on","The stability literature this page's appendix draws on","The bibliography of the reference document's own §2–§3, which is the shape of Appendix A . Bibliographic entries only. Bode, Network Analysis and Feedback Amplifier Design , Van Nostrand, 1945. Barkhausen, Lehrbuch der Elektronen-Röhren , Band 3: Rückkopplung , Hirzel, 1935. Rollett, \"Stability and Power-Gain Invariants of Linear Twoports,\" IRE Trans. Circuit Theory , vol. CT-9, no. 1, pp. 29–32, March 1962. Edwards and J. H. Sinsky, \"A New Criterion for Linear 2-Port Stability Using a Single Geometrically Derived Parameter,\" IEEE Trans. Microwave Theory Tech. , vol. 40, no. 12, pp. 2303–2311, December 1992. Kurokawa, \"Some Basic Characteristics of Broadband Negative Resistance Oscillator Circuits,\" Bell System Technical Journal , vol. 48, no. 6, pp. 1937–1955, July–August 1969. Platzker, W. Struble and K. T. Hetzler, \"Instabilities Diagnosis and the Role of K in Microwave Circuits,\" IEEE MTT-S International Microwave Symposium Digest , pp. 1185–1188, 1993. Struble and A. Platzker, \"A Rigorous yet Simple Method for Determining Stability of Linear N-Port Networks,\" 15th Annual GaAs IC Symposium Digest , pp. 251–254, 1993. Jackson, \"Rollett Proviso in the Stability of Linear Microwave Circuits — A Tutorial,\" IEEE Trans. Microwave Theory Tech. , vol. 54, no. 3, pp. 993–1000, March 2006. Middlebrook, \"Measurement of Loop Gain in Feedback Systems,\" International Journal of Electronics , vol. 38, no. 4, pp. 485–512, 1975. Tian, V. Visvanathan, J. Hantgan and K. Kundert, \"Striving for Small-Signal Stability,\" IEEE Circuits and Devices Magazine , vol. 17, no. 1, pp. 31–41, January 2001. Hurst, \"A Comparison of Two Approaches to Feedback Circuit Analysis,\" IEEE Trans. Education , vol. 35, no. 3, pp. 253–261, August 1992. Ohtomo, \"Stability Analysis and Numerical Simulation of Multidevice Amplifiers,\" IEEE Trans. Microwave Theory Tech. , vol. 41, no. 6/7, June/July 1993. Suarez, Analysis and Design of Autonomous Microwave Circuits , Wiley, 2009. Maas, Nonlinear Microwave and RF Circuits , 2nd ed., Artech House, 2003. Anakabe, N. Ayllon, J.-M. Collantes, A. Mallet, G. Soubercaze-Pun and K. Narendra, \"Automatic Pole-Zero Identification for Multivariable Large-Signal Stability Analysis of RF and Microwave Circuits,\" 40th European Microwave Conference , 2010. Narhi and M. Valtonen, \"Stability Envelope — A New Tool for Generalised Stability Analysis,\" IEEE MTT-S International Microwave Symposium Digest , 1997. Roberg and J. Hoversten, \"Nonlinear Stability Analysis of Power Amplifiers Using the Stability Envelope,\" IEEE Topical Conference on RF/Microwave Power Amplifiers , 2015."],[27,"appendix-stability","Appendix A — Stability, from the beginning","This appendix is the road that leads to the WSProbe. It follows the order the reference document's own §2–§3 takes, one short section per method, and each ends with in circuitRF: — where that quantity is, or that it is not here and why. It is written to be read straight through by someone who has never had to argue with a stability result. Nothing in it is required in order to use the probe."],[27,"a-bibo","A.1 What \"stable\" means","A linear circuit is stable when every bounded excitation produces a bounded response — BIBO stability. Equivalently, and more usefully: the poles of every transfer function of the network lie strictly in the left half of the complex s plane. A pole with a positive real part is a response that grows on its own, and it is a pole of the network , not of one transfer function — which is why a quantity computed from a two-port view of a circuit can miss one. Everything that follows is a way of answering \"are there right-half-plane poles?\" without factoring a polynomial you do not have. In circuitRF: the whole of this page."],[27,"a-negr","A.2 Negative resistance","The oldest microwave answer, and the one that still frames the language. An oscillator is a one-port with negative resistance facing a passive load; it starts up when the total resistance seen around the loop is negative and the total reactance cancels (Eq. 9–10). Kurokawa's two-block picture. The whole of the WSProbe's primary metric is this condition written at an arbitrary internal node rather than at a designed oscillator's port. Kurokawa's contribution was to make the sense of the crossing part of the condition (Eq. 105–108): it is not enough for the reactance to cancel where the resistance is negative — the imaginary part must be increasing through the crossing, which distinguishes a growing solution from the ordinary steady-state resonance every passive tank has. In circuitRF: this is the primary metric — the three conditions applied to 1/H0 and 1/Y0 at any node, by wsp_unstable_freq_kurokawa and the trace card's own reading."],[27,"a-rollett","A.3 Rollett's K, and the proviso","For a two-port, Rollett's factor and the auxiliary condition (Eq. 5–8): K = (1 − |S11|² − |S22|² + |Δ|²) / (2·|S12·S21|) Δ = S11·S22 − S12·S21 unconditionally stable ⟺ K > 1 AND |Δ| < 1 K > 1 alone is not a criterion, which is why circuitRF offers |Δ| as its own trace rather than folding it in. The proviso is the part that gets forgotten. Rollett's derivation assumes the two-port has no right-half-plane poles when its ports are terminated in the reference impedance . K and |Δ| are computed from the terminal S-parameters, and terminal S-parameters are a reduction of the network. A pole the reduction hides is a pole K cannot see — and §10's example is a two-port whose terminal S-parameters are those of a 6 dB pad and which oscillates. In circuitRF: K , |Δ| , μ and μ′ are on the derived-metrics page , for any network source — including the reduced two-port at a probe, and the inner block of a probe pair . The proviso is what the NDF exists to discharge."],[27,"a-barkhausen","A.4 Barkhausen's loop gain","Break the loop, inject, measure what comes back: oscillation when the returned signal equals the injected one, T = 1 at zero phase (Eq. 11–13). It is the intuition everyone carries and it is not a criterion. You cannot break the loop of a circuit that is biased through it , real circuits have more than one loop, and the answer depends on where you broke it. Every objection the modern methods raise is an objection to this step. In circuitRF: all eight wsp_loopgain kinds — none of which requires breaking anything, because the injections are mathematical. And all eight carry the same caveat ."],[27,"a-bode","A.5 Bode's return ratio and return difference","Bode's reformulation is the one that survives. For a dependent source of gain k in a network whose determinant is Δ , define the return difference F = Δ / Δ0 (Eq. 14–16) where Δ0 is the same determinant with that source's gain set to zero. F = 1 + T , where T is the return ratio — the loop gain, but defined as a ratio of two determinants of the whole network rather than as something you measure after cutting a wire. The return difference of one dependent source: the determinant of the network with the source active, over the determinant with its gain set to zero. Nothing is disconnected. In circuitRF: F = 1 − LG is one of every probe's six default outputs , and the four F_LG… return differences of a probe pair are on the card. The sign convention here is the reference document's — it defines LG = −T — so F = 1 − LG and Bode's F = 1 + T are the same number."],[27,"a-ndf","A.6 The NDF","Bode's return difference for one source generalises to a whole network of them. Take the determinant with every dependent source active over the determinant with every one of them passivated (Eq. 17): the ratio has no poles, and its zeros are the network's poles. Count the clockwise encirclements of the origin and you have counted the right-half-plane poles — rigorously, for an N-port, with no proviso and no loop to break. Struble and Platzker's sequential form makes the same object out of return differences taken one device at a time, which is a useful way to see which device is responsible. In circuitRF: a native analysis , NDF=yes , exact for every built-in active model. The reason the reference document treats the NDF as difficult and this program does not is entirely that circuitRF owns its own device models and a black-box model withholds exactly what Δ0 needs."],[27,"a-jackson","A.7 Jackson's stability index","A reformulation of the proviso as a measurement rather than an assumption: sum the reflections around the loop formed by the two subnetworks either side of a plane, and read the result as a Nyquist locus (Eq. 18–20, App. B). With one plane it is the product of the two reflection coefficients looking each way. In circuitRF: it is Ohtomo's G_i at N = 1 — wsp_loopgain_ohtomo over a single-probe set returns exactly ΓP·ΓA ."],[27,"a-middlebrook","A.8 Middlebrook's double injection","The measurement answer to \"you cannot break the loop\": inject a small voltage in series and a small current in shunt at the same point, without disconnecting anything, and combine the two results (Eq. 21–23): 1/T = 1/Tv + 1/Ti Tv from the series injection, Ti from the shunt one. It is the first method that gets a loop gain out of an intact circuit, and it is the direct ancestor of what the WSProbe does. One node, two injections, nothing disconnected. A WSProbe applies exactly these two stimuli — and reads two responses at every probe rather than one at the injection point, which is what turns a loop gain into a matrix. In circuitRF: wsp_loopgain(…, \"MB\") and \"MBR\" . Tv and Ti are not offered individually — the combination is the quantity, and the document's own sign chain for the pair is inconsistent as printed, so circuitRF implements the closed form that a direct derivation confirms."],[27,"a-tian","A.9 Tian's bilateral return ratio","Middlebrook's combination assumes the injection point is unilateral. Tian's correction removes that assumption and gives the bilateral return ratio, which is exact at any node of any network (Eq. 24–25). It is the loop gain circuitRF reports as LG : LG = −(y12 + y21) / (y11 + y22) = (z12 + z21) / (z11 + z22) (Eq. 53–60, 92) over the reduced two-port at the probe. In circuitRF: every probe's default LG , and wsp_loopgain(…, \"BI\") — one implementation, so the run's cube and the card's function are the same number."],[27,"a-auxgen","A.10 Auxiliary generators and pole-zero identification","The modern industrial answer to large-signal stability: attach a small auxiliary generator at a node, sweep it, and identify the poles and zeros of the resulting transfer function from the fitted response. It is rigorous and it works on driven, nonlinear circuits. Its price is that a transfer function has zeros as well as poles , and a zero of the particular function you fitted can cancel the pole you were looking for. That is why the identification is run at several nodes and with more than one observable. The connection to this page is Eq. 26–30. By Cramer's rule, the driving-point impedance at node n is a cofactor over the network determinant, Zn = Cof11/|Y| , and a general transfer impedance is Hij = Cofij/|Y| . So H0 and Y0 carry the whole network determinant in their denominators — every pole of the network is in them — while their numerators are cofactors, which is exactly where a masking zero can live. That is the algebraic reason both must be checked , and the reason a loop gain, which is a ratio of cofactors, carries no determinant at all. In circuitRF: not available as a pole-zero identification. H0 and Y0 are the two functions that carry the determinant, and the margin is what makes their reading a distance rather than a verdict."],[27,"a-sprobe","A.11 The S-parameter probe, and why it fails with feedback","A natural idea: put a port at the node, measure S11 , and read the impedance each way. It works when the two sides of the node are otherwise unconnected, and it fails as soon as they are not — because a single reflection coefficient at a node cannot separate what is on one side from what is on the other when a path joins them around the port. This is the same statement as ZG ≠ 1/YG : under feedback there are two impedances at that node and one measurement cannot report both. In circuitRF: ZG , ZL , YG and YL — four numbers where the naive probe has one, from a single sweep, correct under any amount of feedback."],[27,"a-ohtomo","A.12 Ohtomo, and the multi-device case","A parallel-combined amplifier has a mode the ports cannot see. Ohtomo's method splits the network at several planes at once, forms the wave-domain loop matrix and reads its principal minors as a set of global loop gains whose product is the Nyquist determinant of the whole loop. In circuitRF: wsp_loopgain_ohtomo , over any probe set, with the active side named explicitly."],[27,"a-envelope","A.13 The stability envelope","Närhi and Valtonen's insight was that \"is it stable\" is the wrong question for a circuit that will be sold into an unknown VSWR: the useful answer is the set of terminations at which it remains stable. Roberg and Hoversten carried it into the large-signal regime. What [E] changed is the cost and the resolution. The 2025 paper's envelope is computed from the already-solved wsp matrix — no re-simulation per termination — and it reports the margin rather than a binary verdict, so the envelope has contours instead of a boundary ([E] §I). In circuitRF: the envelope , including the NDF over the same grid for the comparison [E] makes."],[27,"a-summary","A.14 How four transfer functions reproduce all of it","The reference document's own summary (§3.12) is that the four transfer functions per probe are sufficient for every method above, at any node, from one sweep: Method Recovered from wsp as Rollett's K, μ, |Δ|, stability circles the reduced two-port at the probe (Eq. 44/48), fed to the ordinary network metrics Barkhausen / a broken loop any of the eight wsp_loopgain kinds Bode's return difference F = 1 − LG , and the four F_LG… of a probe pair Middlebrook's double injection \"MB\" , \"MBR\" Tian's bilateral return ratio LG , the default Hurst's two-block form \"HST\" , and LGH for a pair Jackson's stability index Ohtomo's G_i at one probe Ohtomo's global loop gains wsp_loopgain_ohtomo over a probe set The NDF, reduced wsp_ndf over a probe set, against a passivated run Negative resistance at a node ZG , ZL , YG , YL — four numbers, not one Kurokawa's start-up condition 1/H0 and 1/Y0 , the two that carry the determinant The stability envelope the rank-1 re-termination of the whole matrix"],[27,"a-howclose","A.15 And then: how close?","Every method above answers a yes/no question. That is the right question for a circuit you are about to build once. It is the wrong one for a circuit that will be built ten thousand times across a process corner, because it says nothing about which of two stable designs is one component tolerance from oscillating. [M] §I is the step from is it stable to how close , and it is the last thing this page has to say about the subject: the two driving-point functions are each a sum of two immittances, and the normalised comparison of those two halves is a bounded, unitless distance that means the same thing at every node of every circuit. In circuitRF: SM_Y0 and SM_H0 , beside the six defaults, on every probe, in every analysis, at no extra cost."],[28,"","","The Data Display offers four plot types. Which one you can use depends on whether the data is real (scalar) or complex — that's the single most important rule below. Scalar vs. complex — the rule Rectangular shows scalar (real) data only. Smith and Polar show complex data only. Table is the exception — it shows both complex and scalar data together in one grid. So a complex quantity like S(1,1) goes on a Smith/Polar/Table; a real quantity like Pout in dBm goes on a Rectangular/Table; and a Table can hold both at once."],[28,"rectangular","Rectangular","The familiar Cartesian X–Y plot. Scalar data only. A complex signal must be reduced to a real quantity first via a per-trace transform — magnitude in dB ( dB20 / dB10 ), phase in degrees, real part, imaginary part, magnitude. This is the plot for gain vs. frequency, Pout vs. drive, efficiency vs. load, harmonic levels, and most swept results. A rectangular plot of the shipped FET test bench's S-parameters, 1-10 GHz."],[28,"smith","Smith","The Smith chart maps impedance/reflection onto the unit disk. Complex data only — it plots the complex value directly (no transform needed). Use it for reflection coefficients ( S(1,1) ), input/output impedance, and matching-network design. Loadpull contours are drawn on the Smith chart's Γ-plane. A Smith chart carrying the FET test bench's own S(1,1), swept 1-10 GHz."],[28,"polar","Polar","A magnitude-and-angle plot on concentric circles. Complex data only. Use it when you care about a complex quantity's phase and magnitude but don't want the Smith chart's impedance grid — e.g. viewing a reflection or transmission coefficient as a vector. The same run on a polar plot: magnitude and angle, without the impedance grid. It is also where a node's stability is read. The reference document behind the WSProbe reads 1/H0 and 1/Y0 on a polar chart and looks for a clockwise crossing of the negative real axis — so those two, and the envelope's 1/H0env and 1/Y0env , are offered on Polar and on nothing else. An Ohtomo global loop gain is polar-only for the same reason, with one difference that matters: its critical point is +1 , not the origin . A polar plot carrying any WSProbe trace draws small reference marks at both points, because a reader who has to remember which family reads against which is a reader who will occasionally use the wrong one."],[28,"table","Table","The numbers in a grid. Table is special: it shows complex and scalar data simultaneously in the same table — something no other plot type does. A complex column displays in a chosen complex format (magnitude/angle, real/imag, or dB/angle), and a scalar column shows its real value, side by side. It's ideal for reading exact values, building a results summary, and copying data out. (The loadpull summary table is a specialized Table view.) A table of the same run, a complex column beside a scalar one."],[28,"traces","Adding traces & transforms","Each plot holds one or more traces , configured in the trace card: Data source — which run/file the trace reads from. Measured files overlay simulated ones on the same axes. Signal — the quantity: an S-parameter ( S(2,1) ), a node voltage, a branch current, a measurement, or a cube slice. Transform (Rectangular/Table) — how a complex value becomes scalar: dB20 , dB10 , Mag , Phase , Real , Imag . Not needed on Smith/Polar (they take the complex value directly). Complex format (Table) — magnitude/angle, real/imag, or dB/angle for complex columns. Markers — drop interactive markers to read values; on a loadpull contour, markers read the surface, and the max-power / max-efficiency optima are flagged. See also: Simulations (what each analysis produces) · Components ."],[29,"","","Every run's results are written to disk as a self-describing NumPy `.npy` file you can read from Python, MATLAB, or any tool that speaks NumPy — no re-running the simulation. This chapter covers where results live, how to export them in other formats, and how to read the `.npy` in a few lines of Python."],[29,"datacube","The result model: DataSet & DataCube","Every analysis returns a DataSet — a named collection of DataCubes . A DataCube is an N-dimensional array of one kind (Real or Complex) with named, labelled axes . For example, a harmonic-balance voltage cube V has axes [node, harmonic, sweep] : the node axis carries string labels like X1.drain , the harmonic axis carries integer orders, and the sweep axis carries the swept values and their unit. This self-describing structure is exactly what the .npy file preserves — so a trace, a marker, or a Python script all address the same data the same way. Measurements ( figures of merit ) are added to the DataSet as named cubes too."],[29,"where","Where results live","A run writes one file holding every analysis it produced, plus a measurements group. It goes to the workspace's shared results folder: <workspace>/results/<name>.npy You choose <name> , in the Analyses panel. Above the analyses list there is a single Results file field — one field for the whole run, not one per analysis card, because a run produces one file. Results file field What happens Left blank (the default) The run writes <schematicKey>.npy , where the key is the cell or schematic name. Every run under that schematic overwrites it. A name you type The run writes that file instead. This is how you keep a baseline: name one run before-tuning.npy , clear the field, and subsequent runs go back to the default file while the baseline stays put. The name is sanitised to a plain file name — .npy is appended if you leave it off, and path separators are stripped, so the field names a file , not a folder. Every result of every schematic in the workspace lands side by side in that one results/ directory, which is what lets the Data Display offer them all in one picker. A named file still overwrites Naming a file preserves it only until you run again under the same name . There is no automatic run history and no numbered suffixes: the named file is overwritten silently on every subsequent run under that name. Clear the field once you have the baseline you want. A scratch schematic — one you have not saved into a workspace — still writes its results, into the scratch recovery session's own results/ folder, so a quick experiment is plottable without saving anything first. See Grouped results below for what is inside the file. The Data Display reads these files directly; nothing is recomputed to plot a result you already ran."],[29,"export","Exporting in other formats","Open the exporter from File → Export… or the Export button on the Data Display toolbar. Pick a run (any results file in the workspace's results/ folder), choose what to include, and pick a format: Format Extension What it's for NumPy .npy The full grouped DataSet, lossless — the format this chapter documents. MATLAB .mat The same cubes as MATLAB struct fields. Tab-delimited text .txt Columns of numbers for spreadsheets / quick inspection. Touchstone .s N p S-parameters for a single group — renormalized to one real reference impedance (you set Z0 ); choose magnitude-angle, dB, or real-imag. Loadpull SPL .spl A loadpull-shaped result as an SPL dataset (offered only for loadpull runs). Loadpull LP-CWave .lpcwave A loadpull-shaped result as an LP-CWave dataset (offered only for loadpull runs). You can include or exclude the measurements group, and Touchstone/loadpull exports let you pin or iterate sweep axes to slice out the block you want. Multi-frequency loadpull results export across all their frequency blocks. The same formats come out of a headless run. Every run verb takes -o <path> , and the extension picks the format — hb , lp and lpp write .mat , .npy or .txt ; lp also writes .spl and .lpcwave ; sparam always writes Touchstone; em writes both a Touchstone and a grouped .npy carrying its diagnostics. See The Command Line ."],[29,"groups","Grouped results","A run that has several analyses produces one grouped DataSet : a group per analysis (named for the analysis — HB1 , SP1 , DC1 , …) plus a measurements group for your figures of merit. In the .npy file: The metadata lists the groups in order, and every cube records which group and cube it is. A cube is addressed by its qualified name ( HB1.V ) or, when unambiguous, its bare name ( V ) — the same way a measurement references it. A single-analysis run (e.g. a CLI S-parameter export) typically has just one group plus possibly measurements ."],[29,"python","Reading a .npy in Python","The file is a single NumPy structured array. The __meta__ field is a JSON blob describing every cube — its group, kind, and axes (names, units, values, labels). Read the metadata, then index the cube field: import json, numpy as np arr = np.load('run.npy', allow_pickle=False) meta = json.loads(arr['__meta__'][0]) # bytes → dict assert meta['format_version'] == 2 print('groups:', meta['groups']) # e.g. ['HB1', 'measurements'] # Map (group, cube) → the numpy field name. Read group/cube from __meta__; # the field name itself is opaque — never parse it. field_of = { (e['group'], e['cube']): f for f, e in meta.items() if isinstance(e, dict) and 'cube' in e } # Pull the HB voltage cube and its axes vf = field_of[('HB1', 'V')] V = arr[vf][0] # shape [node, harmonic, sweep], complex128 axes = meta[vf]['axes'] # Find a node by its label, plot its fundamental vs the sweep nodes = axes[0]['labels'] # ['X1.gate', 'X1.drain', ...] drain = nodes.index('X1.drain') sweep = axes[2]['values'] # e.g. Pin in dBm fund = V[drain, 1, :] # harmonic order 1, all sweep points for pin, v in zip(sweep, fund): print(f'Pin={pin} dBm |V_drain|={abs(v):.4f} V') Reconstructing un-probed nodes An export can optionally carry the run's linear network (the MNA matrices and source vectors), which lets a consumer reconstruct any linear-interior node voltage or branch current that wasn't stored as a cube — by solving the linear system rather than re-running the simulation. The full math, field-by-field layout, and a worked Python example are in the developer guide docs/design/npy-data-consumer-guide.md ."],[29,"stability","Format stability","Alpha — not yet stable The on-disk .npy layout is not stable during the alpha. The current format_version is 2 ; always check it before reading and reject a mismatch. Backward compatibility is explicitly declined — if the exporter changes, regenerate your files. Don't build persistent archives or third-party tooling on this format until it is declared stable (post-v1.0). See also: Measurements · Plot types · Simulations . Full developer-facing format spec (groups, the __meta__ schema, and linear-network reconstruction): docs/design/npy-data-consumer-guide.md ."],[30,"","","circuitRF runs without the GUI — not just its engines, but authoring, validation, resolution and drawing too. One executable, fifteen verbs — S-parameters, DC, harmonic balance, loadpull, loadpull pursuit, electromagnetic extraction, layout interchange, creating a workspace or a cell, importing a part, rendering a document as a picture, checking a design, explaining what it resolved to, reading a result back, an elaborated-netlist dump, and an MCP server. Every one of them answers --json. This chapter is the operational reference for all of them, including a worked EM run and a worked render, each from an empty folder."],[30,"invoking","Invoking it","The command-line driver is the same program as the GUI's Run button with the window taken off. It reads the same files, elaborates them with the same elaborator, runs the same engines, and evaluates the test bench's measure lines with the same evaluator. $ circuitrf <verb> <file> [options] From a source checkout there is no circuitrf on your path yet, so put dotnet run --project src/Cli -- wherever circuitrf appears: $ dotnet run --project src/Cli -- sparam mycircuit.cnl --freq 1GHz:3GHz:50MHz Run it with no arguments for the built-in help. A file that works headless works when opened This is the point of the command line being the same code rather than a second implementation. A .cnl that runs here runs when you open it in the workspace, and an EM setup run with em writes the byte-identical Touchstone the Simulate button writes. There is one elaborator, one set of engines, one measurement evaluator and one results-path convention behind both."],[30,"verbs","The verbs at a glance","Verb Takes Runs Writes sparam .cnl or .csch The linear S-parameter engine over a frequency sweep A Touchstone .sNp , always dc .cnl or .csch The nonlinear DC engine Node voltages and probe currents, to stdout hb .cnl or .csch Harmonic balance, single- or multi-tone Spectra tables to stdout; -o .mat/.npy/.txt lp .cnl or .csch Loadpull over the directive's Γ grid A per-Γ-point table; -o .mat/.npy/.txt/.spl/.lpcwave lpp .cnl or .csch Loadpull pursuit — searches for the optima Optima + the follow-on grid; -o as hb ; --out-grid writes a .gam em .cem The EM kernel the setup resolves to A Touchstone .sNp and a grouped .npy , where Simulate writes them convert any layout format The same importer and exporter File ▸ Import/Export runs The layout in the format you asked for new workspace a directory The same code File ▸ New Workspace runs A .cws and, unless you say otherwise, a copied technology new cell a workspace + a name The same code New Cell runs A cell folder and one empty-but-valid file per view import part a component file or folder The same code Import Component runs A cell folder holding the land patterns and the symbol render a .csch , .csym , .clay or .cdd , a cell folder, or a workspace The same Skia renderers the editors draw every frame with A .svg , .pdf or .png , where -o says check a workspace, a cell folder, or one document Every validator the application already uses Nothing — findings to stdout explain the same Resolution only — no analysis Nothing — the walk and the answer, to stdout read a result file, or one of circuitRF's own documents The same loaders the Data Display reads a file with Nothing — what the file holds, to stdout netlist a .csch , a cell folder, or a workspace The same extraction Simulate performs A .cnl , or the netlist text to stdout plot a result file Builds a one-plot data display and draws it A .svg , .pdf or .png , where -o says find a directory Nothing — it reads documents Nothing — the workspaces, cells, views and analyses under it reference nothing Nothing — it reads no file Nothing — the reference pages, and every netlist primitive with its terminals and parameters elab .cnl or .csch Elaboration only, no analysis The elaborated netlist, to stdout serve --root <dir> An MCP server for an external client Whatever the tool it is asked for writes hb , lp and lpp all run the whole parametric sweep when one wraps the analysis — see naming the wrapper . Every run verb takes a schematic as well as a netlist. Hand it a .csch and it extracts the netlist in memory first — the same extraction Simulate performs — so you do not have to write one out to run a design you drew. netlist is how you see what it will run."],[30,"channels","Results on stdout, everything else on stderr","stdout is the result. stderr is everything else — progress, per-grid-point engine chatter, [circuitRF] notes, elaboration and engine warnings, device-worker logs. That split is what makes the output pipeable while the terminal still shows a long run moving: $ circuitrf lp hero3.cnl > table.txt table.txt gets the loadpull table and nothing else; the per-drive-step [LP] lines and the convergence notes still scroll past on screen. Redirect 2&gt;/dev/null to silence them, or 2&gt;run.log to keep them."],[30,"common","Options every verb takes","Option What it does --kits <dir> A folder of installed kits, so an externally-supplied device model ( ExtDevice Provider=… ) resolves headlessly the way opening a workspace resolves it in the GUI. Repeatable. --json Put one JSON document on stdout and nothing else — see below . stderr is untouched. --only a,b Narrow that document's result to these cubes. --group g,h Narrow that document's result to these groups. --at axis=value Narrow it to one point of an axis — --at freq=2GHz . The nearest grid point, and the document says which one it gave you. --interp Make every --at interpolate between the two bracketing points instead. Never the default: it returns a number the run did not compute, and the document says so. --range axis=lo:hi Keep a band of an axis — --range freq=1GHz:3GHz . --result full\\|summary summary returns the result's shape — group and cube names, units, axis lengths and extents — and no values at all. --summary Report the informational notes as counts by severity instead of in full. Warnings and errors always travel in full, and stderr is untouched. Frequencies are written as 1GHz , 100MHz , or bare Hz ( 1e9 ) anywhere a frequency is accepted. Ask for the part you want, not the whole result --only and --group narrow by cube name , which does nothing when the result has one cube. A 551-point two-port S-parameter run is about 173 kB of JSON; if the question is \"what is S21 at 2 GHz\", --at freq=2GHz --only S is a few hundred bytes. Every value carries its own unit — a bare 2 could be 2 Hz or 2 GHz — and an axis name nothing in the result has is refused, listing the ones that exist, rather than quietly handing you everything. Every run returns result.shape whether or not it returns the values, so --result summary is how you find out what a run produced before deciding what to ask for. An option a verb does not take is refused, never ignored Every verb stops with unknown option '…' and exit 1 rather than dropping a flag it does not recognise. This matters more than it sounds: most verbs find their input file as the first argument that is not an option , so a silently dropped flag's value would be read as the file name — and a flag that carries an override, like --set , would simply not be applied, giving you a run that answers a different question with nothing to say so."],[30,"sparam","sparam — S-parameters","$ circuitrf sparam <file.cnl|.csch> [--freq start:stop:step] [-o out.sNp] $ circuitrf sparam hero1.cnl --freq 1GHz:3GHz:1GHz -o hero1.s2p S-parameter analysis: 3 points, 1–3 GHz Wrote hero1.s2p Option What it does --freq start:stop:step Override the sweep. Omit it and the netlist's own sparam analysis is used , segments and all — which is almost always what you want, because it is the sweep the design was set up with. -o , --output <path> Where the result goes, and its extension picks the format : .s1p … .s99p for a Touchstone, or .npy / .mat / .txt for the cubes. Omitted, it is the input file with its extension changed to .sNp for the port count found. There is no stdout table. The port count in the default extension comes from the network, so a circuit that grew a port writes .s3p without you editing the command. An extension naming no format this verb writes is refused, listing the ones it does — you never get a Touchstone under a name that says otherwise. A run carrying WSProbes prints one line per probe after the S summary, and a run with NDF=yes on its directive prints the right-half-plane pole count: $ circuitrf sparam amp.cnl S-parameter analysis 'SP1': 2001 points, 0.5-3 GHz (1 segment(s)) NDF: 2 right-half-plane pole(s) (net clockwise encirclement 1.989; NDF(100 GHz)=1 ∠ 1.8) WSProbe P idx=1 H0(0.5 GHz)=11.597 ∠ -1.4 ZG(0.5 GHz)=10.482 ∠ 17.4 SM_Y0 min -18.1 dB @ 1.59125 GHz SM_H0 min -19.8 dB @ 1.73375 GHz --json carries the same under wsprobes and ndf . The margins are linear there and dB on the line , because dB is a display convention and a document should carry the number. A circuit that has WSProbes and no ports at all is a legitimate run — it writes every wsp cube and no S — and asking it for a Touchstone is refused, naming the spellings that do carry the result. Ports with different reference impedances A Touchstone file declares one reference impedance, and circuitRF writes port 1's on the option line. When the ports differ, the file also carries a header note listing each port's own impedance and saying that the data is referenced to those — and circuitRF reads that note back, so circuitrf read on the file reports the real per-port references rather than the option line repeated. Nothing is renormalized: the numbers are the ones the solve produced. If you want the per-port references in a form every tool reads, write .npy instead."],[30,"dc","dc — the operating point","$ circuitrf dc <file.cnl|.csch> $ circuitrf dc hero2.cnl DC: converged in 3 iteration(s), residual 7.27E-16 Node voltages: 0 0 n_src 0 n_gate -3.05 n_drain 48 No options beyond the common ones. It prints the converged node voltages and any probe currents, and exits 2 if the solve did not converge — the operating point is the one thing every nonlinear analysis is built on, so a non-converged DC is a failed run, not a partial one."],[30,"hb","hb — harmonic balance","$ circuitrf hb <file.cnl|.csch> [-a name] [--set var=expr] [-o out.npy] The same verb runs single- and multi-tone — which it is comes from the netlist's directive, not from a flag. $ circuitrf hb hero2.cnl --rows 6 HB 'HB1': f0=2 GHz, MaxHarm=4, tol=1E-06 Analysis: HB1 (hero2.cnl) Converged: yes (1 solve(s)) Residual: 1.24E-09 (worst) Tones: 2 GHz V [node:7 x harmonic:5] (mag ∠deg) 0 1 2 n_gate 3.05 ∠ 180.0 0.029814 ∠ 0.1 0 n_drain 48 ∠ 0.0 0.15004 ∠ -172.4 1.6824E-05 ∠ -179.8 … 1 more row(s) — use --all or --rows N Option What it does -a , --analysis <name> Which analysis to run. Optional when the file declares one HB chain. --set <var=expr> Override a global variable before elaboration . Repeatable. --maxharm K Override MaxHarm . --maxmix M Override MaxMixOrder (multi-tone only). --tol t , --max-iter N Override the convergence tolerance and the iteration cap. --rows N , --all How much of each printed table to show. Default is a truncated head. --diag Engine convergence diagnostics, on stderr. -o , --export <path> Export the results. The extension picks the format : .mat , .npy or .txt ."],[30,"set","--set overrides the VARIABLE, not the number","--set Pavl_dbm=0 replaces the global variable in the test bench's own scope, then elaborates. So every expression derived from it re-derives — a bias that was written Vg = Vth + 0.2 follows a changed Vth , and a sweep computed from the variable sweeps the new values. An override pushed at the engine instead would move one number and leave everything computed from it stale, which is why there is no such option."],[30,"wrapper","Name the wrapper, or name nothing","When a parametric sweep wraps an analysis, the sweep is what runs. Naming the inner analysis with -a is promoted to its outermost enabled wrapper, and the promotion is announced: [circuitRF] 'HB1' is the inner analysis of 'SW1' — running 'SW1' so the sweep axis is not lost. Why it is promoted rather than obeyed Running the inner analysis alone produces a converged, plausible, complete-looking result at one operating point — with the sweep axis silently missing . Nothing about it looks wrong. A frequency-swept loadpull has exactly this shape, which is why the rule is the same for every verb rather than something harmonic balance does on its own. If more than one runnable chain exists, all their names are printed and the first runs; if none does, the message says whether the netlist declares no such analysis or declares one that is disabled."],[30,"measurements","Measurements","The measure lines on the test bench are evaluated exactly as the GUI evaluates them, and the results join the exported DataSet as named cubes. A measurement that fails to evaluate is reported on stderr and the run continues — one bad expression does not throw away a run that took minutes: [circuitRF] measurement: Measurement 'Gain_dB': failed to evaluate 'Pout_dBm - Pavl_dbm': Unresolved name 'Pout_dBm' in scope 'measurements'"],[30,"lp","lp — loadpull","$ circuitrf lp <file.cnl|.csch> [--grid grid.gam] [--pin start:step:max] [-o out.spl] lp sweeps the load (or source) termination over the directive's Γ grid, runs a harmonic-balance drive ladder at each point, and reports the figures of merit. $ circuitrf lp hero3.cnl --rows 8 Analysis: LP1 (hero3.cnl) Grid: 20 point(s) — 0 reached compression, 20 stopped at max drive Nothing reached compression — raise --pin's max (or the directive's PinMax). # GammaLoad ZLoad (ohm) stop Pavl Pout Gt DE% PAE% 0 0.0000 ∠ 0.0 50.00+j0.00 max drive 10.00 20.54 10.54 3.35 3.09 1 0.2000 ∠ 0.0 75.00+j0.00 max drive 10.00 22.31 12.31 5.03 4.76 2 0.2000 ∠ 90.0 46.15+j19.23 max drive 10.00 20.19 10.19 3.09 2.83 … 12 more point(s) — use --all or --rows N Option What it does -a , --analysis <name> Which loadpull analysis to run. --set <var=expr> Override a global variable before elaboration. Repeatable. --grid <file.gam> Override the Γ grid the directive reads. Resolved against your working directory , not the netlist's. --pin start:step:max Override the drive ladder, in dBm. --compression dB Override the compression target. --maxharm K , --tol t , --max-iter N Override the inner HB settings. --rows N , --all --all dumps every cube instead of the summary table. --diag Engine diagnostics, on stderr. -o , --export <path> .mat , .npy , .txt — or .spl / .lpcwave , the loadpull interchange formats."],[30,"lp-rows","One row per Γ point, at the point that answers the question","A loadpull's raw cubes are [gridPoint × driveStep] — a 61-point grid driven up in 1 dB steps is a 61 × 30 table per figure of merit , and eight of those scroll a terminal without answering anything. So the default table is one row per Γ grid point : where it was, how it stopped, and its FOMs at the last converged, non-tickle drive step — the compression point where the point compressed, the highest drive it managed otherwise. Reading a fixed drive index instead would mix compressed and uncompressed points in one column. --all still dumps everything. A swept run prints one table per sweep point."],[30,"lp-export",".spl and .lpcwave","-o out.spl writes the loadpull interchange format the Data Display reads back as a measured surface, so a headless run can produce a file the GUI opens. lp also runs the same post-processor a GUI run does, so the exported cubes carry the derived display metrics ( Pout_dBm , Zin , IRL_dB , AMPM_deg ) — a .npy written here and one written by the GUI carry the same cubes."],[30,"lpp","lpp — loadpull pursuit","$ circuitrf lpp <file.cnl|.csch> [--out-grid found.gam] [-o out.npy] A pursuit searches for the max-power (MXP) and max-efficiency (MXE) terminations rather than reading a grid, then runs a follow-on loadpull over the terminations it recommends. $ circuitrf lpp hero3B_at_compression.cnl Analysis: LP1 (hero3B_at_compression.cnl) Pursuit optima: MXP (max power) converged Pout=40.625 dBm Zload=80.48+j0.00 Zsource=50.00+j0.00 MXE (max efficiency) converged Eff=69.617 % Zload=140.31-j4.95 Zsource=50.00+j0.00 21 termination(s) queried, 45 recommended termination(s) Grid: 45 point(s) — 45 reached compression # GammaLoad ZLoad (ohm) stop Pavl Pout Gt DE% PAE% 0 0.2690 ∠ 7.0 86.15+j6.11 compressed 26.00 40.56 14.56 67.08 64.74 1 0.2030 ∠ 9.6 74.80+j5.30 compressed 27.00 40.68 13.68 63.55 60.82 lpp takes every lp option except --grid , and adds --out-grid : Option What it does --out-grid <file.gam> Where the terminations the pursuit found are written, as a .gam you can feed back to lp . Resolved against your working directory. The two grid options are refused, not ignored --grid on lpp and --out-grid on lp each stop the run with a sentence naming the verb that owns them. A grid option silently doing nothing would be a run that answered a different question and said nothing about it. A non-converged optimum is still printed, with its status. The engine publishes the last termination it looked at, and printing nothing there reads as \"the search found nothing\" when what actually happened is \"nothing it tried reached compression\"."],[30,"em","em — electromagnetic extraction","$ circuitrf em <setup.cem> [-o out.sNp] [--workspace file.cws] em is the only verb that does not take a .cnl . It takes a .cem EM setup — the document the EM Setup panel edits — and runs it: extracts the geometry from the layout the setup names, resolves the stackup, meshes, solves the frequency plan, de-embeds, and writes the results. It needs no other arguments. Everything else it needs is already recorded in the files."],[30,"em-inputs","What an EM run takes","Four files, and three of them are things you already have if you have drawn a layout: File What it supplies Where it comes from .cem The setup: which layout, which analysis, the frequency plan, port impedances and types, mesh settings, solver switches File ▸ New ▸ EM Setup… , or the layout editor's EM button .clay The artwork — the metal, and the port labels for a full-wave run The layout editor .ctech The stackup : layer thicknesses, ε_r, tanδ, conductivity, which conductor is ground, and which drawing layers map onto what The technology editor, or one of the shipped starter technologies .cws The workspace marker, carrying DefaultTechRef — the technology a layout uses when it does not name one itself Created with the workspace Author the setup in the GUI; run it from the command line The em verb runs a setup — it does not create or edit one, and it will not repair one. A setup with no ports, no technology or no signal conductor is refused with the sentence explaining what is missing . Build the .cem once in the EM Setup panel , where every control tells you as you type whether the run is blocked and why, then commit it beside the layout and run it headlessly from then on."],[30,"em-resolution","Both file references resolve by walking UP, and neither is a flag","A .cem names a layout; the layout names — or inherits — a technology. Neither reference is stored absolutely, and neither needs an argument: The layout. The setup's layout reference is relative to the workspace root : the nearest ancestor .cws found by walking up from the .cem . With no workspace above it at all, the reference falls back to the .cem 's own directory, so a loose .cem sitting beside its .clay simply works. The technology. Resolved against the layout's own parent workspace , found by walking up from the .clay — never against \"the workspace you are in\", of which there is none headlessly. A .clay that names no technology picks up its workspace's DefaultTechRef . The two walks start from different files, and that is deliberate. A .cem in one workspace may point at a layout in another, and that layout's layers have to be read by its technology, not by whichever workspace the setup happened to live in. --workspace <file.cws> overrides the first walk, for a .cem being run from outside its own tree. It is never required. The three resolutions are echoed on stderr before anything expensive starts, so you can see what the run is actually about to read: [circuitRF] workspace: /work/amp/.cws [circuitRF] layout: /work/amp/Line/layout/Line.clay [circuitRF] technology: /work/amp/pcb.ctech"],[30,"em-example","A worked example, from an empty folder","Here is a complete, minimal EM workspace — a single 20 mm × 2.9 mm microstrip line on a two-layer PCB technology, swept 1–10 GHz in 3 points. Four files: amp/ ├─ .cws the workspace marker, naming the default technology ├─ pcb.ctech the stackup ├─ line.cem the EM setup └─ Line/ └─ layout/ └─ Line.clay the artwork The .cem is JSON, and this is all of it — every field not written takes its documented default: { \"FormatVersion\": 1, \"Name\": \"line\", \"LayoutRef\": \"Line/layout/Line.clay\", \"Frequency\": { \"StartExpr\": \"1\", \"StopExpr\": \"10\", \"NumPoints\": 3, \"Mode\": \"PointCount\", \"Kind\": \"Linear\", \"StartUnit\": \"GHz\", \"StopUnit\": \"GHz\" }, \"Port1Z0Real\": 50, \"Port2Z0Real\": 50 } LayoutRef is workspace-relative — relative to the directory holding .cws , not to the .cem . The .cws supplies the technology: { \"DefaultTechRef\": \"pcb.ctech\" } Nothing in the .cem names a technology, a kernel, a mesh or a port. The technology is inherited, the kernel is chosen from the geometry, the mesh settings are the engine's own defaults, and this structure's ports are the two ends of a uniform line by construction. Then: $ circuitrf em amp/line.cem [circuitRF] workspace: amp/.cws [circuitRF] layout: amp/Line/layout/Line.clay [circuitRF] technology: amp/pcb.ctech [0] solving the cross-section [3] solving the cross-section note: Automatic chose \"Uniform transmission line\": this geometry is a uniform cross-section, which that analysis solves exactly and is about a thousand times cheaper than \"Full-wave planar\". Set Analysis to \"Full-wave planar\" if you want the full-wave answer anyway. note: Dielectric interfaces truncated 20 substrate heights (32000 µm) beyond the outermost conductor on each side. EM setup: line Kernel: Quasi-static cross-section (CrossSection) Points: 3 Wrote amp/results/line.s2p Wrote amp/results/line_em.npy Everything from EM setup: down is on stdout ; the resolution lines, the progress and the notes are on stderr. A full-wave run differs only in what the files say, not in how you invoke it: draw port labels in the layout with the layout editor's Port tool, set the setup's analysis to Planar (or leave it Auto and let the geometry decide), and run exactly the same command. It will take very much longer — a de-embedded full-wave point costs tens of seconds at the shipping mesh — which is why the progress lines exist."],[30,"em-output","Where the results go, and what -o moves","With no -o , the run writes exactly where the Simulate button writes : into the workspace's results/ folder. Two files come out, and they are not redundant: File Holds <name>.sNp S-parameters only — the artefact a schematic's SnP component references by path <name>_em.npy The whole DataSet , including the per-kernel diagnostics group — Z_c, γ, ε_eff, RLGC for the cross-section kernel; the calibration residual and usability flags for the full-wave one Why the default path is not the CLI's to choose That results path is predictable by design , so a schematic's SnP reference stays valid across re-runs. A headless run that minted its own file name would orphan every one of them — so circuitrf em writes the same file Simulate does, and the acceptance test for the verb compares the two Touchstones byte for byte . -o moves the Touchstone only . The .npy stays where it was, because it is the diagnostics record of the run rather than the deliverable: $ circuitrf em amp/line.cem -o /tmp/mine.s2p Wrote /tmp/mine.s2p Wrote amp/results/line_em.npy You do not have to get the extension right — the port count decides it, so a .s2p you typed for a structure that turned out to have four ports is written .s4p . With no workspace above the .cem , results/ is created beside the .cem itself."],[30,"em-messages","note, warning, error — three lists, kept apart","An EM run has three different things to say and they ask three different things of you, so they are printed under three labels rather than flattened into one stream: Prefix Means note: The run explaining itself — which kernel it chose and why, the mesh's own sentences, RLGC, the ports it found. Read these; they are the cheapest check that the tool is looking at the structure you think it is. warning: Something to act on — a stale .sNp about to be replaced, a technology that resolved but failed validation. error: Something you asked for and did not get — a results file that could not be written."],[30,"em-refusals","A refusal is a result","The EM engine declines geometry it cannot solve correctly rather than returning a plausible number. Each refusal carries a written explanation of what is wrong with this setup, and em prints that explanation rather than collapsing it into \"EM failed\": [circuitRF] workspace: amp/.cws warning: Layout file not found: amp/Line/layout/Missing.clay No layout: The layout 'Line/layout/Missing.clay' could not be found, so there is no geometry to analyse. Point this EM setup at a layout that exists. Status Means Exit Refused The extractor or the kernel declined this geometry — see what the engine refuses 1 No layout The layout reference did not resolve 1 Engine error The solve failed 1 Cancelled Stopped at a work boundary 130"],[30,"convert","convert — layout interchange","$ circuitrf convert <input> -o <output> [options] Reads a layout in any format circuitRF understands and writes it in any other. It is the same reader and the same writer File ▸ Import and File ▸ Export run — see Interchange for what each format can and cannot carry — so a conversion here and the same conversion through the GUI produce the same bytes. Format Named by As input As output circuitRF layout .clay the file a folder of cells plus a .ctech GDSII .gds , .gdsii , .gds2 ✓ ✓ DXF .dxf ✓ ✓ Gerber + Excellon a folder , or one Gerber/drill file ✓ a folder Board .kicad_pcb ✓ ✓ Every ordered pair works — DXF to Gerber, Gerber to board, GDSII to DXF, board to GDSII, and the rest. There is no privileged direction and no hub format you have to route through by hand: a conversion is an import followed by an export, and convert does both. Formats are read off the paths. A folder means Gerber; a file with no telling extension is classified by its content , through the same classifier the Gerber import uses. --from and --to override that, and --to is required when the output is a folder, since a folder could be either Gerber or .clay ."],[30,"convert-examples","Examples","Board file out to a fab house as artwork plus drill: $ circuitrf convert board.kicad_pcb -o fab/ --to gerber A folder of Gerbers back to a board file: $ circuitrf convert fab/ -o recovered.kicad_pcb A mechanical drawing straight to artwork — no board tool in the middle: $ circuitrf convert outline.dxf -o gerbers/ --to gerber A mask set to a drawing your mechanical engineer can open, at the DXF version their tool wants: $ circuitrf convert mmic.gds -o mmic.dxf --dxf-version AC1015 Bring a board in as editable circuitRF cells and keep the technology it declared: $ circuitrf convert board.kicad_pcb -o cells/ --to clay A clay target is a folder , not a file An import writes one cell folder per structure the source holds, plus the technology beside them — so -o cells/ is the shape, and -o cells/board.clay is refused with the folder spelling in the message. Point it at a folder and look inside: the .clay is at cells/<Cell>/layout/<Cell>.clay , which is where every other circuitRF tool expects a layout view to be. One cell out of a GDSII library that holds many: $ circuitrf convert lib.gds --list-cells $ circuitrf convert lib.gds -o coupler.dxf --cell COUPLER Convert a directory of drawings in one line: $ for f in dxf/*.dxf; do circuitrf convert \"$f\" -o \"gds/$(basename \"${f%.dxf}\").gds\"; done"],[30,"convert-options","Options","Option What it does -o, --output <path> The file to write — or the folder , for gerber and clay ; a file-shaped path there is refused. Required. --from <fmt> , --to <fmt> clay , gdsii , dxf , gerber , board . Say it when the path does not. --cell <name> Which cell to export, when the source holds several. --list-cells Report what the input holds and write nothing. --name <stem> What to call the written Gerber file set. Default: the cell's name. --tech <file.ctech> The technology to convert against, instead of the one the layout resolves. --workspace <file.cws> The workspace a .clay 's references resolve against. Default: the nearest one above it. --keep-cells <dir> Keep the cells the import produced instead of discarding them. --dbu <n> Database units per micron for an imported design. Default 1000 — one DBU is one nanometre. --dxf-version <v> AC1015 (R2000), AC1018 (R2004), AC1032 (R2018, the default). --dxf-units <n> The $INSUNITS value for a DXF that declares none. --drill-units <mm or inch> Excellon coordinate units, when the file does not say. Applies to every drill file in the set. --drill-format <int>:<dec> Excellon digit counts, e.g. 2:4 . Applies to every drill file in the set. --drill-zeros <leading or trailing> Excellon zero suppression. Applies to every drill file in the set. --accept-inferred-drill-format Take each drill file's own inference rather than refusing. --open-archives Look inside an archive when a Gerber folder holds no artwork of its own. It is unpacked to a temporary folder, imported from there, and deleted again; nothing is added to the folder you named. Without this flag, such a folder is a refusal that names the flag."],[30,"convert-cell","Which cell gets exported","A GDSII library, a DXF drawing and a board file can all hold more than one cell, and an export writes one design. Unless --cell says otherwise, convert takes the source's own idea of the top: the GDSII structure nothing else instances, DXF's model space (the drawing itself, not a BLOCK definition), the board rather than one of its footprints. A Gerber set is always one flat cell. When the source genuinely has no unambiguous top, the conversion stops and tells you to name one — --list-cells prints the choices."],[30,"convert-tech","The technology, and why it matters here","An import brings a layer table with it, and in the GUI those layers land on the technology your workspace already has open. Headless there is no open workspace, so convert writes a .ctech of its own from what the file declared, exactly as File ▸ Import ▸ Gerber does. That is what keeps layer names, colours and Gerber file suffixes alive across a conversion instead of leaving every layer a bare number. Two consequences worth knowing: --tech is how you convert against a process you already have. Point it at a .ctech and the source's layers reconcile against it — matched layers keep your names and your Gerber suffixes, unmatched ones are added. Without it, an intermediate technology is invented from the file alone, and a Gerber export then names its files from synthetic suffixes. --keep-cells <dir> leaves a design you can open. Cells plus the technology they point at — the honest way to see what a conversion actually understood before you send the result anywhere. GDSII is the one exception, and it is the format's own doing. GDSII identifies a layer by a number, not a name, so an import has nothing to name it with : the numbers come through exactly, the names do not. Convert from GDSII with --tech pointing at the technology those numbers belong to and the names come back."],[30,"convert-refusals","When it refuses","A drill file that does not state its format is a refusal, not a guess Many Excellon files do not say whether their coordinates are inches or millimetres, or whether leading or trailing zeros are suppressed — and leading versus trailing differ by four orders of magnitude on identical text. The GUI asks you. There is nobody to ask here, so the conversion stops, prints what it inferred and the evidence behind it — including whether the holes land inside the artwork's own outline — and names the flags that answer it. Accept the inference with --accept-inferred-drill-format , or state it outright with --drill-units , --drill-format and --drill-zeros . A --drill-* flag settles the whole set, not the first file. A drill flag is a statement about the run — one exporter wrote the .drl and the .rou next to it in one format — so it applies to every drill file the conversion reads, and the refusal is printed once rather than once per file. --accept-inferred-drill-format works the same way, with one difference worth knowing: it accepts each file's own inference rather than forcing the first file's format onto the rest. $ circuitrf convert fab/ -o board.kicad_pcb --drill-units mm --drill-format 3:4 --drill-zeros leading Reach for the flags less often than you might expect: a file that writes every coordinate at its full width — same number of digits throughout, leading zeros intact — states its own format by doing so, and the conversion reads it off the coordinates and says as much. The flags are for the files that leave a genuine question, and the note printed for every drill file names which parts of its format were declared , which were inferred , and from what. It also stops, rather than guessing, when a design instantiates cells drawn against a different technology and the layer mapping needs confirming; when coordinates overflow GDSII's 32-bit range; and when the source holds several cells and none of them is an unambiguous top. Every refusal exits 1 and writes nothing at all. Everything short of a refusal is a note on stderr , counted and named: labels flattened to geometry, curves turned into polygons, holes keyholed, bitmaps dropped, unresolved instance references, layers with no mapping in the target format. stdout carries only the paths written, one per line, so a script can consume them directly: $ circuitrf convert board.kicad_pcb -o fab/ --to gerber 2> convert.log | zip -j fab.zip -@"],[30,"new","new — a workspace or a cell","$ circuitrf new workspace <dir> [--name N] [--tech <id>|none] $ circuitrf new cell <workspace> <cellName> [--views schematic,symbol,layout] These create the first correct document — the thing that is awkward to write by hand because the folder structure and the primacy files have to be right before anything will open it. They are not a second implementation. new workspace calls the same function File ▸ New Workspace calls, and new cell the same one New Cell calls, so a tree created here is a tree the application created. $ circuitrf new workspace ~/designs/Amp --tech pcb-4layer_FR-4_62mil_1oz /home/you/designs/Amp /home/you/designs/Amp/.cws /home/you/designs/Amp/tech/pcb-4layer_FR-4_62mil_1oz.ctech $ circuitrf new cell ~/designs/Amp Stage1 --views schematic,symbol /home/you/designs/Amp/Stage1 /home/you/designs/Amp/Stage1/schematic/Stage1.csch /home/you/designs/Amp/Stage1/symbol/Stage1.csym The paths it creates are the result , on stdout, because what you do next is almost always read or rewrite one of them. Every default is the dialog's Whatever the GUI's dialog pre-selects, the verb selects with no flag: --tech opens on the same technology the New Workspace combo box opens on ( --tech none is its \"None\" row), and --views defaults to schematic , which is what New Cell creates. Anything the dialog would have asked is a refusal that names the flag answering it — never a guess. There are deliberately no per-primitive edit verbs. There is no place-instance and no set-parameter : once a document exists, the way to change it is to write it. Every format circuitRF owns is readable, versioned JSON — see File formats — and that file is the interface."],[30,"new-add","`new` is one verb with a noun","new workspace and new cell are two nouns of one verb, not two verbs. It reads better and, more to the point, the number of top-level verbs is a cost every reader of --help pays."],[30,"import","import part — a footprint and its symbol","$ circuitrf import part <file-or-folder> --into <workspace> [--cell N] [--variant V] [--list-parts] [--tech f.ctech] [--add-layers] The same code Import Component runs: it reads a downloaded component — a land pattern, a symbol, and the pin-to-pad map that joins them — and writes it into your workspace as one cell. $ circuitrf import part downloads/SOT-23.zip --into ~/designs/Amp --list-parts SOT-23-3 SOT-23-5 $ circuitrf import part downloads/SOT-23.zip --into ~/designs/Amp --cell SOT-23-3 /home/you/designs/Amp/SOT-23-3 /home/you/designs/Amp/SOT-23-3/layout/SOT-23-3.clay /home/you/designs/Amp/SOT-23-3/symbol/SOT-23-3.csym A source holding several parts is refused with them listed , never resolved by taking the first. Layers the technology does not have are reported, and nothing is written , unless you pass --add-layers . The GUI's own install is session-only and writes nothing to disk either, so this is the same behaviour and not a headless restriction."],[30,"render","render — a picture of a document","$ circuitrf render <path> -o <out.svg|.pdf|.png> [options] Turns a schematic, a symbol, a layout or a data display into a file you can look at, put in a report, or diff between two commits. Every pixel comes out of the renderers the editors draw each frame with — the same Skia that produces the picture on the canvas — so the file is what the window would have shown, not an approximation of it. One verb over every document kind , inferred from the path exactly as check and explain infer it. There is no render-schematic . Path Resolved by a .csch , .csym , .clay or .cdd directly — including one in a folder with no workspace above it a cell folder --view , or the one view it holds. More than one is a refusal listing them a workspace --cell <name> — rendering \"the workspace\" is not a picture of anything $ circuitrf render ~/designs/Amp/Stage1/layout/Stage1.clay -o stage1.png Wrote stage1.png (1600x1200 device-pixels, 11,540 bytes) 4 of 4 shape(s) drawn, 12 vertices emitted, 7 draw call(s) -o is required and its extension picks the format — .svg , .pdf or .png ; --format overrides it. There is no picture on stdout: stdout is the result , and --json has to be able to co-exist with the write. A document with no workspace above it renders It resolves no technology, draws on the generated fallback palette exactly as the layout editor does with an unresolved technology, and says so as a note — not a warning and never a refusal. A .clay a converter just handed you has no workspace by construction, and treating that as a problem would teach you to skip the warnings that are problems."],[30,"render-viewport","The viewport, and the unit rule","$ circuitrf render <path> -o out.png --fit [--margin 0.10] $ circuitrf render <path> -o out.png --window x0,y0,x1,y1 $ circuitrf render <path> -o out.png --center x,y --span w The three are refused together rather than ordered . A precedence nobody stated is an invention, so --fit --window … stops rather than quietly picking one. On a layout, every coordinate carries a unit — zero included --window 0,0,500,300 is refused . It could mean database units, micrometres or millimetres, and those are three pictures six orders of magnitude apart — all of which come back looking entirely reasonable. Write --window 0um,0um,500um,300um ; the suffixes are nm , um (or µm , or u ), mm , mil and in . The refusal prints the same number spelled the two ways you most plausibly meant, plus the document's own display unit when it is neither. A schematic or a symbol takes bare numbers , because its coordinates really are dimensionless design units — and --json says \"unit\": \"design-units\" rather than leaving you to assume metres. You do not have to write one from scratch. explain --extents prints a ready-made --window line for the whole document and for each layer, in exactly this spelling. Copy it; there is no conversion to get wrong. A window of a different shape from the page is letterboxed, never cropped and never stretched. You get the whole region you asked for, with bars; the resolved window is in the --json document with letterboxed beside it. Silently giving you less of a region than you asked for is not something you could notice from the picture. --fit frames the box that gets painted , not the box that is stored. A label's stored extent is its anchor point, an EM port paints a width bar and an arrow past its own geometry, and an instance's extent resolves through the cell it places. On a symbol the two genuinely differ: pin names are drawn in pixels, at a size with a floor, so they have no world extent until a page size is chosen — a fitted symbol page is therefore wider than the box explain --extents reports, which is the box you want when you are sizing a --window yourself."],[30,"render-detail","Size, and what `--detail` costs","Option What it does --size WxH Device pixels for .png , points for .svg / .pdf . Default 1600x1200 . --scale n Raster multiplier — 2 is \"@2x\". .png only ; a refusal on a vector format, which has no pixels to multiply. --dpi n The same number spelled relative to 96 dpi. Refused together with --scale . --detail full , screen , or a pixel budget. How much geometry comes out. Layout only. --detail full is the default, and it is not free. full turns every level-of-detail tier off, so what is stored is what is drawn ; screen engages the tiers exactly as a canvas does at that zoom, which is the picture a person actually looks at. Measured on a real six-layer board — 3,284 shapes and 764,032 vertices — framed whole at 1600x1200 : Format --detail full --detail screen .svg 23.5 MB , 1.44 s 6.2 MB, 0.36 s .pdf 9.6 MB, 1.22 s 2.6 MB, 0.51 s .png 1.4 MB, 0.55 s 1.0 MB, 0.35 s A vector file stores every vertex, so full is nearly four times the SVG for a picture that cannot show the difference at that zoom. A raster barely moves, because its size is set by its pixels and not by the geometry behind them. So: --detail screen when you want the picture. --detail full when you want the geometry — a plot you will zoom into, an SVG something downstream will read the paths out of, a diff between two revisions of the artwork. A pixel budget ( --detail 0.5 ) sits between them; --json reports the toleranceDbu it actually resolved to, which is bucketed by octave and so is rarely the number you asked for. The verb prints the vertex count and the file size it produced, and --json carries both, so you find this out from the answer rather than from a 23 MB file."],[30,"render-layers","Layers and colour","$ circuitrf render Stage1.clay -o top.png --layers \"Top Copper,Silk Top\" $ circuitrf render Stage1.clay -o nosilk.png --hide-layers \"Silk Top,Silk Bottom\" Layout only, mutually exclusive, and the default is every layer the resolved technology marks visible — which is what the editor honours, not \"all layers regardless\". Ask explain --layers before render --layers A technology's layer list and the layers a document draws on are different sets, and only the second one puts anything in the picture. explain --layers gives you both at once: every layer the technology defines, with how many shapes this document has on it. A name that is neither in the technology nor drawn on by the document is a refusal that lists the real ones. It is not a silent skip, because a misspelled layer and a genuinely empty layer produce the same picture and you would have no way to tell which you were looking at. A layer the document draws on that the technology does not define — ordinary after an import — is accepted under the generated L<layer>/<datatype> name explain --layers prints for it, and is excluded like any other when you name a different one. Framing on some layers and drawing all of them A fit frames what it draws. So hiding a layer takes it out of the framing as well as out of the picture — which is usually what you want, and occasionally not. The case that bites is an imported board: the drill-map fabrication drawing sits far outside the board outline, and framed with everything else it shrinks the board to a corner of the page. $ circuitrf render board.clay -o board.png --fit-layers \"Top Copper,Bottom Copper\" --fit-layers frames on those and draws everything — so the drill map is still there, it simply falls outside the frame. It is refused with --window and --center/--span , which state the frame outright, and refused when the layers you named draw nothing, because framing on nothing is not a page. With --json each layer row says whether the frame was taken from it. Recolouring a layer for one picture $ circuitrf render board.clay -o copper.png \\ --layers \"L1 Copper,L2 Copper,L3 Copper\" \\ --layer-colors \"L1 Copper=#e04030,L2 Copper=#30a050,L3 Copper=#3060e0\" A Gerber import gives every copper layer nearly the same colour, so a copper overlay comes out unreadable. --layer-colors says how a layer draws for this render only — nothing is written to the technology, because changing a design to change a picture of it is not a fix. The flag can be repeated, and one entry can carry several comma-separated pairs. Colours are #rgb , #rrggbb or #rrggbbaa . The eight-digit form sets the layer's fill opacity , which is the alpha the renderer actually paints a fill through — a layer's own colour alpha is not read at all, so an override that only set it would look applied and change nothing. Layer names are the ones explain --layers prints, including the generated L<layer>/<datatype> names an import produces; a name that is not one of them, or a value that is not a colour, is a refusal rather than a silent skip. --json reports each layer's color and fillOpacity as drawn , which is how you check an override landed — a colour change is the one thing a picture alone cannot confirm. Option What it does --theme A theme name , or the path to a .ccolor file. Default: the workspace's own recorded theme, else the shipped one. A name that resolves to nothing is a refusal listing where it looked. --variant light or dark . Default light . --background opaque or transparent . Default opaque . --grid Draw the grid. Off by default, as every export is. --no-rulers Leave the rulers out. Rulers are on by default and everything else is off. A ruler is document content — it is in the .clay and the layout editor treats it that way — so an export that dropped it would be showing you a different document. Selection chrome, handles, the marquee, PCell pin overlays, snap glyphs and the EM/DRC overlays are all views of editor state rather than of the file, and none of them can"],[30,"render-cdd","A data display — the same verb, a different anatomy","$ circuitrf render Amp.cdd -o amp.pdf [--data run.npy]... [--tab name|n] [--plot n] [--all-tabs] A .cdd is the fourth document kind, and the one that is not a drawing: it holds no data. Its traces name a source — a file, or the sentinel meaning \"whatever this display has selected\" — and every curve is re-resolved from a file on disk each time it opens. A source that does not resolve is a refusal, never an empty plot This is the rule the whole .cdd path is arranged around. An empty plot is a valid picture: it draws, it exports cleanly, and it looks exactly like a measurement that genuinely came back empty. So every source the pages you asked for reference is resolved before anything is drawn , and one that cannot be is a refusal naming --data . --data may be repeated and binds in order; the first one satisfies the document's own selected source. A --data that binds nothing is a refusal too — handing over last week's run must not silently get you a picture drawn from whatever was lying beside the document. A reference is looked for beside the .cdd , then under the nearest ancestor workspace's results/ . --tab takes a name or a 1-based number (a tab literally called 2 beats the second tab), --plot a 1-based number within it, and the default is the tab the display opens on. --all-tabs is PDF's alone — a PDF is a multi-page format and SVG and PNG are not, and inventing out-1.svg , out-2.svg from one -o would be this tool naming your files for you. On those it is a refusal naming --tab . --size replaces the page and nothing else about the composition changes. The default is the 792×612 pt landscape page with 36 pt margins, and the plots, axis label strips and any marker info boxes you dragged are fitted to it as one group — so a box you moved lands in the file where it sits on screen. The options that describe a drawing — --window , --center , --span , --fit , --layers , --hide-layers , --detail , --view , --cell , --grid , --no-rulers — are each a refusal naming themselves, because a display has no world coordinates and no layers, and a --window that silently did nothing would give you a full picture you believed was a crop. With --json , result.render.dataDisplay names every source and the file it actually resolved to , and which of --data or the document bound it. That is the part the picture cannot tell you: \"the plot is empty\" and \"the plot read the wrong run\" look identical."],[30,"render-example","A worked example, from an empty folder","Two commands to set it up, three questions, one picture. The point of the sequence is that the three questions are what make the last command writable : you cannot name a layer or size a window without first asking what the document has. $ mkdir work && cd work $ circuitrf new workspace Amp --tech pcb-2layer_FR-4_70mil_1oz $ circuitrf new cell Amp Stage1 --views layout That is a correct, empty layout. Give it some artwork — a 20 mm line on the top copper, a ground plane under it, and a label on the top silk: { \"FormatVersion\": 1, \"DbuPerMicron\": 1000, \"DisplayUnit\": \"Um\", \"Shapes\": [ { \"$type\": \"Rect\", \"Layer\": { \"Layer\": 1, \"Datatype\": 0 }, \"X1\": 0, \"Y1\": 0, \"X2\": 20000000, \"Y2\": 2900000 }, { \"$type\": \"Rect\", \"Layer\": { \"Layer\": 2, \"Datatype\": 0 }, \"X1\": 2000000, \"Y1\": -3000000, \"X2\": 18000000, \"Y2\": -1000000 }, { \"$type\": \"Label\", \"Layer\": { \"Layer\": 5, \"Datatype\": 0 }, \"X\": 400000, \"Y\": 3400000, \"Text\": \"STAGE 1\", \"Height\": 500000 } ] } Which cells are in here, and which views does each have? $ circuitrf explain Amp --cells Amp (workspace) workspace /home/you/work/Amp/.cws via nearest ancestor .cws cells: 1 Stage1 /home/you/work/Amp/Stage1 schematic no-view (none) symbol no-view (none) layout sole-file Stage1.clay What may I ask for with --layers ? The technology defines eight; this document uses three. $ circuitrf explain Amp/Stage1/layout/Stage1.clay --layers layers PCB 2-Layer FR-4 (70mil, 1oz) — 8 defined, 3 used Top Copper 1/0 #c87a3e solid purpose=drawing 1 shape(s) Bottom Copper 2/0 #8a5028 solid purpose=drawing 1 shape(s) Soldermask Top 3/0 #1e6b3c solid purpose=drawing 0 shape(s) … Silk Top 5/0 #f2f2f2 solid purpose=drawing 1 shape(s) … How big is it, so I can write a window? In base SI, with the unit and the scale named. $ circuitrf explain Amp/Stage1/layout/Stage1.clay --extents extents 0 -0.003 .. 0.02 0.00376719 (0.02 x 0.00676719 m, scale 1E-06) --window 0um,-3000um,20000um,3767.188um Top Copper 0 0 .. 0.02 0.0029 --window 0um,0um,20000um,2900um Bottom Copper 0.002 -0.003 .. 0.018 -0.001 --window 2000um,-3000um,18000um,-1000um Silk Top 0.0004 0.00338437 .. 0.00234306 0.00376719 --window 400um,3384.375um,2343.062um,3767.188um The line is 20 mm long and 2.9 mm wide, on Top Copper — and the --window line beside each box is already in the spelling the next command takes. Now the left 6 mm of it, that layer only: $ circuitrf render Amp --cell Stage1 --layers \"Top Copper\" \\ --window 0um,0um,6000um,3000um -o stage1-top.png Wrote stage1-top.png (1600x1200 device-pixels, 10,399 bytes) 1 of 2 shape(s) drawn, 4 vertices emitted, 2 draw call(s) Three options and one verb, not five commands — because all three questions are the one question explain already exists for: what did circuitRF decide?"],[30,"check","check — is it well formed, does it resolve, is it sound?","$ circuitrf check <path> [--recursive] [--severity warning|error] Point it at a workspace, a cell folder, or one document. It runs no analysis and it writes nothing , which is what makes it cheap enough to call after every edit — and safe to run on a read-only tree, or on a workspace you have open in the GUI at the same time. $ circuitrf check ~/designs/Amp Amp/Stage1/schematic/Stage1.csch error two labels name one physical net: 'vout' and 'out' Amp/Stage1/layout/Stage1.clay warning no technology resolves for this layout 5 document(s): 1 error, 1 warning Every finding comes from a validator the application already uses — the same view-file check, the same primacy rule, the same technology walk-up, the same net extractor, the same elaborator, the same DRC engine. A rule that lived only in check would be a rule the application does not enforce, and a design would pass here and be refused the moment somebody opened it. Warnings are reported and still exit 0 --severity decides the exit code, and it defaults to error . Warnings are always printed — a check that hid them to keep the exit code clean would make the exit code useless. Two states are warnings on purpose: a cell folder holding several views with none named primary, and a layout that resolves no technology. Both are normal. The kind of document is inferred from the path, exactly as convert infers a format. A GDSII or Gerber file is named as interchange rather than called unreadable — it is simply not validated, because there is nothing to validate it against."],[30,"check-touchstone","Checking a Touchstone file","$ circuitrf check part.s2p note: part.s2p: 2-port, 401 points, 1 MHz to 1 GHz, reference 50 Ω. note: part.s2p: causality not evaluated — the frequency grid is not uniformly spaced. 1 document(s) checked: 0 error(s), 0 warning(s), 2 note(s). An .sNp is data rather than a design, and that changes the severity rule in one place. Passivity, reciprocity and causality are warnings and never errors , each carrying the measured number and the frequency it occurred at: nothing in a Touchstone file says what the part is, so an amplifier is supposed to have gain and a circulator is supposed to be non-reciprocal. Only the unambiguous defects — unreadable, a port count that contradicts the file's own name, a frequency axis that is not sorted, a reference impedance no renormalisation can use — are errors. A folder walk deliberately skips Touchstone files. A kit directory holds hundreds of them, and measuring passivity and causality across all of them would bury a workspace's own findings under notes about parts you did not author. Naming the file is what checks it. What each finding means, and the limits of the causality measurement, are on the Derived Metrics page."],[30,"explain","explain — what did circuitRF decide?","$ circuitrf explain <path> [--expr \"<expression>\"] [--set var=expr] [--analysis [<name>]] [--ref <relative-ref>] [--cells [--all]] [--layers] [--extents] [--view <name>] check answers \"is something wrong\". explain answers the question that is not a failure: which technology did this layout get, which analysis would actually run, what does this expression evaluate to here, what does this cell reference point at, what cells and layers are in here, and how big is it? Six questions, and they are refused together rather than ordered — one per run. A precedence nobody stated is an invention, and it would answer a question you did not ask while looking like it answered the one you did. It reports the walk as well as the answer , and that is the useful half. Resolution in circuitRF is a series of walk-ups — a document's ancestor workspace, a layout's technology, a .cem 's two independent references — and which one produced an answer is exactly what you cannot see from the file. $ circuitrf explain Amp.cem workspace from Amp.cem → /home/you/designs/Amp/.cws (nearest ancestor .cws) layout from Amp.cem → Amp/Line/layout/Line.clay (workspace-relative) workspace from Line.clay → /home/you/parts/.cws (nearest ancestor .cws) technology from /home/you/parts → parts/tech/pcb-2layer.ctech (the .cws DefaultTechRef) Two different workspaces there, and that is legitimate: a .cem in one workspace may point at a layout in another, and that layout's layers must be read by its technology."],[30,"explain-analysis","`--analysis` — which chain would run","$ circuitrf explain pa.cnl --analysis SWEEP1 parametric_sweep enabled runnable root → dispatched by hb chain: SWEEP1 → HB1 sweep: Pavl over 1.0000e+09 … 3.0000e+09 step 5.0000e+08 Hz (stated GHz, scale 1e9) HB1 hb enabled runnable → promoted to SWEEP1 A sweep is reported in base SI with its unit and its scale . Reading a mark without its scale has already produced a run at 2 Hz that looked entirely normal. runnable is two claims, not one: the chain reaches an enabled analysis, and every reference the analysis names resolves — the tuner instances, the inner analysis a sweep wraps, the swept variable, and the variables a tone expression reads. When one does not, the chain is not runnable and the report says which: $ circuitrf explain pa.cnl --analysis analyses: LP1 loadpull-pursuit LP1 not-runnable unresolved: LoadTuner=NoSuchTuner: no instance of that name in this document. Its Tuners are: Load. unresolved: SourceTuner=AlsoMissing: no instance of that name in this document. Its Tuners are: Load. Changed in 1.0 runnable used to mean only the first half, so the analysis above was reported runnable and dispatched by lpp while naming two tuners that do not exist. Whether a thing will run is the question this verb exists to answer, and a caller that acts on an optimistic yes gets its refusal later, about something it has already been told is fine. What explain still does not claim is anything that would need a solve — \"this bench has no bias source\" is a property of the solved circuit, and this verb solves nothing. It reports what it can establish and stays quiet about the rest."],[30,"explain-touchstone","`explain` on a Touchstone file — what IS this part?","$ circuitrf explain part.s2p part.s2p (touchstone) ports 2 sweep 401 points, 1 MHz to 1 GHz, non-uniform reference impedance 50 Ω SRF (shunt-through) 22.507906 MHz |Z| min (shunt-through) |Z| = 5.057 mΩ at 22.387211 MHz, ESR 5 mΩ SRF (series-through) (nothing) |Z| min (series-through) |Z| = 796.177 Ω at 1 GHz, ESR 1.268 Ω The self-resonance and the impedance floor are what a decoupling capacitor is chosen on, and before this verb the only way to see them was to build a schematic around the file and plot it. Every applicable fixture is reported side by side rather than one being chosen. Nothing in the file records how the part was measured, and seeing all the readings is the fastest way to identify an unlabelled one — above, the series reading finds no resonance at all and puts the floor five orders of magnitude out, so the part is plainly a shunt-mounted capacitor. The equations behind each reading are on the Derived Metrics page."],[30,"explain-expr","`--expr` — evaluate in the design's own scope","$ circuitrf explain pa.cnl --expr \"Zopt*2\" --set Zopt=12.5 Zopt*2 = 25 (real) Through the one expression engine, in the design's own resolved scope — never by substitution — with --set applied first exactly as a run verb applies it. The kind is reported, never coerced."],[30,"explain-ref","`--ref` — where does this reference land","$ circuitrf explain Stage1.csch --ref ../parts/SOT-23-3 ../parts/SOT-23-3 → /home/you/designs/parts/SOT-23-3 resolved outside this workspace: no Where resolution fails, that is the answer — a sentence naming what was looked for and where it was looked. You are usually running this verb precisely because something did not resolve."],[30,"explain-cells","`--cells` — what cells are in here?","$ circuitrf explain ~/designs/Amp --cells Amp (workspace) workspace /home/you/designs/Amp/.cws via nearest ancestor .cws cells: 2 Balun /home/you/designs/Amp/Balun schematic named-present Balun.csch of 2: alt.csch, Balun.csch symbol no-view (none) layout missing-named-primary Balun_rev3.clay of 2: Balun.clay, Balun_rev2.clay Stage1 /home/you/designs/Amp/Stage1 schematic sole-file Stage1.csch symbol sole-file Stage1.csym layout sole-file Stage1.clay It reports the resolution, not a directory listing , and that is the whole reason to run it rather than ls . The interesting rows are the ones where the answer is not obvious, and there are five states, never collapsed into fewer: State Means sole-file One file in the sub-folder; it is primary by being the only one. named-present Several files, and the cell names one of them. missing-named-primary Several files, and the cell names one that is not there . A flat contradiction, and the state you most need to see — the name it looked for is printed. no-primary Several files and none named. Not an error; nothing has chosen yet. no-view The sub-folder is empty, or there is none. Each is listed with its state — never omitted, and never quietly resolved to the alphabetically first file, which is the answer that would look right and be wrong. Point it at a cell folder and you get that one cell in the same shape, which is what makes it compose with render : ask what views a cell has, then render one. --all includes generated cells, which are hidden by default exactly as the project tree hides them. This is the same enumeration render --cell resolves through, so a cell listed here is a cell that verb can draw."],[30,"explain-layers","`--layers` — what may I ask for?","$ circuitrf explain Stage1.clay --layers technology /home/you/designs/Amp/tech/pcb-2layer_FR-4_70mil_1oz.ctech via the workspace's DefaultTechRef — the layout states none layers PCB 2-Layer FR-4 (70mil, 1oz) — 8 defined, 3 used Top Copper 1/0 #c87a3e solid purpose=drawing 1 shape(s) Bottom Copper 2/0 #8a5028 solid purpose=drawing 1 shape(s) Soldermask Top 3/0 #1e6b3c solid purpose=drawing 0 shape(s) Silk Top 5/0 #f2f2f2 solid purpose=drawing 1 shape(s) … Two different sets, side by side, and the second is the one that draws anything. The technology's layer table is what render --layers will accept; the shape count is what this document actually has on each. A layer with a colour, a purpose and 0 shapes is a name you may pass that will produce nothing. The count walks the hierarchy : a layer used only inside a placed sub-cell counts, and an array placement counts its shapes once per element. It is deliberately not render --json 's counters.shapesDrawn , which counts only the top-level shapes one frame issued a draw call for. Two things it cannot tell you, both worth knowing: A via is reported on its barrel layer only. A via carries a barrel layer and a landing layer, and the renderer draws the whole annulus on the barrel one — so a landing layer a via field's pads are notionally on can report zero shapes. A layer the fallback palette invented is listed too, and marked. A key the document draws on that the technology does not define is ordinary after an import, and it renders . Omitting those rows would report a document as drawing on layers it does not and hide the ones it does. The generated name is a name render --layers accepts."],[30,"explain-extents","`--extents` — how big is it?","$ circuitrf explain Stage1.clay --extents extents 0 -0.003 .. 0.02 0.00376719 (0.02 x 0.00676719 m, scale 1E-06) --window 0um,-3000um,20000um,3767.188um Top Copper 0 0 .. 0.02 0.0029 --window 0um,0um,20000um,2900um Bottom Copper 0.002 -0.003 .. 0.018 -0.001 --window 2000um,-3000um,18000um,-1000um Silk Top 0.0004 0.00338437 .. 0.00234306 0.00376719 --window 400um,3384.375um,2343.062um,3767.188um In base SI, with the unit and the scale named — the rule --analysis already follows, for the reason it follows it: reading a mark without its scale has already produced a run at 2 Hz that looked entirely normal. A schematic or a symbol reports design-units at scale 1, because its coordinates are dimensionless and dressing them up in metres would be a lie. This is the same function render --fit frames on , which is why you can size a --window from it and get exactly the region you expected. The per-layer boxes are the document's own shapes; an instance's extent arrives as one box for the whole placement. The --window line is meant to be pasted Every box comes with the same numbers written the way render --window takes them — in this document's own display unit, whole document and per layer, and in --json as a window field beside the coordinates. The metres above are deliberately not what you type : a layout coordinate carries a unit there, and m is not one of the suffixes it reads. Copy the --window line and you get exactly that box; there is no conversion to do and no chance of being three orders of magnitude out. So framing on one layer is a copy rather than a calculation — and if you want to frame on one layer while still drawing the others, that is render --fit-layers . An empty document says so rather than reporting a zero box, because a zero box is a point at the origin and that is a different fact. On a symbol , or on a layout carrying a fixed-size ruler, the report adds a note: the fit adds room for marks that are drawn in pixels — a pin's name, a ruler's readout — which have no world extent until a page size is chosen. Those marks are why a fitted symbol page is wider than the box reported here."],[30,"read","read — a result, or a document, back","$ circuitrf read <path> [--only a,b] [--group g] [--json] The inverse of a run verb. A .npy or a Touchstone file is loaded back into cubes — through the same two loaders the Data Display's source library reads a file with — and one of circuitRF's own documents comes back as its own bytes , unchanged. $ circuitrf read results/Amp_em.npy results/Amp_em.npy (npy) group (default): S Complex freq[201] Hz x i[2] port x j[2] port Z0 Complex port[2] port group planar: MeshCells Real cell[1544] It writes nothing, and it takes one file at a time. For \"what is in this workspace\", use check or explain ; for a GDSII or a Gerber set, use convert . With --json , --only and --group narrow what comes back — which matters, because reading a 20,000-point swept loadpull in full is the expensive direction: $ circuitrf read hero3.npy --only Pout_dBm --json"],[30,"netlist","netlist — the netlist a schematic runs as","$ circuitrf netlist <path.csch | cell-folder | workspace --cell N> [-o out.cnl] The extraction Simulate performs, as a file you can read. It takes a .csch , a cell folder, or a workspace with --cell — the same three inputs render takes, resolved the same way. $ circuitrf netlist Example_SParam_LC.csch ; extracted from Example_SParam_LC Port:Term1 n1 0 Num=1 Z=50 Ohm L:L1 n1 n2 L=2 nH C:C1 n2 0 C=0.8 pF Port:Term2 n2 0 Num=2 Z=50 Ohm analysis SP1 type=sparam start=\"1\" startUnit=GHz stop=\"5\" stopUnit=GHz npts=201 Without -o the netlist goes to stdout, so circuitrf netlist Stage1.csch &gt; stage1.cnl is the file and nothing else. With -o it is written there, and the path is what goes to stdout. This is the netlist a run actually consumes — the same bytes You do not need it to simulate a drawing: every run verb takes a .csch directly and extracts it in memory. What this verb is for is seeing that extraction — and checking a .cnl you wrote by hand against what circuitRF produces for the equivalent schematic. Both go through one function, so the file here is not merely equivalent to what a run reads, it is byte for byte the same text. -o takes a .cnl and refuses any other extension — there is one format here. A .cnl input is refused too: passing it through the reader and the writer would hand back a file that is not the one you gave (comments gone, directives reordered) and call it an extraction."],[30,"plot","plot — a picture of a result","$ circuitrf plot <result.npy|.sNp> -o <out.svg|.pdf|.png> --trace <spec> [--trace <spec>]… One plot, one axis pair, without writing a data display first. $ circuitrf plot lc.s2p -o match.png \\ --trace cube=S,i=1,j=1,y=db --trace cube=S,i=2,j=1,y=db \\ --title \"LC lowpass\" --ylabel dB --x 1:5 --y -40:5 Wrote match.png (792x612 device-pixels, 19,785 bytes) 1 plot(s) on 1 page(s), 1 data source(s) A trace spec is comma-separated key=value : Key What it means cube Which cube. Required. It is the same shorthand the trace card's spec box takes, so S , S[:,2,1] , Pout and mag(V[:,\"X1.drain\"]) all work. i , j The port numbers of a matrix cube — i=2,j=1 is S21. Refused together with a bracketed slice: they are the convenience over writing one. y db , db10 , db20 , mag , phase , real , imag or conj . axis left (the default) or right . cut An antenna pattern cut: a bearing in degrees pins the cube's phi axis and sweeps theta ; all keeps every phi as a curve family. The verb prints the φ it landed on. port The port number on a cube's port axis — not an index. A port the run does not hold is refused, listing the ones it does. freq Pins a freq axis to its nearest sample. Takes an SI suffix: 2.45G . probe A WSProbe label. Given, it turns a cube=<analysis>.wsp trace into a probe metric. metric Which of the reference document's quantities — H0 , 1/Y0 , ZG , SM_Y0 , LGa , SMenv , … See The WSProbe . Some take with= , set= , z0= , side= , gi= or the envelope's grid keys. circuitrf plot amp.npy -o margin.svg --trace cube=SP1.wsp,probe=GATE,metric=SM_Y0,y=db Case is load-bearing in that notation and is not folded away — LGF is one probe's forward synthetic-circulator loop gain and LGf is a probe pair's feedback-as-synthetic-FET loop gain. A name whose canonical form is shared by two quantities resolves only when it is spelled exactly. circuitrf read <result> lists the cubes a file holds and their axes, which is where the names come from. A cube the file does not hold is refused, listing the ones it does. Option What it does -o <path> Required. Its extension picks the format: .svg , .pdf or .png . --type rect\\|smith\\|polar\\|table Default rect . --freq-unit Hz\\|kHz\\|MHz\\|GHz Default GHz. It is also the unit --x is read in. --title , --xlabel , --ylabel , --y2label Custom labels. Omitted, the plot labels itself. --x lo:hi , --y lo:hi , --y2 lo:hi Axis windows. An axis you leave out autoscales. Refused on a Smith or Polar chart, whose window is the complex plane. --size WxH , --scale , --dpi The page. Default 792×612 points — the same page File ▸ Export writes. --scale / --dpi are .png only. --variant light\\|dark , --background opaque\\|transparent As render . --radial linear\\|db How a polar plot's radius is read. db makes it an antenna pattern plot. Refused on any other --type . --db-floor <dB> The centre of a db plot, relative to the outer ring . Default −40. --db-ring <dB> Ring spacing. Default 10. --db-ref peak\\|<dB> The outer ring: the data's own peak (normalised, the default) or an absolute level. --db-unit <text> What the radial numbers are in — dBi , dB(W/sr) . --write-cdd <path> Also write the data display this drew. circuitrf plot run.npy -o eplane.svg --type polar --radial db --db-unit \"dB(W/sr)\" \\ --trace cube=farfield.U,cut=0,port=1,y=db10 A pattern plot says what it is On a --radial db plot the outer ring is a reference and the centre is a floor, and the picture states which reference it is using — a 0 dB peak with no reference is not a result. Values below the floor are drawn AT the floor, never dropped : a gap in a pattern trace reads as a null in the antenna, and a real null and a clipped value must not look the same. 0° is at the top and angles increase clockwise, and the note under the plot says what the θ range covers — with an infinite ground plane there is no field below the horizon, so a pattern occupying part of the disc is the model saying so rather than a drawing fault. --write-cdd is how you go further This verb draws one plot"],[30,"find","find — what is in this folder?","$ circuitrf find <root> [--depth n] [--no-analyses] The workspaces under a directory, their cells, each cell's views, and the analyses each cell declares. $ circuitrf find ./projects /home/you/projects (1 workspace(s), 2 cell(s), depth 4) Amp /home/you/projects/Amp [tech tech/generic-mmic.ctech] LC schematic: LC.csch analyses: SP1 Stage1 schematic: Stage1.csch, layout: Stage1.clay analyses: HB1, SWEEP1 It reads what the other verbs read, so a cell listed here is the cell render would draw and the analyses are the ones hb would dispatch. It writes nothing, so it runs on a read-only tree and on a workspace the application has open. Option What it does --depth n How many levels below the root a workspace is looked for. Default 4, at most 12. --no-analyses Skip the analyses. Each one costs an extraction, which adds up on a large tree. The walk is bounded, and it says when it stopped A listing that quietly gave up reads as \"the workspace is not here\". So when the walk hits --depth with directories still below it, it says so — a warning on stderr and truncated: true in the --json document. Raise --depth and ask again. A directory symbolic link is never followed, so nothing outside the root you named can appear in the answer."],[30,"reference","reference — what may I write?","$ circuitrf reference $ circuitrf reference netlist $ circuitrf reference components $ circuitrf reference components MLIN $ circuitrf reference analyses $ circuitrf reference analyses sparam $ circuitrf reference data-display $ circuitrf reference technology They are, in order: the list of topics with what each costs; one page as text; every netlist primitive; just that one; every analysis directive with every key it takes; just that one; and the two document formats nothing else here creates for you. check tells you that what you wrote is wrong. explain tells you what circuitRF made of it. This one tells you what you are allowed to write in the first place — the primitive type names, how many nets each takes and in what order, what its parameters are called and what they default to. It reads no file, runs nothing and writes nothing. There is no path argument: it answers about no particular design, which is exactly why it is useful before one exists. Why this matters more for a script than for you A netlist naming a type that does not exist fails at elaboration and says so. A component given a plausible but wrong parameter name does not: the name is ignored, the parameter takes its default, and the run converges and produces a complete-looking answer to a different circuit. Getting the spelling from here rather than from memory is what avoids that."],[30,"reference-topics","The topics","With no arguments you get the list, with each topic's size — because reading is the expensive direction and a 4 kB page and an 84 kB one should not look alike: $ circuitrf reference Reference topics — circuitrf reference <topic> netlist 17.7 kB The Netlist (.cnl) Format expressions 9.1 kB Expressions units 10.9 kB Units measurements 5.8 kB Measurements pins-ports-terms 4.3 kB Pins, Ports & Terms sdd 12.2 kB The SDD (Symbolically-Defined Device) file-formats 11.6 kB File Formats component-notes 74.2 kB Components data-display 12.8 kB The .cdd data-display format technology 6.1 kB The .ctech technology format analyses 8.5 kB Analysis directives components 91.3 kB Component types circuitrf reference components <TYPE> one primitive circuitrf reference analyses <TYPE> one analysis directive These are the same pages you are reading now, shipped inside the program so they are there on a machine that has no copy of this site. components is the generated catalogue — read from the live component registry every time you ask, so it cannot go stale — while component-notes is this site's Components page, which explains what each part is for . The catalogue answers \"what may I write\"; the page answers \"what does it mean\". Ask for both if you want both. Asking for a topic prints it as its own Markdown, so it redirects cleanly: $ circuitrf reference netlist > netlist.md"],[30,"reference-components","The component catalogue","$ circuitrf reference components MLIN MLIN nets: 2 terminals: 2 1 2 order: The two terminals are interchangeable: the line is uniform, so neither end is the input. Mlin (MLIN) — Microstrip search: MLIN, microstrip, microstrip line, line, hammerstad terminals: 2 1 2 W 2.9 mm shown L 10 mm shown SignalLayer - - - GroundReference - - - The columns are the parameter's name , its default expression , its unit , whether it shows on the schematic by default, and — where the registry has one — what it means. terminals is the part you cannot get from a picture: the pins the symbol draws, in the order a netlist line writes their nets. That order is a contract the models themselves read, and it is not always the one you would guess — a MESFET is gate, drain, source while a JFET is drain, gate, source, and a diode is anode then cathode. `nets` and `terminals` are two different numbers, and the first is the one to write to How many nets the instance line binds is nets . How many pins the symbol draws is terminals . They are usually the same and they are not always: a Tuner draws one pin and its line takes two, because its reference terminal is implicit on the glyph. So do Port , Term , Vdc and IProbe ; a 2-port SDD takes four nets, as ± pairs. $ circuitrf reference components Tuner Tuner nets: 2 terminals: 1 1 Writing Tuner:T1 n_drain Z[1]=50 BiasTee=on Vbias=48 — one net — is now refused by name. It used to run: the bias tee delivered nothing, every diagnostic was clean, and the output sat at the engine's floor at every drive point. Some counts are not fixed, and they say so An SDD's, a Z_Port 's and an SnP 's follow a port-count parameter; a Verilog-A model's follows Pins ; an ideal switch's follows Throws ; a wBond's follows the arrays it places. Those report the rule rather than a number: $ circuitrf reference components SDD SDD nets: SddPortCount=N binds 2N nets. terminals: set by NumPorts — at NumPorts=2: 1+ 1- 2+ 2- Note that the two lines name different parameters , and that is not a mistake: the parameter panel calls it NumPorts , and a .cnl instance line spells it SddPortCount . A number printed where the honest answer is \"it depends\" is worse than no number at all."],[30,"reference-analyses","Analysis directives","$ circuitrf reference analyses sparam sparam also: sp, s_param, sparameter, s_parameters bare words: log type required The analysis kind. One of the tokens this page lists. enabled true false skips the analysis at run time without deleting it. start required First frequency. Bare number in Hz unless a unit follows or Unit=/startUnit= is given. stop required Last frequency, same spelling rules as start. step 1e8 Step size. Mutually exclusive with npts; npts wins when both are given. npts - Point count. Selects the point-count sweep mode. Unit - Sets startUnit, stopUnit and stepUnit at once. Any one of those given individually overrides it. ... Every analysis type= token, every other spelling of it that is accepted, and every key it may carry with its default and whether it is required. It is read from the same table the .cnl reader validates against, so a key that is not on this page is a key the reader refuses — it is not ignored, and it has not been since the netlist contract landed."],[30,"reference-formats","The two formats nothing creates for you","circuitrf reference data-display and circuitrf reference technology describe the .cdd and .ctech files field by field, generated from the types their readers actually deserialise into, each with a minimal example that was written out and run. They are here because those are the two formats you may have to write by hand: new workspace copies a technology in for you, but nothing creates a data display. A few entries carry a note instead of a clean answer, and the note is the useful part. GND , VAR , MEAS and Pin are schematic elements the netlist extractor consumes rather than components you can place in a .cnl . Chain , ExtDevice , SemiC , Short , Term , V_nTone and I_nTone are the other way round — writable in a .cnl , with no palette tile and so no declared defaults. Unknown names are refused with the real list, never guessed at: $ circuitrf reference components MLINE No primitive type 'MLINE'. Types: Amp, Atten, BJT_NPN, BJT_PNP, Balun, Bead, C, Chain, … With --json the whole catalogue comes back structured — type token, terminals, and every parameter with its default, unit, dimension and visibility: $ circuitrf reference components --json | jq -r '.result.reference.components[].type'"],[30,"elab","elab — the elaborated netlist","$ circuitrf elab <file.cnl|.csch> Elaborates and stops: flattens the hierarchy, resolves every parameter and expression top-down, and numbers the nodes, then prints the elaborated netlist — the exact thing the engines consume. No analysis runs. This is the debugging verb. When a value is not what you expected, elab is where you find out whether the expression resolved to something different from what you meant, or resolved correctly and the analysis is doing something else."],[30,"json","--json — one machine-readable document","Every verb takes --json , spelled that way everywhere. It changes exactly one thing: stdout carries a single JSON document and nothing else. stderr is untouched — progress, [circuitRF] notes, warnings and refusal sentences stream exactly as they always did — so a script watching stderr cannot tell whether the flag was passed. $ circuitrf sparam amp.cnl --json | jq '.outputs[].path' \"amp.s2p\" One schema serves every verb: { \"circuitrf\": { \"version\": …, \"verb\": … }, \"input\": { \"path\": …, \"analysis\": … }, \"status\": \"ok\" | \"not-converged\" | \"failed\", \"exitCode\": 0 | 1 | 2 | 130, \"outputs\": [ { \"kind\": …, \"path\": … }, … ], \"diagnostics\": [ { \"id\": …, \"severity\": …, \"message\": …, \"arguments\": { … } }, … ], \"result\": { … } } input.analysis is the chain that actually ran , after promotion — not what you asked for. The difference is a whole sweep axis, so you must be able to see it from the document alone. A failed run still emits a document. The failure is the payload, so you never have to tell \"no output\" apart from \"output I could not parse\". The diagnostic id is the contract; the message is not. Match on id . Templates are reworded freely, and the sentence is always English and culture-invariant. result holds cubes ( groups ) for a run, a summary for lp / lpp — the same one-row-per-Γ- point projection the table prints, and --all adds the cubes — a check or explain report for those two verbs, a document for read , a reference report for reference , and a render report for render . result.render is everything the picture cannot say about itself: viewport (with letterboxed ), the document's extents , the size , the theme and which step of the chain resolved it , the layers with whether each was drawn and how many shapes the document has on it, detail with its effective tolerance, counters , and bytes . Extents and viewport come back in base SI with the unit and the scale named . A .cdd adds dataDisplay , naming every source and the file it resolved to. There is no duration — counters is a work count, deterministic and machine-independent, so it is something a build can assert on; a wall clock is not. result.explain gains cells , layers and extents for the three questions of the same name. extents carries perLayer boxes beside the whole; layers carries the resolved technology and the walk that found it. result.shape is always there , on every run and every read that produced cubes, whether or not the values are: the groups, the cube names, each cube's kind and unit, how many numbers it holds, and every axis with its name, unit, length and end points. It is what lets you find out what a run produced before deciding what to ask for, and it costs a couple of kilobytes on any result. result.narrowed appears when you used --at or --range , and says per axis what you asked for, what you got, and whether it was the nearest grid point or interpolated . Numbers are raw, invariant and unrounded. NaN and infinity are written as JSON's named literals, because a loadpull grid genuinely contains NaN wherever a point never converged."],[30,"json-units","Every cube says what its numbers are in","A cube's unit is always present and never empty. SI symbols as they are written — Hz , V , A , W , Ohm , F , H , K , m , s — and dB and dBm likewise; % for a ratio already scaled to a percentage, 1 for one that is not, index for a flag or a count, and unknown where circuitRF cannot say. A measure line you wrote yourself has whatever unit your expression has, and saying unknown is an answer where a guess would not be. Why this is worth a field of its own One loadpull-pursuit result carried Efficiency reading 65.84 and MXE_Eff reading 0.7087 at the same operating point — the cube in percent, the scalar as a fraction. Reading the scalar and formatting it as a percentage gives 0.7%; multiplying the cube by 100 gives 6584%. Both are one plausible line of code, and until the unit was carried there was nothing anywhere to say which was which. Nothing was rescaled to fix it: the numbers are what the engine computed, and the document now says what they are."],[30,"json-summary","--summary : the notes as counts","Some verbs are deliberately talkative. The Gerber import names every inference it made as an inference, which is exactly what lets you decide whether to trust the result — but it is not what you want back from convert --list-cells , whose answer is one cell name. --summary reports the informational notes as counts by severity and adds a diagnosticSummary block saying how many were left out. Warnings and errors are never collapsed , and stderr still carries everything, so nothing is hidden: what you stop paying for is thirty notes describing inferences that all went fine."],[30,"serve","serve — the MCP server","$ circuitrf serve --root <dir> [--kits <dir>] This is circuitRF as an MCP server. It speaks the Model Context Protocol over its standard input and output — the stdio transport, newline-delimited JSON-RPC 2.0, with initialize , tools/list and tools/call — so any MCP client can discover what circuitRF can do and ask it to do it. Protocol versions 2025-06-18 , 2025-03-26 and 2024-11-05 are accepted. In practice that means an assistant, a design agent, a CI job or an automation harness can run a simulation, check a design, create a workspace or import a part without a human at a terminal. It is started by that program, not by you, and it ends when that program disconnects. Point an MCP client at it A client is configured with a command and its arguments. The command is the circuitRF executable, the arguments are serve --root <dir> , and --root is the only directory tree the server will read or write, and a path escaping it is refused rather than clamped (see What it will not do below). Nothing else about the host matters, because the transport is the process's own stdin and stdout. Thirteen tools, and each is a verb you already have: Tool Runs run sparam , dc , hb , lp , lpp or em , chosen by an argument check check explain explain , including --cells , --layers and --extents create new workspace or new cell import import part or convert render render — one tool over every document kind, as the verb is read read netlist netlist — the extraction a schematic runs as plot plot — one picture from one result file. It takes attachImage too find find — the workspaces, cells, views and analyses under a directory history history checkpoint , list or restore — the correction nouns ( rename , retitle , correct , review ) are on the verb but not on this server reference reference batch The only tool with no verb behind it: it holds a restore-point batch open across several calls, which a process that exits after one command cannot Every tool returns exactly the document --json writes , byte for byte, because the server calls the verb rather than re-implementing it. Nothing is reachable through the server that is not reachable from your own shell, and nothing is reachable from your shell that the server cannot do. render and plot can hand the picture back, not just its path. Pass attachImage: true and the result carries the rendered file itself — a .png or .svg as image content, a .pdf as an embedded resource with its own type. It is off by default , because an image is expensive in a way a JSON document is not, and a client that only wanted the path should not pay for one. Over 4 MB it hands back the path and says what to narrow The file is still written and still named in outputs ; only the attachment is withheld, and the answer says how large it came to and which argument would bring it under — --detail screen , --layers , --window , a smaller --size , or .png instead of a vector format. It is never truncated and never dropped in silence: half a PNG is not a smaller PNG, and a request that comes back empty with no explanation just gets sent again. The number is the measured one . A whole six-layer board is 1.4 MB as a PNG and 23.5 MB as an undecimated SVG, so the cap admits every raster this verb plausibly produces and refuses exactly the case where you should have narrowed the render. The attached bytes are the bytes on disk. The server attaches the file the verb wrote; it never draws a second time, and it never converts one format into another to make it attachable — that would be the adapter making a rendering decision, which is the one thing it does not do. What it will not do --root is required , and every path a client names resolves under it. A path that escapes — through ../ , through an absolute path, or through a symbolic link — is refused, naming the root . It is never quietly clamped to something inside. Nothing deletes, and nothing overwrites an existing workspace. There is no person at the other end to confirm with, so the"],[30,"exit","Exit codes","Code Meaning 0 Ran, and produced something usable 1 Could not run — bad arguments, a missing file, no matching analysis, a refusal, an exception 2 Ran, but did not converge 130 Stopped — a run cancelled at a work boundary, by em 's own stop or by a serve client's cancellation 2 is deliberately not the same test for every verb. hb and dc fail on any non-converged solve. A loadpull grid in which some points do not converge is a normal and useful result — the edge of a Γ grid routinely will not — so lp returns 2 only when every grid point failed, and lpp only when neither optimum converged and there is no follow-on grid. A rule that failed the whole run on one bad point would make the exit code useless in a script."],[30,"scripting","Scripting patterns","Keep the table, keep the log, and still see it run. $ circuitrf lp hero3.cnl -o hero3.npy > hero3-table.txt 2> hero3-run.log Sweep a variable the netlist already has , without editing the netlist: $ for p in -10 -5 0 5; do circuitrf hb pa.cnl --set Pavl_dbm=$p -o pa_$p.npy; done Fail a build on a regression , using the exit code: $ circuitrf hb pa.cnl -o out.npy || echo \"PA did not converge\" >&2 Validate a whole workspace in CI , before anything is run: $ circuitrf check ~/designs/Amp --severity error || exit 1 Author, validate and simulate with no display at any step — the whole loop: $ circuitrf new workspace build/Amp $ circuitrf new cell build/Amp Stage1 $ cat > build/Amp/Stage1/Stage1.cnl <<'EOF' … your netlist … $ EOF $ circuitrf check build/Amp || exit 1 $ circuitrf hb build/Amp/Stage1/Stage1.cnl -o out.npy Pull one number out of a result , without parsing a table: $ circuitrf read out.npy --only Pout_dBm --json | jq '.result.groups[\"\"].Pout_dBm.values[-1]' Ask why a file resolved the way it did , when a run used a technology you did not expect: $ circuitrf explain Amp.cem Put a picture of every cell into a build's artefacts , at the size a report reads at: $ circuitrf explain ~/designs/Amp --cells --json \\ | jq -r '.result.explain.cells[].name' \\ | while read -r c; do circuitrf render ~/designs/Amp --cell \"$c\" --detail screen -o \"artefacts/$c.png\" done Watch a layout change across two revisions , without opening either: $ git show HEAD~1:Stage1/layout/Stage1.clay > Stage1/layout/before.clay $ circuitrf render Stage1/layout/before.clay -o before.png --window 0um,0um,6000um,3000um $ circuitrf render Stage1/layout/Stage1.clay -o after.png --window 0um,0um,6000um,3000um Two things make those two pictures comparable, and both are easy to lose. The explicit --window : --fit frames each file's own extents, so a change in size would move everything in the frame and the diff would be of the framing rather than of the artwork. And the older revision written inside the workspace : a .clay somewhere else resolves no technology and draws on the fallback palette, so the whole picture would change colour. Re-extract every EM setup in a workspace after a technology edit — the layout and stackup references resolve themselves, so the loop needs nothing but the file names: $ for f in em/*.cem; do circuitrf em \"$f\" || exit 1; done Because each of those writes the same file Simulate writes, a schematic that references the extracted Touchstones picks the new results up with no further action. See also: Simulations (what each analysis computes) · The netlist format · EM Setup · The layout editor · The Data Display · The MoM engine · Results & data export · Kits and external device models ."],[31,"","","Drawing artwork: the technology, the tools, snapping, and the schematic it belongs to. The layout view holds a cell's physical geometry — the shapes that get manufactured. It serves two consumers that pull in slightly different directions, and it is worth knowing which one you are serving at any moment: Fab handoff. GDSII to a semiconductor fab, Gerber plus drill to a PCB house, DXF to a mechanical flow, .kicad_pcb to and from a board tool. Here the layout is a document : fidelity, units and layer mapping are everything. EM simulation. A 2.5D method-of-moments solve. Here the layout is a model : it needs a substrate stackup, a mesh and ports. That half has its own chapters — The MoM engine and EM Setup ."],[31,"orientation","Orientation","The layout editor: a microstrip run with a mitred bend, a crossing stub and a ground via. A layout is a view of a cell , exactly as its schematic and its symbol are. The cell folder holds schematic/ , symbol/ and layout/ side by side, and a cell need not have all three. The project tree shows them together; opening one opens a document tab. The other two views have their own editors and their own pages: the Schematic Editor for the electrical contents, and the Symbol Editor for the glyph an instance draws. Coordinates are integers in database units , and the display unit you read them in is a separate, free-to-change setting. That distinction is the single most common source of early confusion and it has its own page: Units ."],[31,"technology","The technology","A layout resolves against a technology — a .ctech file that holds everything true of the process rather than of one cell: the layer table : GDSII layer/datatype pairs, display names, colours, purposes; the substrate stackup : dielectric and conductor layers with thickness, ε r , tanδ, σ, µ r ; the mapping from a drawing layer to a stackup conductor; interchange mappings — GDSII layer/datatype ↔ DXF layer name ↔ Gerber file suffix; the default display unit and snap grid for layouts created against it; the DRC rules . Technologies live in a tech/ folder at the workspace root, and the workspace records which is the default. A layout stores a relative reference to one, or leaves it blank to mean \"the workspace default\". \"The workspace\" means the document's workspace A blank technology reference resolves against the workspace containing that layout file — found by walking up from the file to the nearest .cws — not against whichever workspace happens to be open. This matters because the starter technologies use the same low layer keys starting at (1,0), so resolving against the wrong one would silently reinterpret every layer in the design. Two starter technologies ship. They differ only in data , never in code path: PCB starter MMIC starter Display unit / snap mil / 1 mil µm / 5 nm Layers Top & Bottom Copper, Soldermask ×2, Silk ×2, Drill, Outline Metal1, Metal2, Via, Resistor, Cap Dielectric, Nitride, Substrate, Backside Via, MIM Metal, MIM Via Stackup 1.6 mm FR-4, ε r 4.4, tanδ 0.02, 1 oz copper, bottom ground 100 µm GaAs, ε r 12.9, tanδ 0.0006, 3 µm plated gold, backside ground Primary interchange Gerber + Excellon GDSII The MMIC starter also carries a thin-film (MIM) capacitor module — a MIM Metal plate, a MIM Dielectric under it and a MIM Via up to Metal2, with two matching drawing layers. It costs plain interconnect work nothing, because the capacitor dielectric is tied to its plate : a run with no plate artwork in it carries air in the film's place and extracts exactly as it did before the module existed. Draw a capacitor and the film is real. See A thin-film (MIM) capacitor , which also explains why you should not read a capacitance off a raw run. A layer's colour is literal user data , not a theme role: \"M2 is teal\" is a fact about the process that must survive a light/dark switch and must match what you see in your other tools. The editor's own chrome — background, grid, rulers, selection, handles — does follow the theme. A missing technology does not block you. Layers render from a generated fallback palette keyed by their (layer, datatype) pair, a warning is posted, and editing proceeds. The one thing it does block is an EM run, which refuses rather than inventing a stackup."],[31,"tools","Drawing and editing","Drawing tools: Select · Rect · RoundedRect · Circle · Polygon · Curve · Path · Label · Port · Instance-place · Array · Ruler. Curves, circles and rounded rectangles are first-class primitives — they are stored as curves, not as pre-flattened polygons — and are flattened on demand, always with an explicit tolerance prompt, and automatically on GDSII export. Edit operations: boolean Union (grouped per layer — one result per distinct layer in the selection), Intersect / Difference / XOR (which need a same-layer pair to enable), Size (grow or shrink by a signed offset), Scale (a numeric factor or a target size, plus bounding-box handles: corner for uniform, side for one axis), Clip and Cut Out , Align and distribute , Move to layer , Set net , Flatten to Polygon… , Flatten Hierarchy and Group into cell . Clip and Cut Out are not Intersect Intersect is the region shared by all the selected shapes, folded across the whole selection — so a selection holding any two shapes that do not overlap intersects to nothing. That is the right answer to the question it asks, and it is almost never the question you meant when you drew a rectangle over a region of interest. Clip and Cut Out ask the other one. Draw the region, select the shapes you want clipped , then right-click the region and choose Clip (keep what is inside it) or Cut Out (keep what is outside). Both name the shape they found under your click in their tooltip, so you can check your aim before you commit to it. Every selected shape is clipped on its own and keeps its own identity, layer and net, so the operands may sit on any mix of layers. The right-clicked shape is the stencil, not an operand: it is never clipped against itself and it survives the operation, so the same rectangle can clip a second layer straight afterwards. Nothing you did not select is touched, on any layer. The two \"flatten\" commands are different operations Flatten to Polygon turns a curve into a polygon and always asks for the tolerance. Flatten Hierarchy turns a placed instance into geometry in this cell. They are labelled distinctly for that reason — \"Flatten\" alone gets misread. Every command either does something or is disabled with a stated reason. Context-menu items are disabled rather than hidden, so their positions stay stable, and a disabled item's tooltip names the condition — \"Select 2 or more shapes on the same layer\" . A command that is legitimately enabled but changes nothing in a particular case (a union of shapes that do not touch) reports that through the Messages pane rather than appearing to fail."],[31,"selection","Selection and vertex editing","Repeated clicks at the same point cycle through overlapping shapes. The order is layer Z-order descending, then ascending area — so a small shape sitting on a large one is reachable, which is the case that actually matters. The status bar shows where you are: Rect · M2 · 2 of 5 . Alt-click is the explicit \"next candidate\" for anyone who expects it. With exactly one shape selected you get handles: square handles on vertices, round handles at polygon edge midpoints. Hit priority is strict — vertex handle, then edge handle, then edge line, then shape interior — because getting that order wrong makes vertex dragging feel broken. Gesture Effect Drag a vertex Move it, snapped and angle-mode constrained Drag an edge Translate that edge perpendicular to itself, preserving the adjacent edges' directions — this is what makes \"widen this trace\" one gesture Ctrl/Cmd-click an edge Insert a vertex there Delete on a selected vertex Remove it (blocked below three vertices) Drag the interior Move the whole shape Curved shapes get their own handles. An arc edge shows a midpoint bulge handle — drag perpendicular to the chord to change the radius, past the chord to flip the sweep, which is the fastest way to radius a corner. A cubic edge shows two control points with tangent lines drawn to their anchors; a circle shows a radius handle; a rounded rectangle shows corner-radius and bounding-box handles. Right-click an edge to convert it between Line, Arc and Cubic, so a polygon can grow a curve without being redrawn. Self-intersection is allowed during a drag, flagged on release, and offered an automatic repair — it is not rejected mid-gesture. Pasting between cells carries the source layout's own resolution and layer definitions with it. Same DBU pastes as-is; a different DBU rescales by the exact ratio, and warns through Messages naming the offending shapes if the ratio would round. Layers absent from the destination technology are offered for adding or mapping — never silently dropped."],[31,"hierarchy","Hierarchy, instances and arrays","Hierarchy here is the same idea as in the schematic, on the same cells. Read Hierarchy in the Schematic Editor once; everything it says about making a cell, placing it and pushing into it holds in this view, on the cell's layout instead of its schematic. Push in and pop out are the same two toolbar buttons, the same ⌘] / ⌘[ , and the same clickable breadcrumb. Pop Out also leads the canvas context menu, greyed out with its reason when you are already at the top level. Three things are the layout's own: An instance references another cell's layout view — where the schematic instance referenced its symbol. A cell with a schematic but no layout has nothing to place here. Arrays (rows × columns at a pitch) are stored as one object, which is what makes a via farm or an MMIC unit-cell array nearly free to render. Flatten (one level or all levels) turns instances back into geometry, and Group into new cell does the reverse — hierarchy you can build after the fact, out of artwork you have already drawn. Cycles are detected at edit time, not discovered at render time."],[31,"schematic-flow","Schematic ⇄ layout","Two commands under the Design menu move work between the two views. They are deliberately symmetric, and neither ever runs by itself — there is no save hook, no open hook, no activation hook. They run when you invoke them and at no other time."],[31,"update-layout-from-schematic","Update Layout from Schematic","⌘U . Walks the schematic's instances, resolves each cell's layout view, and emits a starting layout: Each component instance resolves its layout view. A PCell resolves too — its layout is generated rather than stored (see PCells ). Components with no layout view at all — VAR, MEAS, Ground, and any un-laid-out cell — are reported to Messages and skipped , not silently omitted. Instances are placed in a packed, non-overlapping arrangement, roughly following schematic order. Net names are carried onto instance pins, and a ratsnest is drawn: thin straight lines on a system layer between pins sharing a net. That is the guide that makes manual routing tractable. There is no auto-router, and there is not going to be one Step 3 places parts. It does not route them. Auto-routing is a separate multi-month product and pretending otherwise would set the wrong expectation. Re-running updates, it does not duplicate. Each generated instance remembers the schematic component's stable id, so a second run keeps and leaves in place what is already there, adds what is new, and reports — never auto-deletes — what no longer exists in the schematic. You will hand-place after the first generation and run it again after a schematic edit; a generator that stomped your placement would be one you used exactly once."],[31,"update-schematic-from-layout","Update Schematic from Layout","⇧⌘U . The mechanical inverse: it walks the layout's instances and writes the schematic to match. Same file targeting (create the view if absent, open it, focus it, and leave a differently-named primary alone with a report), same change report, one undoable action. What moves in this direction depends on what the instance is: Instance in the layout What reaches the schematic A PCell — an MLIN whose W and L you dragged The component and its parameters . This is how a layout-driven edit gets back into the circuit. An ordinary hierarchical cell — a hand-drawn cell placed in this layout A plain cell-reference component. There is no parameter for the layout to have moved. A wBond Its arrays and loop heights, reconciled against the live design — see wBond . A cell placed this way that has no symbol view at all would render as a bare box with no pins, so you are asked once — for the whole run, not once per cell — whether to generate symbols for them. What cannot be represented in the other view Neither direction wires anything: Update Schematic from Layout places and updates components and does not draw nets. And hand-drawn artwork in a layout has no schematic counterpart at all — only instances cross the boundary, never loose shapes. Both cases are reported, so the omission is visible rather than silent. A layout-first PCell has no existing schematic parameter to inherit a unit from, so a newly created component's length-dimensioned parameters take the technology's own default display unit — mil on a PCB, µm on an MMIC die."],[31,"geometry-snap","Geometry snap","Two different things are called \"snap\" and it is worth separating them before anything else. What it is Control Grid snap The pitch new and edited vertices land on. A property of the document. The Snap field in the toolbar; F9 toggles it Geometry snap Snapping to a feature of existing artwork — a pin, a corner, a midpoint. The magnet toggle; F3 or S Geometry snap is on by default , and Alt held during a gesture suppresses it — Alt never enables it, so the escape hatch is always in the same direction."],[31,"what-it-snaps-to","What it snaps to","Six feature kinds, in priority order, highest first : The six geometry-snap glyphs, each drawn by the editor's own renderer from a real query. Priority Feature Glyph What it is 1 Pin A declared connection point — a PCell's own pin, or one recovered from imported artwork 2 Corner / endpoint A shape's vertex, or the end of an open path 3 Intersection Where two edges cross 4 Midpoint The middle of an edge 5 Centroid A shape's area centre 6 Nearest The closest point on an edge — no named feature, just \"on this line\" The priority is what makes the behaviour predictable. The more intentional a feature is, the higher it ranks: when several candidates sit inside the capture radius at once — and near a corner they usually do — the one that wins is the one you almost certainly meant. A pin beats a corner, a corner beats a mere crossing, and \"somewhere on this edge\" is always the last resort. The marker is drawn in the source layer's own colour , tinted for contrast against the canvas background. That is not decoration: geometry snap crosses layers, and without it you cannot tell which layer you are about to snap to."],[31,"the-capture-radius","The capture radius","The snap tolerance is not a setting Geometry snap's capture radius is a fixed 8 device pixels , converted to DBU from the current zoom on every query. So it stays the same size on screen at every zoom level, and shrinks in real distance as you zoom in — which is what you want, because zooming in is how you ask for precision. The toolbar's Snap field is the grid pitch, a different quantity entirely; see Layout units and the snap grid ."],[31,"intersections-are-different-and-are-off-by-default","Intersections are different, and are off by default","Intersections are computed live near the cursor, not indexed. Corners, midpoints, centroids and pins are properties of one shape, so they are cached per cell; an intersection is a property of a pair , possibly spanning two cells, two instances or two layers, and the number of pairs is quadratic. It is therefore computed on demand over a bounded set of candidates near the cursor. Two consequences you will notice: It has its own toolbar toggle , and it is off by default — it is the one feature kind dense enough to be noisy, and leaving it off costs nothing when unused. With it on, snapping in a dense region does more work than snapping in a sparse one. That is the quadratic showing through, and it is why it is opt-in. Turning either toggle recomputes the marker at the last known cursor position immediately — you do not have to jiggle the mouse to see the mode change take effect."],[31,"ruler","The Ruler","Ruler ( D , or the ruler button on the toolbar) places a two-point measurement inside the layout: a line between two points with the distance between them drawn at its midpoint. Click once to set the first endpoint, move — the whole ruler previews, readout included, so you see the number before you commit it — and click again to set the second. The tool stays armed, because measuring is something you do several times in a row; Escape disarms it. Three ruler annotations on the same artwork: a trace width, a free-angle clearance carrying a caption and its dx/dy components, and a Scaled-text ruler across the whole run. A ruler is not geometry and never reaches a manufacturing file. It serves two jobs. The first is the one you do a dozen times an hour while placing and routing: how far apart are these two things? — answered on the canvas, without a dialog and without touching the artwork. The second is annotation: a ruler stays where you put it, saves with the cell, and comes out in a copy-paste into a slide or a report, so a design review can point at a clearance rather than describe it. Not the ruler strip along the canvas edge The strips along the top and left of the canvas are chrome: they track the viewport, show a scale, and cannot be placed, saved or selected. Everything on this page is the in-design ruler, which is a different object with its own colours in the theme editor."],[31,"placing-one-accurately","Placing one accurately","Both endpoints go through the editor's snap stack unchanged — grid snap and geometry snap , with the same markers, exactly as a Path vertex does. That is what makes the measurement trustworthy: an endpoint that lands 3 DBU short of a corner reports a number that is wrong in a way nobody notices. Shift locks the second endpoint to horizontal, vertical or 45°. It is not governed by the document's angle mode — a Manhattan document is a statement about manufacturable artwork, and the diagonal gap between two Manhattan traces is exactly the measurement you most want. Geometry snap outranks the Shift constraint when a snap feature is in tolerance, because a snapped endpoint is a stronger statement of intent than a held modifier. A ruler whose endpoints coincide after snapping is discarded rather than committed."],[31,"what-the-readout-says","What the readout says","Three parts, top to bottom, each independently omittable: ╱ ╱ 3.59 mm the distance — always shown, never editable ╱ Δx 2.54 Δy 2.54 when Show components is on ╱ bond wire span when a Caption has been typed The distance is computed and cannot be typed over. A ruler whose number can be overwritten is not a measurement, and in a design review it is worse than no ruler at all — the caption is the free text. It renders in the document's own display unit, with a decimal count and number format you can set, so switching the document from mm to mil re-renders every ruler with no stored value changing. The text is always drawn upright regardless of the ruler's angle, and is offset clear of the line."],[31,"fixed-and-scaled-text","Fixed and Scaled text","One property, two honest behaviours, chosen per ruler: Mode The text is Use it for Fixed (default, 11 pt) n points on screen, the same size at every zoom Temporary measurement — zoom out to the whole board and the readout is still legible Scaled a physical height in the layout, like a label Annotation — the ruler keeps its proportion to the artwork, so a review figure reproduces at any scale The Properties panel shows one size field whose label and units follow the mode. Both values are stored, so switching modes and switching back does not destroy the other one's setting. A multi-selection whose modes differ disables the field rather than guessing — set them all to one mode first, which is itself a single multi-edit."],[31,"selecting-editing-deleting","Selecting, editing, deleting","Rulers select like anything else: click the line, either endpoint, or the readout text — clicking the number is the affordance most people reach for. Move, nudge, delete and copy all work, and a mixed selection of shapes, instances and rulers still moves and deletes as one undo entry. Drag an endpoint to re-measure. The Properties panel edits both endpoints numerically, the size mode and size, the font style, the decimals and number format, the caption, the Δx/Δy toggle and the readout's own position and alignment. Right-clicking one offers Edit Ruler… , Reset Ruler Label Position , Delete Ruler and Clear All Rulers ( Ctrl+K ). A per-document Show Rulers toggle hides them all without deleting anything; it is view state and is deliberately not saved in the .clay ."],[31,"a-ruler-is-not-geometry","A ruler is not geometry","This is worth stating plainly, because it is the guarantee the feature is built on. A ruler is not a shape. It lives in its own collection in the cell file, it has no layer, it obeys no layer visibility, and nothing that walks the layout's shapes — GDSII, Gerber, Excellon, board export, booleans, offset, flatten, DRC, the EM mesher — can see it. A dimension line etched in copper is a scrapped board; the only way to make that impossible is for the annotation never to be in the collection those writers read. The two places it does come out are the two where a measurement is the point: The clipboard. Copy a selection and the pasted picture carries its rulers, which is what makes the review-slide use case work. DXF , as genuine aligned DIMENSION entities on their own RULER layer — see below."],[31,"interchange","Interchange","File ▸ Import offers GDSII, DXF, Gerber and Board; File ▸ Export offers the same four. Every export says what it could not carry at full fidelity before it writes anything , and that preview is produced by running the real write into a null stream — so it can never disagree with what lands on disk. Imports report the same class of thing afterwards, in the Messages panel: what was skipped, by type, with a count. A layer is identified by its integer (layer, datatype) pair, not by its name — that is GDSII's model and it is the right one, since names are for humans and change, while the numeric pair is what a fab's process assumption is keyed to. DXF and board files are name-keyed instead, so bringing one in goes through an explicit name↔pair mapping held in the technology; one shared mapping dialog serves every import that needs it. Gerber has no layer concept at all — one file per layer — so the mapping is declared on the way out and reconstructed on the way in."],[31,"gdsii","GDSII","Import and export. Hierarchy is preserved — one structure per cell, instances stay instances, and a board with four hundred identical parts stays four hundred instances rather than four hundred copies of geometry. Curved primitives are flattened to polygons and holes are keyholed, both because the format has no other representation; the export report counts each. Vias export as barrel plus landing pad, and a via with no landing layer exports its barrel only and is named in the report."],[31,"dxf","DXF","Import and export. The exporter writes R2000 (AC1015), R2004 (AC1018) or R2018 (AC1032) , selectable, defaulting to AC1032 . The choice is about colour, not geometry : every entity circuitRF emits — LWPOLYLINE with bulge, LINE , ARC , CIRCLE , ELLIPSE , SPLINE , HATCH , TEXT , INSERT , BLOCK — exists unchanged in R2000, but 24-bit true layer colour (group 420) arrived in R2004 and R2018 carries the identical capability. Choose R2000 only if a downstream tool refuses the newer header, and accept nearest-index colour when you do. R12 is deliberately not offered — it has no LWPOLYLINE , no ELLIPSE , no SPLINE and no HATCH , so writing it would mean flattening arcs and losing hole fills. Arcs are never flattened on DXF export. circuitRF's own bulge convention and DXF's are the same quantity, so an arc edge exports by copying the number. Rulers export here and only here , as real aligned DIMENSION entities on a layer named RULER , so the recipient can freeze or delete every one of them at once. A DIMENSION rather than a line plus some text, because loose geometry does not report a measurement, does not update if the recipient stretches the drawing, cannot be styled, and does not appear in anything that enumerates dimensions. A Fixed -size ruler has no meaning in a world-coordinate drawing with no screen, so its point size is resolved once, at export, to the height that occupies the same fraction of the drawing that its point size occupied of a nominal viewport — legible when the recipient zooms to extents, which is what the mode meant on screen."],[31,"gerber","Gerber and Excellon","Import and export. A board goes out to the fab house as artwork plus drill, and the same set comes back in as a cell you can edit, crop and mesh — your own, or someone else's. Export One Gerber file per layer, plus an Excellon drill file. Arcs are native ( G02 / G03 ) and a hole is a clear region, so neither is a lossy conversion; the pre-flight report lists only genuinely structural changes. A bare circle drawn directly on a drill-function layer still contributes a drill hit — it is never silently dropped — but it is unpaired : no matching pad, no annular-ring data, and the report suggests Convert to Via for it. Import — File ▸ Import ▸ Gerber… Point at a folder and you get the whole board. Point at a single file and circuitRF asks whether its folder was the real intent, telling you what that folder holds — one Gerber file is one layer, with no drill data, no other copper and no board outline, which is occasionally what you want and usually not. The folder is the default. There is a third option for pointing at a different folder outright, and Cancel creates nothing. What a file is gets decided by content, never by extension. Anything in the folder that is not artwork or drill data is skipped and named in the summary, so a folder of mixed junk is safe to point at. Which layer is which comes from the strongest source available, in this order: the .gbrjob job file , if the set has one — it settles set membership and identity together, and carries the stackup; the file's own X2 attributes ( %TF.FileFunction ), which also give the copper's position in the stack; a GerberSuffix in your technology that matches the file's extension — this is what closes the loop on a set circuitRF itself exported; a generic name heuristic (copper top/bottom/inner, mask, silk, paste, outline, drill); the shared layer-mapping dialog for anything left. The summary reports which rung identified each layer, and flags the ones that came from the heuristic — that is the rung that can be confidently wrong. Drill data. Excellon files often do not say what their numbers mean, so units and zero suppression are inferred from the format comment, the INCH / METRIC word, M71 / M72 , the tool diameters, and a cross-check of where the hits land against the artwork's own bounding box. You are only prompted when that inference actually had to guess or the cross-check disagrees, and the prompt shows you the evidence. Cancelling it cancels the import — a board read at the wrong scale is the worst kind of silent failure. Vias are rebuilt wherever a drill hit and a copper flash share a coordinate exactly: pad diameter, drill diameter, barrel and landing layer, back to a real ViaShape . Unpaired hits become circles on the drill layer, counted in the summary — a lot of them usually means the artwork and the drill file do not belong to the same board. Where the files declare via-vs-component drilling (X2 ViaDrill / ComponentDrill ) that declaration is used; where they don't, the two are genuinely indistinguishable from artwork alone and both come back as vias, and the summary says so. What you get is one flat cell — Gerber has no hierarchy, and reconstructing footprints from artwork would be guesswork — inside its own import folder, with its own new .ctech . Your workspace technology is read but never modified. The stackup is built from the job file when there is one, and from the artwork when there is not — the number of coppers, their order, and which drawing layer each is bound to were all worked out by the import already. What no Gerber file states at all is the substrate , and rather than leave it blank circuitRF fills it in with one ordinary FR-4 board: 35 µm outer copper, 18 µm inner, ε r 4.4, tan δ 0.02, and the dielectrics sharing out the board thickness the files state — or 1.778 mm when they state none. Every one of those numbers is named in the import summary, in a paragraph of its own , kept apart from the paragraph reporting what the files actually said. They"],[31,"board","Board files ( .kicad_pcb )","Import and export, through File ▸ Import ▸ Board… and File ▸ Export ▸ Board… . Which versions Import branches on the tokens present, never on the version stamp , and no file is refused for its version. That is not a stylistic preference: four epochs of one real board were measured, and the spellings are mixed within a single file — at the newest epoch every footprint line carries (stroke (width W)) while a graphic polygon still carries a bare (width W) . The layer table moves too ( B.Cu is ordinal 31 in older files and 2 in the newest), and in the oldest a renamed layer's user name occupies the canonical name slot, so a file may contain no string F.Cu at all. Everything therefore resolves through the file's own layer table, and \"is this copper\" is that table's type word — never a name and never an ordinal range. The epoch is reported in the import log, so you can see what circuitRF thought it was reading. Epoch Stroke width Arcs Fill flag Nets 20171130 (width W) centre + angle absent (net 7) + a top-level table 20211014 (width W) three-point (mid x y) (fill none / yes) (net 7) + a table 20221018 (stroke (width W) …) three-point (fill none / yes) (net 7) + a table 20260206 (stroke (width W) …) three-point (fill no / yes) (net \"GND\") , no table Export writes one dialect, 20221018 , and does not offer a choice. A reader has to accept files of every epoch because they arrive unbidden; a writer has the opposite problem, and must pick one every downstream reader accepts. 20221018 is late enough to be free of design rules and net classes — those left the board file at the 20211014 epoch — and early enough that every later release still opens it. What comes across Board-level geometry and footprint geometry are both handled; a footprint's items are in its own frame and compose through its placement, at an arbitrary angle, with a back-layer part being a mirror combined with that angle. In the board file Becomes, in circuitRF segment (track) a path, round-ended, carrying its net arc (three-point) a path with a single arc edge — the bulge is exact, not sampled via a via primitive: barrel on a drill layer, pad on the landing layer gr_line a two-point path at the stroke width gr_rect , filled / unfilled a rectangle / a path tracing the four edges at the stroke width gr_circle , filled / unfilled a circle / an annulus at ± half the stroke width gr_arc a path with one arc edge gr_poly , filled / unfilled a polygon / an outline path gr_curve , bezier a curve or path with a cubic edge gr_text a label zone → filled_polygon polygons, with their holes footprint / module one generated cell per distinct definition, one instance per placement pads: circle , rect , oval , roundrect , trapezoid , custom real copper geometry — see below images, dimensions, groups, tables, tuning patterns, teardrops skipped, reported by type with a count Pads are the fiddly part and are handled by shape , not approximated by a bounding box. A roundrect becomes a rounded rectangle; one carrying a chamfer — corners cut rather than rounded, at the chamfer ratio times the pad's short side — or one rotated off a cardinal angle builds the general boundary instead, straight edges plus quarter-circle arc edges. A trapezoid honours its own offset, which puts copper beyond the nominal size on one side and inside it on the other. A custom pad is its anchor shape unioned with every one of its primitives , all of them — taking the anchor alone frequently left no copper at all under the pin. An oval drill is drawn as the slot it is. Three import rules that exist because getting them wrong is silent An unfilled outline never becomes a filled region. An unfilled rectangle imported as a solid one is an entire copper pour that does not exist on the board — and it would be meshed and simulated as one. Zones import their fill , not their outline. The outline is the author's request; the filled polygons are the copper that exists, and the outline includes every area the fill would have"],[31,"drc","Design-rule checking","Design ▸ Check Design Rules , or the Check button on the DRC panel. The rules come from the technology , not from the layout, so a design retargeted to a different process is checked against that process's rules without anything being re-entered. The rule kinds a technology can declare are minimum width, minimum spacing, minimum separation, minimum enclosure, minimum overlap and metal density; each names the layer (or the layer pair) it applies to and the value it requires. .ctech files carry them, so a kit's own rules arrive with the kit. A design-rule check on the MMIC starter process: a 2 um neck breaking minimum width and a 2 um gap breaking minimum spacing, listed in the DRC panel and marked on the artwork. Each violation says which rule it broke, between which shapes, and by how much — and clicking one zooms the canvas to it. Markers draw over the offending geometry and can be toggled off. The header always names the technology that was checked against : a clean result against the wrong process looks exactly like a clean result against the right one, which is why the panel never leaves it to be inferred. A violation can be waived, with a reason . A waived hit stays in the list, greyed and counted separately, rather than disappearing — a known exception you can still see is worth more than a clean report you cannot trust. DRC, not LVS circuitRF checks the geometry against the process rules. It does not compare the layout against the schematic: there is no layout-versus-schematic check, and the connectivity the DRC computes internally exists to tell one net's shapes from another's, not to verify a netlist. Two different nets overlapping is a short, and finding it is an LVS job circuitRF does not do."],[31,"toolbar","The toolbar","Layout editor toolbar # Button What it does 1 Zoom to Fit (F) 2 Zoom In 3 Zoom Out 4 Zoom 1:1 (1 px per display-unit tick) 5 Select 6 Rectangle 7 Rounded Rectangle 8 Circle (center-radius) 9 Polygon (double-click/Enter to close, Backspace removes last vertex) 10 Path (trace) 11 Label 12 Via: this technology's stackup has no via layer — draw geometry on a via/drill layer directly instead. 13 Port — click a conductor end to place an EM port (auto-numbered) 14 Ruler (D) — click two points to measure (Shift locks to 0/45/90°; Ctrl+K clears every ruler) 15 Insert Bitmap… 16 Rotate 90° CCW (R) 17 Rotate 90° CW (Shift+R) 18 Mirror Horizontal (M) 19 Mirror Vertical (Shift+M) 20 Instance… (place a cell reference) 21 Show PCell pins 22 Geometry snap (F3 / S) 23 Snap to intersections 24 Save layout (Ctrl+S) 25 Push Into Cell (Ctrl+]) 26 Pop Out (Ctrl+[) 27 Check Design Rules (Ctrl/⌘+Shift+K) 28 EM Setup for this layout — opens it, creating it the first time 29 Show EM cross-section mesh 30 Show full-wave surface mesh 31 Export GDSII… 32 Export DXF… 33 Export Gerber… 34 — Layer: 35 — no tooltip The groups, left to right: the drawing tools , which include the Ruler ; the edit and boolean operations; the geometry snap toggles — the magnet and the intersections toggle described above; the hierarchy navigation (push in, pop out); EM setup and results , which sit here rather than among the drawing tools because all three are the same subject; and save . See also: Schematic Editor · Symbol Editor · Units · PCells · EM Setup · File formats · wBond ."],[32,"","","Parameterised cells: what they are, placing one, driving it, and editing it by its handles."],[32,"what","What a PCell is","A PCell — a parameterised cell — is a cell whose layout artwork is generated from its parameters rather than stored as a file. The gap it closes is easy to state. A microstrip line's artwork depends on its width and its length, and no stored file can express \"the width is the W parameter\" . Draw a 200 µm × 40 µm rectangle and you have one microstrip line; declare a PCell and you have every microstrip line. A PCell is an ordinary cell in every other respect It has a cell folder, a parameter list, a symbol and a project-tree node. Hierarchy, instances, arrays, push-in navigation and the geometry cache all work on it unchanged. Exactly one thing differs: when the editor asks the cell for its layout view, the answer is \"this cell's layout is generated\" rather than the name of a file. That is the whole of the difference, and it is what keeps PCells cheap — a PCell that were a new kind of thing would need parallel handling everywhere."],[32,"why","Why a parameterised cell beats a fixed one","Three things follow from generating rather than storing, and they are the reasons to prefer a PCell whenever the geometry is a function of numbers: One cell covers the whole family. Fifty microstrip lines of fifty different lengths are fifty placements of one cell, not fifty cells. The schematic and the layout cannot drift. The generator reads the same parameter list the symbol displays — one list, not two. A symbol showing W and a generator reading a different W is a defect that only ever surfaces as wrong artwork. A retarget is a re-run. Change the technology and every placement regenerates against the new stackup and layer table. Stored artwork would have to be redrawn. There is a cost, and it is the honest one: generated artwork is read-only. You cannot nudge a vertex of a PCell's output, because the next regeneration would discard the nudge with nothing to say about it. What you can do instead is drag its parameter handles — see below — or, when you genuinely want to hand-edit, Flatten Hierarchy to turn the instance into ordinary geometry and give up being parametric."],[32,"placing","Placing one, and driving it","A PCell is placed like any other cell — from the Library palette, or by Update Layout from Schematic , which resolves a parametric cell just as it resolves a stored one. To change it, edit its parameters: select the instance and use the Properties Inspector, or double-click it for the parameter dialog. Type a new value and the artwork regenerates. Parameters are kinded values — Real, Int, Bool or String — not bare numbers. That matters because a real cell names a model, counts fingers and picks a display mode, and forcing each of those through as a number the generator decodes by private convention is how kits become fragile. A count that arrives as 3.0000000000000004 either gets rounded by a rule you cannot see or produces geometry nobody asked for. Length parameters are stored in SI metres , and the conversion to database units happens in exactly one place with one documented rounding rule — so two generators can never disagree about where a 2.9 mm edge lands. You still type them with unit suffixes as everywhere else ( 2.9mm , 115mil , 50u ); see Units . A kit's parameters are often declared as text Vendor PCells commonly declare every parameter as a string , and a numeric value supplied where a string is expected can be silently ignored — the generator falls back to its default. The symptom is not an error message: it is wrong artwork . If a parameter change appears to have done nothing, look at the geometry it produced before looking anywhere else. See PDK Authoring ."],[32,"mlin","The worked example: MLIN","MLIN — a straight microstrip line — is the simplest PCell that is still a real one, and it is the example the whole contract is written around. Place it. Drop MLIN into a layout. It resolves against the layout's technology, which supplies the signal layer it draws on and the substrate underneath. Set W and L. Two parameters. W is the trace width, L its length. Type W = 40u , L = 200u . The artwork regenerates. One rectangle on the signal layer, running from the origin along +X, centred on y = 0 , with a connection pin at each end. That last sentence is not a description of MLIN in particular — it is the origin and orientation rule every PCell follows: Pin 1 sits at the cell origin, and the cell's principal axis runs along +X. For a line, the origin is the input end and the line runs right. For a bend, it is the input arm. For a symmetric junction, it is the centre with arm 1 along +X. The rule exists because otherwise every author chooses differently and nothing abuts."],[32,"pins-are-part-of-the-output-not-inferred-from-it","Pins are part of the output, not inferred from it","A PCell returns pins alongside shapes , because geometry alone cannot be connected. Each pin carries: Field Why it is needed Name It must match the symbol's pin, or the schematic and the layout disagree about connectivity Location Where it sits, in the cell's own coordinates Layer Which conductor it lands on Width and outward direction A microstrip connection is an edge , not a point — and a bend has to know which way its arm faces That last row is the one most easily left out and the most expensive to add later. It is also what makes a PCell's pins snappable: they are the highest-priority geometry-snap feature there is."],[32,"handles","Parameter handles — dragging is editing a parameter","A layout-driven user thinks \"this trace needs to reach that pad\" , not \"L is 3.4 mm\" . Every other primitive in the editor answers the first sentence directly: grab an edge, drag it, watch the number follow. Parameter handles give a PCell the same gesture without giving up being parametric. Select a single PCell instance and its handles appear on the artwork. Drag one, and the parameter that produced that piece of geometry changes; the cell regenerates around it. MLIN declares two: the far end , travelling along +X — that grip is L ; the top edge at mid-length, travelling along +Y — that grip is W . When shown A single selected instance whose cell is PCell-backed and whose generator declares handles. Never on a multi-selection. Readout Label = value , live, in the document's display unit. This is the part you actually read while dragging. Snap The projected point snaps to the layout's grid in world space — you are aligning to the grid you can see. Alt suspends it, as everywhere. Escape Cancels. Nothing is committed and no undo entry is left. Commit One undo entry per drag, however many pointer moves it took. Parameter grips are not geometry handles, and they do not look like them A geometry handle edits a coordinate; a parameter grip edits a number and the artwork is rebuilt around it. Confusing the two is surprising in a way that is hard to undo, so a grip is drawn in its own colour, with a hollow centre and a dashed axis hint showing which way it travels. A shape shows geometry handles; an instance shows parameter grips; the two sets are never on screen at once. A PCell that declares no handles behaves exactly as it always did — the Properties Inspector stays the primary surface, and no existing cell in any kit has to be revisited to keep working."],[32,"what-handles-are-not","What handles are not","They are not an inverse function. Nothing asks a generator to read its own output back and work out what parameters produced it. The generator declares what is editable; the editor never guesses. An editor that guessed would have to answer \"the user moved this edge — which parameter did they mean?\" from geometry alone, and for anything more complex than a straight line that guess is wrong often enough to matter. The failure mode would be the worst kind: the artwork regenerates, renders perfectly, and one parameter is now a value you never chose, with nothing on screen saying so."],[32,"retarget","When the technology changes underneath","A PCell reads the resolved technology as an input, alongside its parameters. Retarget the layout to a different technology and every PCell placement regenerates against the new layer table and the new substrate — a 50 Ω line on FR-4 becomes whatever that width means on the new stack, and the artwork lands on the new technology's signal layer. Two things follow that are worth knowing: Geometry is cached per unique parameter set, not per placement. A 50 × 50 array of one PCell evaluates the generator once and draws 2,500 times. That cache is keyed on (cell, parameter values, technology). It is the reason a generator is required to be deterministic : the same inputs must produce byte-identical output, on any machine, in any process, at any time. A generator that consults the clock, a random number, or ambient state breaks the cache silently — stale or inconsistent geometry, no error anywhere, and no way to tell from the result that anything went wrong. Two people on two machines getting different artwork is the same failure seen from the other end."],[32,"validity","Out-of-range parameters","A PCell may report a parameter outside its published validity range — once per distinct violation, naming the parameter and the bound. It is a report, not a refusal: the formula still evaluates. That is deliberate, and it matches how the microstrip models behave. The thing worth avoiding is silent extrapolation, not extrapolation. See also: The Layout Editor · PDK Authoring (writing your own, in Python) · PDK Integration · Units · MLIN in the component reference ."],[33,"","","Importing a process design kit and simulating the parts it supplies. A kit is a read-only tree supplied by a foundry or a device maker. It typically holds symbol descriptions, netlists defining its parts, palette icons, model data files, and — usually in a separate package beside it — the compiled model library those netlists name but never define. circuitRF reads a kit structurally . It does not carry a list of who makes what; nothing about a kit's identity is written into the product. Everything circuitRF knows about a kit it learned from the kit at run time."],[33,"what","What a kit contributes","Four things, and they arrive by four different routes: What it is Symbols Translated from the kit's own symbol descriptions into circuitRF symbols, in memory Models The device equations — usually a compiled library the kit names but ships separately Technology Layers, stackup and DRC rules, if the kit supplies them Artwork Layout for its parts, often as PCells generated from parameters A kit part is an ordinary cell reference This is the load-bearing decision of the whole area. A cell reference is already the component whose artwork lives in an external file and resolves at render time — so placement, rendering, pin geometry, hit-testing, net extraction and the symbol editor all work on a kit part unchanged. There is no \"external part\" species, and there deliberately is not going to be one. Three things a kit does not tell circuitRF, which it therefore works out and records: Which library implements its device types. A delivery is often several kits sitting beside one shared library package, and no kit says which. circuitRF establishes it by scanning candidate libraries for the entry points it calls. Which build of that library to use. The same library name arrives in a dozen toolchain builds. The most specifically named build for your platform wins — and the choice is reported , because it was made automatically. Which internal nodes of a model are not free unknowns. Detectable structurally; which node each one follows is not derivable from anything the model reports, and is supplied as run-time data."],[33,"openpdk","OpenPDK","circuitRF supports the OpenPDK file structure — https://github.com/fossi-foundation/open-pdks . If you are building a kit yourself, or normalising a supplied one, that is the layout to follow: it is an open standard, it is what circuitRF's importer expects to find, and it keeps a kit portable between tools. PDK Authoring covers producing one."],[33,"importing","Importing one","File ▸ Manage PDKs… ▸ Add… ( Ctrl/⌘ P ), then pick the kit's folder. An import writes nothing into your workspace No symbols are copied in, no parameter interfaces, no icons. The workspace records a reference to the kit plus the decisions circuitRF made about it — the kit path, the provider name, the resolved model-library path per platform, the chosen variant defaults, and a translation version. Everything else is rebuilt in memory each time the workspace opens. The principle is persist what was decided, rebuild what was translated . The decisions are tiny and carry no geometry; they are the difference between a workspace that opens the same way twice and one that quietly re-decides."],[33,"the-import-report","The import report","Every import ends in a report, success included — because the interesting result is almost never all-or-nothing. A kit typically holds parts circuitRF can place, drawings it cannot yet read, and files it does not recognise at all, and the difference is what you need to see. The report an import writes: what was read, what it holds, and the notes that go with it. The kit is invented - real kits are licensed and none is in this repository - but the report and the dialog rendering it are the application's own. The report groups what it found into read , recognised but not yet supported , and not recognised , and every finding carries the action that would close the gap. It is selectable text, so the whole thing can be pasted into a note or a bug report. The kit in that figure is invented for the documentation — real kits are licensed and none ships with circuitRF — but the report and the dialog rendering it are the application's own. A vendor delivery is often several part kits beside one shared package holding the compiled models, and discovery finds that package by adjacency . Once you reference a kit from somewhere else — a workspace folder, most obviously — that adjacency is gone and nothing on disk can recover it. So Add… also accepts a folder with no parts in it, when that folder holds the model libraries: adding the package directly is how you restore the link."],[33,"where","Where the parts go","Kit parts appear in the Library palette , ready to place. They do not appear in the Project Tree, because nothing is on disk — they are the kit's cells, not yours. Place one and it behaves like any other cell reference: it draws its symbol, it has pins you can wire to, and its parameters are seeded from the interface the kit published. A part that ships several formulations arrives on the one that works, so your first Run answers rather than explains. A welcome side effect of parts being virtual rather than on disk: placing one does not require a saved schematic. An ordinary cell reference is computed relative to the schematic's own folder, so it does; a kit part has no such need."],[33,"models","How a kit's models are evaluated","A kit's device models are usually compiled code — a shared library the vendor built, not equations you can read. circuitRF runs them in a separate worker process . That is a deliberate design, and it buys three things: a model that crashes takes the worker with it and not your session; a model built for a different operating system can still be reached (below); and circuitRF learns every device type, parameter name, pin count and node role at run time from the library itself, rather than carrying a table that would go stale."],[33,"what-to-expect","What to expect","Nothing to configure. The path is import kit → place part → set up an analysis → Run : Import reads the kit and, when the kit ships no provider manifest of its own — the ordinary case for an unmodified vendor kit — synthesises one , recording which library it found. It is written into your workspace as plain JSON, because anything chosen automatically should be visible and one line to correct. Placement seeds the instance's parameters from the published interface. Run starts the worker on demand. Opening a workspace starts no processes, and a kit your design never uses is never launched. The first evaluation of a run is slower than the rest — the worker is starting, loading the library and interrogating it. After that it is a pipe."],[33,"node-roles-are-measured-not-declared","Node roles are measured, not declared","Before a model is used, circuitRF probes it: it perturbs each node and compares the model's own analytic Jacobian against finite differences of its own currents. From that it learns, per node, whether the node is a free unknown, whether it is conductively coupled to its neighbours, and therefore whether it is electrical or thermal . A thermal terminal needs a reference, and a floating one has no solution An unconnected ordinary pin is fine — it simply gets its own net. A thermal pin is not: an open thermal terminal is a floating node fed by a constant current source, and it has no operating point at all. circuitRF supplies the reference the design did not, so an unconnected thermal pin does not stop a run. If you are modelling self-heating deliberately, build the thermal RC network yourself, between the thermal node and an ambient source — the models do not build it for you, and a non-zero thermal diagonal in the model is self-heating feedback, not a reference."],[33,"on-macos","On macOS","Vendor model libraries usually ship Linux and Windows builds only. A Linux library cannot be loaded on macOS at all — that is a binary-format and OS-ABI mismatch, not an instruction-set one, so nothing at the library level bridges it. circuitRF ships a small virtual machine for exactly this, and starts it for you; there is nothing to install. Expect a few seconds of startup on the first run of a session."],[33,"failures","When something goes wrong","A broken kit reference is a first-class, repairable state. A workspace whose kits are missing still opens; its kit parts draw as the \"not found\" placeholder. Your design is your data — a missing dependency degrades, it does not deny. Reporting is one summary per kit, not one message per part — forty parts must not produce forty warnings. The details go to a log file in the workspace, and the Messages pane carries the summary with a clickable path to it. What to check first, in order: Is the kit still where the workspace thinks it is? Manage PDKs… shows the stored path and whether it resolved. A kit is normally outside the workspace, so an absolute path is the common case and a moved kit is the common failure. Was the model library found? The resolved library path per platform is recorded per kit. If the shared model package was never referenced — see Importing one — the parts will draw and place but will not simulate. Run Validate PDK (below). It tells you whether the kit still holds the parts your design placed. Read the Messages pane for the worker's own words. A refused evaluation usually explains itself only in the worker's own error output, and that output is attached to the failure. A model that stamps cleanly but will not converge is worth a specific mention: every number is finite, nothing errors, and the solve simply does not settle. That is the signature of a misread node role — worth reporting with the kit's details rather than tuning tolerances against."],[33,"managing","Managing kits","Manage PDKs: the workspace's kit references, what each one resolved to, how many parts it loaded, and the Add / Remove / Reveal / Validate actions. File ▸ Manage PDKs… ( Ctrl/⌘ P ) lists every referenced kit with its name, stored path, whether it resolved, its part count and its translation version. Action What it does Add… Folder picker; imports and adds the reference through the ordinary import path Remove Drops the reference, after warning how many placed parts it will leave unresolved Reveal Opens the kit folder in your file manager Validate Runs the check below on the selected kit Every action reports in the dialog and posts to the Messages pane — the dialog gets dismissed, and the record of what you added, removed or validated should outlive it."],[33,"validate-pdk","Validate PDK","Re-reads the kit and reports drift , not just breakage: a part your design placed that the kit no longer offers, or a recorded translation version that no longer matches the reader. It reports what it checked as well as what was wrong — parts offered, placed parts checked, problems, notes. \"No problems found\" on its own cannot be told apart from a check that did nothing, and that is the one thing a validation must never be ambiguous about. A kit that could not be read at all reports −1 parts offered rather than 0, because \"offers nothing\" and \"could not be read\" are different answers to different questions. It checks your placed parts against a fresh read of the kit , never against whatever happens to be loaded in this session. The question is whether the kit still holds the part — conflating that with whether this session managed to load it turns \"your kit changed\" into \"something went wrong at startup\" . Why a translation version exists Symbol pins are snapped to the connection grid when a kit's symbols are translated. If that reader ever changes — a scale fix, a snap fix, anything touching pin placement — re-deriving symbols would move pins, and wires attached to them would silently disconnect . The recorded translation version is what catches that: a mismatch is reported rather than re-derived underneath your wiring."],[33,"authoring","Authoring a kit","PCells can be authored in Python — the generator, its parameters, its pins and its parameter handles all in one function in one file. That, the OpenPDK layout, and a complete worked example are in PDK Authoring . Kits headless A command-line run has no workspace to read the kit list from, so it takes the folders directly: --kits <dir> , repeatable, valid on every verb. That is what makes a design placing externally-supplied device models runnable in a script or a CI job. See also: PDK Authoring · PCells · Compiled Verilog-A models · Technology · The command line ."],[34,"","","Building a kit: the OpenPDK layout, Python PCells, models, technology and artwork. This chapter is about producing a kit. If you are consuming one, read PDK Integration instead."],[34,"openpdk","Follow OpenPDK","circuitRF supports the OpenPDK standard — https://github.com/fossi-foundation/open-pdks . It is what the importer expects to find, it keeps a kit portable between tools, and it means the structure of your kit is a published thing rather than a convention you have to explain to every user. Produce that structure. Beyond the structure itself, circuitRF reads a kit entirely at run time . Nothing about your kit is compiled into the product: every device type, parameter name, pin count and node role is learned from what you supply. Anything circuitRF cannot derive, you declare in a run-time data file beside the kit — never in a list inside circuitRF."],[34,"four-halves","The four things a part needs","A complete part is four artefacts, and it is worth being explicit that they are separate: What it is Where it comes from Schematic side A symbol with pins, and a published parameter interface A symbol description your kit ships, translated on import Model The device equations A compiled model library, or Verilog-A , or an SDD written in circuitRF's own expression language Technology Layers, stackup, DRC rules A .ctech , or the OpenPDK technology description Artwork The layout A stored .clay for a fixed cell, or a PCell generator for a parameterised one The parameter interface is the join between the first two and the fourth. A PCell reads the same parameter list its symbol displays — one list, not two. A symbol showing W and a generator reading a different W is a defect that only surfaces as wrong artwork."],[34,"python","PCell generation with Python","A generated cell is a function of its parameters and its technology, and nothing else. Write the function, declare its parameters, call run() . from circuitrf_pcell import Parameter, Pin, Rect, Result, generator, run @generator(\"MLIN\", [Parameter.length(\"W\"), Parameter.length(\"L\")]) def mlin(params, tech): w = params.length(\"W\") # already in database units length = params.length(\"L\") layer = tech.signal_layer return Result( shapes=[Rect(layer, 0, -(w // 2), length, w // 2)], pins=[Pin(\"1\", 0, 0, layer, w, 180.0), Pin(\"2\", length, 0, layer, w, 0.0)], ) run() The support package is tools/pcell-python/circuitrf_pcell . Python 3.9 or later, and no third-party packages — a cell author should need nothing but an interpreter to get started."],[34,"what-you-get","What you get","Shapes Rect RoundedRect Circle Polygon (with holes) Curve Path Via Label Curved edges Edge.line() Edge.arc(bulge) Edge.cubic(…) Pins Pin(name, x, y, layer, width, outward_deg) Parameters params.length() .real() .integer() .flag() .text() , each with a default Technology tech.signal_layer tech.ground_layer tech.layer_named(…) tech.stackup Warnings Result(diagnostics=[…]) Refusing raise — circuitRF reports it, naming the cell tech.signal_layer and tech.ground_layer are the resolved answer, worked out by circuitRF before the request was sent. Do not re-derive them from the stackup: a second implementation of that rule fails silently, putting geometry on a plausible but wrong layer. They can be None — a layout with no technology still generates geometry — so have a fallback. diagnostics is not an error channel. It is for a generator that did produce geometry and has a caveat about it. To refuse outright, raise."],[34,"five-things-worth-knowing-before-writing-a-real-cell","Five things worth knowing before writing a real cell","1. There are no metres. Every length arrives in database units , converted by circuitRF with its own single rounding rule before it was sent. The resolution is deliberately not on the wire, so a script cannot do its own conversion — there is nothing to do it with. That is what keeps one rounding rule across the process boundary instead of two. 2. Round your own arithmetic with dbu() . Not round() , which is banker's rounding in Python ( round(0.5) is 0 , round(2.5) is 2 ), and not int() , which truncates. Both disagree with circuitRF at exactly the midpoints where two adjacent shapes decide whether they abut. Passing a fractional float where a coordinate is expected is refused by name rather than silently rounded — being made to write dbu(w / 2) is the point. 3. A pin carries width and outward direction, and they are required arguments. A connection is an edge , not a point; a bend needs to know which way its arm faces. This is the field most easily omitted and the most expensive to add later, which is why the constructor will not let you. 4. Declare each parameter's dimension correctly. Parameter.length(\"W\") is what tells circuitRF to convert W from metres. A length you forget to declare arrives unconverted and your geometry is off by nine orders of magnitude; a non-length you declare as one is silently scaled. This is the one declaration worth checking twice. 5. A generator must be deterministic given its declared inputs. No clock, no ambient or global state, no randomness, no set-iteration order, no accumulation whose order varies between runs. Two users on different machines must get identical geometry — when they do not, what they see is a design that changed by itself, and the cache keyed on those inputs is quietly poisoned. Reading a file is fine provided its content is part of your cell's cache key — see dataFiles . stdout is the wire A stray print() lands in the middle of a frame and desynchronises the stream, which surfaces as circuitRF reporting a malformed reply nowhere near the print. Write to sys.stderr ; circuitRF surfaces that."],[34,"worked","A complete worked example: a microstrip line","The full, running version is tools/pcell-python/example/mlin.py in the repository. It is deliberately written from the contract , not transcribed from circuitRF's own built-in MLIN — the test that compares the two asserts byte-identical geometry, and that proves something only because they were arrived at independently. Walking the four artefacts for this one part: Schematic side. MLIN's symbol declares two pins and two parameters, W and L . Whatever your kit ships as a symbol description is translated on import. Model. MLIN's electrical model is circuitRF's own microstrip line — it reads W , L and the substrate from the technology. A kit supplying its own device would point at a compiled model library instead; see PDK Integration . Technology. The generator asks for tech.signal_layer and gets the resolved answer. The substrate underneath it comes from the same .ctech , which is also what the electrical model reads — so the artwork and the model cannot disagree about which stack they are on. Artwork. The generator above. Three things in it are the contract rather than a choice: w = params.length(\"W\") # DATABASE UNITS, already converted length = params.length(\"L\") half = w // 2 # integer halving — an odd width lands the same way in C# and Python return Result( shapes=[Rect(layer, 0, -half, length, half)], pins=[ Pin(\"1\", 0, 0, layer, w, 180.0), # pin 1 AT THE ORIGIN, facing out to the left Pin(\"2\", length, 0, layer, w, 0.0), # the line runs along +X ], ) Pin 1 sits at the cell origin, and the principal axis runs along +X. Every PCell follows this, or nothing abuts. The pins carry width and outward direction. That is what makes them connectable, snappable and usable as EM ports. The width is halved in integer arithmetic. circuitRF's built-in does the same, so an odd width straddles the axis identically on both sides."],[34,"checking-your-work","Checking your work","python3 tools/pcell-python/verify.py Self-contained: no circuitRF, no .NET, no test framework. It drives the example generators as real subprocesses over the real wire."],[34,"handles","Parameter handles","A parameter handle makes a piece of your generated artwork draggable , with the drag editing the parameter that produced it. Declaring one is a list argument on the same Result , in the same function, in the same file — there is no separate declaration file, no second language and no registration step: @generator(\"MLIN\", [Parameter.length(\"W\"), Parameter.length(\"L\")]) def mlin(params, tech): w = params.length(\"W\") l = params.length(\"L\") layer = tech.signal_layer return Result( shapes=[Rect(layer, 0, -(w // 2), l, w // 2)], pins=[Pin(\"1\", 0, 0, layer, w, 180.0), Pin(\"2\", l, 0, layer, w, 0.0)], handles=[ Handle(\"L\", anchor=(0, 0), at=(l, 0), axis=0), Handle(\"W\", anchor=(l // 2, 0), at=(l // 2, w // 2), axis=90), ], ) A handle states four things: which parameter it drives, where the grip is in cell-local database units, which way it moves , and — optionally — a label and a legal range . That is all. You never state how much the parameter changes per unit of travel circuitRF measures it, by asking your generator: at the start of a drag it perturbs the parameter, regenerates in memory, and reads where the same handle moved to. That is why no units appear in the declaration above — and why the same cell written in C# and in Python reads identically. Declaring no handles is a complete and correct choice. A generator that declares none behaves exactly as it always did, and no existing cell has to be revisited."],[34,"spiral","A second example: a spiral inductor","tools/pcell-python/example/spiral.py . Where MLIN is the smallest cell that is still real, this is the smallest cell that is still interesting : four parameters that interact, so it shows what a parameterised cell actually buys you. @generator(\"SPIRAL\", [Parameter.length(\"Width\"), Parameter.length(\"Space\"), Parameter.length(\"Inner\"), Parameter.integer(\"Turns\")]) def spiral(params, tech): w = params.length(\"Width\", 10_000) s = params.length(\"Space\", 10_000) inner = params.length(\"Inner\", 100_000) turns = params.integer(\"Turns\", 3) if turns < 1: raise ValueError(f\"a spiral needs at least one turn; got {turns}\") layer = tech.signal_layer or FALLBACK_LAYER pitch = w + s half, half_inner = dbu(w / 2), dbu(inner / 2) shapes = [] for t in range(turns): r = half_inner + half + (turns - 1 - t) * pitch # this turn's centre-line radius r_next = r - pitch shapes.append(Rect(layer, -r - half, r - half, r + half, r + half)) # top shapes.append(Rect(layer, r - half, -r - half, r + half, r + half)) # right shapes.append(Rect(layer, -r - half, -r - half, r + half, -r + half)) # bottom top = r_next + half if t < turns - 1 else half # left, stopped short shapes.append(Rect(layer, -r - half, -r + half, -r + half, top)) if t < turns - 1: # step inward shapes.append(Rect(layer, -r - half, r_next - half, -r_next + half, r_next + half)) ... Three things in it are worth copying: Turns is an integer , not a length . It carries no dimension, so circuitRF does not scale it. Getting this wrong is trap 4 above, in its most literal form. The artwork is a chain of overlapping rectangles, not one closed outline. A spiral drawn as a single polygon needs a mitre rule at every corner, and getting that rule wrong is the classic way a spiral's inductance comes out plausible and wrong. Overlapping rectangles on one layer union exactly, because the coordinates are integers, and every corner is square by construction. Zero turns raises. An empty Result would be a silently empty cell, which looks like it worked. The inner terminal is left facing +Y rather than being routed out: reaching it needs an air bridge or an underpass on another layer, which belongs to whatever places the cell, not to the cell."],[34,"declaring","Declaring your generators to circuitRF","A pcell-generators.json beside the kit — run-time data, never a list inside circuitRF: { \"schemaVersion\": 1, \"entry\": \"pcells/main.py\", \"pythonPath\": [\"lib\"], \"sources\": [\"pcells\", \"lib\"], \"dataFiles\": [\"tables/pads.csv\"], \"interpreter\": null } Key What it is for entry The script that calls run() . Relative to this file's own folder, so the kit can be moved or copied whole and still resolve. pythonPath Added to PYTHONPATH , so your kit's own modules import with nothing to configure. sources What your generators are built from — the cache key that decides whether an already-generated cell can be reused. Omit it and the entry script's own directory is used, which is the ordinary layout. Not pythonPath : that may point at a shared environment you do not own. dataFiles Files your geometry depends on but which are not source — a table of pad sizes, a device list. Reading a file is fine provided its content is part of your cell's cache key, and declaring it here is how it becomes part of it. Undeclared, editing it changes nothing: circuitRF keeps handing out the cells built before you changed it. interpreter Usually omitted; circuitRF finds one. Set it when your cells need packages that live in a particular environment. circuitRF does not bundle an interpreter and does not install packages on your behalf — a kit that needs an environment declares it. It deliberately does not list the generators the kit offers. The describe call is the only source of that, and a second one would be a cache that can silently disagree with the script."],[34,"traps","Traps worth a callout","A parameter declared as text silently ignores a number Kit PCell parameters are commonly declared as strings . Supply a numeric value where a string is expected and it can be ignored — the generator falls back to its default. There is no error message. The symptom is wrong artwork , and it will look entirely plausible. Tell your users to check the produced geometry , not the messages. And when you author a parameter, ask whether it really needs to be text: a number declared as a number cannot fail this way. A unit belongs in the row's unit field, not in the value circuitRF's expression parser has no unit-suffix production, so a value written as 60u in an expression is a parse error — and an unresolvable value is skipped rather than reported at the point you typed it. Some kits' own scripting accepts that spelling, which means the difference does not show up until Run. See Units . Non-determinism poisons the cache silently. It is worth repeating because it is the failure with no symptom: generated cells are cached on (cell, parameter values, technology) , so a generator that consults anything outside those inputs produces stale or inconsistent geometry with no error anywhere. Do not flatten your own curves. Emit Edge.arc(bulge) and Edge.cubic(…) and let circuitRF flatten at screen resolution. Flattening is a rendering decision, and a generator that pre-flattens has made it once, at the wrong time, for every zoom level. See also: PCells · PDK Integration · Units · Technology · Compiled Verilog-A models · The SDD ."],[35,"","","The layers your board is actually made of — and, because it decides the ground plane, where the negative terminal of every EM port is. Your layout is a set of flat shapes. The stackup is what turns those shapes into a physical structure: what each conductor is made of, how thick it is, what dielectric separates it from the next one, and — the part that catches people — which conductor is ground . That last one is not a detail. An EM port has two terminals, and only one of them is anywhere near your artwork: the other is the ground plane. You do not set it per port. The stackup does, for every port in the run at once."],[35,"where","Where the stackup lives","The stackup is part of a technology — a .ctech file in the workspace's tech/ folder, edited in the technology editor's Stackup tab. It belongs to the technology rather than to a layout, because it describes a process : every board built on that process has the same layers, whatever is drawn on them. A layout names a technology (or leaves the name blank to mean the workspace default). Everything else — the layer table, the DRC rules, the default display unit — lives in the same file. See The Layout Editor for the rest of it. A missing technology does not block drawing, but it does block an EM run. Layers fall back to a generated palette and editing carries on; the EM run refuses rather than inventing a stackup, because there is no honest default for \"how far above the ground plane is this trace\"."],[35,"anatomy","Anatomy of a stackup","A stackup is an ordered list, top to bottom , of three kinds of entry, plus two boundary conditions for what is above and below the whole sandwich. An MMIC stackup in cross-section: two signal metals over a GaAs substrate, a backside ground plane, the thin-film capacitor module between the metals, and the three vias that connect them. The heavy edge marks the ground-designated conductor - the negative terminal of every port in an EM run; the capacitor dielectric is marked with the plate it is patterned with, which is what keeps it out of runs that have no capacitor in them. Thicknesses are printed rather than drawn to scale. Entry kind What it is What it carries Conductor A metal layer Thickness, conductivity σ, the drawing layers that map onto it, and whether it is the ground reference Dielectric The material between two conductors Thickness, ε r , tanδ, µ r Via A connection between two conductors — not a layer of the sandwich The drawing layer via shapes are drawn on, the two conductors it spans by name , and its fill (plated or solid) with a wall thickness A field an EM run cannot use is marked as you look at it A thickness of zero, a permittivity below 1, a conductivity of zero — anything an EM run would refuse outright — gets a faint amber outline on the box itself, and hovering it says what is wrong and what would fix it. It is amber and not red on purpose: an unfilled field is a normal state of a technology somebody is still writing, not a fault. It marks unusable, never merely unlikely. A value that is present and plausible is never marked, because nothing in circuitRF knows which board you are looking at — so a mark means the run will stop, and an unmarked field is not a claim that the number is right. The two boundary conditions are properties of the stack as a whole: Top — Open (free space above, the usual case) or Ground . Bottom — Ground (the usual case) or Open . A dielectric is never drawn, and it is laterally infinite A conductor entry has drawing layers; a dielectric entry has none . It is a sheet of material spanning the whole problem at its stated thickness, everywhere, because that is what the solver's Green's function is built on — a stratified medium, uniform in x and y. This matters most for a thin dielectric that a real process only leaves under and just beyond a structure — a capacitor dielectric, say. The model carries it as a full sheet at its true height. That is the standard trade in this class of tool, and it is a good one where the structure is; it is not free everywhere else, and A thin-film (MIM) capacitor gives the measured cost. A drawing layer is not a conductor What you draw on is a drawing layer — a GDSII layer/datatype pair with a name and a colour. What gets simulated is a stackup conductor . The link between them is the conductor entry's drawing layers list, and a shape on a layer that is bound to nothing is simply not part of the EM problem. If a run tells you it found \"nothing on a layer bound to a signal conductor\", that binding is what is missing — not the artwork."],[35,"tab","The Stackup tab, field by field","Below is the shipped four-layer FR-4 technology — the one a new workspace can be created against — open in the Technology Editor's Stackup tab. Nine entries: four coppers, three dielectrics between them, and two via entries. It is worth opening yourself alongside this page; everything on it is described below. The Technology Editor's Stackup tab on the shipped four-layer FR-4 technology: four copper entries, three dielectrics between them, and two via entries that span different pairs of conductors. The boundary conditions and the three Add buttons are on the top row; the summary beneath them is the stack height and what the stack is made of."],[35,"the-top-row-what-is-above-and-below-and-how-to-add-an-entry","The top row: what is above and below, and how to add an entry","Control What it does Top The boundary condition above the whole sandwich — Open (free space, the usual case) or Ground . Bottom The boundary condition below it — Ground (the usual case) or Open . ＋ Dielectric / ＋ Conductor / ＋ Via Appends an entry of that kind. A new entry lands at the bottom of the list; the ↑ ↓ buttons on its own row move it to where it belongs."],[35,"the-summary-row-what-the-stack-adds-up-to-and-what-it-is-made-of","The summary row: what the stack adds up to, and what it is made of","Readout What it means Stack height The sum of every Conductor and Dielectric thickness, in the technology's display unit. Via entries are excluded — a via occupies no slice of the stack; it traverses the dielectrics between the conductors it spans. 62.13 mil above is the 62 mil the board is sold as. conductors / dielectrics / vias How many entries of each kind. conductors: 4 is the layer count a fabricator quotes; vias: 2 is two kinds of connection, not two holes. board Shown only when some other document stated an overall board thickness — a Gerber job file's, today. If it disagrees with the stack height by more than 1%, the row says so and nothing is corrected : which of the two numbers is wrong is not something circuitRF can know. The filter box below it narrows the list by entry name, and the count beside it is how many of the nine are showing."],[35,"every-entry-kind-name-order","Every entry: kind, name, order","Each entry is a card. The grey word at its left is its kind , fixed when it was added. The box beside it is its name , and the name is not decoration: a via entry names the two conductors it spans by name , and an EM setup names its analysis levels the same way. The three buttons at the right are ↑ and ↓ — the list is ordered top to bottom and that order is z — and ✕ , which removes the entry."],[35,"a-conductor-entry","A Conductor entry","Field What it is Thickness The metal's thickness, in the display unit. Used by the cross-section kernel and by interchange; the full-wave planar kernel solves a zero-thickness sheet and uses this only to place it — see What the EM engine does not read . σ (S/m) Conductivity at 20 °C. Sets conductor loss and skin depth in the cross-section kernel. The combo beside it is a shortcut that fills the box with a metal's bulk value (copper 5.8e7, silver 6.3e7, gold 4.1e7, aluminium 3.77e7, nickel 1.43e7); the box stays typeable and is what gets saved, and a value matching none of them reads as Custom . Ground reference Marks this conductor as the ground plane — the negative terminal of every port in an EM run . In the figure it is ticked on Inner 1 and on Bottom Copper ; the rule for which one a given run uses is in the next section . Metal thickness goes to The full-wave solver has no thickness for metal, so this conductor's thickness is given to the dielectric above it or the one below it. the layer above is the default and is what every technology authored before the field existed means. The stack height is the same either way. Full explanation, and the one case that needs the other setting: Where a conductor's thickness goes . Drawing layer Which drawing layers map onto this conductor — a checkbox list , because a conductor may carry several, with a filter box above it for a process that has hundreds. A shape on a layer bound to nothing is simply not part of the EM problem."],[35,"a-dielectric-entry","A Dielectric entry","Field What it is Thickness The separation this material provides. On the figure, 8 mil of prepreg either side of a 42 mil core. ε r Relative permittivity. tanδ Loss tangent. µ r Relative permeability — 1 for everything that is not a magnetic material. Patterned with (none) for an ordinary, laterally continuous dielectric, which is what all three on this board are. Naming a conductor here says this film exists only where that metal's artwork is — the thin-film capacitor case, described in The capacitor dielectric rides along only when its plate is analysed . A dielectric has no drawing layer, and there is no picker for one. It is not drawn: it is a sheet of material spanning the whole problem at its stated thickness."],[35,"a-via-entry","A Via entry","Via entries are listed as their own group below the ordered ones , under a line saying why: a via is not a layer of the sandwich and has no position in the top-to-bottom order. Field What it is Spans The two conductor entries this via connects, by name . The two on the figure differ, and that is the point of a four-layer board: Plated Through-Hole runs Top Copper → Bottom Copper, while Ground Via (L1-L2) runs Top Copper → Inner 1. Fill · Plated Whether these holes are metal at all. Unticked means a non-plated hole — a mounting hole, a routed cutout — drawn as it is but not extracted as a vertical conductor. Fill kind Plated or solid. Carried for thermal work; the RF solve does not read it, because a plated wall a few µm thick is already many skin depths. Wall Plated wall thickness — the metal on the barrel wall, not the hole radius. 20–25 µm (about 1 mil) is typical and is what the shipped technologies use. Drawing layer The one drawing layer via shapes are drawn on. One , not a list: a via binds at most one, so this is a plain picker with an explicit (none) . The ? in the corner opens this page The Help button at the top right of the Technology Editor follows the tab you are on: the Stackup tab opens this chapter, and the Layers, DRC Rules and Interchange tabs open their own sections of The Layout Editor ."],[35,"ground","Where your port's negative terminal is","Every port in an EM run returns through one plane, and the stackup picks it. Not the panel, not the port label, not per port. The rule, in order: The ground-designated conductor. Mark a conductor entry as the ground reference in the technology editor. The plane is the top surface of the highest ground-designated conductor that lies below the signal level being simulated . Failing that, the stack's bottom. If no conductor is marked and Bottom = Ground , the plane is taken at the bottom of the stack. The run says so in its notes, and asks you to mark one instead — this fallback places the plane by boundary condition rather than by a real conductor's surface, which is a different height by the thickness of whatever is down there. Failing both, the run is refused. No ground reference and no grounded bottom means no second terminal for any port, and there is nothing to solve. Three consequences worth having in mind: The plane is modelled as laterally infinite. It is the boundary condition the Green's function handles analytically, not a meshed pour — so a ground pour drawn as artwork is not it, and a finite plane's edges are not modelled. A conductor marked as the ground reference is not meshed and cannot also be an analysis level; naming one as a level is refused. An intervening metal layer does not become ground just by being underneath. The rule keys on the designation , not on stack position, precisely so that an MMIC's second metal level is not mistaken for a plane. Mark what you mean. This is the port's negative terminal for every port type — an edge port at a conductor end, an internal delta gap , or an internal port whose whole purpose is to reach it. It is why an internal port needs a path down to the plane at all. If the answer looks wrong by a constant factor, check this first The height between the signal conductor and the ground plane is the single number a microstrip's impedance depends on most. A ground reference marked one entry too low — on the far side of a dielectric you forgot was there — changes every impedance in the run while leaving the result looking completely plausible. The run's own notes name the conductor it used and its height; read them."],[35,"levels","Which conductors get simulated","A conductor entry that is not the ground reference is a signal conductor, and a signal conductor with artwork on it is an analysis level . With one signal conductor, that is the level. Nothing to choose. With more than one, the EM setup can name the levels it wants (the analysis-level list); with none named, every signal conductor that actually carries artwork is included. Levels are ordered bottom to top , and the lowest one sits on the slab's top surface."],[35,"sheet-surface","Where a conductor's thickness goes","The full-wave solver has no thickness for metal at all. It models a conductor as a zero-thickness sheet at one height — so the slice of the stack that conductor occupies has to be filled with something , and the stackup does not say what fills a metal layer where no metal is drawn. The answer is that the metal's thickness is given to the dielectric on one side of it, and which side is a per-conductor setting: Metal thickness goes to on the conductor's row in the Stackup tab. Setting The metal's thickness is added to The sheet ends up at Height of a line on it the layer above (default) the dielectric above the conductor the top surface of the dielectric below the substrate under it, exactly as entered the substrate below the dielectric below the conductor the bottom surface of the dielectric above the substrate plus this conductor's own thickness Neither setting moves anything, and the stack height is identical either way — the total is the same number in both cases. All that changes is which neighbour absorbs the vanished thickness, and therefore how high above the ground plane the sheet lands. \"The layer above\" is what you want almost everywhere , and it is what a conductor that says nothing means: a trace deposited on a substrate and encapsulated by whatever comes next, whose height above the ground plane comes out as the substrate thickness. It is also the setting the closed-form microstrip models agree with — they model real metal of real thickness and measure their height to the metal's underside . \"The substrate below\" is for the lower plate of a capacitor. With the thickness given to the layer above, that plate's whole metal thickness lands inside the plate gap, and the solver separates the two sheets by the capacitor dielectric plus that metal — 3.2 µm rather than 0.2 on a typical MMIC metal. Giving it to the substrate below instead puts the gap back to the dielectric alone. The shipped MMIC technology does exactly this on Metal1 . And it applies only where the capacitor does. A run that is not analysing the plate has no plate gap to get right, so a conductor directly under a patterned dielectric is put back to the default for that run — the same run note that reports the dielectric reports this. That is what lets one technology set it at all: an ordinary line on Metal1 still solves against 100 µm of GaAs, exactly as it did before the capacitor module existed. This is not \"expand up / expand down / symmetric\" Tools whose stackup gives you an interface and a metal thickness have to decide which way the copper grows from it — up, down, or half each way. circuitRF never asks that , because its stackup is an explicit ordered list: the conductor is its own row with its own thickness, already sitting between two named dielectric rows. Its extent is fully determined before any EM question is asked, and nothing is ever displaced. This setting answers a later question, and one that exists only for the full-wave planar kernel: given that the metal already occupies a known slice, and the solver will represent it as a sheet at a single height, which face is that height and which neighbour fills the gap. There is deliberately no \"symmetric\". Half to each side is representable, but no structure wants it: it would place every ordinary line permanently half a metal thickness away from the closed-form models circuitRF validates itself against, and for the ground-referenced case that matters the field is in the substrate under the strip — the underside is the right face there, not a crude default that a midpoint would refine. The cross-section kernel ignores this entirely It models real metal of real thickness and has no sheet to place, so the setting is read by the full-wave planar path only. It also never applies to the ground plane , whose boundary is always taken at its top surface whatever its own row says. The trade is stated rather than hidden: in a capacitor run a Metal1 microstrip's EM substrate is 103 µm of GaAs rather than"],[35,"slab","The slab, and what makes one solvable","The solver works on the dielectric between the ground plane and the lowest analysis level . Two stackup mistakes are refused by name rather than solved around: The signal conductor sits at or below the ground plane. There is no slab between them. Almost always a stackup written in the wrong order — the list runs top to bottom. There is no dielectric entry between the plane and the conductor. A conductor floating directly on a plane with nothing in between has no substrate to be a microstrip over. Several dielectric entries between the two are carried as several layers , each at its own thickness, ε r and tanδ — the medium is stratified and the solver solves it that way. (It used to refuse a stratified region under the feed and tell you to merge the entries into one; merging them changes the physics, and the reason for it is gone.) The run's notes name the layers it found and print the single ε r it uses to size the calibration standards and the mesh — the series-capacitance equivalent of the stack. That number is a mesh-sizing average and never the reference impedance the answer is published against. Layers above the top conductor are not part of that slab: the top boundary is what is above, and it is Open unless you say otherwise."],[35,"vias","Vias","A via entry is not a layer — it is a connection between two conductor entries, named by name in its Span from / Span to fields. Draw on the drawing layer the entry is bound to, and the EM extractor turns what you drew into a real vertical current path. Two kinds of artwork count , and both go through the same rules below: a via primitive , the pad-and-drill point you place with the Via tool; a filled region — a rectangle or a polygon, holes included. This is what a thin-film capacitor's plate connection is: a patch nearly as large as the plate itself, rather than a point. Several regions on one via entry are several footprints of the same connection. A path on a via layer is ignored, with a note — a path is a centreline and encloses no area, so there is no footprint to mesh. Draw the region instead. Three rules the extractor applies, each reported when it bites: The two conductors must be adjacent in the stack. A via that skips a level is not built, because there is nothing to connect it to in between. A via may terminate on the ground plane , and that is the common backside/through-hole case: the named conductor is the ground-designated one, so the via runs from a signal level down to the plane. A via naming some other non-analysis conductor is ignored with a note — it would otherwise silently model a structure you did not draw. A via primitive's footprint is squared. A round barrel staircased onto the mesh grid would cost a gridline per facet for no physics, so it is replaced by the equal-area square , which preserves the conducting cross-section. The run reports that it did. A drawn region is not squared — the substitution exists so a circle nobody drew does not staircase, and an outline you drew already is the footprint, so it is meshed as it stands. An internal port does not need you to draw one An internal port is placed on the metal and returns to the ground plane, so it needs a path down there. If you drew a via, it drives yours. If you did not, the solver builds one — a square of the technology's default via drill (or, if the technology declares none, a quarter of the substrate height), reported by size in the run's notes. That path is real metal and its inductance is in the answer, which is why you can override it simply by drawing the via you want."],[35,"mim","A thin-film (MIM) capacitor","A MIM capacitor is a thin dielectric between two metal plates inside the interlayer dielectric. In a stackup it is three entries : Entry Kind What it is MIM Metal Conductor The top plate — a thin metal with its own drawing layer MIM Dielectric Dielectric The capacitor dielectric: thin, higher ε r , no drawing layer MIM Via Via The plate's connection up to the routing metal above it The bottom plate is the interconnect metal underneath — no fourth entry. You draw the two plates and the plate connection; the dielectric is never drawn. A Cap Dielectric or Nitride layer in a layer table is mask documentation, not this. Both capacitor forms are ordinary multi-level artwork: Shunt — bottom plate on the lower metal over one or more backside vias, top plate on the plate metal, feed landing on the upper metal through a plate-via region. Series — feed in on the lower metal, which is the bottom plate, and out on the upper metal through the plate via. The thin-film capacitor module in cross-section: the plate metal, the capacitor dielectric under it - marked with the plate it is patterned with, which is what keeps it out of runs that analyse no plate - and the plate via up to the routing metal. Thicknesses are printed rather than drawn to scale."],[35,"mim-tied","The capacitor dielectric rides along only when its plate is analysed","There is one MMIC technology, and it carries the capacitor module. That is only possible because the capacitor dielectric is tied to its plate — the Patterned with setting on its row names MIM Metal , and it means this film exists where that metal's artwork is, and nowhere else , which is what a thin film physically is: deposited under the plate and etched away everywhere around it. So an EM run carries the film only if the plate is one of its analysis levels. Draw a capacitor and the plate has artwork on it, so the film is there and the plates are 0.2 µm apart. Draw plain interconnect and it is not, so the film enters the medium as air at the same thickness — nothing above it moves — and the lower metal's thickness goes back to the layer above. That run is then identical, number for number, to the same run on a stackup that never had a capacitor module in it. No setting to remember, and nothing to switch. It is always reported. A run whose tie deactivated says so in its notes, naming the dielectric, the plate that would switch it back on, and the thickness setting it reverted. A medium you did not author and cannot see is exactly the kind of change that produces a complete, believable answer to a question nobody asked. Why it has to work this way: a stackup dielectric is laterally infinite , so a film that was simply present would be present in every run. That is not free, and both costs are large enough to see: Airbridge posts would stop solving. A post between the two metals would cross a dielectric interface, and a via that crosses one is refused by name — the closed-form integral along a via is written in one region's coefficients. That refuses the whole run , it does not drop a shape. A line on the lower metal would move. The 0.2 µm ε r 6.8 sheet would sit on it as superstrate: measured on the acceptance line, ε eff rises ~1.7% and Z₀ falls ~2.8%. Both are correct physics for a run that has a capacitor in it , and wrong for one that does not. The tie is what tells the two apart. (circuitRF shipped two MMIC technologies for exactly this reason until the tie existed; if you have a workspace holding its own copy of the older pair, they still work — nothing in a workspace is rewritten by a new release.) What is still bounded: with the plate level in the analysis, a post between the two interconnect metals skips a level, and a via must span two conductors that are adjacent in the analysis . It is dropped with a note. Capacitor plates and airbridge posts in one EM setup remain separate runs — but they are now separate runs on one technology, not two."],[35,"mim-import","Adding a capacitor module to an imported technology","A technology imported from a process description often arrives without its capacitor module. A process stack description states the interconnect — the metals, the insulation between them, the vias that join them — and treats an optional thin-film module as exactly that: optional, and frequently left out, while the layer table shipped beside it still lists the module's drawing layers. The import is faithful to what it was handed, so what you get is a valid technology whose plate layers draw perfectly and connect to nothing. The import report says so, by name. Two of its notes are about this and only this: \"No via in the file names these conductors…\" — a conductor the stack describes that no via entry reaches. It cannot be connected, and the file never said how it should be. \"The layer table defines N drawing layer(s) no stackup entry is bound to…\" — layers you can draw on that the stack does not model. A shape on one is artwork, not structure. A real layer table has plenty of these legitimately, so the note counts them and names the first dozen. Neither is an error, and neither is a guess: nothing in the file states the missing piece, so circuitRF names the gap rather than inventing an entry to fill it. The fix is three rows in the Technology Editor's Stackup tab, in stack order, and it takes about two minutes. Numbers below are silicon-nitride-class examples — take yours from the process: Row Kind What to set The capacitor dielectric Dielectric Thickness d (50–300 nm; 0.2 µm here) and ε r . No drawing layer. The top plate Conductor Its thickness (0.25 µm here) and its drawing layer — the plate layer the import already brought in The plate connection Via Span from the plate to the metal above it, and its own drawing layer — the plate-via layer the import already brought in Insert them between the two interconnect metals, in that order, top plate above dielectric. The bottom plate is the interconnect metal underneath — there is no fourth row, and nothing is drawn for the dielectric. ε r comes from the capacitance density the process quotes , which is the number a process actually publishes: ε r = C″· d / ε₀ At C″ = 0.30 fF/µm² over d = 0.2 µm that is 6.8 — the value the shipped MMIC technology carries. Work it out from your own two numbers rather than copying a permittivity out of a materials table: the capacitance is what you want the model to reproduce, and d and ε r only ever appear together in it. Then two settings on the rows around it , and neither is optional: Tie the dielectric to the plate — Patterned with on the dielectric's own row, set to the top plate you just added. Without it the film is a continuous layer of the medium, present in every run on this technology, and the two costs in the tie's own section land on your interconnect: airbridge posts between those metals refuse the whole run, and a line on either metal moves in ε eff and Z₀. Give the lower metal's thickness to the substrate below — see Where a conductor's thickness goes . A conductor is solved as a zero-thickness sheet, and by default its thickness is given to the layer above it, so the modelled plate separation would be your dielectric plus the lower plate's whole metal thickness — several times the gap you just entered, with a plausible capacitance to show for it. The tie reverts this setting for you in any run that is not analysing the plate, which is why it is safe to set at all. Add the rows to the technology your designs already use — there is no copy to make and no retargeting to do. With the tie set, a run that draws no plate artwork extracts exactly as it did before you added the rows: the film enters the medium as air at the same thickness and the sheet surface goes back, and the run's notes say so. What activation does change , stated so it is not a surprise the first time a capacitor run disagrees with an interconnect one: in a run that does analyse the plate, the film is real, and the lower metal's line sees it. Its thickness is given to"],[35,"mim-accuracy","Reading a capacitance off a MIM run","Never read a small element off a RAW solve, and mesh the gap The solver models the plate separation the process states — 0.2 µm on the shipped MMIC technology, the capacitor dielectric and nothing else. (It used to model 3.2 µm: a conductor is solved as a zero-thickness sheet, and with that thickness given to the layer above it the lower plate's whole metal thickness fell inside the gap. The lower plate's entry now gives its thickness to the substrate below instead — see Where a conductor's thickness goes .) The run's notes print every level's z and the surface it sits on, so you can always read back the separation it used. A port on upper metal now de-embeds. It used to be refused: the reference impedance a de-embedded answer is published against is Z c = γ/(jωC pul ), and C pul came from an electrostatic image series over one grounded slab — the right problem for a trace on the substrate's own top surface and the wrong one for metal buried in the interlayer dielectric. The solver now solves that electrostatics at the port level's own height in the real stack, so an ordinary two-port MIM network de-embeds like anything else. The same change lets a technology carry several dielectrics under the lowest analysis level, which used to be refused with \"merge the layers\". Read the capacitance from the de-embedded answer, never from a raw one. A raw solve's s-parameters include each port's own discontinuity, and that discontinuity is a fraction-of-a-femtofarad series element: read a small capacitance through it and you read the port, not the capacitor, whatever the plates do. That is not special to a capacitor — a matched 50 Ω GaAs microstrip reads |S 21 | = 0.07 raw at 10 GHz. (An earlier revision of this page said the plate capacitance was \"not modelled\", on a measurement taken through exactly that raw path — retracted.) And mesh the gap. The one thing that genuinely limits a plate capacitance is the mesh: the cross-level part of the fill degrades as the cell size grows against the plate separation, and the extracted capacitance follows it — within 10% of ε₀ε r A/d while the ratio is at most 5, 1.46× at 12.5, and the wrong sign at 25. The shipped MMIC technology's default mesh sits outside that (a 10 µm plate pair 0.2 µm apart meshes at 2.5 µm, i.e. 12.5), and the run says so in its notes — it is a note rather than a refusal because the rest of the structure is unaffected. Refine the mesh over the plates until the note stops firing before you trust the value. The shunt form's backside via sets an upper frequency. A vertical basis carries uniform current along its whole run, so a via is refused above k·ℓ = 0.3. Through 100 µm of GaAs that ceiling is just under 40 GHz; the refusal says so and names the number it computed. The series form has no such via and no such bound."],[35,"ignored","What the EM engine does not read","Stated plainly, because a field that is carried but unused is worse than one that is absent: Conductor σ is not used by the full-wave planar kernel. It models metal as a perfect conductor; conductor loss is not in the answer. The cross-section (uniform-line) kernel does use it, for its Wheeler-incremental-inductance surface resistance. Conductor thickness is not modelled by the full-wave kernel either — it solves a zero-thickness sheet. The thickness itself is used by the cross-section kernel and by interchange, and the full-wave kernel gives it to one of the two dielectrics beside the conductor. Which one is normally invisible (3 µm of metal under a 100 µm substrate moves nothing), and matters exactly once: between the two plates of a thin-film capacitor . It is a per-conductor setting — see Where a conductor's thickness goes . A via's fill and wall thickness are carried for thermal work , not read by the RF solve: a plated wall a few µm thick is many skin depths at RF, so plated and solid behave the same."],[35,"check","Checking what it resolved to","Every EM run says which conductor it used as ground, and at what height , in its notes — along with the level list, the slab it built, and any via it ignored or built for you. That report is the authoritative answer to \"where is my port's negative terminal\", and it is written by the code that actually did it rather than re-derived for display. If you are chasing a result that looks plausible but wrong, read those notes before anything else: almost every stackup mistake produces a complete, believable answer for a structure you did not draw. See also: The MoM engine for the ports themselves, EM Setup for the panel that runs them, and The Layout Editor for the rest of the technology file."],[36,"","","The planar method-of-moments solver: what it does, what it will not do, and how it works."],[36,"plain","In plain terms","A circuit model of a microstrip bend is a formula: someone measured a family of bends, fitted a closed-form expression, and published its validity range. It is fast, it is accurate inside that range, and it knows nothing about your bend — not that there is a via 200 µm away, not that the ground plane has a slot under it, not that the adjacent trace couples into it. An electromagnetic solve computes the fields for the artwork you actually drew. It takes your layout, your stackup and your frequency range, and returns S-parameters. No formula, no validity range, no family of measured parts — just Maxwell's equations discretised over your geometry. What that buys you, concretely: Discontinuities you have no model for — a bend into a taper into a pad, a coupled section with a via in the middle, a matching stub whose neighbour matters. Coupling you did not intend. A circuit model connects what you wired; an EM solve reports what the metal actually does, including the parts you would rather it did not. A number you can hand to a harmonic-balance run. An EM result is a Touchstone file, and a Touchstone file drops into a test bench beside the real device model. That is the workflow that makes this worth having rather than a curiosity — see Co-simulation . It is not free. A circuit model answers in microseconds; a full-wave solve of a small structure takes seconds per frequency point. Use the model where the model is valid, and the solver where it is not."],[36,"can-cannot","What can and cannot be simulated","This is a planar (2.5D) solver . It solves conductors embedded in a laterally infinite, vertically stratified medium: metal is horizontal and thin, current flows in-plane, and z-directed current flows through vias. That is the same class of tool as the commercial planar solvers, and it is not FEM and not general 3D."],[36,"can","Can","Planar conductors on a layered substrate — any number of dielectric layers, with real ε r and tanδ. (Dielectric loss, in both kernels. Conductor loss is the quasi-static kernel only — see below.) Arbitrary planar shapes : lines, bends, tapers, stubs, spirals, pads, coupled sections. Multiple metal levels , with vias carrying z-directed current between them. A via is either a placed via primitive or a drawn region — a rectangle or polygon on the via entry's drawing layer, which is how a plate-sized connection is expressed. See Vias . A ground plane at the bottom of the stack, or an open boundary. Discontinuities and the coupling between them — which is the entire point. Frequency sweeps , with adaptive sampling so a resonant response does not need a fine uniform grid. Radiation. The top of the stack is an open half-space and the radiation condition is exact — no airbox, no absorbing boundary — so an antenna is an ordinary run with one checkbox added. Far-field pattern, directivity, gain, radiation efficiency, the itemised power budget, beamwidth per plane and polarization all come out of the same solve. See Antennas , which also carries the limits that bound every one of those numbers."],[36,"cannot","Cannot","Read this list before you trust a result A user who discovers a limit by getting a wrong answer has been failed by the documentation. Anything genuinely three-dimensional. A wirebond arcing through air is not a planar structure. Use wBond , which is a separate 3D kernel built for exactly that. Finite substrate extent. The dielectric layers are laterally infinite . A board edge, a cavity wall, a shielding can — none of these exist to this solver. A finite ground plane. The plane is laterally infinite too, so there is no field behind it at all: no back radiation, no front-to-back ratio (the metric is present and refused by name), and a directivity that reads optimistic against a real board, whose plane is finite and puts substantial power behind it. A run that can see your pour reports how large it is in wavelengths — the example antenna's is 0.78 λ₀ across, the measured board that prompted this work 0.41 λ₀ — but the analysis still terminates on the infinite plane. See Antennas . Apertures in the ground plane. A slot in the plane is not representable, so a slot antenna, a CPW-fed slot and an aperture-coupled patch are out. Edge-fed, inset-fed and coupled-patch feeds are in — see Antennas for which feed to use instead. A dielectric that stops somewhere. This is the limit most often mis-remembered, because circuitRF does model drawn dielectric artwork — a thin-film (MIM) capacitor's patterned film, tied to its plate. That mechanism decides whether a layer is in the run, never where it stops : inside a run every dielectric is laterally infinite. So a radome, a conformal coating or a gain-raising superstrate drawn over the patch alone is modelled as covering the whole run, to infinity — which moves resonance, gain and surface-wave launch. A uniform cover layer is a fair model of a real one and is supported; a patterned one is not the thing you drew. Enclosures and absorbing boundaries. There is no box. Real planar tools suppress port-to-port radiative and surface-wave coupling with box walls or absorbing boundaries; this kernel has neither, by design , and that is what sets the de-embedding accuracy floor . Vertical conductors other than vias. A via is a z-directed current path through the stack. An edge-plated wall, a connector barrel, a heatsink is not. Non-planar dielectrics. A partially milled cavity, a moulded package body, a coating that follows the metal — the medium is stratified, so a dielectric is a slab spanning the whole plane or it is not representable. Magnetic materials beyond a scalar µ r per layer. No ferrites, no anisotropy. Non-linearity of any kind. This solves a linear problem and returns S-parameters. A plate capacitance read off a mesh that does not resolve the plate gap. A thin-film (MIM) capacitor's network, feeds and vias are solved normally, and since a port on upper metal de-embeds the capacitance itself is in the de-embedded answer — to within 10% of ε₀ε r A/d while the mesh cell is at most five times the plate separation, 1.46× at 12.5 and the wrong sign at 25. The shipped MMIC technology's default mesh sits outside that and the run says so in a note. Two things that are no longer limits, because they were fixed: the plate sheets used to be separated by the capacitor dielectric plus the lower plate's own metal thickness , and a port on upper metal used to be refused. See A thin-film (MIM) capacitor . Conductor loss, in the full-wave kernel. Its metal is a perfect conductor : the conductivity and thickness you set in the stackup are carried but not used in the field solve. Dielectric loss (tanδ) is modelled, and it dominates on ordinary substrates — measured on 1.6 mm FR-4, the missing conductor term is 6.5% / 3.0% / 2.1% of the total conducted loss at 2 / 10 / 20 GHz. So a full-wave insertion loss reads slightly optimistic, by a known and shrinking amount. The quasi-static kernel does model conductor loss, through Wheeler's incremental inductance rule over every lossy surface including the ground plane"],[36,"implementation","For advanced users: how circuitRF implements MoM","Two kernels ship. circuitRF chooses between them from the geometry and tells you which one it picked and why."],[36,"the-quasi-static-kernel-uniform-cross-sections","The quasi-static kernel — uniform cross-sections","When the selected geometry reduces to straight, mutually parallel, constant-width conductors, it is not solved as a field problem at all. Its cross-section is solved for per-unit-length RLGC, and everything else follows in closed form. The unknowns are charge density on boundary segments — free charge on conductor perimeters, bound polarisation charge on dielectric interfaces. Carrying bound charge explicitly is the decision that makes this cheap: the Green's function stays the free-space 2D logarithmic potential , so there are no Sommerfeld integrals and no special functions, and an arbitrary number of dielectrics costs nothing. A ground plane is one image. From there: [C] from the real stackup, with ε* = εr(1 − j·tanδ) carried through → C = Re(C), G = −ω·Im(C) [C₀] from the same geometry with every dielectric replaced by air εeff = C/C₀ [L] = µ₀ε₀[C₀]⁻¹ (the TEM identity) [R] from Wheeler's incremental inductance rule, summed over EVERY lossy surface — including the ground plane γ = √((R+jωL)(G+jωC)) Z_c = √((R+jωL)/(G+jωC)) → ABCD of a length-ℓ line → S [C], [C₀] and ∂L/∂n are frequency-independent. A 1001-point sweep is therefore one matrix solve plus 1001 closed-form evaluations — effectively instantaneous. An optional closed-form Kirschning–Jansen dispersion correction extends the useful frequency range without needing a solver. The one place it can be quietly wrong is that dielectric interfaces are laterally infinite and must be truncated. The truncation distance is a visible setting with a sensible default, and extending it must not move Z₀ — which is a convergence test you can run yourself."],[36,"the-full-wave-planar-kernel","The full-wave planar kernel","The general case. It solves the mixed-potential integral equation over the metal. Green's function. The spatial-domain Green's function for a layered medium requires inverting the spectral form through a Sommerfeld integral — oscillatory, slowly convergent, with branch points and surface-wave poles. circuitRF uses DCIM (the Discrete Complex Image Method): the spectral Green's function is approximated as a sum of complex exponentials by matrix-pencil fitting, and each term inverts in closed form by the Sommerfeld identity. It is validated against direct Sommerfeld integration — a second, independent formulation — over ρ/λ from 10 -4 to 10 on both starter substrates. As a fraction of the free-space kernel at the same ρ, which is what a matrix fill actually experiences, the error is ≤ 6 × 10 -3 across that span ; strict relative error is ≤ 10 -2 out to ρ/λ ≈ 1, beyond which the fit refuses rather than extrapolating. Basis functions and mesh. Rectangular rooftop basis functions on a tensor-product grid . Diagonals and curves are staircased by default, or cut conformally — see Conformal boundary cells . The unknown count is basis functions, not cells: a rooftop spans a pair of adjacent cells. Edge mesh. Current density has a 1/√d singularity at a conductor edge, and a uniform mesh under-resolves it badly — which shows up directly as wrong loss and wrong Z₀. So every conductor edge gets 2–4 geometrically graded cells, the outermost a small fraction of the conductor width and growing inward by a ratio of about 1.5–2. The reference length for that grading is the conductor width , and that choice is measured rather than assumed: on the FR-4 reference structure it lands 0.18% from the converged limit at N = 552, where the alternative reference needs N = 7,562 for a comparable answer. The fill. With a rectangular mesh and source and observer in one plane, the inner integral is closed form — six of them, checked against adaptive quadrature to 10 -12 . That is why the classic \"nearly touching cells\" problem is not where the difficulty lives here: only the outer integral is numerical, and it sees a continuous function with a kink, which is a quadrature-order question. Against an ε r = 1 reduction where the kernel is exact, the assembled matrix is right to 5.0 × 10 -6 . The solve. A dense complex factorisation per frequency, or — optionally — an iterative solve against a grid-accelerated matrix–vector product. The dense one exploits the fact that the matrix is symmetric: it factors it in place , in half the arithmetic a general solver would use, across every core your core-count setting allows. The accelerated path computes the same answer a different way, to its own accuracy gates; its win is working-set memory , roughly 4× less past about 900 unknowns, while the time crossover is much later, around 3,700 unknowns. Below that the dense path is faster. It does raise the unknown ceiling — from 5,000 to 12,000 — but only on a single-metal-level structure with no vias , which is the only case it can accelerate at all. De-embedding. A two-line calibration; see De-embedding ."],[36,"which-kernel-ran-and-why","Which kernel ran, and why","The EM Setup panel names it, with the reason. The quasi-static kernel is chosen when the geometry reduces to a uniform cross-section — and when it does, the panel shows you the cross-section it extracted: \"uniform 2-conductor cross-section · W = 2.9 mm · ℓ = 20 mm\" . If it does not reduce, the refusal is specific: \"this geometry has a bend at (x, y)\" , not a vague failure."],[36,"ports","Ports",""],[36,"what-a-port-is","What a port is","A port is where power enters or leaves the structure. In this engine every port is the same thing: a voltage source impressed across a cut in the metal , driving the current that crosses that cut. The solve excites one port at a time to build the S-matrix. The two port types differ in where the cut is — at the end of a conductor, or in the middle of one — and everything else about them follows from that one difference."],[36,"how-you-define-one","How you define one","A port is a label, not a new kind of shape. The Port tool places an ordinary layout label with its port flag set, which is why a layout carrying ports is still just a layout. Numbering comes from the label's own text. 1 , P1 , p2 , #3 , Port 4 all parse. A label that names no number is auto-numbered to the lowest free one rather than refused. Two labels naming the same number is a refusal by name , not a silent win for one of them. The direction is inferred from geometry where it can be, reported, and refused when it cannot. A label at the exact corner of a conductor is equally close to two edges — and guessing reverses the direction of current into the structure, which is a hard π in S₂₁: smooth, plausible, and completely invisible in a magnitude plot. So it is named and refused: \"Port 1 is ambiguous… Move the label.\" Every resolved port reports the cut it landed on and which way current flows in. Read that report. The type and the reference impedance live in the EM setup , per port, not on the shape. A layout is geometry; how you drive it is an analysis setting. The same artwork can be an edge-driven filter in one setup and a structure with a gap in the middle in another, without editing the drawing."],[36,"the-three-port-types","The three port types","Type Where the cut is Is it de-embedded? Use it for Edge port (the default) One mesh cell in from a conductor's end face , referenced to the ground plane Yes — the two-line calibration removes the port discontinuity, so what you get back is your structure's own response Anything power flows into or out of : a line, a bend, a filter, a matching network, a coupler — every structure you would measure on a fixture or a probe station Internal delta-gap port An interior cut of a conductor, with metal on both sides, at the mesh gridline nearest where you put the label No, and it cannot be — there is no feed outside the cut to remove. Its S-parameters are reported at the gap, in the reference impedance you set A series element embedded in the metal (a series R, L or C you will attach in the schematic), or a device terminal in the middle of a structure Internal port Between the metal and the ground plane , at the point you put the label. It does not cut the trace No, and it cannot be , for the same reason. Its S-parameters are reported there, in the reference impedance you set Anything that returns to ground : a bypass capacitor, a grounded resistor, a device terminal that returns to ground The decision rule is two questions. Does the power cross the boundary of your drawn metal? If it comes in from a connector, a probe, or the next block along — it is an edge port , on that boundary. If not: does your component go IN LINE with the trace, or DOWN TO GROUND? In line means breaking the conductor and putting something across the break — an internal delta-gap port . Down to ground means an internal port , placed on the metal at the point that goes to ground. You do not have to draw the via An internal port's second terminal is the ground plane, so its current has to get there — and in a planar solver the only thing that carries current downward is a via. That via is the solver's job, not yours. Put the port on the metal at the point you want referenced to ground: If you drew a via there, the port drives your via , at the size and shape you drew it. If you did not, one is built for you before meshing, and the run's notes say so and give its size. How big is the built one? A square of your technology's default via drill — 0.305 mm on the PCB starter, 60 µm on the MMIC one. If the technology declares no default via size, a quarter of the substrate height is used instead and the notes say that is what happened. It is deliberately not a mesh cell : that path is real metal and its inductance is part of what the port sees, so sizing it from the mesh would make your answer move when you refine the mesh — which is the one thing refining a mesh must never do. Either way the path is in the answer . If its size matters to you, draw the via and the solver will use yours. Where the negative terminal actually is The ground plane an internal port returns to is not chosen per port: it is the ground-designated conductor in your technology's stackup, and it is the negative terminal of every port in the run — edge ports included. Which conductor that is, and how to change it, is The stackup ."],[36,"what-each-one-looks-like-in-the-layout","What each one looks like in the layout","Edge ports at both ends of a Klopfenstein taper: the bar across each end face is where current crosses into the structure, and the arrow is which way it flows in. A Klopfenstein taper with an edge port on each end — the ordinary two-port setup, and what a correct one looks like. Each port draws a bar across its own end face (where current crosses into the structure) and an arrow along the direction it flows in . A dashed leader ties the label to the bar when the two are not in the same place, so the mark stays readable wherever you put the text. Three things worth reading off this picture: Each bar is the width of the metal at ITS OWN end , not the width of the part. On a taper those are very different numbers, and the bar is how you check the solver agrees with you about which face you named. The labels sit at the centre of each end face , not at a corner. A label at a corner is equally close to two edges, and the run refuses it rather than guessing — because guessing reverses the direction of current into the structure, which is a hard π in S₂₁ and invisible in a magnitude plot. Both arrows point inward , at each other. Current flows into the structure through every port; that is what a port's direction means, on both ends of a two-port. A 50 ohm line with edge ports at both ends and an internal delta-gap port in the middle - where a series component would go. The gap's mark is two bracketed bars facing each other across a break in the metal, with the arrow running through the break: a cut in the conductor, not a boundary of it. A 50 Ω line with the same two edge ports on its ends and an internal delta-gap port in the middle — where a series component would go. Holding the width constant is what makes the comparison readable: the only thing that differs between port 3's mark and ports 1 and 2's is the mark itself. An internal delta-gap port is drawn as a different mark on purpose : two bracketed bars facing each other across a break in the metal, overhanging the conductor top and bottom, with the arrow running through the break. An edge port's mark says a boundary, and which way in ; a gap's says a break, and which way across . At a glance they are not each other. The gap is drawn where you placed the label , because that is where the cut is. An edge port's bar is always snapped to the conductor's end face, however far from it you put the text. The same 50 ohm line with an internal port at its centre - where a component that returns to ground would attach. Its mark is a ring round the point with a ground symbol on it: the port's other terminal is the ground plane, so its current leaves the metal downward rather than crossing a plane in the layout, and the mark claims no direction in the plane. A via is drawn here too, which the port then drives; without one the solver builds that path itself. The same line with an internal port at its centre — where a bypass capacitor or a grounded resistor would attach. (A via is drawn here as well, to show the case where you have one; the port does not need it.) Its mark is deliberately unlike the other two: a plain ring round the point. The reason is that the other two marks are statements about a plane the current crosses in the layout — an edge port's bar across an end face, a gap's brackets either side of a break — and both are oriented by the direction current flows. An internal port has no such direction : its current leaves the metal downward, out of the plane the layout draws. The ring says the port is here , without claiming a direction the port does not have. The ring used to carry a schematic ground symbol hanging below it. It said the other terminal is the plane — which is true of every internal port and never varies, so it was ink on every one of them that told none of them apart, and over dense artwork it read as clutter. The ring alone carries what actually varies. Its polarity is fixed and you do not set it : the + terminal is the metal and the − terminal is the ground"],[36,"how-wide-is-the-gap-really","How wide is the gap, really?","The gap is the mesh's, not the artwork's. The cut is a mesh gridline, and the excitation drives the pair of cells either side of it — so the length of conductor the gap occupies is those two cells, set by Cells per wavelength and the rest of the mesh settings. Nothing you can draw changes it. The drawing follows that, and says which of the two things it is showing: Before you compute a mesh there is nothing to measure, so the break is drawn at a fixed fraction of the port's width — a legible glyph, not a dimension. Do not measure it. Once the mesh is computed the break is drawn at the real thing : the two cells either side of the cut, with the brackets on the mesh's own gridlines so you can read them against the overlay. The same gap once the mesh has been computed: the break is drawn at the width the solve will actually use - the two mesh cells either side of the cut - so it can be read against the gridlines under it. Without a mesh it reverts to a fixed legible width. The mark also moves to the cut. A gap can only fall on a gridline, so if your label sits between two, the brackets go to the gridline that was chosen and a dashed leader runs back to your label — the snap, drawn , rather than only reported in the notes. If you edit the layout, the mesh is dropped and the break reverts to its glyph width. That is deliberate: a stale width left on screen would look exactly like a live one. Refine the mesh and the gap gets shorter , which is what makes it a better approximation to a point discontinuity. There is no other lever. Do not draw a slot in your metal for an internal port The conductor stays continuous and the port cuts it. Drawing a physical gap makes two separate conductors with two end faces — which is a pair of edge ports, and a different structure."],[36,"how-to-choose-in-practice","How to choose, in practice","Two-port and multi-port passive structures — everything from a bend to a Wilkinson — are all edge ports . This is the overwhelmingly common case. A structure you will attach a component to in the middle — a series capacitor breaking a line, a resistor across a gap — gets an internal delta-gap port at the break, plus edge ports wherever power actually enters. Simulate the metal in EM, connect the component in the schematic, and the two meet at the gap. A component that returns to ground — a bypass capacitor, a grounded resistor — gets an internal port at the point it attaches, plus edge ports wherever power enters. Drawing the via is optional; the solver builds one if you have not. An active device embedded in your artwork is the same pattern: an internal port at each terminal you will attach the device model to — a delta gap where the terminal sits in the trace, an internal port where it returns to ground. If power crosses your metal's boundary, use an edge port. The two types report at different planes and give different answers; which one is right is set by where the power actually goes, not by which is cheaper to compute. What an internal port costs you, stated plainly A delta gap is one mesh cell wide , and the cut lands on the nearest mesh gridline to where you put the label — not exactly where you clicked. The run reports how far it moved, and refining the mesh there is what closes both gaps: it puts the cut closer to where you asked, and it makes the gap a better approximation to a point discontinuity. An internal port's cost is the same in a different currency: it drives the cells of the via footprint that the mesh resolved, so a coarse mesh drives a smaller via than the one that is there. The run reports the area it got. Neither has a calibration to fall back on, so the mesh is the only lever for both."],[36,"setting-the-type","Setting the type","EM Setup panel → Ports. Each port gets a row with its reference impedance and a type dropdown — Edge, Internal delta gap or Internal. The rows come from the port labels in your layout, so the port count is the geometry's; the type and the impedance are yours. Two EM setups on one layout may disagree, and that is allowed Because the type belongs to the analysis, nothing stops two .cem files that reference the same layout from calling the same port different things — a gap in the middle of a trace in one, a pair of edge ports in the other. That is a legitimate thing to want. The layout can only draw one of them. It follows the setup you last worked in , and if that takes the marks off a different setup that disagreed, the Messages panel says so and names both. If the marks are not what you expect, that line tells you which setup they belong to. The type only appears for a full-wave planar analysis. Both internal port types are full-wave features, and the uniform-line (quasi-static) kernel has neither — the row shows a reference impedance and nothing else there. That is a property of what the uniform-line kernel is , not a gap in it. It never meshes the plane at all: it solves a cross-section for per-unit-length RLGC and forms the network of a length-ℓ line in closed form, so its ports are the two ends of that line by construction. There is no gridline in the middle to cut, no pair of cells to drive across, and no mesh whose refinement could shrink a gap. (It is the same fact that makes de-embedding a no-op there: the reference planes are the line's ends exactly, so there is no port discontinuity to remove.) If you need an internal port on a uniform line, set Analysis to the full-wave planar kernel explicitly. Do not leave it on Auto for this. A uniform line with a gap or a via on it is still a uniform cross-section as far as the geometry is concerned, so Auto picks the cheaper uniform-line kernel — and that kernel has nowhere to put the port. circuitRF refuses that combination by name rather than running it , because the alternative is a complete, plausible two-port answer for a line without the gap you asked for. The refusal names the remedy: change the analysis, or change the port back to an edge port. Three rules the panel enforces: An internal port must sit on the metal , not just near it. An edge port's label may sit slightly off the end face it names; an internal one cuts the conductor, so it has to be on the conductor. A delta-gap port must state its direction. For an edge port the direction can be inferred from the nearest conductor boundary. A label in the middle of a conductor is roughly equally far from all four edges, so there is nothing to infer from — and the direction is what decides which way positive current crosses the gap. Rotate the port to point the way current should flow across the cut. An internal port states no direction at all : its terminals are the metal and the ground plane, so the polarity is decided by what the port is — + is the metal, − is the plane. Rotating the label does nothing. It does need to be on the metal , because that is where its + terminal is. A gap in the middle of a conductor is a series source: it drives the two halves in antiphase, so a gap at the centre of a symmetric line gives S₁₃ = −S₂₃, not +. That sign is real physics, not a convention you can flip, and it is why the direction is required rather than guessed."],[36,"feed","Auto-ports and the feed extension","You do not have to add a feed line to your artwork. Place a port on the part you drew and press Simulate. This matters more than it sounds. The de-embedding calibration standard is an isolated uniform line of the port's cross-section, and the calibration is only valid if your metal looks like that line for the distance the standard replaces. A taper's flanks are oblique from the first cell, so it does not. So before meshing, each port's own polygon is extruded outward from its drawn end face by however much uniform line the calibration is short of; afterwards the lead is removed exactly, as a matched section in the line's own Z_c, using the propagation constant the calibration already measured. Three properties of that are load-bearing: The reference plane is still your drawn metal edge. The lead moves where the error box is measured, never where the answer is reported. A feed that is already uniform grows nothing , so a structure that never needed this is unchanged bit for bit. Running out of metal counts as uniform — a short line is a short structure, not a flared one. Every case it cannot be sure of is declined, not guessed. An end face that is not a single straight segment, a port whose level is ambiguous, a lead that would run into other metal: all declined, with the warning you would have got anyway. Moving metal you drew would be a worse failure than the one being fixed."],[36,"port-z0","Port Z0","Default 50 Ω, editable per port in the EM Setup panel. It is a renormalisation applied to the result, not a property of the geometry — the solve does not change."],[36,"the-ground-reference-every-ports-negative-terminal","The ground reference — every port's negative terminal","Get this wrong and everything downstream is wrong , and there is no per-port control for it. Every port in a full-wave run returns through the same plane: the stackup's ground. That is the negative terminal of every edge port and of every internal delta gap, and it is not something you set on a port — it comes from the technology , by one rule: the top surface of the highest conductor marked as a ground reference that lies below the signal level . If no conductor is marked, the stackup's bottom boundary condition is used instead, and the run says so. On a stackup with several metal layers this matters and is worth checking. Which levels take part in the analysis is yours to choose (the EM Setup panel's analysis-level checkboxes). Which one is ground is not a port setting — to return through a different conductor, designate that conductor as the ground reference in the technology editor. The run's notes name the plane it resolved, its height, and the signal level it sits below. Two consequences of it being the stackup's plane: It is modelled as laterally infinite. A finite ground pour is not that, and a via to a ground-designated conductor that is not the resolved reference is dropped by name rather than treated as the plane. If you have drawn the pour, the run now measures it and tells you how big it is in wavelengths — its size, its area, and, the number that actually matters, how far it extends beyond your own metal. The pour is still not meshed and reading it changes none of the numbers beside the note; what it buys you is knowing whether \"laterally infinite\" was a fair description of your board before you trust a result that assumed it. On a plane that is a fraction of a wavelength across it is not, and the errors all run the same way: no back radiation at all, and a gain and directivity that read high. Any other reference is refused, not approximated — a coplanar ground, a second signal conductor, a differential pair, or a port driven between two levels at a via. Each is named in the refusal. Coplanar waveguide is the case people expect to work and it does not: its return is the adjacent coplanar conductors, which is a different port model rather than a different layer, and this kernel does not build one."],[36,"deembedding","De-embedding",""],[36,"what-it-does","What it does","A raw port excitation includes the port discontinuity — the local field disturbance where the excitation is applied, which is an artefact of the simulation and not a property of your structure. Reporting those S-parameters as the structure's response is simply wrong. De-embedding removes it. circuitRF uses a two-line calibration : it simulates a short and a longer uniform reference line of the port's cross-section, extracts the port's own reflection and the line's propagation constant, and removes them. This is about edge ports. An internal delta-gap port is not de-embedded. A two-line calibration removes a feed . An interior cut has metal on both sides, so there is no feed outside it — nothing to calibrate against, nothing to remove, and no line impedance to reference the answer to. An internal port's S-parameters are reported at the gap itself, in the reference impedance you set for it , and the run's notes say so whenever one is present. Everything below in this section concerns edge ports."],[36,"where-the-reference-plane-sits","Where the reference plane sits","The reference plane is not user-positionable, and that is a stated limitation It sits one mesh cell in from the drawn metal edge , because that is where the calibration actually removes the port discontinuity. There is deliberately no offset knob: offering one would offer a way to get a different answer for the same structure. The planes are drawn over your layout , from coordinates the engine reports, so their location is never a mystery. The corollary: if you need the reference plane somewhere else, move the drawn metal edge — that is, change where your structure ends — rather than looking for a setting. One more property of the method, worth stating because it is a property and not a gap: the de-embedded S-matrix is referenced to the line's own Z_c, and the calibration cannot determine it. Z_c is recovered from γ and the per-unit-length capacitance, differenced between the two standards so the end effects cancel exactly. The assumption that C is frequency-independent is that route's real cost, and it is measured at 0.4% / 2.3% / 6.3% at 1 / 5 / 20 GHz."],[36,"the-accuracy-limit-is-radiation-not-the-algebra","The accuracy limit is radiation, not the algebra","The calibration algebra is exact : a de-embedded uniform section comes out perfectly matched at the two lengths the calibration was solved from (|S₁₁| = 8.5 × 10 -16 ), and two independent routes to γ agree to between 2.5 × 10 -4 and 3.9 × 10 -3 across 2–10 GHz. What limits the answer is direct radiative and surface-wave coupling between the two ports . It decays only algebraically, and there is no term for it in a \"box + matched line + box\" model. Measured on 1.6 mm FR-4, a section that should be perfectly matched reads: Frequency |S₁₁| of a section that should read zero 2 GHz 3.9 × 10 -4 10 GHz 6.0 × 10 -3 That is an f² scaling — and, importantly, it is not monotone in the standard's length , which is how it was identified as coupling rather than as calibration error. What that means for you, in one line A de-embedded answer here is good to a few parts in 10 3 at 2 GHz and a few parts in 10 2 at 10 GHz , and a longer feed does not improve it . Real planar tools suppress this with box walls or absorbing boundaries; this kernel has neither."],[36,"rules-of-thumb-for-port-setup","Rules of thumb for port setup","All of these follow from one fact: de-embedding accuracy is limited by radiation, so a port whose feed radiates cannot be cleanly de-embedded. Keep ports apart. Port-to-port coupling is the error floor, and it grows as f². If you can place the two ports on opposite ends of the structure rather than on the same side, do. Rule of thumb: at least a few substrate heights of separation, and more at the top of your band. Do not lengthen the feed to fix accuracy. It does not work — the coupling is direct, not through the line — and it costs solve time. This is the counter-intuitive one, and it is measured. Keep the feed's own cross-section uniform for the calibration's run , and keep other metal out of that run. The solver grows the uniform lead it needs, but a lead lengthens a feed, it cannot move a neighbour sideways. Metal running alongside the port inside the calibration's own run is still a limitation and is still warned about. Use a feed width the mesh can resolve. The edge mesh needs several cells across the conductor; a feed narrower than a few cells is under-resolved exactly where the port excitation is applied. Rule of thumb: at least 3–5 cells across the feed width, which is what the default mesh settings give you if you leave them alone. Port the end face , not a corner. A label at a corner is ambiguous and will be refused; a label on a clean straight end face resolves without a guess. None of this applies to an internal delta-gap port , which is not de-embedded at all. Its accuracy is set by the mesh at the cut, not by radiation between feeds. Watch the band, not just the centre. Everything above degrades with frequency. If your structure is fine at 2 GHz and strange at 12 GHz, suspect the port before the geometry."],[36,"what-a-good-and-a-bad-de-embedded-result-look-like","What a good and a bad de-embedded result look like","Good Bad Passivity σ max (S) ≤ 1 at every frequency σ max > 1 — the analysis, not the design Σ|S|² for a low-loss structure Slightly below 1, decreasing smoothly with frequency Above 1, or wandering A section you know is matched |S₁₁| in the 10 -4 –10 -2 range, rising smoothly with f |S₁₁| near 1, or jumping between adjacent frequency points ∠S₂₁ Smooth, monotone, ≈ −βℓ Discontinuous, or with a hard π step (a reversed port side) Refining the mesh Moves the answer a little, and in one direction Moves it a lot, or in different directions each time A non-passive result is reported, not shipped quietly A de-embedded sweep that publishes σ max (S) > 1 says so — at the frequency, and by how much. The excess is the analysis, never the design , and you need to know that before you read the plot. A famous example: a 2000 mil 50 → 12 Ω Klopfenstein taper once came back as |S₁₁| = 1.0000, |S₂₁| = 0.0008, Σ|S|² = 1.06 — a non-passive open circuit — because the calibration standard did not resemble the taper's own flanks. That is what the automatic feed extension exists to prevent, and what the passivity check exists to catch if anything like it ever happens again."],[36,"adaptive","Adaptive frequency sampling",""],[36,"the-problem-it-solves","The problem it solves","Fill is O(N²) and solve is O(N³) per frequency , because the Green's function is frequency-dependent. So a sweep costs the number of points times the cost of a point. Sample finely enough to see every feature and you have paid for that resolution across the whole band; sample coarsely and the sweep is cheap but thin. Adaptive sampling breaks that link: it lets you ask for a fine grid and pay closer to what a coarse one costs , because it solves only where the response is actually doing something."],[36,"how-it-works","How it works","Solve a sparse subset of the requested frequencies. Fit a complex cubic spline to what has been solved. Solve a midpoint and compare it with what the model predicted. Where they disagree, add samples there and refit. Where they agree, stop. The tolerance is agreement to 10 -3 in |S|. Three properties are worth knowing: The published sweep is always exactly the grid you asked for. Adaptive sampling changes which points are solved , never which are reported . Every solved point carries the solver's own result, unchanged. Nothing that was actually solved is replaced by the model. It never adds a frequency you did not ask for. So it cannot rescue a feature that falls between two of your requested points — a resonance narrower than your frequency step is lost with the setting on or off. Resolving a feature is the grid's job; adaptive sampling only makes a fine grid affordable. The one exception is the resonance search , which is off by default and which flags every point it adds."],[36,"what-the-tolerance-trades","What the tolerance trades","Tighter tolerance means more solved points, so more time, and a curve that tracks the true response more closely between the ones you asked for. Looser means fewer solves and a curve that may smooth a feature the solver would have found. Two things to know before turning either knob. The tolerance is a local stopping test, not an error bar : it is checked at the midpoints the refinement probes, and the worst realised difference against a fully-solved sweep can be up to about ten times it. And reducing the number of requested points is usually the wrong move — with adaptive sampling on, how many points get solved is set by the structure rather than by your grid, so a coarser grid mostly costs you resolution without saving much time. On a 20 mm microstrip over 1–20 GHz, asking for 401 points and letting it sample adaptively is faster than asking for 101 and solving them all."],[36,"how-to-tell-it-converged-and-what-to-do-if-it-did-not","How to tell it converged, and what to do if it did not","The run reports how many points it solved out of how many you requested. Two checks: Solve fraction. A response that is smooth on the grid you asked for converges after solving a fraction of it. A run that solved nearly every point is telling you the response is not smooth on that grid — which is information, not a failure, and on a wide band over an electrically long structure it is the normal answer, because the transmission phase alone rotates a long way between neighbouring points. Re-run with the sampling off. If the curve does not move, it converged. This is the direct test and it costs one full sweep; it is worth doing once per new class of structure, not once per run. If it will not settle, the usual cause is a genuine sharp resonance. Narrow the band around it and sweep that region on its own rather than fighting the whole span — or turn on the resonance search , which is built for exactly this case. Adaptive sampling applies to the full-wave kernel only. The quasi-static kernel evaluates in closed form after one solve, so there is nothing to sample adaptively, and the control says so rather than sitting there greyed out with no explanation."],[36,"resonance-search","The resonance search",""],[36,"the-problem-it-solves-1","The problem it solves","Adaptive sampling bisects the grid you gave it . That is what makes every published point yours, and on a high-Q structure it is also what puts the answer out of reach: if the resonance is narrower than your frequency step, there is no point refinement is allowed to look at that is near it. The symptom is a sweep that solves nearly every point you asked for and still reports that it did not converge — all of the cost, none of the saving, and the feature still invisible. You cannot fix that by refining harder. Either the grid gets finer, or something is allowed to look between its points."],[36,"what-it-does-1","What it does","The resonance search is the one setting in circuitRF that lets a sweep publish a frequency you did not ask for. It is off by default. With it on: It looks for Im(Z in ) crossing zero — which costs no solve, because it reads the same model adaptive sampling already built. It brackets each crossing by bisection , solving as it goes, until f₀ is pinned to 1 part in 10 4 . It reports f₀, Q, the resistance at resonance, the half-power bandwidth and the measured −10 dB bandwidth as a diagnostic — so \"where is it\" comes back as a number rather than a curve you have to read off. It then spends what is left of its budget resolving the shape of the curve around each resonance, on the same |ΔS| criterion adaptive sampling uses. Three properties are worth knowing: Every added point is flagged. Your own grid is published exactly as it was — same frequencies, same values — with the found points spliced in between, each marked as found rather than requested. It finds all of them. A patch has higher-order modes; the search reports every resonance in the span, not the first or the strongest. The number of added solves is capped, and the cap is reported when it binds. A full-wave point costs tens of seconds, so this is a budget you are spending deliberately. Q here is the resonator's own Q at that port — radiation and loss, everything inside Z in — not the loaded Q of a matched system. It is the number that sets the bandwidth you can achieve before any matching network is designed."],[36,"what-it-cannot-do","What it cannot do","It needs a sign to bracket. The search finds a resonance your solved points straddle in sign; a resonance whose entire reactance swing falls between two neighbouring solved points leaves nothing for it to notice. When it finds nothing it says so in those words, rather than implying there is nothing there. So the search does not remove the need for a sensible grid — it removes the need for that grid to resolve the resonance. Asking for a few hundred points across the band you care about, with both adaptive sampling and the search on, is the combination this is built for: the grid notices the feature, the search pins it, and adaptive sampling keeps you from paying for the rest of the band. It needs adaptive sampling on, because it seeds itself from the model refinement builds. With adaptive sampling off, the control is disabled and says why."],[36,"conformal","Conformal boundary cells",""],[36,"what-it-is","What it is","The mesh is a rectangular tensor-product grid. Where a conductor's edge is oblique or curved, the grid has to decide what to do with the cells the edge passes through. Two options: Staircase (the default) — a cell is either in the metal or not, so a diagonal becomes a staircase. Conformal — the boundary cells are cut to follow the metal: one straight cut through an otherwise rectangular cell."],[36,"what-it-buys-measured","What it buys, measured","On a 96-sided disc refined from 316 to 3,964 unknowns: Staircase Conformal Does the simulated shape match what you drew? No — between 0.2% and 0.8% of the area is wrong, and the amount changes every time you refine Yes, exactly — to round-off, at every refinement Does refining converge? No — the value wanders up and down Yes — it steps steadily toward a limit Spread over the last three refinements 0.669% 0.279% The first row is the real result. Under a staircase, refining the mesh quietly changes which shape is being simulated, which is why the second row is possible at all."],[36,"what-it-costs","What it costs","Essentially nothing. At matched settings the conformal mesh was slightly smaller than the staircased one (316 unknowns vs 324 coarse; 3,964 vs 3,972 fine), and solve time is set by the unknown count. Building the mesh does a little more geometry work, but meshing is milliseconds against a solve measured in seconds to minutes."],[36,"it-ships-off-and-that-is-not-an-oversight","It ships OFF, and that is not an oversight","Do not read this as a free win Two reasons it is opt-in. The bookkeeping one: every accuracy figure recorded for this engine was taken with the staircase , and anyone reproducing one has to be able to. The real one: a Klopfenstein taper comes out slightly worse under conformal cells at coarse PCB settings — 0.593% area error becomes 0.766%. Making something worse by default is not defensible even when it is better in most cases. Turn it on when you are simulating a bend, a linear taper, a disc, or any curved outline, and you want an answer you can refine toward with confidence. On a bend or a linear taper the area error goes from 0.10–0.47% to exact . Leave it off for all-Manhattan artwork, where it has nothing to do, and for a Klopfenstein taper at coarse settings."],[36,"convex-decomposition-the-reflex-vertex-fallback","Convex decomposition — the reflex-vertex fallback","The cut works by clipping a rectangular cell against the metal outline, and the result has to be a region the fill's integrals can evaluate exactly. Where the clipped region fails that test — most often because the outline bends back on itself inside a single cell — the cell falls back to the staircase rule rather than producing an answer it cannot stand behind. The consequence you can observe: on artwork with many reflex vertices, the number of fallback cells saturates . Measured on a Klopfenstein taper at cells/λ of 20 / 40 / 80 / 160 / 320, the fallback count runs 52 / 78 / 126 / 126 / 126 — and the outline has exactly 126 reflex vertices. Once each reflex vertex owns a cell, refining the mesh cannot reduce the count any further. That is why \"exact\" holds for a bend, a linear taper and a disc but not for a Klopfenstein taper , and why refining does not fix it. It is a property of the artwork, not of the settings."],[36,"mesh-convergence","Mesh convergence, and how to check it","A mesh is a discretisation, so an EM answer is only as good as its mesh, and the only honest test is refinement . The procedure: Run at the default mesh. Note the unknown count and the answer at a frequency you care about. Raise Cells per wavelength by roughly 1.4× and re-run. Repeat until the answer stops moving by more than you care about. What to look for: A monotone, decreasing step. Each refinement should move the answer less than the last, in the same direction. That is convergence. A wandering value is not converging , and on curved or oblique artwork it usually means the staircase is changing the shape at each refinement — see Conformal boundary cells . Edge mesh matters more than cell size. If loss or Z₀ is off, refine the edge cells before refining the whole grid; the edge singularity is where the error lives. The Mesh button computes the mesh without solving , so the unknown count, the smallest and largest cell and the truncation extent are all visible before you commit to a run. Use it."],[36,"budget","What makes a run infeasible","The full-wave matrix is dense and complex: N unknowns is N² × 16 bytes. The matrix is most of what a frequency point costs, and the frequency-independent geometry cache is the rest — so the figure the refusal quotes, and the one below, is both together. The factorisation itself is free of charge: it is written into the matrix rather than beside it. N Matrix Held while one frequency solves Character 500 4 MB 5 MB A short line or a bend lives here 2,000 61 MB 84 MB Interactive: under a second per frequency 5,000 381 MB 527 MB The practical ceiling for a lightweight tool 10,000 1.5 GB 2.1 GB Out of scope A de-embedded run holds more again — every calibration standard's geometry cache is live alongside the structure's own. There is a hard ceiling at 5,000 unknowns for the dense solve, the predicted N is shown before you solve, and a mesh above it is refused with a message pointing at the remedies that actually bind. A tool that silently tried to allocate 12 GB would not be lightweight. The accelerated solve raises that ceiling to 12,000 — on a single-metal-level structure with no vias, which is the only kind it can accelerate. A multi-level or via-bearing mesh is refused by name regardless, so the ceiling there is still 5,000, and the refusal names turning the accelerator on as the first remedy whenever doing so would let your mesh through. Two numbers that surprise people, both measured on 1.6 mm FR-4 at 10 GHz: A 4 MB matrix is not a 4 MB sweep. At N = 552, one frequency costs about 1.7 s once the frequency-independent core is built — so a 101-point sweep is about three minutes , not \"instant\". De-embedding multiplies that by about 4.4×. The calibration standards are not small — on that same structure they measure 2.58× the DUT's own unknowns — and they are 78% of the total cost . A de-embedded 101-point sweep of that structure is about 13 minutes. That is the arithmetic behind adaptive sampling being on by default: the per-point cost went up 4.4× and the number of points did not. The accelerated solve option buys memory first and time second: roughly 4× less working set past about 900 unknowns, and a faster frequency point past about 1,100 — its frequency-independent state is built once per structure and the dense factorisation's cost grows as the cube of the unknown count, so the gap widens with size (about 2× at 1,900 unknowns, an order of magnitude at 3,700). Below the crossover it is somewhat slower than the dense solve. It is single-metal-level only, with no vias — and within that, it raises the ceiling from 5,000 unknowns to 12,000. It now covers de-embedding's own reference-impedance step too Working out a de-embedded result's reference impedance needs a separate static calculation on each calibration standard, and a standard reproduces your port's own cross-section — so a wide port's standard can be as large as the whole structure. That step used to stay dense whatever this option said, which meant a run could be refused up front even though the structure's own accelerated solve would have succeeded. It is accelerated as well now, and judged against the same 12,000-unknown ceiling as everything else. If a run is still refused because a calibration standard is too large, the remaining options are a coarser mesh, or turning de-embedding off and reading the raw solve — knowing those S-parameters include the port discontinuity and are for diagnostics only."],[36,"aim","The accelerated solve, and when to use it","Accelerated solve is a checkbox in the EM Setup panel, off by default. It changes how the linear system is solved, not what is solved: the same mixed-potential integral equation, the same mesh, the same basis functions, the same Green's function, checked against the dense path to its own accuracy gates. What changes is that the matrix is never formed."],[36,"what-aim-is","What AIM is","The method is the Adaptive Integral Method (AIM) — the standard grid-based acceleration for method-of-moments problems, and the same family as the pre-corrected FFT and IE-FFT schemes you will find in the literature. The idea in one paragraph. A dense MoM matrix is expensive because every basis function talks to every other one, and the direct solve costs O(N³). But at any useful distance a basis function's field is indistinguishable from that of a small cluster of point sources carrying the same low-order multipole moments. AIM exploits that in three steps: Project. Lay a uniform auxiliary grid over the structure and replace each basis function, for far-field purposes only, by weights on a small block of grid nodes chosen to reproduce the same moments. circuitRF projects three densities per basis — the x̂ current, the ŷ current, and the charge ∇·f — because the operator has two blocks with two different kernels and projecting the current without its divergence would accelerate half of it and quietly leave the rest dense. Convolve. On a uniform grid the interaction is a convolution, so the whole far field is three FFT convolutions per matrix–vector product — two for the vector potential, one for the scalar — rather than an N² sum. Correct. Near pairs — where the multipole approximation is not good enough — are computed exactly , as they are on the dense path, and the grid's approximation of them is subtracted back off. That correction is sparse and O(N). The system is then solved iteratively (GMRES), preconditioned by the near-field block's own sparse LU. No dense matrix is ever formed and nothing dense is ever factored. Two details worth knowing, because they are the reason this works on a real mesh: Your mesh is not the grid. circuitRF's mesher is edge-graded and conformally cut, which is what buys accuracy at low unknown counts — a uniform mesh needs roughly twenty times the unknowns to match it. AIM keeps that mesh untouched; only the auxiliary grid it projects onto is uniform. This is not the \"uniform mesh makes the matrix Toeplitz\" trick, which would cost you the grading. A non-converged solve throws. GMRES has an iteration cap, and reaching it raises an error rather than returning a current distribution that is smooth, plausible and wrong. That is the backstop behind the higher unknown ceiling."],[36,"when-to-turn-it-on","When to turn it on","Turn it on when the structure is large. The switch buys memory first and time second, and both thresholds are measured rather than estimated: Where you are What the accelerator does Below ~900 unknowns Nothing useful. Working sets are megabytes either way. Above ~900 unknowns Roughly 4× less working set than the dense path. Above ~1,100 unknowns Also faster per frequency point , and the gap widens with size: about 2× at 1,900 unknowns, an order of magnitude at 3,700. Above 5,000 unknowns The only way to run at all — the dense ceiling is 5,000 and the accelerated ceiling is 12,000. Concretely, the cases where the answer is yes: The mesh was refused for being past the 5,000-unknown ceiling and the structure is on one metal level. This is the common one, and the refusal names the switch. A long or wide board at a fine mesh — anything where N is in the thousands. Measured on a length-growth ladder to N ≈ 12,900, the accelerator's working set went 53 MB → 188 MB; the dense matrix alone at that size would be 2.3 GB, which is why it is structurally unreachable. A de-embedded run whose calibration standard was refused. A standard reproduces your port's own cross-section, so a wide port's standard can be larger than the DUT. Both the standards' frequency-domain solves and their static capacitance step are accelerated, and are judged against the same 12,000-unknown ceiling. A long frequency sweep on a big structure , where the frequency-independent projection state is built once and reused at every point."],[36,"when-to-leave-it-off","When to leave it off","Small structures. Below about 1,100 unknowns it is 1.3–1.5× slower than the dense solve, and it saves memory you were not short of. A bend, a short line, a single stub — leave it off. This is why off is the default. A multi-level or via-bearing layout. The checkbox is disabled, with the reason stated beside it: the accelerator projects the single-level horizontal basis family, and a via's vertical current needs its own grid kernel per height pairing. Such a mesh is judged against the 5,000-unknown dense ceiling whatever the switch says. A cross-section (quasi-static) setup. There is no dense system to accelerate — that kernel's solve is closed-form per frequency — so the checkbox is disabled there too. When you are checking a result you do not trust. The two paths are separate implementations of the same operator, so running a suspect point both ways is a genuine cross-check. They agree to the accelerator's own gates; a disagreement is worth reporting."],[36,"what-it-does-not-do","What it does not do","It does not change the mesh, the ports, the de-embedding or the physics, so it cannot fix an accuracy problem — a mesh too coarse to resolve a gap is equally wrong on both paths. It does not raise the ceiling for multi-level geometry. And it is not a licence to ask for an arbitrarily fine mesh: the ceiling was set from a length-growth ladder that stayed healthy to N ≈ 12,900, while refining the resolution at a fixed footprint was measured pushing GMRES from 5–8 iterations to 372 by cells/λ = 120 and failing to converge at 140. Growing the structure is well behaved; over-refining a small one is where the iteration count goes."],[36,"refusals","What the engine refuses, and why a refusal is better","A wrong EM answer is smooth, plausible and expensive — it goes into a design, and the board comes back wrong. So this engine refuses rather than extrapolating, and every refusal names the specific feature and says where the capability arrives. The refusals you are most likely to meet: Refusal What to do \"This geometry has a bend at (x, y)\" — from the quasi-static kernel Nothing: the full-wave kernel takes it. This message means the cross-section extractor declined, which is how the two kernels divide the work. The predicted unknown count exceeds the ceiling Coarsen the mesh, or simulate less of the structure. See the budget . \"Port n is ambiguous\" Move the label off the corner onto a clean end face. Two ports naming the same number Renumber one. \"Port n is an internal delta-gap port… not ON any conductor\" An internal port cuts the metal, so it has to be placed on the metal. Move the label onto the conductor, or make it an edge port if you meant the end. \"…an internal delta-gap port with no direction on it\" Rotate the port to point the way current should flow across the cut. There is no nearby conductor end to infer a direction from in the middle of a trace, and the sign is not guessed. \"Port n is an internal port… not ON any conductor\" An internal port's + terminal is the metal, so it has to be on the metal. Move the label onto the conductor. \"…there is nothing to build its path to the ground plane from\" The rare case: no via drawn, no default via size in the technology, and no substrate height to fall back on. Draw a via, or give the technology a default via size. This run has no ground plane Every port's negative terminal is the stackup's ground-designated conductor, so a stackup without one cannot be solved at all. See The stackup . \"…the conductor under it has no interior cut to gap\" The conductor is only one cell long where you put the gap, so there is no pair of adjacent cells to break between. Raise Cells per wavelength, move the port into the middle of a longer run, or make it an edge port. The DCIM fit is outside its validated range The structure is electrically larger than the fitted kernel covers at that frequency. Narrow the band. A via separation the vertical kernel cannot resolve Turn on the direct vertical (via) kernel , which replaces the fitted Green's function with direct numerical integration for that one term, at 15–45% more per frequency point per via span. No stackup An EM run refuses without a technology rather than inventing one — the one place a missing technology is not degraded gracefully. A refusal you can read is worth more than a number you cannot check."],[36,"cosim","Using EM results in a circuit simulation","An EM run produces a Touchstone file. That is the whole co-simulation story, and it needs no new machinery. Lay out the structure — a matching network, a coupler, a bias tee. Set up and run the EM analysis. It writes an .sNp to a predictable path derived from the layout and setup names. Drop an SnP component into your test bench and point it at that file. Run harmonic balance with the real device model beside it."],[36,"putting-a-component-in-the-middle-of-the-metal","Putting a component in the middle of the metal","An internal delta-gap port is how a component gets into the metal rather than beside it. The workflow is the ordinary one with one extra port: Draw the conductor as one continuous piece — do not draw a slot where the component goes. Put edge ports where power enters and leaves, and an internal delta-gap port where the component goes. Three ports, so the EM run writes an .s3p . Drop that .s3p into a schematic and connect the component to the gap's port . The EM result used in a schematic: the .s3p's ports 1 and 2 are the line's ends, and the series capacitor sits on port 3 - the gap - where it acts in series in the metal. Ports 1 and 2 are the line's ends. The 1.2 pF capacitor sits on port 3 — the gap — and it is in series in the metal: everything that gets from port 1 to port 2 goes through it. It looks like it goes to ground. It does not. Port 3's two terminals are the two lips of the cut — neither of them is the ground plane. The schematic draws every port of an N-port against a shared ground because that is how an N-port is written down, not because one lip is grounded. What matters is the constraint, and it is the right one: terminating port 3 with an impedance imposes V₃ = −Z·I₃ on the gap voltage and the current crossing the gap , which is exactly \"put Z into the cut\". Ground here is bookkeeping. For a component that genuinely does return to ground — one that takes current from the trace down to the ground plane — the port you want is an internal port , placed on the metal where it attaches. Same schematic step, different port: there, port 3's − terminal really is the plane."],[36,"putting-a-component-from-the-metal-down-to-ground","Putting a component from the metal down to ground","Same three steps, one port type further: Draw the conductor. (You may draw the via to ground where the component attaches, and the port will use it — but you do not have to; the solver builds that path if it is not there.) Put edge ports where power enters and leaves, and an internal port where the component attaches. Three ports again, so the run writes an .s3p . Connect the component between that port and ground in the schematic. Port 3's + terminal is the metal and its − terminal is the ground plane, so the connection in the schematic means what it looks like this time. The EM run models the whole path down to the plane, including its inductance ; your component model is only the component. Which conductor the plane is comes from the stackup . Two practical notes, for either kind of internal port: Leave port 3's reference impedance at 50 Ω unless you have a reason. It is a reference the answer is expressed in, not a property of the gap; the .sNp header records it and the schematic reads it back, so the two cannot disagree. Use it for anything the gap can carry : a series R, L or C, a measured 2-port of a real part, or a device terminal. The EM run models the artwork; the schematic models the part. Neither approximates the other, which is the whole point of splitting them."],[36,"everything-else-about-co-simulation","Everything else about co-simulation","Three consequences, all good ones: The artefact is inspectable and portable. It is a Touchstone file: plot it, archive it, hand it to a colleague, diff it against a measurement. Re-running is a file update , so the schematic picks up a new result the same way it picks up any changed source. Staleness is detectable. The file's header is stamped with the stackup, the mesh settings, the port definitions and a hash of the geometry — hashed separately, so a warning can say which of the three moved. A stale .sNp sitting beside an edited layout is reported rather than silently wrong. The EM result also carries a diagnostics group alongside S and the per-port Z0 , and the two kernels deliberately do not share its name: the quasi-static kernel's is tline (Z_c, γ, ε_eff, attenuation, R/L/G/C per unit length) and the full-wave kernel's is planar (γ, Z_c, ε_eff, attenuation, C per unit length, and the calibration's own residual and usability flags). A per-unit-length quantity from a 2D quasi-static solve and one back-solved from a de-embedded full-wave S-matrix are different claims. They agree on a uniform line, and they diverge with frequency — which is dispersion, and is a result ."],[36,"worked","Worked example: a microstrip line with a bend","A 50 Ω line on the PCB starter technology, with a right-angle bend in it. The point of the exercise is that the bend is exactly what a circuit model handles badly and an EM solve handles well."],[36,"draw-it","1. Draw it","New layout on the PCB starter technology — 1.6 mm FR-4, ε r 4.4, tanδ 0.02, 1 oz copper, bottom ground. Display unit mil. A 50 Ω line on that stack is W ≈ 2.9 mm (114 mil) . Draw two rectangles on Top Copper: horizontal arm: from (0, 0) to (400 mil, 114 mil) vertical arm: from (286 mil, 0) to (400 mil, 400 mil) They overlap in the corner square, which is what makes it one conductor. Leave the corner square — an unmitred bend is the thing being measured."],[36,"set-the-stackup","2. Set the stackup","Nothing to do. The stackup comes from the technology, and the EM Setup panel shows it back to you: FR-4 1.6 mm εr 4.4 tanδ 0.02 between Top Copper 35 µm and the ground plane. Check the Ground reference row reads the ground plane and not something else."],[36,"place-ports","3. Place ports","Port tool; click the horizontal arm's left end face, then the vertical arm's top end face. Two labels, 1 and 2 . Check the notes: each port should report the edge it resolved to and the direction current flows in. If either says \"ambiguous\", the label is on a corner — move it. Leave both reference impedances at 50 Ω."],[36,"choose-a-mesh","4. Choose a mesh","Leave the mesh on its defaults for the first run and press Mesh — not Simulate. Read the mesh report: Unknowns : expect a few hundred. Well under the ceiling. Smallest / largest cell : the smallest should be a small fraction of 114 mil — that is the edge mesh doing its job. At 10 GHz on FR-4, λ_g ≈ 16.5 mm, so λ_g/20 ≈ 0.8 mm gives roughly 24 cells along a 20 mm run and about 6 across the width, before edge refinement. A few hundred unknowns is the expected answer, and a number wildly different from that means something is wrong with the geometry or the layer mapping."],[36,"set-the-sweep-and-run","5. Set the sweep and run","1 to 10 GHz, 101 points. Leave adaptive sampling on. Press Simulate . Expect roughly a minute or two — most of it in the calibration standards, not the structure. The run reports how many points it actually solved."],[36,"read-the-result","6. Read the result","The result opens in the Data Display . Plot |S₂₁| in dB and |S₁₁| in dB against frequency. What to look for What it means |S₂₁| close to 0 dB at 1 GHz, falling smoothly Ordinary conductor and dielectric loss on FR-4 |S₁₁| low at 1 GHz, rising with frequency The bend's shunt capacitance beginning to matter A smooth ∠S₂₁, roughly −βℓ The electrical length of the two arms Σ|S|² slightly below 1, smooth Passive and lossy, as expected"],[36,"sanity-check-it-against-the-circuit-model","7. Sanity-check it against the circuit model","This is the step people skip and should not. Build the same thing in a schematic from MLIN and MBEND with the same widths, lengths and substrate, and run an S-parameter analysis over the same band. Overlay the two in one Data Display — the EM result is a Touchstone file, so add it as a second data source and put both traces on one plot. They should agree closely at the bottom of the band and separate at the top. If they disagree at 1 GHz, something is wrong with your setup, not with the physics — check, in this order: the line width (is it really 50 Ω on this stack?), the substrate (does the schematic's MLIN carry the same ε r and h?), the ground reference, and the port sides."],[36,"refine-once","8. Refine once","Raise cells per wavelength by 1.4× and re-run. If |S₁₁| at the top of the band moves by less than you care about, you are converged. If it moves a lot, refine again — and if it moves differently each time on a mitred bend, turn on conformal boundary cells ."],[36,"run-it-again-without-the-gui","9. Run it again without the GUI","Once the setup is right, it does not need the window any more. circuitrf em Bend.cem runs the same extraction, the same mesh and the same solve, and writes the same two files to the same place — so a re-extraction after a technology edit is one line in a script, and the schematic that references the Touchstone picks the new result up with no further action. See the command line, EM section . See also: EM Setup — the panel, control by control · The Layout Editor · Running an EM setup headless · wBond (3D bondwires) · The microstrip component family · The Data Display ."],[37,"","","The EM Setup panel, control by control, and where the results land. This page is the panel walkthrough. The engine behind it — what it solves, what it will not solve, how ports and de-embedding work and what the numbers mean — is the MoM engine chapter , and this page links into it rather than repeating it. The EM Setup editor: stackup, ports and the frequency sweep."],[37,"creating","Creating an EM setup","An EM setup is a document of its own, saved as a .cem . It holds everything that affects the answer : which layout, which analysis, the frequency plan, the port impedances, the mesh settings and the solver switches. Nothing that changes a result lives in a transient dialog, so a .cem committed beside a layout reproduces that run later. Two ways in, and they agree: The layout editor's EM button. One gesture from an open layout to its setup. Amp.clay gets Amp.cem , and if that setup already exists it is opened rather than a second one created. File ▸ New ▸ EM Setup… Prompts for a name and defaults the layout reference to the layout you are looking at. Either way the file lands in the workspace's em/ folder, where the project tree lists it. Setups are workspace-scoped — there is no scratch EM setup. The defaults are meant to be run, not configured. A fresh setup arrives at 50 Ω on every port, a 1–20 GHz 101-point linear sweep, Auto analysis, and mesh settings that are the engine's own. On a small structure the only thing you have to supply is which layout."],[37,"header","The header: name, output file, layout","Name — the setup's display name, taken from the file stem. It is also what names the written result, so two setups over one layout do not collide. Output file — where the S-parameters are written. Leave it blank and you get the layout's own name with an .sNp suffix, in the workspace's results/ folder. A relative path is taken from results/ ; an absolute path is used as given. The .sNp suffix is added from the port count , so you do not type it: the same structure re-run with a coupled pair goes from .s2p to .s4p on its own. The … button browses; a path inside the workspace is stored relative so the workspace stays portable. Layout — the .clay this setup analyses, and the … button re-points it. Re-pointing re-derives the cross-section, the ports and the mesh against the new artwork, and it is undoable. The reference is by path, resolved when the panel refreshes and again when it runs. If the layout is moved or deleted the panel says so in place of the extraction, rather than failing at Simulate time."],[37,"toolbar","The toolbar: Mesh, Simulate, Cancel, Save","Button What it does Mesh Meshes and stops. No solve. This is the cheap \"is my mesh sane?\" answer — press it as often as you like. Simulate Meshes and solves the whole frequency plan, writing the result files. Cancel Replaces whichever of the two is running. It stops at the next work boundary — a grid row when meshing, a frequency point when solving — so a full-wave run can keep going for tens of seconds after you press it. The button reads Cancelling… until it actually stops, and a cancelled run writes nothing. Undo / Redo Every setting in this panel is undoable, including Change Layout. Save / Save As… Save As… writes the setup to a different .cem and the editor then follows the new file; the original is left as it was on disk. Ctrl+S ( Cmd+S on macOS) saves, docked or torn off. Press Mesh before Simulate on anything unfamiliar. The mesh report tells you the unknown count, and the unknown count is what decides whether the run takes seconds or is refused — see what makes a run infeasible ."],[37,"analysis","Analysis","The same panel with a layout resolved: the kernel the registry chose and why, the resolved ports, the mesh report and the technology's stackup. Analysis chooses which kernel you are asking for: Choice Meaning Auto (default) Picks from the geometry, preferring the faster kernel whenever it applies, and always says which it picked and why. Cross-section For a straight, constant-width line: solves its cross-section for Z₀, ε_eff, loss and delay. Exact for that geometry and effectively instant, because the answer does not change with frequency. Planar For arbitrary artwork: bends, stubs, gaps, coupled structures and multi-level metal with vias. It sees discontinuities, coupling and radiation — and costs a full solve at every frequency. Underneath the selector, in bold, is the kernel that was actually chosen , with the registry's own one-line reason. That distinction matters: Auto is a request , and the line below is the outcome . It updates as you type, not when you press Simulate, so a setup that is about to take the slow path says so while you can still do something about it. Asking for a kernel the geometry does not support is refused with the reason, rather than silently demoted to the other one. Cross-section shows a readback — the propagation axis, the signal layer and the ground reference the extractor resolved, plus a one-line summary of the conductors it found. Read it. It is the cheapest possible check that the tool is looking at the structure you think it is. Notes collects everything the extraction had to say that is not an error: a shape that was merged, a generator-produced instance that was flattened, an inferred port direction. Notes are not warnings, but they are the first place to look when a result surprises you."],[37,"conductors","Conductors","Signal conductor names which stackup conductor layer the cross-section analysis is about. Leave it at infer from the drawn geometry unless the layout carries metal on more than one level and you mean a particular one. For the full-wave kernel there is a second, separate control: an expander headed \"n of m included\" listing the analysis levels — which conductor levels the solve meshes, bottom to top. Leave every box unchecked and the solve includes every level that carries artwork, which is what you want almost always. Check a subset when you deliberately want a single-level answer out of a multi-level board — it is a smaller, faster problem, and it is a different problem. The two controls are not the same thing and deliberately do not share a control: one picks the single conductor a uniform cross-section is about, the other picks a set of levels for a 3D-stacked solve."],[37,"frequency","Frequency","The same sweep editor the analyses use: Start , Stop , and either a Step or a Points count, linear or log. Each field takes a coefficient and a unit from the combo beside it, and prints the resolved value underneath — so 1 + GHz reads back as 1 GHz and there is no ambiguity about what was entered. See Units for what the text boxes accept. Default: 1 to 20 GHz, 101 points, linear. For the cross-section kernel the sweep is nearly free — the quasi-static answer does not change with frequency, and the sweep exists so that the written Touchstone has the frequencies your test bench wants. For the full-wave kernel every point is a solve , and the sweep is the single biggest driver of run time. Which is why: Adaptive sampling (on by default, full-wave only) solves a subset of the requested frequencies and models the rest, refining until a solved midpoint agrees with the model to 1e-3 in |S|. The published sweep is always exactly the grid you asked for , and every solved point carries the solver's own result unchanged — the interpolant fills in between them, it never overwrites them. Turn it off to solve every point. The full mechanism, and how to tell it converged, is in the engine chapter . When adaptive sampling is unavailable the checkbox is disabled and says why in place of a tooltip."],[37,"ports","Ports","Hover the Ports header for the explanation that matches the chosen kernel, because the two answer \"where is the port?\" completely differently: Full-wave. Each port is a port label in the layout — place them with the layout editor's Port tool. Which cut of a conductor a label names is inferred from the geometry and reported in the notes; an ambiguous one is refused rather than guessed. Cross-section. There is no meshed port at all. The ports are the ends of the extracted conductors by construction: port 2k−1 is conductor k's near end and port 2k its far end, so two conductors give four ports. There is nothing to place, and de-embedding is a no-op. Port Z₀. Two fields for the near-end and far-end defaults, both 50 Ω. A complex reference impedance is accepted — type it as an expression, and a value that will not parse is reported under the field rather than silently ignored. Beyond that, the panel shows a per-port list — one row per port in the engine's own order, each independently overridable. The list appears for every full-wave setup , and for a cross-section setup once it resolves to more than two ports. Ports may sit on different conductors; that is what a coupled or multi-port structure is. Port type (full-wave only) . Each row in the per-port list carries a type as well as an impedance: Edge (the default) — the port is at a conductor's end face, and it is de-embedded. This is the right answer for anything power flows into or out of. Internal delta gap — the port is a cut across the middle of a conductor, with metal on both sides, for a lumped element or a device terminal embedded in the metal. It is not de-embedded, because there is no feed outside the cut to remove; its S-parameters are reported at the gap in the reference impedance set beside it. Internal — the port is between the metal and the ground plane , at the point you put the label, for a component or a device terminal that returns to ground. It does not cut the trace, it is not de-embedded for the same reason, and its polarity is not yours to set: + is the metal, − is the plane. You do not have to draw the via down to the plane — the solver builds that path and reports its size, and uses a via you drew when there is one. Which conductor the plane actually is comes from The stackup . Changing a type clears the mesh report , because it changes which cells the excitation drives — the old report is about a different excitation. Changing an impedance does not; that is a renormalisation applied to the answer. The cross-section kernel offers no type. Its ports are the two ends of a uniform line by construction, so an interior gap would mean nothing there. Which type to use, what an internal port costs you, auto-ports, and how the reference impedance interacts with de-embedding are in the engine chapter: Ports ."],[37,"mesh","Mesh — the uniform-line kernel","This group appears for the cross-section kernel. It meshes a 2D cross-section, so its knobs are about the width of a conductor and how far out the truncation goes. Setting Default What it does Min cells across width 6 The floor on how many cells span the narrowest conductor. Raise it when the answer still moves as you refine. Edge cells 3 How many refined cells are placed at each conductor edge, where the current density is singular. Edge fraction of width 0.03 The size of the first edge cell, as a fraction of the reference width. Edge growth ratio 1.7 How fast cells grow away from an edge back to the bulk size. Truncation (substrate heights) 20 How far past the metal the discretised ground/dielectric extends, in substrate heights. Too small truncates the fringing field; too large wastes unknowns. Truncation tail cells 12 How many cells cover that tail. A value that will not parse, or is out of range, is reported under the group and the run is blocked until it is fixed. Mesh report — pressing Mesh fills in the unknown count, the smallest and largest cell, and the truncation half-extent, in the layout's own display units. The unknown count is the number to watch ; everything about cost scales with it."],[37,"surface-mesh","Surface mesh — the full-wave kernel","This group replaces the one above when the planar kernel is chosen. It meshes metal surfaces, so its knobs are about wavelength and edges. Setting Default What it does Cells per wavelength 20 The cell-size cap, at the mesh frequency. The usual accuracy/cost dial: 20 is a sane default, 30 is a refinement check, below ~10 is not a serious answer. Edge mesh on Refine at conductor edges, where current crowds. Turning it off is faster and worse; it is there so you can measure what it buys. Edge cells 3 How many refined cells sit at each edge when edge meshing is on. Boundary cells Staircase How curved and oblique edges are treated. Staircase approximates them on the rectangular grid. Conformal cuts the boundary cells to follow the metal, which is more accurate on tapers, bends and curves — and is not a free win. Read conformal boundary cells before turning it on; it ships off because it regresses on one class of board. Mesh frequency blank The frequency the cell-size cap is sized at. Blank uses the top of the sweep. Sizing lower gives fewer unknowns and a faster run, at the cost of resolution at the top of the band — the notes under the group say what the trade actually is for your geometry. Below the fields sits the mesh summary — unknown count, cells across the narrowest conductor, and the mesher's own verdict on whether that is enough — plus the mesh notes. This is the readout that tells you a run is about to be too big before you start it."],[37,"solver","Solver options","Four switches, all of which either change how the same answer is computed or trade time for memory. None of them changes what is being solved. Vertical (via) kernel — \"Integrate G_A^zz directly\" (off by default) . Replaces the fitted Green's function with direct numerical integration for the one term that couples vias to each other. It costs roughly 15–45% more per frequency point per via span . Turn it on when a run is refused for its via separation — the refusal names this switch. Accelerated solve — \"Use the AIM accelerator\" (off by default) . Solves the same system iteratively against a grid-accelerated matrix–vector product instead of forming and factoring the full dense matrix, to its own accuracy gates. What the accelerator is actually for The win is working-set memory — roughly 4× less past about 900 unknowns. The time crossover is much later, around 3,700 unknowns; below that the dense path is faster. It does not raise the unknown ceiling: a mesh past the ceiling is refused before a solver is chosen. Single metal level only, and no vias. Dispersion — Kirschning–Jansen correction (on by default) . The quasi-static cross-section holds ε_eff and Z₀ constant with frequency; this correction restores the frequency dependence of a microstrip line. It is on by default because the default sweep runs to 20 GHz, where an uncorrected quasi-static answer is visibly wrong at the top of its own band. It is derived for a single microstrip — one conductor over a ground plane on one substrate — so on any other cross-section the checkbox is disabled and says so. Cores. How many cores the full-wave solver may use at once. Automatic uses the whole machine. Lowering it leaves cores free for other work and makes the run slower; it never changes the answer. Every one of these four disables itself with a stated reason when it does not apply, rather than sitting enabled and doing nothing."],[37,"stackup","Stackup","The bottom group shows the technology the layout resolves to, one row per stackup entry: kind, name, thickness, the electrical properties (ε_r and tanδ for a dielectric, conductivity for metal), and which drawing layers map onto it. The signal conductor and the ground reference are marked. It is shown, not edited. The stackup belongs to the .ctech , which is the one place it can be edited and the one place every layout, PCell and EM setup reads it from. Edit technology… opens it. If the layout has no technology resolved, the panel says so and blocks the run: nothing states how thick the metal is, what is underneath it, or where the ground plane sits, and there is no defensible default for any of those."],[37,"blocked","When Simulate is greyed out","A banner under the toolbar carries the blocking reason — one sentence saying what is unresolved, in the same words the tooltip on the disabled Simulate button gives. The panel computes it as you type, not at run time. The usual causes, in the order the panel checks them: no layout selected, or the reference does not resolve; the layout has no technology; the extractor refused the geometry for the requested kernel; the chosen kernel refused the extracted problem; a port label is ambiguous, or a port could not be resolved (including an internal delta-gap port that is not on the metal, or has no direction on it); the mesh exceeds the run budget. A refusal is a result. The engine declines geometry it cannot solve correctly rather than returning a number that looks plausible; what the engine refuses lists the refusals and what each of them means you should do."],[37,"results","Where the results land","A successful run writes two files into the workspace's results/ folder: The Touchstone .sNp , at the output path above. This is the artefact you point a schematic's SnP component at — that is the whole co-simulation route, and it is described in Using EM results in a circuit simulation . Its header is stamped with the stackup, the mesh settings, the port definitions and a hash of the geometry, so a stale file beside an edited layout is reported , not silently trusted. The .npy dataset , carrying what Touchstone cannot: the per-kernel diagnostics group — Z_c, γ, ε_eff, attenuation and per-unit-length RLGC for the cross-section kernel, and the calibration's own residual and usability flags for the full-wave one. Open it in the Data Display like any other run. One flat results folder results/ is shared with schematic runs and is keyed by name. An EM setup named after a schematic that also writes results will collide with it — both resolve to the same stem. Give the setup its own name, or set the output file explicitly. The .sNp is not placed into a schematic for you. Back-annotation into an SnP component exists in the engine and is exercised by the test suite, but no button in this release invokes it: place the component yourself and point its File parameter at the written path. Re-running updates the file in place, so the schematic picks the new result up the way it picks up any changed source."],[37,"headless","Running the setup without the GUI","A .cem is a complete, self-contained description of a run, which means the panel is only one way to start one. The command line is the other: $ circuitrf em Amp.cem No other arguments. The layout reference resolves against the nearest workspace above the .cem , and the technology against the layout's own workspace — the same two walk-ups the panel does, because it is the same code. The run writes the same two files to the same place as Simulate, so a schematic's SnP reference stays valid across a headless re-run; -o moves the Touchstone if you want it elsewhere. That is what makes re-extraction scriptable: edit the stackup once, then re-run every setup in the workspace and let each schematic pick up its new Touchstone. The verb, its options, the three message lists, the exit codes and a worked example from an empty folder are in The Command Line ."],[37,"overlays","What the layout shows after a run","The layout canvas for the analysed .clay draws three overlays, all of them from the engine's own coordinates rather than re-derived here: The mesh , after Mesh or Simulate. The current-density map for a full-wave run, with its scale and normalisation printed in the panel under Current density . It is a per-cell |J| map — read it for where the current actually goes, not as a calibrated number. The de-embedding reference planes , drawn where the engine reports them. That is worth looking at once per new structure: the plane's exact position is a property of the method and is not user-positionable . Every overlay is dropped the moment the artwork changes. A mesh or a current map drawn over edited geometry looks like it still matches, which is worse than showing nothing."],[38,"","","Patch antennas in the planar solver — the pattern, the numbers that go with it, and the limits that bound both. The planar kernel radiates by construction: the top of the stack is an open half-space and the radiation condition is exact, so there is no airbox, no absorbing boundary and nothing to size. An antenna is an ordinary EM run with one checkbox added. Everything on this page follows from one fact: the ground plane and every dielectric layer are laterally infinite. That is what makes the radiation condition exact, and it is also what the limits are."],[38,"on","Turning it on, and what you get","EM Setup ▸ Radiation pattern ▸ Compute the radiation pattern. Off by default, because it is only meaningful on a radiator. It changes no s-parameter and no mesh cell — the pattern is a post-process of currents the solve has already paid for — so switching it on does not invalidate anything. The one control that turns an ordinary planar run into an antenna run. Off by default, because it is only meaningful on a radiator; it changes no s-parameter and no mesh cell, and it disables itself with a reason when the pattern could not be computed. Headless there is nothing to add: the flag lives in the .cem , so circuitrf em patch.cem produces the same results. The results land in the farfield group of the same result the s-parameters do. Output Axes Unit U freq, θ, φ, port W/sr — radiation intensity Etheta , Ephi freq, θ, φ, port V, r-normalised (r·E with e −jk₀r removed) DirectivityDbi , GainDbi freq, port dBi DirectivityPeakThetaDeg , …PhiDeg freq, port deg — where the peak is RadiationEfficiency freq, port fraction, not dB PowerAccepted , PowerRadiated , PowerSurfaceWave , PowerDielectric , PowerConductor freq, port W BeamwidthDeg freq, cut , port deg — the cut axis carries each plane's φ AxialRatioDb , PolarizationSense freq, θ, φ, port dB, and signed Stokes V CoPolLudwig3Db , CrossPolLudwig3Db freq, θ, φ, port dB Two are present and refused , each with its own sentence in the run's notes: FrontToBackDb (there is no field behind an infinite plane) and RealizedGainDbi (see the numbers )."],[38,"feeds","Which feeds work","Feed Pattern? Why Inset-fed / edge-fed microstrip yes the current stays in the plane Proximity- / gap-coupled patch yes same reason Coaxial probe (an Internal port) no — refused by name the probe's current is z-directed, and this kernel does not radiate vertical current yet. The s-parameters are still right. Slot, CPW-fed slot, aperture-coupled patch no an aperture in the ground plane is not representable at all So: feed a patch from the edge. A probe feed still gives you Z in and the resonance — it is refused for the pattern only, and the run says so."],[38,"setup","Setting a patch up","The shipped 5.8 GHz example: a 16.94 x 13.30 mm inset-fed patch on top copper, the 40 x 40 mm ground pour under it, and one edge port on the feed's end face. The pour is drawn so the run can report how large the real plane is; the ANALYSIS still terminates on a laterally infinite one. Draw the patch and its feed as ordinary metal on one conductor level. Do not draw the ground plane for the solver's benefit — the stackup's ground reference already is the plane. Drawing the pour is still worth doing, because it is the only way the run can report how big your real plane is (see limits ). Put the port on the feed's end face , pointing in. An edge port is de-embedded, so your reference plane ends up at the end of the drawn feed and the solver grows whatever uniform lead the calibration needs. EM Setup ▸ Surface mesh ▸ This metal is → Radiating sheet. A patch is not a line: the current varies on the scale of a wavelength in both directions and there is no \"along\". With Transmission line selected instead, the mesher looks for a current direction, fails to find one on a wide sheet, and declines — leaving the cell size set by the narrowest metal on the board. Boundary cells must be Staircase. The far field does not transform a conformal (cut) cell — a cut cell's metal is not its rectangle — so a conformal mesh refuses the pattern by name. The checkbox disables itself and says so. The example's EM Setup, from the top down to the mesh: the kernel the registry chose and why, the conductor level, the sweep with adaptive sampling and the resonance search on, and the resolved port with its side, its reference plane and its impedance. Edge refinement matters more here, not less The radiating edges set the effective length, which sets the resonant frequency, which sets everything. Leave Edge mesh on. If the cell count is the problem, the Detail floor is the control that fixes it — it stops sub-wavelength import artefacts from sizing the whole grid."],[38,"resonance","Finding the resonance","A patch resonance is narrower than any sweep you would draw across a band, and adaptive sampling cannot rescue it : adaptive sampling bisects the grid you gave it and never adds a frequency you did not ask for. Ten points across a decade will miss a 1 % bandwidth completely, and the run will tell you it did not converge without telling you where the feature is. Two things to do, in order: Sweep the band you expect, not the decade. Start from the cavity-model estimate: f ≈ c / (2(L + 2ΔL)√ε eff ). Turn on EM Setup ▸ Frequency ▸ Resonance search. It is the one setting in circuitRF that lets a sweep publish a frequency you did not ask for: it looks for Im(Z in ) crossing zero, brackets each crossing by bisection, and reports f₀, Q and the bandwidth. Every added point is flagged, and every point of your own grid is published exactly as it was. It needs adaptive sampling on, because it seeds itself from the model refinement builds."],[38,"pattern","Reading the pattern","θ spans 0…90° only, and the axis stops there rather than being padded. With an infinite ground plane the field below the plane is not small — it is identically zero — so half a sphere of structural zeros would read as a measured front-to-back ratio. In the Data Display: A cut is one φ, swept over θ. Plot farfield.U on a Polar plot and set the radial axis to dB : the outer ring is the reference and each ring is a step down. The plot states which reference it is using, so a normalised pattern cannot be mistaken for an absolute one. Normalised means the peak of this trace is the outer ring — right for comparing shapes, useless for comparing two antennas. Absolute pins the outer ring to a dB value you choose, which is what you want when the levels are the point. Two cuts on one polar plot is the comparison worth making: the E-plane (the plane containing the current axis) against the H-plane . They are not the same width, and a beamwidth quoted without its plane is not a number. The 3D surface is for seeing that a pattern is not the shape you assumed, and for the picture that goes in a report. The principal-plane cuts say more."],[38,"numbers","Reading the numbers",""],[38,"which-gain","Which gain","Includes loss Includes mismatch DirectivityDbi no no GainDbi yes no Realized gain yes yes Neither published result is called just \"gain\", because the usual failure is comparing one against a datasheet that quotes the other. Realized gain is refused, and the substitute is exact. The mismatch factor needs the reflection a source would see at the port; what the analysis has where the pattern is taken is the raw admittance of the port's delta-gap excitation, which at a de-embedded edge port is the gap's own parasitic rather than the antenna's input. So compute it yourself from the two published results: realized gain (dBi) = GainDbi + 10·log10(1 − |S11|²)"],[38,"the-loss-itemisation-and-what-to-change-for-each-term","The loss itemisation, and what to change for each term","The run prints a power budget at every pattern point: accepted = radiated + surface wave + dielectric + conductor. Term What to change Radiated this is the output, not a loss Dielectric lower tanδ, or a thicker substrate (the same current radiates more of its power) Surface wave thinner substrate, or lower ε r . Booked as a permanent loss here, because an infinite substrate never gives it back — on a real board it reaches the edge and radiates, usually badly Conductor always exactly zero: the full-wave kernel's metal is a perfect conductor The two corrections do not cancel, so read both. The surface-wave term makes the reported efficiency a lower bound on what a finite board does; the missing copper loss makes it read high . The pattern is also missing the edge-diffracted contribution entirely."],[38,"beamwidth-polarization-cross-pol","Beamwidth, polarization, cross-pol","Beamwidth is per named cut , and the cut is either named by you or derived from the plane containing the peak and the dominant current axis. The φ that was used is on the result's own cut axis. With an infinite ground plane the E-plane reads much broader than a real board measures : the pattern only reaches zero at exact grazing, so the half-power point sits near 78° where a finite board puts it nearer 40°. It is refused outright when the cut has no half-power crossing inside 0…90°. Co- and cross-pol are Ludwig-3 , about a reference azimuth φ₀ that is derived from the dominant current axis unless you name one. It is the same axis the derived beamwidth cut uses. PolarizationSense is IEEE , as the signed, normalised Stokes V : its sign is the sense and its magnitude is how circular the direction is, so a nearly linear direction reads near zero rather than being assigned a handedness it does not have. A principal-plane cross-pol of −45 dB may be −45 dB of mesh Cross-polarization is generated by asymmetry. On a nominally symmetric patch the physical principal-plane cross-pol is very low, so what is reported there is dominated by the mesh's asymmetry — a staircased boundary is not symmetric, and neither is a grid whose lines were placed by an edge attractor at one rim. Read it as a ceiling on what the analysis can resolve: refine the mesh and watch whether the number moves. Cross-pol on the diagonal planes is a different thing — it is physical, and it is ideally non-zero."],[38,"cannot","What this will not tell you","Read this before you trust a number A user who discovers a limit by getting a wrong answer has been failed by the documentation. No back radiation, and no front-to-back ratio. The ground plane is laterally infinite, so the field behind it is identically zero. FrontToBackDb is present and refused rather than printed as a large number. Directivity therefore reads optimistic against a real board, whose finite plane puts substantial power behind it and tilts and ripples the pattern. Drawing the pour does not make the plane finite , but it does make its size reportable : a run that finds artwork on the return plane prints the plane's bounding box, equal-area diameter and — the electrically meaningful one — how far the plane reaches beyond the radiating metal, all in wavelengths at both ends of the sweep. There is deliberately no \"your ground plane is big enough\" verdict, because no measurement here yields a threshold. No apertures in the ground plane. Slot antennas, CPW-fed slots and aperture-coupled patches are out entirely. Feed from the edge instead. No vertical current in the pattern. A probe feed, a monopole, an IFA, a via-fenced patch: the s-parameters are computed, the pattern is refused by name. Edge- and inset-fed structures are in. The metal is a perfect conductor , so radiation efficiency reads high by the copper's share. Surface-wave power is a permanent loss , so efficiency is a lower bound and the pattern is missing what a real board's edge re-radiates. Every dielectric layer is laterally infinite — including one you drew. circuitRF does model patterned dielectric artwork (a thin-film capacitor's film, tied to its plate), and it is reasonable to assume a superstrate drawn over the patch alone is modelled where it is drawn. It is not. That mechanism decides whether a layer is in the run, never where it stops . A radome, a conformal coating or a gain-raising superstrate is modelled as covering the whole run, to infinity — which moves resonance, gain and surface-wave launch by enough to matter. A uniform cover layer is therefore a fair model and is supported ; see the measured example below. A patterned one is not the thing you drew."],[38,"example","Worked example: a 5.8 GHz inset-fed patch","The workspace is testdata/antenna/ in the circuitRF source tree: one inset-fed patch on the shipped PCB 2-Layer RO4350B (30 mil, 1 oz) technology — ε r 3.66, tanδ 0.0037, 762 µm to the ground plane. It is a test as well as an example, so every number below is re-derivable. circuitrf em testdata/antenna/patch/em/patch-5p8GHz.cem The port, close up: the bar across the 1.68 mm feed's end face is where current crosses into the structure, and the arrow is which way it flows in. Everything outside that plane is the port discontinuity, and the two-line calibration removes it. Patch 16.94 × 13.30 mm — W from λ₀/2 · √(2/(ε r +1)), L from the cavity model Feed 1.68 mm microstrip (50 Ω), inset 4.0 mm into a notch with 1.0 mm gaps Ground pour 40 × 40 mm, drawn on the bottom copper so the run can report its size Port one edge port on the feed's end face, 50 Ω, de-embedded Mesh radiating sheet, staircase cells, shipped defaults → N = 1,611 Sweep 5.3–6.3 GHz, 21 points, adaptive sampling + resonance search Run 4.9 minutes on 10 cores, with a pattern at every one of the 21 points"],[38,"what-it-finds","What it finds","The resonance search reports f₀ = 5.8131 GHz , series, Q = 35.1 at R = 38.9 Ω — |S₁₁| = −18.1 dB there, with a −10 dB bandwidth of 91 MHz (1.6 %) . It also finds the parallel resonance at 5.9425 GHz, where |S₁₁| only reaches −3.7 dB: the resonance is real; it is the match that is not there. Both are points the 50 MHz sweep grid could not have shown. The cavity model puts it at 5.7968 GHz — f = c / (2(L + 2ΔL)√ε eff ) with ε eff = 3.402 and ΔL = 0.361 mm — so the solver and an independent analytic reference agree to +0.28 % . That is the comparison to judge the tool by; nothing in it is a circuitRF number checked against another circuitRF number. At 5.85 GHz, the requested grid point nearest resonance: S₁₁ −14.3 dB, Z in = 58.6 + j19.4 Ω Directivity 6.70 dBi, peak at θ = 0° (broadside) Gain 4.66 dBi — efficiency only Realized gain 4.50 dBi — GainDbi + 10·log₁₀(1 − |S₁₁|²), done by hand per above Radiation efficiency 62.6 % E-plane 3 dB beamwidth 146° — and see the infinite-ground caveat before quoting it Ground plane 0.71 λ₀ across at 5.3 GHz rising to 0.84 λ₀ at 6.3, with 0.15–0.18 λ₀ beyond the metal The power budget at that point, in the run's own words: 27.50 µW accepted = 17.22 µW radiated (62.6 %) + 1.93 µW surface wave (7.0 %) + 8.35 µW dielectric (30.4 %) + 0 conductor. Dielectric loss is the term to attack, and the explicit zero is the perfect metal."],[38,"the-mesh-is-not-what-limits-this-example-the-frequency-grid-is","The mesh is not what limits this example — the frequency grid is","The example runs on the shipped default mesh , not a cut-down one. Re-meshed at 30 cells/λ (N = 1,983 against 1,611) the radiation efficiency moves 0.2 pp, Z in moves 2 %, and f₀ moves about 2.5 MHz — 0.04 %. So one convergence check is worth doing and the default passes it. What is coarse is the sweep. 21 points across 1 GHz is a 50 MHz step against a 91 MHz bandwidth, which is why the example leans on the resonance search. For design work, narrow the band once you know where f₀ is — 5.70–5.95 GHz at 5 MHz is 51 points and resolves the notch from the grid alone, at about four times the solve time."],[38,"a-cover-layer-over-the-patch-does-nothing-and-the-run-says-so","A cover layer over the patch does nothing, and the run says so","Adding a uniform 0.5 mm, ε r 3.0 radome to this technology's stackup produces s-parameters bit-identical to the uncovered run at every frequency. The medium is built from the ground plane up to the topmost analysis level and terminated in air there, so a dielectric above the metal is not in the solve. That is measured, not inferred, and the run now warns by name — but read the numbers as the bare board's."],[39,"","","Interactive harmonic load-pull on a single device, at the speed of a mouse drag."],[39,"what","What it is","harmonicaRF answers one question, continuously, while your mouse is moving: If I terminate this device like this at f₀, 2f₀, 3f₀ … on both the source and the load side, what is the current generator actually doing, and what does it cost me in power and efficiency? Every other tool in this space is a post-process: set up a sweep, wait, read a static contour. harmonicaRF runs harmonic balance during the drag , so the relationship between a termination and the loadline is felt rather than inferred. The second thing that makes it different is that the intrinsic plane is the primary view — wherever it is meaningful you are shown the voltage and current of the current generator , not of the package terminals, because that is the plane a designer reasons in when inventing a termination strategy. What it is not , stated plainly so you reach for the right tool: Not a circuit simulator. There is exactly one DUT, two termination planes and an optional linear embedding. Anything more structured is a circuitRF schematic. Not a replacement for the batch load-pull analyses . Those stay the high-point-count, reference-grade path. harmonicaRF trades grid density for interactivity. Not two-tone. Single tone, single frequency. It opens from circuitRF's Tools menu, works with or without a workspace open, and also ships as a standalone binary for people who want the instrument and nothing else."],[39,"active-loadpull","Active load-pull, before you touch the bench","Used deliberately, harmonicaRF is an on-wafer active load-pull simulator . An active load-pull bench synthesises the reflection it presents by injecting a signal back at the DUT, which is what makes harmonic terminations reachable at all on-wafer. It is also a bench on which it is entirely possible to present a device a termination that destroys it — or to walk probes across a wafer for hours collecting points that were never going to be interesting. The practical argument for exploring first, in a tool that runs at frame rate: Decide which terminations are worth measuring before committing measurement time to them. A drag across the Γ plane costs seconds; the same exploration on a bench costs a session. See where the loadline is going before the device does. The loadline plot is live and it is drawn against the device's own DCIV family, so a termination that drives the device into knee or breakdown territory is visible as geometry, not as smoke. Bracket the harmonic terminations first. Which of 2f₀ and 3f₀ actually moves efficiency on this device is a question you can answer in a minute here and then verify at three points on the bench, instead of gridding blind. The device and the probes survive the exploration , because none of it happened on the wafer. None of this replaces the measurement. It decides what the measurement should be."],[39,"circuit","The circuit it solves","One signal path, fixed: ┌─────────── embedding stack ──────────┐ SourceTuner ─ s2p_in ─┐ ┌─ s2p_out ─ LoadTuner ├─ s4p / s6p (1,2 outer · 3,4/5,6 DUT) ┤ └─ lumped package ─ DUT ─ package ────┘ Every element of the embedding is optional and any combination is legal. The cascade order is fixed, outside in: s2p → s4p/s6p → lumped → DUT. s2p_in / s2p_out — a Touchstone two-port at either external port. Port 1 faces the tuner, port 2 faces inward. s4p / s6p — one block embedding the whole DUT. Ports 1,2 face outward; ports 3,4 (or 3,4,5,6 for a 3-port DUT) face the DUT. The lumped package — a fixed-topology extrinsic network with editable values: series Rg, Lg / Rd, Ld / Rs, Ls , and shunt Cpg, Cpd, Cgd_ext . Any value may be zero. It is deliberately not an arbitrary sub-network — an arbitrary network is what circuitRF is for. Terminations are per harmonic band. A marker on a Smith chart is a band's termination: S1 / L1 are the source and load fundamentals and are always present; S2 / L2 , S3 / L3 … are added and removed from the Markers menu. A band with no marker is terminated in a near-short (1e-6 Ω) , which is worth knowing before you conclude that the third harmonic does nothing on your device. DC is not a marker: bias is an ideal choke and DC block, so band 0 is a hard short to the supply. Markers are linked across charts — a marker belongs to the circuit, not to a plot, so moving L2 on the power chart moves it on the efficiency chart in the same frame."],[39,"dut","Supported DUT models","The source is always grounded. A two-port device is used as it is; a three-port device has its source port grounded — so there are only ever two termination planes and two marker families. (The source is grounded at the package plane, which is why a shared source lead Rs / Ls shows up in the source-side intrinsic impedance.) DUT What it is Native FET Any of the five large-signal models — Angelov, Curtice, Curtice cubic, Materka, Statz . Choose the law, then edit the model's own parameters in the strip. SDD Drain-current (and gate-current) equations you type, in the standard expression language . Two-port ( _v1 = Vgs, _v2 = Vds) or three-port, which adds the source terminal against ground as _v3 . Optional Cgs , Cdg , Cds across the device's own terminals, each either linear or a polynomial C(V). External model A compiled Verilog-A .osdi , or a part from a vendor kit through the device worker — see PDK integration . You must name which internal node is the intrinsic gate and drain and which pin is the source: nothing can guess that, so until it is answered the intrinsic glyphs and the loadline stay empty rather than plausibly wrong. Diode A two-terminal built-in, for teaching and for the degenerate cases. Load one with File ▸ Set DUT… , or drag a part in from the circuitRF Library palette when a workspace is open. Refresh DUT re-reads a model that changed on disk. The DUT is embedded in the same s2p / s4p / s6p /lumped stack described above, so a bare die and a packaged part are the same document with a different embedding."],[39,"ui","The interface","harmonicaRF on its default document: power and efficiency contours on the load plane, the loadline, the power sweep, and the readout strip. Four panels and a strip: Two Smith charts, side by side — power on the left, efficiency on the right, both as unfilled iso-lines over the Γ plane. Each chart has its own plane (load or source) and its own harmonic, so you can put 2f₀ next to f₀ and watch both. Iso-lines fade by level, not by position. The highest-level contour — the one bounding the best region — is fully opaque wherever it lands, and lower levels fade out. The top contour is the answer; the rest is context. Iso-line labels are off by default. A dense unfilled contour set reads better without them; turn them on from the Display menu when you need the numbers. Grid points are visible and draggable. A point that could not be solved (unreachable at the requested compression) is drawn hollow — a hole, not a missing point. The efficiency chart plots DE or PAE , per chart, drain efficiency by default. The loadline plot — the device's DCIV family with the time-domain loadline superimposed, live during a drag. One plane toggle moves both curves together between intrinsic and extrinsic, so the two can never be misleadingly superimposed, and a persistent indicator on the panel says which plane you are looking at. The power sweep — gain on the left axis, efficiency on the right, against output power. The X-axis unit cycles when you click it : Pout (dBm) → Pout (W) → available Pin (dBm) → available Pin (W). It carries the operating-point cursor , the drive level at which the intrinsic glyphs, the loadline and the readouts are evaluated — drag it, or snap it to compression. The readout strip , below. The readout strip: settings on the left, then the source and load markers, then the grid's best-power and best-efficiency summaries. The strip is deliberately dense: small text, no section titles, no decoration. Every element has a tooltip, and all of it is selectable text , so any readout can be copied straight out. Left : the settings — bias ( Vgs , or Idq , which solves Vgs for you), Vds , frequency, HB order, the compression level, Z0 , and every parameter the loaded model declares , so periphery or finger count appears exactly when the model actually has it. Middle : the operating point at the cursor — Pout, gain, DE, PAE, Pdc, Zin , AM/PM — and the MXP and MXE summaries from the grid, each carrying its own Zin , because Zin moves with the load on a non-unilateral device and one number for both would be a lie. Right : each marker's Γ and Z, and the Fourier coefficients of Ids and Vds per harmonic, in both planes. Idq and the dynamic mean Id under drive are both shown: they differ, and the difference is worth seeing."],[39,"interaction","Interaction: dragging and editing","The two gestures that matter Markers are dragged. Click a marker on either Smith chart and drag it: the terminations, the contours behind it, the loadline and every readout update while your hand is moving. Configuration is double-clicked. Double-click any settings value in the readout strip to edit it in place. Enter commits, Escape reverts. More precisely: Dragging a marker re-solves the circuit each frame. The contour layer is carried forward frozen during the drag and re-rastered when you let go, so the frame rate stays in your hand rather than in the grid. Dragging a grid point moves that Γ sample. The grid is yours to shape: presets are 3 × 12, 5 × 12 and 7 × 16 rings × spokes, and a whole grid can be imported or exported as a .gam file — which is also the route for a hand-authored or a measured grid. Dragging an intrinsic glyph runs the inverse solve : you state what you want the current generator to see and the tool works out the terminal termination that produces it. It is available only when the inversion is exact — an SDD DUT, linear capacitances, no Cdg feedback, and a package that does not couple the input and output loops. When it is not available the tool says which of those conditions failed instead of silently approximating. Right-click a marker for its context menu: set it as an impedance (R + jX) or as a gamma (polar or rectangular), toggle normalisation, snap it to MXP or MXE, copy Γ, copy Z, or remove it. Double-click in the readout strip edits a setting in place — bias, Vds, frequency, HB order, compression, Z0, and any model parameter. A nonlinear capacitance row is edited from its own right-click menu instead, because it carries a polynomial rather than a value."],[39,"presets","Preset terminations — B, J, J* and F, F⁻¹","Markers ▸ Preset Terminations writes a whole class of load terminations in one gesture: Preset Shortcut What it writes at the intrinsic plane Class B Ctrl+B / ⌘B f₀ at Z0 ; every harmonic above it a near-short. Class J Ctrl+J / ⌘J f₀ at Z0·(1 − j0.5) ; 2f₀ at Z0·j·3π·0.5/8 (reactive); 3f₀ and above a near-short. Class J* Ctrl+Shift+J / ⌘⇧J The complex conjugate of Class J, band by band — same magnitudes, opposite reactance sign. Class F Ctrl+F / ⌘F f₀ at 2·Z0/√3 ; even harmonics shorted, odd harmonics open. Class F⁻¹ Ctrl+Shift+F / ⌘⇧F f₀ at (√2/2)·Z0 / (½ − 8/9π²) ; even harmonics open, odd harmonics shorted — the inverse arrangement. Three things a user needs to know about them: They are intrinsic targets, and Z0 means R_opt. The presets are written against the document's own Z0 , which is what makes them meaningful — set Z0 to the device's optimum resistance ( Display ▸ Set Z0… ) before applying one, or you are asking for a Class-F termination around the wrong load. The tool transforms them out to the terminal plane for you. A \"short\" is Z0/100 and an \"open\" is Z0·100 , not a mathematical zero and infinity. At Z0 = 50 Ω that is 0.5 Ω and 5 kΩ — |Γ| = 0.980 either way, which is a short and an open for every practical purpose, while leaving the solver a well-scaled problem. An eleven-orders-of-magnitude termination makes the contour raster around that band degenerate; this does not. A preset writes only the bands you have markers for. Add L2 and L3 first if you want a Class-F arrangement to have anywhere to put its harmonics. Bands with no marker stay at the unmarked near-short. When to reach for which: B is the reference case and the sanity check. J / J* buy you Class-B efficiency over a wider bandwidth by trading fundamental reactance against a reactive second harmonic — useful when a real matching network cannot hold a short at 2f₀ anyway. F and F⁻¹ are the harmonic-tuned high-efficiency arrangements; which of the two suits a device depends on whether its current or its voltage waveform is the one you can shape, and comparing them here — two keystrokes apart — is exactly the comparison this tool exists to make cheap. The termination values come from: Sharma, T. (2018). Modelling and Design Methodology of Higher-Efficiency Harmonic Tuned Power Amplifiers for 5G Applications (Doctoral thesis, University of Calgary). https://prism.ucalgary.ca/handle/1880/106695"],[39,"menus","The menus","harmonicaRF documents carry their own menu set — notably no Simulate menu , because it is always simulating. On macOS these appear in the system menu bar; elsewhere they are in the window. Menu What is in it File New · Open .charm · Save · Save As · Set DUT… · Refresh DUT · Import/Export .gam · Export Data · Export Testbench… · Close Edit Undo · Redo · Settings… Markers Source Bands · Load Bands (add or remove a band's marker) · Preset Terminations · Add Load Marker ( Ctrl+A ) · Add Source Marker Display Contour plane (load/source) · contour harmonic · efficiency metric (DE/PAE) · loadline plane (intrinsic/extrinsic) · contour levels (5/10/20) · iso-line labels · grid points · Power Sweep… · Set Z0… Grid Grid preset (3 × 12, 5 × 12, 7 × 16) · Reset grid · Import/Export .gam Help This page."],[39,"charm","The .charm document, and getting results out","A harmonicaRF document saves as .charm — the DUT, the embedding, the bias, the terminations and markers, the grid, the settings, and your panel layout. A DUT can be embedded in the document or referenced , so a .charm is either self-contained or tracks a model that keeps changing, whichever you need. Three ways out: Export Data publishes harmonicaRF's own DataSet — every quantity it solved, in the same form every circuitRF analysis produces, so it opens in the Data Display and exports to .npy / .mat like any other run. That is also why Edit Display 's trace picker can plot anything harmonicaRF solved, not a fixed list. Export Testbench… writes a runnable .cnl that reproduces the current state through the ordinary HB path, so any finding here is checkable by the reference engine. (It writes a P1Tone source and a no-tone PnTone load rather than a Tuner pair, because those are the components the HB engine gives a harmonic-band ruler to.) Copy termination set puts a Tuner pair on the clipboard, ready to paste into a schematic where the load-pull engine drives it. Edit Display unlocks the panel layout: add, move, resize and delete plots and readouts, change text size and alignment, and add a trace of anything in the published DataSet . Lock it again and the layout is yours; it is saved in the .charm ."],[40,"","","Bondwire arrays: geometry, inductance, the 3D kernel, and S-parameters out."],[40,"what","What wBond is: a bondwire model","A bond wire, or bondwire, is a thin curved conductor arcing through air over a ground reference. Below a few GHz you can call it 1 nH and move on; above that its inductance depends on the loop you actually bonded, on the wires beside it, and on how far the return path is — and by then it is often the dominant element in your match. wBond models the bondwire geometry you drew. You draw the wires over the pads they land on, group them into arrays, and the component computes each array's inductance, the mutual inductance between arrays, and the capacitance to the plane below — as a circuit element you simulate with, or as a Touchstone file. Everything in the fast path is frequency-domain, quasi-static and closed-form : no meshing, no solver, no Sommerfeld integral. That is what lets it re-solve inside a drag. A 3D method-of-moments kernel sits behind it for when the quasi-static assumptions run out; both are described below. It is a bondwire solver and nothing else — the geometry it knows about is wires, pads and one ground plane."],[40,"schematic","The schematic side: one symbol, one pin pair per array","The schematic symbol the same design generates: one pin pair per array, named after it. A wBond component's symbol is generated from the design it carries . Each array becomes one pin pair named after the array, so the figure above — arrays G1 , G2 , D1 and D2 — has four pin pairs with those names. Rename an array and the pin renames with it. Wires are designed in the layout view, not in the schematic The symbol is the circuit-side handle : it is where the arrays join your netlist and where the component's parameters live. The geometry lives in layout — where the pads are, how high each loop flies, how far apart the wires sit. There is no way to draw a wire on a schematic, and that is deliberate: a bond wire's inductance is a property of its position in space. The REF pin, and why it is not decoration. An array's inductance is a loop inductance, and a loop needs a return path. Two legitimate configurations: Ground plane enabled (the default). The return is the plane; the image terms in the physics are the return path. REF ties to the plane's net. Ground plane disabled. You must nominate one or more arrays as the return — downbonds. The reduction then runs on the remaining arrays against those, and the ground wires get their real inductance and coupling instead of a free perfect plane. The component refuses to stamp when the plane is off and no array is declared as the return. Quoting \"the inductance of this bond wire\" with no stated return path is the single most common way a bondwire model is wrong, so it is refused rather than guessed. With capacitance on the shunt charge has to leave through something: it stamps to REF when that pin is exposed, and to node 0 otherwise."],[40,"layout","Drawing wires: the layout view","Four bond arrays - G1, G2, D1, D2 - and their ten wires, drawn over their pads in the layout editor. In a workspace, a wBond is the wire layer of a layout cell . You get the ordinary layout editor — its technology, its snap, its display unit, its DRC — with wire tools added: draw a wire, rotate one, transform one, and the two dock panels below. The pads are ordinary layout artwork, because that is what they are: a wire flies over a layout, it does not replace it. The wires follow the layout's own snap pitch and display unit . There is one Snap box and one Unit box in that editor and they govern the wires too — see Units . A wire is a polyline : its points are its shape. There is no separate named loop object a wire binds to, so two wires never share a shape by reference and any wire can be reshaped on its own."],[40,"loop-span","Loop height and span","The Wire Profile panel: the same wires from the side, where loop height and span are the two things you can see. The Wire Profile panel is the side view: height against distance along the wire. It is where the two defining numbers are visible, and it carries a plane selector — Auto , XZ , YZ or any angle in degrees — because an array bonded at 37° is ordinary and a foreshortened picture would lie about it. The two definitions, exactly Loop height is the wire's maximum z minus its minimum z. Not the rise above the chord. In chip-and-wire the two feet are usually at different heights — a die pad up to a substrate lead — so the straight line joining them is tilted, and the crest's height above that is smaller. A wire-bonder is set up against the first number: loop height is what an operator measures from the lower pad to the top of the loop. Span is the XY distance between the two feet. There is no z in it anywhere. It is a plan distance, so raising a loop does not change its span. Two consequences worth knowing before you type a number: A wire's loop height can never be below its own foot drop. With the feet |z₁ − z₂| apart, even a dead-straight wire measures that much. A smaller request is clamped to the floor rather than refused (the wire is perfectly drawable) and rather than arched upward to fake the number. Level feet hide both distinctions. If your test case has both feet at the same z, loop height and rise-above-chord coincide and so do span and chord length. The ordinary chip-and-wire case is the one that separates them."],[40,"alt-drag","The alt-drag","Hold alt and drag in the Wire Profile view: it scales LOOP HEIGHT AND SPAN together This is the gesture the whole editor is built around. Without alt , dragging moves a point. With alt held, the drag scales the wire — vertical travel scales the loop height, horizontal travel scales the span, and a diagonal drag does both at once, live, every frame. The details that make it predictable: Both axes, independently, every frame. A diagonal alt-drag really does both; neither axis is guessed from the first few pixels of travel. It scales the whole array group , not the one wire under your hand. A bond group is one loop program on one bonder, and the profile view draws the group as one superimposed shape. The anchor is the foot further from your grab , so the end you are pulling is the end that moves. The result lands on the snap pitch — the span and the loop height are what snap, not the cursor and not the scale factor. That is what makes an alt-drag land on 30 mil instead of 29.87. Uniquely in the application, alt does not suppress the snap here: alt is what selects this gesture, so it cannot also mean \"ignore the grid\". In the layout view, alt-drag scales span only. That view has no z axis for you to have meant anything by, and the drag is projected onto the wire's own chord — so a drag across the wire correctly does nothing."],[40,"arrays","Arrays, and editing a whole group","An array is a named group of wires that share a pair of landing points — a gate bond group, a drain bond group, a set of downbonds. Arrays are what the circuit sees: one pin pair, one terminal current, one inductance. Grouping matters physically, not just cosmetically. The reduction below assumes every wire in an array starts on one pad and ends on another, so the array is what carries the current and the wires inside it share it out among themselves. Editing is array-scoped. The profile view's alt-drag scales every wire in the group; the group-level loop-height, diameter and material commands apply to all of its members; and the properties inspector edits one wire when you want that instead. If you have read an older design note Wires used to bind to a named loop profile that several arrays could share, and editing the profile edited every wire carrying it. That object was removed in favour of per-wire points: a wire's own polyline is now the only truth about its shape. The array is the unit of bulk editing , and that is what this page describes."],[40,"physics","How the inductance is computed","The Array Inductance panel, computed from those ten wires. The Array Inductance panel is the live readout: each array's own inductance, the mutual inductance between every pair, and the frequency it is quoted at. Everything behind it is closed form. The formulas are Grover's Every closed form in this section — the mutual inductance of two filaments in general position, the parallel-filament special case, and the geometric-mean-distance treatment of a round conductor's self inductance — comes from Frederick W. Grover, Inductance Calculations: Working Formulas and Tables . Use the second edition (Dover, which reprints the 1946 Van Nostrand text); it is still in print and still the reference for this whole family of problems. Grover's companion work with Edward B. Rosa, Formulas and Tables for the Calculation of Mutual and Self-Inductance (NBS Bulletin, 1916), is where the same results are derived at length. Nothing here improves on Grover. What circuitRF adds is the bookkeeping around them: the filament decomposition of an arbitrary polyline, the image treatment of the ground plane, the frequency-dependent internal impedance, and the array reduction below. 1. Every wire is a polyline; every polyline is a chain of straight filaments. The mutual inductance between two wires is the double sum of the mutual inductances of their filament pairs, from Grover's two closed forms: Skew filaments in general position — lengths l and m , angle ε between the axes, shortest distance d , offsets μ , ν along their own lines: M = (µ₀/4π) · 2·cos ε · [ T − Ω·d / (2·sin ε) ] T = (µ+l)·atanh( m/(R₁+R₂) ) + (ν+m)·atanh( l/(R₁+R₄) ) − µ·atanh( m/(R₃+R₄) ) − ν·atanh( l/(R₂+R₃) ) Ω = atan2( d²cos ε + (µ+l)(ν+m)sin²ε , d·R₁·sin ε ) − atan2( d²cos ε + (µ+l)·ν·sin²ε , d·R₂·sin ε ) + atan2( d²cos ε + µ·ν·sin²ε , d·R₃·sin ε ) − atan2( d²cos ε + µ(ν+m)·sin²ε , d·R₄·sin ε ) with R₁…R₄ the four end-to-end distances. Note the whole bracket is multiplied by cos ε : M is exactly cos ε times a positive double integral, so it vanishes identically for perpendicular filaments. Parallel filaments — lateral separation d , axial intervals [0, l ] and [ s , s + m ]: M = (µ₀/4π) · [ f(s+m) − f(s) − f(s+m−l) + f(s−l) ], f(z) = z·asinh(z/d) − √(z²+d²) which is exact at ε ≡ 0 and about a third cheaper — and most filament pairs in a real array are near-parallel. 2. Self-inductance is the same formula evaluated against itself , at the conductor's geometric mean distance rather than at zero: GMD = a·e −1/4 = 0.7788 a for uniform current (DC), which reproduces the internal inductance term µ/8π automatically; GMD = a for full skin effect, current on the surface, no internal inductance. The shipped path uses GMD = a for the external inductance and adds L int (f) from the same evaluation that produces R(f), so self and mutual share one code path and the whole frequency dependence lives in one place. 3. The ground plane enters by images. The plane is a perfect conductor at z = 0; every wire is mirrored through it, and L_ij = M( i , j ) − M( i , image(j) ) The image is built by mirroring through z = 0 and reversing traversal direction . That single rule gives the right sign for horizontal and vertical current alike — get it wrong and you get a plausible-looking array inductance that is 10–30% off. As a check, a horizontal wire of length ℓ and radius a at height h reduces to the textbook L = (µ₀ℓ/2π)·ln(2h/a) . 4. Resistance and skin effect come from one dimensionless number. The exact internal impedance of a round wire, normalised to its own DC resistance, depends only on q = a/δ — the radius in skin depths, with δ = √(2/ωµσ) : Z_int(ω) = R_dc · (γa/2) · I₀(γa)/I₁(γa), γ = √(jωµσ), R_dc = 1/(σπa²) [per unit length] The loss the model stamps is the real part of that, taken along every filament: R(f) = ℓ · Re{ Z_int(ω) } ← series loss of a filament of length ℓ L_int(f) = ℓ · Im{ Z_int(ω) } / ω ← the internal inductance that goes with it R/R_dc → 1 + q⁴/48 as q → 0 (DC: current uniform"],[40,"array-basis","The array-basis reduction, derived","This is what turns N wires into M circuit terminals, and it is exact under two stated assumptions. Let L be the N × N wire-basis inductance matrix from above (symmetric, positive definite, images folded in), and A the N × M 0/1 mapping matrix : A[i,k] = 1 iff wire i is in array k , exactly one 1 per row. With V the per-wire voltage drops and I the per-wire currents, V = jω L I Assumption 1 — equipotential bond pads. Every wire in an array runs between the same two pads, so all wires in array k share one voltage drop u_k : V = A u . Assumption 2 — KCL at the pads. The array's terminal current is the sum of its members': J = Aᵀ I . Substituting I = (jω)⁻¹ L ⁻¹ A u into the second gives J = (jω)⁻¹ ( Aᵀ L⁻¹ A ) u , so u = jω L_arr J, L_arr = ( Aᵀ L⁻¹ A )⁻¹ A congruence transform on the inverse inductance matrix, inverted back. Equivalently, with Γ = L ⁻¹, the reduction is the plain block sum Γ arr = Aᵀ Γ A — because A is 0/1, that is literally \"add up the sub-blocks of Γ belonging to each array pair\". Three properties fall out of the algebra rather than out of a tolerance: Symmetric by construction. L symmetric ⇒ L ⁻¹ symmetric ⇒ AᵀL⁻¹A symmetric. Reciprocity is structural. Positive definite , provided L is and every array is non-empty. Resistance never appears. The reduction consumes L and A only, which is what makes L arr frequency-independent and cacheable. It reduces to the classic result: N identical wires with self L_s and mutual M in one array give L_arr = (L_s + (N−1)M)/N . Current sharing comes free , and it is worth looking at: back-substituting gives I = L ⁻¹ A L arr J , from which edge wires carry appreciably more current than centre wires — the classic array current-crowding result — and an undriven array tied together at both ends carries a small circulating current, because it is a shorted turn. Both are real physics the reduction captures with no extra machinery, and the editor renders them as a per-wire colour ramp."],[40,"capacitance","Capacitance, Use Capacitance and ε r","Two parameters matter more than the rest, and they are the two most often left wrong."],[40,"use-capacitance","Use Capacitance","On by default. A bond wire has capacitance — to the plane it flies over and to its neighbours — and above a few GHz that is what turns its terminal inductance into a function of frequency and gives it a self-resonance at all. The electrostatic problem is the dual of the inductance fill : the same filament pairs and the same images, summed against the Coulomb kernel instead of Grover's, one charge basis function per wire: V_i = Σ_j P_ij Q_j , P_ij = 1/(4πε ℓ_i ℓ_j) · Σ_p Σ_q [ K(p,q) − K(p, image(q)) ] Turn it off and the component becomes a pure series impedance whose self-resonance is at infinity. That is the right model for a low-frequency estimate and it is what every design did before this existed — but at 20 GHz it is not a bond wire. Cross-wire capacitance is not a separate switch, deliberately: dropping the cross terms biases a multi-wire array's capacitance high by tens of percent, in the optimistic direction."],[40,"r-the-overmold","ε r — the overmold","Default 1.0, which is air. Set it to the relative permittivity of the plastic the wires are moulded in, and the physics change is exact and simple: a non-magnetic encapsulant leaves L untouched and scales P by 1/ε r , so every capacitance rises by ε r , the self-resonance falls as 1/√ε r , and the effective inductance the panel quotes rises with it. What the single number assumes It fills all space above the ground plane — one homogeneous medium, not a mould cap of finite thickness with air above it. A loop that sits well inside the mould body is described well by this; one whose apex breaks the mould surface is bounded by it, not modelled by it, and the number you get is the pessimistic (high-C) end. The quasi-static assumption also gets stricter as ε r rises, because the wavelength in the medium shortens by √ε r . At ε r = 4 a 1 mm wire is electrically twice as long as it was in air — expect the lumped and distributed models to part company sooner than they do in air."],[40,"limits","What the model does not include","The two assumptions behind the array reduction are good, and they are assumptions. Equipotential pads. A long lead-frame finger, or an array spanning 300 mil of a package lead, has real impedance between the landing points. The reduction then over-couples the array: it forces a voltage equality the structure does not enforce. Nothing warns you , and that is deliberate — there is no threshold that separates the good case from the bad one, because whether a 60 mil landing span matters depends on the lead's sheet resistance, the frequency, and how much of the array's total impedance the pad represents. The array's landing span is shown in the panel as an ordinary readout, where it informs without asserting. Resistance is excluded from the inductance reduction by construction — and that sentence is about ONE panel, not about the model. To be explicit, because it is easy to read the wrong way: R(f) is computed, and it is in the answer. Every wire carries the R(f) of §4, the simulation stamps the exact complex reduction Z arr (ω) = ( AᵀZ (ω)⁻¹ A )⁻¹, and the exported Touchstone S-parameters are lossy accordingly. Nothing is thrown away. What is excluded is the ARRAY-BASIS number the panel quotes. The reduction that turns N wires into M terminals — L arr = ( AᵀL ⁻¹ A )⁻¹ — consumes L and A only, which is exactly what makes L arr frequency-independent and cacheable, and therefore what makes the live readout live. So the Array Inductance panel shows inductance per array and mutual inductance per array pair, and no per-array resistance : there is no such quantity in that reduction. Read R off the S-parameters, or off the per-wire properties. The panel does not report a per-array R, or an R/ωL, and that is not an omission to be fixed — the number it would report does not exist in the reduction it performs. When the split matters to you, look at |S| or at the array's own Z(ω) from the exported Touchstone: the frequency at which the reactance stops dominating is visible there directly. No radiation, no retardation in the quasi-static path. A 100 mil arc is λ/10 at about 11.8 GHz; segmented into filaments it is a distributed ladder good well past that, but the coupling is quasi-static. Proximity effect is not in the shipped resistance. The Bessel term is the isolated wire. Real arrays at 4–8 mil pitch with 1 mil wire sit at s/a ≈ 8–16, where neighbour currents raise R above the isolated value. The mesher warns when any wire pair falls below s/a = 6 . The ground plane is infinite, flat and perfect. A stepped or split ground under the wires is a first-order error — 30–50% on L for a plane split — which is why the reference conductor is not optional."],[40,"kernel","The 3D MoM kernel, and how it solves fast","Behind the closed-form path is a thin-wire method-of-moments kernel — the Harrington/Richmond/NEC formulation, which is the founding problem of computational EM and a very good fit for this geometry. What it solves. One unknown per wire segment: the axial current. Free charge at the segment ends pairs with it (the standard PEEC current/charge pairing). Three matrices: [Lp] — partial inductance, Neumann double line integrals over segment pairs, closed-form for straight filaments; [P] — coefficients of potential, free-space Coulomb kernel; [Z_int] — per-segment internal impedance from the exact round-wire Bessel solution, giving R(f) ∝ √f and internal L(f) with no fitting. Assembled, that is an RLC ladder per wire plus full mutual coupling, solved for the N-port. How it solves fast — four mechanisms, none of them an adjective: Unknowns scale with wire count, not with the air between the wires. The 5 mil ↔ 300 mil pitch range is a distance in a Green's function , not two decades of volume mesh. The thin-wire approximation collapses the cross-section into an analytic kernel. At 40 GHz a 1 mil radius is 0.0034 λ₀ — deeply valid. Nothing has to resolve the circumference, and nothing has to resolve the sub-micron skin depth inside it, because the internal impedance is closed-form. The matrices are frequency-independent in the quasi-static stage. Only [Z_int] and the ω-weighting move with frequency, and both are closed-form per segment — so a 1001-point sweep is one fill plus 1001 cheap evaluations. There is no Sommerfeld integral and no domain truncation. The ground plane is an image; the radiation condition is exact. No airbox, no PML, no \"is my boundary far enough\". Sizing, so you know what you are asking for. Segmentation is driven by geometric fidelity of the arc rather than by wavelength — a faithful loop needs roughly 25–30 segments over a 100 mil arc: Case Wire unknowns Dense complex matrix Factorisation per frequency 8-wire GSGSG array ~250 1 MB instant 40-wire ~1,200 23 MB milliseconds 200-wire ~6,000 576 MB a few seconds Only the 200-wire extreme brushes the engine's unknown ceiling, and the predicted count is reported before the solve rather than discovered at allocation time."],[40,"fem","MoM and FEM, compared honestly","If you already solve bond wires in a 3D FEM tool, here is the fair comparison. Neither table is marketing ; both are the reason to pick one tool over the other for a given job."],[40,"where-this-kernel-wins","Where this kernel wins","Why it matters here Unknowns scale with wire count, not with the air between them The 5 mil ↔ 300 mil pitch range is a distance in a Green's function, not a graded 3D volume mesh The 1 mil radius stops being a meshing problem The thin-wire kernel is analytic; FEM must resolve the circumference and the skin depth inside it The radiation condition is exact No airbox, no PML, no domain truncation — a whole class of setup error disappears Skin loss is closed-form R(f) and internal L(f) from a Bessel expression, better than an impedance boundary on a coarse mesh Re-meshing a loop is re-sampling a polyline Sweeping loop height, pitch or wire count — or Monte-Carlo over real bonder variance — is nearly free The output is already the currency An N-port Touchstone from pad to pad, ready for a test bench"],[40,"where-fem-wins-plainly","Where FEM wins — plainly","Why it hurts Inhomogeneous 3D dielectrics Mould compound, die attach, underfill. FEM assigns ε r per element and moves on; this kernel has one homogeneous medium above the plane Complex 3D metal Lead frames with tie bars, clips, stepped cavities, lids. Surface area to mesh grows until the advantage erodes Dense matrix O(N²) memory and O(N³) per frequency, against FEM's sparse. Wirebond N is small enough that it does not bind — but it is a real asymmetry Field visualisation This gives you currents and S-parameters, not a picture of E inside the mould cap Cavity resonance MoM conditions badly near a cavity mode; FEM is comfortable there Maturity An established 3D FEM tool is the validated reference. A newer kernel is not If your problem is a moulded package with a lead frame and a lid, use FEM. If it is 40 wires over a plane and you want to sweep the loop height, this is the faster and better-conditioned tool."],[40,"sparams","S-parameters out: lumped and distributed","The array network exported to Touchstone and plotted: 0.1-20 GHz, terminal basis. Export Touchstone… writes the array network over a frequency grid you state (a bond array is broadband and has no natural band, so the grid is yours). Two choices in that dialog change what is being computed; the rest are formatting."],[40,"port-basis","Port basis","Basis What you get Per terminal (2 per array) (default) Every terminal is its own port, referenced to the ground plane — which is the file's own common reference node. Four arrays give an 8-port. This is the only basis that can carry the capacitance , because Touchstone's implicit common reference node is the plane the shunt capacitors return to. Per array (differential pair) One port per array, its two terminals as the port's + and −. Compact, and it matches the schematic symbol's port pairs — but a floating pair has no terminal for a shunt to leave by, so a design with capacitance loses it . The written file says so in its header."],[40,"model-lumped-or-distributed","Model — lumped or distributed","Model What it is Cost Lumped (analytic) (default) The array-basis model above: one current and one charge basis function per wire, frequency-independent matrices. This is what the schematic component stamps , so an export in this mode is exactly what your circuit simulates. Effectively instant Distributed (MoM) The thin-wire kernel: one current unknown per segment, so a wire is a transmission line rather than a lumped L with an end capacitance. Publishes on the terminal basis only. Segments per wire: 8 fast / 24 balanced / 48 accurate. One dense complex factorisation per frequency point — a 201-point export of a 40-wire array takes seconds, not milliseconds Which to choose. Lumped, until the wire is electrically long: a 100 mil arc is λ/10 around 12 GHz in air, and sooner in overmould. Above that the distributed model sees what the lumped one cannot — the current varying along the wire — and the two parting company is the answer to \"am I past the lumped regime?\". Compare Distributed Model… runs them side by side on a grid you state, which is the cheapest way to find out where your own geometry crosses over."],[40,"parameters","Parameters","On the placed component: Parameter Default What it does IncludeCapacitance true See above . The one parameter whose default changes the answer for a design authored before it existed. er 1 The overmould's relative permittivity. An ordinary real expression, so it can be swept and optimised. GroundPlane as drawn Enable/disable the reference plane, and its z. With it off you must nominate a return array. RefPin false Exposes the REF terminal. Changes the terminal count, 2M vs 2M+1. Temp as drawn Operating temperature — conductivity, and therefore R(f), depends on it. LoopHeight , Diameter , Material blank The controlling parameters: blank means as drawn . Set one and it drives every wire; array-scoped spellings ( LoopHeight_G1 , Diameter_D2 , …) drive one array. Blank is not emitted at all, so an unset parameter never reaches the engine. Source , File Carried Whether the component carries its design or links to a .wBond on disk. Design , Arrays , SymbolPitch — The carried payload, the array list, and the symbol's pin spacing. Not part of the netlist. Blank means \"as drawn\", and it matters LoopHeight , Diameter and Material ship blank on purpose. A wBond that shipped LoopHeight = 20 mil among its defaults would regenerate every placed instance's wires to 20 mil on its next run — silently rewriting geometry somebody drew. Because they are ordinary expressions, the controlling parameters are exactly what a parametric sweep or an optimiser turns: sweep LoopHeight_G1 and watch the match move."],[40,"files","The .wBond file, and DXF","A design saves as .wBond — the arrays, every wire's points, the materials, the ground plane, the temperature and the view state. A placed component either carries the design (self-contained, shareable as one schematic) or links to a .wBond file (one bond drawing, several designs). Both are supported; the Source parameter says which, and only Update Layout from Schematic flips it."],[40,"dxf-the-bridge-to-the-assembly-house","DXF — the bridge to the assembly house","Wires travel in and out through DXF, because assembly houses, package designers and mechanical CAD all speak it. The layer name is the contract: Layer Wires_<group> — one DXF layer per wire array. The Wires_ prefix is what identifies wire geometry on import ; the suffix is the array name, so arrays survive by name rather than by position. Wire one 3D polyline ( POLYLINE with group 70 bit 8 set, a VERTEX per point, SEQEND ). Not an LWPOLYLINE : that is 2D by definition and would silently drop the loop height. Diameter and material XDATA under application name CIRCUITRF_WBOND — group 1000 is the material, group 1040 the diameter. A reader that does not know the application name ignores it, so the file stays valid everywhere. Feet a filled circle at each end, on the same layer, at the wire's own diameter (a CIRCLE plus a solid HATCH , because DXF has no filled-circle entity). On import, wire layers are diverted from the layout entirely. A 3D polyline on a Wires_* layer becomes a wire, never a layout path; anything else on a wire layer — the foot circles — is dropped, because it is decoration regenerated on every export. Letting it back in would grow the design on every round trip while looking plausible each time. Two entry points, because they answer different questions. A full DXF export writes the reference layout and the wires together — that is the file you send out. Import Wires… reads only the 3D polylines into the current document, leaving its layout untouched. Coordinates are written in the file's own $INSUNITS and read back the same way. GDSII is deliberately not offered for wires: it has no 3D polyline and no notion of a diameter, so a wire could only be flattened to a meaningless 2D trace."],[40,"toolbar","The wBond toolbar","wBond also ships as a standalone application — the same editor with one document in it, for people who want the bond drawing and nothing else. Its toolbar carries the wire tools, which inside circuitRF live on the layout editor's toolbar instead: wBond editor toolbar # Button What it does 1 Zoom to Fit 2 Zoom In 3 Zoom Out 4 Zoom 1:1 (1 px per display-unit tick) 5 Save (⌘S / Ctrl+S) 6 Save As… 7 Showing: profile and layout (V cycles) 8 Hide/Show Inductance panel (I) 9 Hide/Show rulers on both views 10 Include capacitance to the reference plane in the reported inductance 11 Select (Esc) 12 Draw wire (W) — click start, click end; Shift constrains 13 Rotate about end point (R) — grab near the end you want to move (Shift: 45° steps) 14 Draw wires at their true diameter 15 Snap wire points — to layout geometry first, then to the grid 16 Marquee selects wires (off: selects layout shapes) 17 Reverse wire direction 18 Straighten 19 Transform Selection (T)… 20 Export DXF… 21 Import Wires from DXF… 22 Export Touchstone (.sNp)… — publish this wirebond's network. Choose the reference impedance, the frequency sweep and the port basis in the dialog. 23 Compare Distributed Model… — run the distributed (MoM) model next to the lumped one on a frequency grid, and show the two side by side. Same design, same ports, no export needed. 24 Copy as a graphic (⇧⌘C / Ctrl+Shift+C) 25 — Wires: 1"]]};
