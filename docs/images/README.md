# README screenshots

The root `README.md` references three screenshots that live here. They are **placeholders** until captured.
Drop PNGs at these exact paths (the README links to them by name):

| File | What to capture |
|---|---|
| `schematic-editor.png` | The schematic editor with a single-FET PA (Hero 2): SDD FET + gate/drain Vdc bias, input/output matching networks (R/L/C), a `P1Tone` RF source, a `Term` on the output. Show the Library Palette (left) and the Analyses panel with an HB power sweep (right/bottom); a few net labels and green junction dots. |
| `symbol-editor.png` | The symbol editor with a custom 2-port cell symbol in progress: body rectangle + a few primitives (lines/arc/text) + two pins on the connection grid with port numbers; the drawing-tool toolbar on the left. |
| `data-display-loadpull-contour.png` | The Data Display showing a loadpull result on a Smith chart: Pout (dBm) and PAE (%) contours over the load-Γ plane, the MXP/MXE optimum markers, a couple of interactive markers reading impedance/value, and a trace inspector card showing metric/colormap selection. |

Suggested width ~1400–1800 px. Light or dark theme is fine — be consistent across the three.
