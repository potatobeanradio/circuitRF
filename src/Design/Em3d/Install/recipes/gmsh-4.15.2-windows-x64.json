{
  "$schema": "./recipe.schema.json",
  "comment": "R-em3d24-4a. The archive's layout was read on macOS (gmsh-4.15.2-Windows64/gmsh.exe); it has not been run on Windows.",
  "id": "gmsh-4.15.2-windows-x64",
  "tool": "gmsh",
  "version": "4.15.2",
  "platform": "windows",
  "architecture": "x64",
  "layout": "relocatable",
  "layoutNote": "Upstream's binary archive: the program finds its libraries relative to itself, so the home is built in .partial and renamed into place.",
  "sources": [
    {
      "id": "archive",
      "url": "https://gmsh.info/bin/Windows/gmsh-4.15.2-Windows64.zip",
      "fetchedBy": "circuitRF",
      "file": "gmsh-4.15.2-Windows64.zip",
      "sha256": null,
      "checksumNote": "gmsh.info publishes no checksum for its binary archives, so this download is not verified against one"
    }
  ],
  "prerequisites": [],
  "steps": [
    {
      "name": "Download Gmsh 4.15.2",
      "kind": "download",
      "source": "archive"
    },
    {
      "name": "Unpack Gmsh",
      "kind": "extract",
      "source": "archive",
      "into": "${home}"
    }
  ],
  "program": "${home}/gmsh-4.15.2-Windows64/gmsh.exe",
  "measured": {
    "minutes": null,
    "diskGB": null,
    "source": "not yet measured: the owner's Windows runs (brief-em3d-26)"
  },
  "knownFailures": []
}
