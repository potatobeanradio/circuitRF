{
  "$schema": "./recipe.schema.json",
  "comment": "R-em3d24-4a. Same family as the arm64 archive; untried on its own architecture.",
  "id": "gmsh-4.15.2-macos-x64",
  "tool": "gmsh",
  "version": "4.15.2",
  "platform": "macos",
  "architecture": "x64",
  "layout": "relocatable",
  "layoutNote": "Upstream's binary archive: the program finds its libraries relative to itself, so the home is built in .partial and renamed into place.",
  "sources": [
    {
      "id": "archive",
      "url": "https://gmsh.info/bin/MacOSX/gmsh-4.15.2-MacOSX-sdk.tgz",
      "fetchedBy": "circuitRF",
      "file": "gmsh-4.15.2-MacOSX-sdk.tgz",
      "sha256": null,
      "checksumNote": "gmsh.info publishes no checksum for its binary archives, so this download is not verified against one"
    }
  ],
  "prerequisites": [],
  "steps": [
    {
      "name": "Download Gmsh 4.15.2",
      "kind": "download",
      "source": "archive"
    },
    {
      "name": "Unpack Gmsh",
      "kind": "extract",
      "source": "archive",
      "into": "${home}"
    }
  ],
  "program": "${home}/gmsh-4.15.2-MacOSX-sdk/bin/gmsh",
  "measured": {
    "minutes": null,
    "diskGB": 0.13,
    "source": "not yet run on an Intel Mac: the archive was unpacked on an Apple M4 (101 MB) but could not be started there, and the version check after the install is what proves it"
  },
  "knownFailures": []
}
