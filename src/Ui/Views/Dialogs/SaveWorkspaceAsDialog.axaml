<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="CircuitRF.Ui.Views.Dialogs.SaveWorkspaceAsDialog"
        Title="Save Workspace As"
        Width="480"
        SizeToContent="Height"
        MinHeight="200"
        CanResize="False"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Grid RowDefinitions="*,Auto" Margin="20,16,20,16">

        <StackPanel Grid.Row="0" Spacing="8">
            <!-- Said once, at the top: this is a folder copy, not a file save. The old command wrote
                 a single manifest and looked identical up to the moment it silently emptied the
                 window, so the dialog leads with what actually happens. -->
            <TextBlock x:Name="SourceLabel"
                       FontSize="11"
                       Opacity="0.75"
                       TextWrapping="Wrap"/>

            <TextBlock Text="Copy to name:" FontSize="12" Margin="0,4,0,0"/>
            <TextBox   x:Name="NameBox"
                       FontSize="12"
                       MaxLength="200"
                       TextChanged="OnNameChanged"
                       KeyDown="OnKeyDown"/>
            <TextBlock x:Name="ValidationMessage"
                       FontSize="11"
                       Foreground="{DynamicResource CrfWarningBrush}"
                       IsVisible="False"
                       TextWrapping="Wrap"/>

            <TextBlock Text="Location:" FontSize="12" Margin="0,4,0,0"/>
            <Grid ColumnDefinitions="*,Auto">
                <TextBox x:Name="LocationBox"
                         Grid.Column="0"
                         FontSize="12"
                         IsReadOnly="True"
                         PlaceholderText="Choose a parent folder…"/>
                <Button x:Name="ChooseButton"
                        Grid.Column="1"
                        Content="Choose…"
                        FontSize="12"
                        Margin="8,0,0,0"
                        Click="OnChooseClick"/>
            </Grid>

            <TextBlock x:Name="PreviewLabel"
                       FontSize="11"
                       Opacity="0.7"
                       IsVisible="False"
                       TextWrapping="Wrap"/>

            <TextBlock Text="References that point outside this workspace are re-spelled in the copy so they still resolve to the same files. The original workspace is left exactly as it is."
                       FontSize="10" Opacity="0.65" TextWrapping="Wrap" Margin="0,4,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="8"
                    Margin="0,12,0,0">
            <Button Content="Cancel"
                    Width="80"
                    FontSize="12"
                    IsCancel="True"
                    HorizontalContentAlignment="Center"
                    Click="OnCancelClick"/>
            <Button x:Name="OkButton"
                    Content="Save"
                    Width="80"
                    IsDefault="True"
                    IsEnabled="False"
                    FontSize="12"
                    HorizontalContentAlignment="Center"
                    Click="OnOkClick"/>
        </StackPanel>

    </Grid>
</Window>
