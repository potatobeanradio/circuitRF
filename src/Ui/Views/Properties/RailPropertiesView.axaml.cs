using Avalonia.Controls;

namespace CircuitRF.Ui.Views.Properties;

/// <summary>R-rail7-10 — the compact summary a selected <c>.crail</c> shows, and its
/// <b>Open railRF…</b> button. No code of its own: the panel edits nothing, so there is nothing for
/// a code-behind to do that the view model does not already.</summary>
public partial class RailPropertiesView : UserControl
{
    public RailPropertiesView() => InitializeComponent();
}
